---
title: CWYD v2 — Bug Registry
description: Canonical defect registry for CWYD v2. Records every observed defect (wrong or broken runtime behavior) with its root cause, fix, and cross-references to the daily worklog and the development plan.
author: CWYD Engineering
ms.date: 2026-06-14
topic: reference
keywords: bugs, defects, registry, v2, root cause, regression, recovery
estimated_reading_time: 14
---

## Purpose

This file is the canonical, file-based registry of **defects** in CWYD v2: behavior that is wrong, broken, or regressed against intent. It exists so bug history survives across sessions and is never trapped in ephemeral agent memory.

Record a bug here the moment it is observed, even before it is fixed. Update the same entry when it is fixed. Every entry is self-contained: a reader should understand the symptom, the root cause, and the fix without opening any other file.

## Relationship to the development plan

[development_plan.md](development_plan.md) §0.1 (backend) and §0.2 (frontend) are the **phase debt and task queues**: deferred work, refactors, and follow-ups that clear on each phase's end-of-phase audit cadence (Hard Rule #12). They are not a defect log.

The split:

* A **defect** (wrong or broken behavior) is recorded here in `bugs.md`. This is the canonical source of truth for defects.
* A **debt or task item** (deferred work, planned refactor, scoped follow-up) stays in the development plan debt queues.

When a defect also needs phase-audit visibility, the development plan keeps a one-line pointer row that references the `BUG-####` id here, and the full detail lives in this file. There is one source of truth per concern, never two.

## Conventions

### Identifiers

Bugs use a sequential, zero-padded id: `BUG-0001`, `BUG-0002`, and so on. Ids are never reused. The next free id is one greater than the highest id already in the registry.

### Closed-set fields

The registry uses three closed sets. Use only these values.

Area: `backend`, `frontend`, `infra`, `functions`, `docs`, `ci`.

Severity: `blocker` (nothing ships or a pipeline is dead), `high` (a core path is broken), `medium` (a path is degraded with a workaround), `low` (cosmetic or minor).

Status: `open`, `in-progress`, `fixed`, `wontfix`, `duplicate`.

### Placeholder rule

This file is tracked and may reach public GitHub. Never write real environment values (subscription, tenant, resource group, azd env name, resource suffix, identity ids, real FQDNs). Use the placeholder tokens defined in [adr/0019-no-env-specific-content-in-tracked-files.md](adr/0019-no-env-specific-content-in-tracked-files.md), for example `<SUFFIX>`, `<RESOURCE_GROUP>`, `<AZURE_SUBSCRIPTION_ID>`. Environment-variable names such as `AZURE_AI_SERVICES_ENDPOINT` are generic and may be written verbatim.

### How to add a bug

1. Allocate the next `BUG-####` id.
2. Add a row to the Registry table with status `open` (or `in-progress` if already being worked).
3. Add a `### BUG-####` subsection under Details with the symptom, root cause, fix, and references.
4. Cross-reference the day's worklog entry under [worklog/](worklog/), and add a pointer row in the development plan debt queue only if the defect needs phase-audit visibility.

## Registry

| ID | Found | Fixed | Area | Severity | Status | Summary |
|---|---|---|---|---|---|---|
| BUG-0001 | 2026-06-10 | 2026-06-10 | backend | blocker | fixed | Embeddings call routes to the Foundry project endpoint (no embeddings path → `404`) and omits `dimensions`. |
| BUG-0002 | 2026-06-10 | 2026-06-10 | backend | blocker | fixed | Parser-minted chunk id is an illegal Azure AI Search document key, so Search push fails with `InvalidDocumentKey`. |
| BUG-0003 | 2026-06-11 | 2026-06-14 | backend | high | fixed | `GET /api/admin/documents` faceted the non-`facetable` `title` field → Azure AI Search `FieldNotFacetable` (HTTP 400) → mapped to `503`. `list_sources()` rewritten to page chunks and tally sources client-side (no faceting). Verified live: a probe captured `FieldNotFacetable` from the old facet query and `200` + 2 sources from the new path; `/api/admin/documents` returns `200` live after restarting the backend (the stale `uvicorn` had been started **without** `--reload`, so it kept serving the pre-fix code). |
| BUG-0004 | 2026-06-11 | 2026-06-11 | frontend | medium | fixed | The admin Orchestrator field is a free-text input; it should be a dropdown of the known orchestrator keys. |
| BUG-0005 | 2026-06-11 | 2026-06-11 | frontend | low | fixed | The admin Configuration labels show internal config-key names such as `(orchestrator_name)`. |
| BUG-0006 | 2026-06-11 | 2026-06-11 | backend | medium | fixed | Content Safety defaults to disabled; it should default to enabled. |
| BUG-0007 | 2026-06-11 | 2026-06-11 | backend | medium | fixed | The default agent instructions do not carry the vetted v1 default prompt text. |
| BUG-0008 | 2026-06-11 | 2026-06-11 | frontend | medium | fixed | The separate Prompt editor page should be removed and folded into the Configuration page, matching v1. |
| BUG-0009 | 2026-06-11 | 2026-06-11 | frontend | medium | fixed | The admin Log level field is a free-text input; it should be a dropdown of the known log levels (`DEBUG`/`INFO`/`WARNING`/`ERROR`). |
| BUG-0010 | 2026-06-11 | 2026-06-11 | frontend | low | fixed | Numeric config fields should validate numeric entry on the frontend; the admin Configuration page already enforces this (`type=number` + bounds), so the affected surface needs confirmation. |
| BUG-0011 | 2026-06-11 | 2026-06-11 | backend | high | fixed | An authored agent prompt fully replaced the system instructions, so it could supersede the system guardrail ("uber") prompt. Fixed by wrapping both the default and any authored override between a fixed, non-overridable `CWYD_GUARDRAIL` (safety / out-of-domain / citation rules); RAI validation of the authored instructions was already in place via `PROMPT_FIELDS`. |
| BUG-0012 | 2026-06-11 | 2026-06-11 | frontend | low | fixed | The assistant robot avatar and the Thinking (reasoning) panel were misaligned in the chat layout. Fixed by rendering the reasoning panel / answer / citations in a column inside the same `.row` as the avatar, so the avatar lines up with the panel; operator-confirmed live 2026-06-14. (Registry row was stale at `open`; the Details section already recorded the 2026-06-11 fix.) |
| BUG-0013 | 2026-06-11 | 2026-06-14 | backend | medium | fixed | The Thinking/reasoning feed never shows **substantive** model reasoning: the backend emitted no model-`reasoning` SSE frames (only the canned KB-search narration), so the correctly-wired FE panel showed no thinking content. Fixed by auto-detecting whether the answer deployment emits reasoning (`llm.supports_reasoning()`, a cached one-shot Responses-API probe) and routing both orchestrators to a Responses-API reasoning summary on the existing `reasoning` channel when it does; `langgraph` via `FoundryIQ.reason()` (whose Responses `input` shape was also fixed) and `agent_framework` via a `reasoning` run option. Live-verified on **both** orchestrators with a parametrized `tests/integration` test that forces each past the persisted cosmos `langgraph` override; that live run also caught BUG-0035. |
| BUG-0014 | 2026-06-11 | 2026-06-14 | frontend | medium | fixed | Assistant answers rendered as raw markdown instead of HTML. Fixed by rendering the answer + reasoning bodies through a new `MarkdownContent` component (`react-markdown` + `remark-gfm`, no `rehype-raw` so raw HTML stays escaped), matching v1's renderer. Inline `[docN]` markers now render as literal text; their clickable-citation behavior is deferred to BUG-0016. |
| BUG-0015 | 2026-06-11 | 2026-06-15 | frontend | low | fixed | The citation/reference block under an answer did not match v1. Fixed by reworking `CitationPanel` into a v1-style collapsible "N references" / "1 reference" toggle (chevron, collapsed by default) over a numbered citation list (circular brand chip + title; no score badge), and wiring `MessageList` to pass the renumbered referenced-citation subset so the chip numbers match the BUG-0016 answer superscripts. The v1 right-side citation detail column stays out of scope. |
| BUG-0016 | 2026-06-11 | 2026-06-15 | frontend | medium | fixed | Inline `[docN]` references rendered as literal text; v1 shows them as compact superscript links. Fixed with a `parseAnswer` helper that rewrites `[docN]` markers into `^K^` superscript tokens (deduped + renumbered in first-appearance order) and a `MarkdownContent` `enableSupersub` prop that runs `remark-supersub` so the answer body renders visual-only `<sup>` citation numbers. |
| BUG-0017 | 2026-06-11 | 2026-06-15 | backend | high | fixed | Starting a conversation does not persist it to chat history, so new conversations never appear in the left history column. |
| BUG-0018 | 2026-06-11 | 2026-06-15 | frontend | low | fixed | The chat history left column shows the backend database name (`backend: <db_type>`); it should not. |
| BUG-0019 | 2026-06-11 | 2026-06-15 | frontend | low | fixed | Remove the New chat section and functionality from the chat history left column. |
| BUG-0020 | 2026-06-11 | 2026-06-11 | backend | high | fixed | Chat fails with `401 Unauthorized` on the Foundry IQ Knowledge Base MCP endpoint: the `agent_framework` orchestrator authenticated only MCP tool calls (SDK `header_provider` hook), leaving the MCP session-initialize connect unauthenticated, so the agent run raised `ToolExecutionException("Failed to enter context manager.")`. |
| BUG-0021 | 2026-06-11 | 2026-06-12 | backend | blocker | fixed | The `agent_framework` chat path fails with `404 Agent 'cwyd' not found`: `get_or_create_agent` provisions an Assistants-API **persistent agent** (`asst_` id) via `azure.ai.agents.AgentsClient.create_agent`, but the orchestrator's `agent_framework_foundry.FoundryAgent(allow_preview=True)` invokes through the **Responses-API `agent_reference` by name**, which resolves a different Foundry named-agent resource type — so the existing `asst_`-typed `cwyd` agent is not found by name. Surfaces downstream of (and distinct from) the BUG-0020 MCP 401. |
| BUG-0022 | 2026-06-11 | 2026-06-12 | backend | medium | fixed | On the default `agent_framework` path an operator-authored instruction override (`cwyd_agent_instructions` in `RuntimeConfig`) was dropped: the orchestrator invoked the agent by name and used the server-side baked instructions (cached by identity, ADR 0008), so a saved prompt silently had no effect (it applied only on `langgraph`). Surfaced by audit during the BUG-0021 fix (GAP-2); fixed by the GA pattern (ADR 0025) resolving instructions per request via `build_agent` → `_resolve_definition`. |
| BUG-0023 | 2026-06-12 | 2026-06-12 | infra | high | fixed | The `post_provision` Foundry IQ knowledge-base seed wires the KB query-planning model to the o-series **reasoning** deployment (`AZURE_OPENAI_REASONING_DEPLOYMENT`, e.g. `o4-mini`), but the knowledge-base API only accepts **chat** models, so the KB PUT fails with a `400 Unsupported model type` -- the API only accepts chat models from a fixed allow-list (e.g. gpt-5.1). Fixed by sourcing the KB model from the chat deployment (`AZURE_OPENAI_GPT_DEPLOYMENT`). |
| BUG-0024 | 2026-06-12 | 2026-06-12 | infra | high | fixed | Foundry IQ knowledge-base retrieval returns nothing (chat then refuses with the out-of-domain fallback, no citations): the knowledge base calls its query-planning chat model with the **search service's system-assigned managed identity** (KB model `authIdentity` is null → defaults to the search MI), but that identity has **no role** on the AI Services account, so the model call fails `401` (`lacks the required data action .../OpenAI/deployments/.../chat/completions/action`). The v2 Bicep grants `Cognitive Services OpenAI User` to the UAMI but never to the search service identity. |
| BUG-0025 | 2026-06-12 | 2026-06-12 | infra | high | fixed | The `agent_framework` chat returned the out-of-domain fallback with no citations: the KB retrieval tool is attached server-side as `MCPTool(project_connection_id=<search connection>)`, so the Responses API runs `mcp_list_tools` / retrieval **server-side under the project search connection's identity**, and the KB MCP endpoint rejected it with `401 Unauthorized` (`McpListTools(tools=[])` → no retrieval tool → the agent refuses). Root cause (confirmed against the read-only reference architecture): the tool pointed at the wrong connection category (`CognitiveSearch` / bare `AAD`, `useWorkspaceManagedIdentity: false`, no `audience`), which presents no usable bearer to the server-side MCP path. Fixed by creating a dedicated `cwyd-kb-mcp` **`RemoteTool` / `ProjectManagedIdentity`** connection (`audience: https://search.azure.com`), pointing `AZURE_AI_SEARCH_CONNECTION_NAME` at it, and granting the project MI `Search Service Contributor` (it already held `Search Index Data Reader`). Verified: the orchestrator now emits `mcp_call.completed`, `citation` frames, and a grounded `Benefit_Options.pdf` answer (no 401, no fallback). Distinct from BUG-0024 (the inner KB → model hop) and BUG-0020 (SDK-side connect, which the GA pattern bypassed by moving MCP server-side). |
| BUG-0026 | 2026-06-12 | 2026-06-12 | backend | medium | fixed | The `agent_framework` KB-search reasoning narration never reaches the FE "thinking" panel: `_update_to_events` emits the narration + `tool` event only for `function_call` content, but server-side Foundry-IQ KB retrieval (GA-FULL `MCPTool(project_connection_id=…)`) surfaces as `mcp_server_tool_call` content (name on `.tool_name`, not `.name`), so the branch is skipped and the grounded stream carries only `citation` + `answer`. Concrete mechanism behind BUG-0013's empty-panel symptom on the `agent_framework` path. |
| BUG-0027 | 2026-06-12 | 2026-06-14 | backend | medium | fixed | The FE "thinking" panel shows only animated dots during the multi-second retrieval wait. After BUG-0026 the KB-search narration ("Searching the knowledge base…") *is* emitted, but on the `agent_framework` path it fires from `_update_to_events` only when the `mcp_server_tool_call` block surfaces — which is *after* server-side Foundry IQ retrieval completes — so `reasoning`/`tool`/`citation`/`answer` all arrive in one burst at the end and the narration never shows *during* the wait; on `langgraph` retrieval runs in-code before any event, so nothing shows upfront either. The narration is correct but emitted too late to indicate activity during the wait. Fix: emit it upfront in the shared `run_chat` pipeline (after the safety guards, before the orchestrator runs), gated on a configured knowledge source (the router computes the flag), so every current + future orchestrator inherits during-the-wait narration with zero per-provider code; then drop the now-redundant late per-call narration from `agent_framework._update_to_events` (keep the `tool` event). |
| BUG-0028 | 2026-06-12 | 2026-06-14 | backend | high | fixed | The `langgraph` orchestrator returned the out-of-domain fallback for in-domain queries (e.g. "tell me about employee benefits"). Retrieval was healthy (client-side Azure AI Search returns 5 relevant hits, injected as a `[docN]` sources system message), but the ported strict v1 `CWYD_GUARDRAIL` "if the retrieved documents do not contain enough information … your only response is [fallback]" clause made `gpt-5.1` over-refuse on the raw chat-completion path. Confirmed with five guardrail-variant probes against the live model: a minimal prompt + the same sources grounds reliably; the strict guardrail refused 8/8 (temp 0.0 + default). `agent_framework` survives the same prompt because Foundry IQ presents KB results as authoritative tool outputs — so in `cosmosdb` mode the intended `agent_framework` path works, but in `postgresql` mode `langgraph` is the only path and this blocked answering entirely. Fixed (with operator consent — edits the BUG-0011-protected shared CWYD safety prompt) by softening the `CWYD_GUARDRAIL` grounding clause + the `CWYD_DEFAULT_BODY` in/out-of-domain section to a relevance-based "answer when relevant, even if brief" directive, while keeping an explicit ungrounded-creative-content ban (stories/poems/jokes/song lyrics), the exact fixed fallback string, and the BUG-0011 guardrail-wrapping invariant. Live-verified on `langgraph`: "employee benefits" grounded 3/3, "health plans" grounded, France + dragon-poem refused. The secondary `complete()` temperature/`max_tokens` threading was attempted but reverted (it surfaced BUG-0029) and deferred. |
| BUG-0029 | 2026-06-14 | 2026-06-19 | backend | medium | fixed | `FoundryIQ.chat()` sends the OpenAI Chat Completions `max_tokens` parameter, but `gpt-5.1` (and other gpt-5 / o-series chat models) reject it with `400 invalid_request_error` (`Unsupported parameter: 'max_tokens' is not supported with this model. Use 'max_completion_tokens' instead.`). Latent today because `BaseLLMProvider.complete()` (the `langgraph` chat path) does not thread `max_tokens`, so the parameter is never sent on that path; surfaced while attempting the BUG-0028 secondary fix (threading `openai_temperature` / `openai_max_tokens` through `complete()`), which made every `langgraph` chat 500 on `gpt-5.1`. That threading was reverted. Fix (separate unit): in `FoundryIQ.chat()` / `chat_stream()` send `max_completion_tokens` instead of `max_tokens` (the forward-compatible name for current Azure OpenAI chat models), then re-attempt threading the configured sampling settings through `complete()`; also confirm `gpt-5.1` accepts a custom `temperature` (gpt-5 reasoning models constrain it) before threading temperature. **Fixed 2026-06-19 (two parts):** (1) `FoundryIQ.chat()` / `chat_stream()` now send `max_completion_tokens` instead of the rejected `max_tokens` (identical semantics). (2) The configured sampling is threaded end-to-end so the admin `openai_temperature` / `openai_max_tokens` settings actually apply: `complete()` (base + the `FoundryIQ` override) forwards `temperature` / `max_tokens` to `chat()` (omitted on the reasoning branch, which reasoning models reject); the conversation router forwards `effective.openai_temperature` / `openai_max_tokens` into orchestrator construction; `langgraph` captures + forwards them to `complete()`; and `agent_framework` reads the effective admin-override values over the `settings.openai` env defaults. A live probe confirmed gpt-5.1 chat completions accepts `temperature` (0.0/0.7/1.0) + `max_completion_tokens` with no constraint (the temperature restriction is reasoning-path-only, BUG-0035, which `complete()` already routes around). Also cleaned `_to_openai_messages` to emit only `{role, content}` (no `metadata` / enum-role leak onto the wire). Full backend suite 1069 passed / 0 failed. |
| BUG-0030 | 2026-06-14 | 2026-06-15 | backend | high | fixed | The two orchestrators emit structurally different citations for the same corpus, so the FE renders inconsistent references. `langgraph` injects a numbered `[docN]: <content>` sources block into the prompt and post-processes the model's `[docN]` markers into `Citation(id="[doc1]", title="Benefit_Options.pdf")` — clean v1-style inline `[doc1]` + a friendly filename label. `agent_framework` grounds via the server-side Foundry IQ KB MCP tool, so the Responses model emits OpenAI **native** citation annotations (inline `【6:0†source】`) and `Content.annotations` whose `file_id`/`url` is the raw search-index document key (`mcp://searchindex/<doc-key>`); `citations_from_annotations` maps that raw id straight through, yielding inline `【6:0†source】` + a raw `mcp://…` source label with no filename. Net: the same question produces `[doc1]`+`Benefit_Options.pdf` on `langgraph` but `【6:0†source】`+`mcp://searchindex/<doc-key>` on `agent_framework`. Root cause: the shared `compose_cwyd_instructions` prompt carries the same `[docN]` citation rule for both, but it is honored only by `langgraph`'s in-prompt source injection; the Foundry IQ KB tool overrides the citation contract with its own native annotation format and surfaces document keys, not filenames. Distinct from the FE-only BUG-0014 / BUG-0015 / BUG-0016 (which all assume a `[docN]` marker already exists). Fix direction: normalize the `agent_framework` path in post-processing to the v1 shape — rewrite native `【N:M†source】` markers to `[docN]` and resolve the friendly filename (from the annotation `title` / KB metadata) so both orchestrators emit one `[docN]` + filename `Citation` shape. **Update 2026-06-14:** does NOT reproduce on the live deployment (real capture: inline `[doc1]` + `Citation(id="[doc1]", title="Benefit_Options.pdf")`); the integration lane now guards the normalized shape. **Fixed 2026-06-15:** operator chose fallback (b) — the `agent_framework` path backfills friendly `title`/`snippet`/`url` onto KB citations via an app-side by-key Search lookup (`enrich_kb_citations` ← `BaseSearch.get_document_by_key`), and a present-but-null `url` maps to `""` (a second `str(None)`→`"None"` site in `_to_result`, distinct from BUG-0033's annotation-boundary fix); live-confirmed `[doc1]` + `Benefit_Options.pdf` + snippet + `url=""`, no native markers — see Details. |
| BUG-0031 | 2026-06-14 | 2026-06-14 | backend | medium | fixed | The BUG-0011 guardrail-wrapping invariant holds only on the `agent_framework` path, not on `langgraph`, for operator-authored instruction overrides. `_resolve_definition` (agent_framework) wraps a saved `cwyd_agent_instructions` override via `compose_cwyd_instructions(text)` so the fixed `CWYD_GUARDRAIL` always bookends the authored persona; but `langgraph` receives `effective.cwyd_agent_instructions` — the raw stored override (the PATCH handler stores it un-wrapped and `resolve_effective_config` overlays that raw value) — and injects it directly as a SYSTEM message with no `compose_cwyd_instructions` call. So with an admin custom prompt, `agent_framework` keeps the guardrail (out-of-domain refusal + `[docN]` citation rule + fixed fallback) while `langgraph` drops it; BUG-0011's "authored text customizes the persona between the guardrail bookends, it does not replace them" invariant is enforced on only one path. In the default state (no override) both resolve to the already-wrapped `CWYD_AGENT.instructions`, so there is no divergence and current default-prompt testing is unaffected; the RAI write-time gate (`PROMPT_FIELDS`) still screens authored prompts, so the malicious-jailbreak vector of BUG-0011 is not reintroduced — what `langgraph` loses is the standing guardrail behaviors. Root cause: instruction composition is not a shared seam — the guardrail wrap lives only in `_resolve_definition` (read-time) and the default constant, with no equivalent on the langgraph wiring. Fix direction: compose the guardrail-wrapped instructions once in a shared seam consumed by both orchestrators (e.g. wrap in `resolve_effective_config` / the router, or in the `LangGraphOrchestrator` constructor) so both receive identical wrapped text; dovetails with the BUG-0030 shared-prompt/grounding-seam normalization. **Fixed** by introducing one shared composition seam, `resolve_cwyd_instructions(override_text)` in `definitions.py`, and routing both consumers through it: `resolve_effective_config` (the value `langgraph` injects as its SYSTEM prompt) and `_resolve_definition` (agent_framework) now each wrap the raw stored override via that single seam — exactly once each, no double-wrap — instead of two parallel/absent wraps. The three composition references collapse to one; an override is guardrail-wrapped identically on both paths and the no-override case stays byte-identical to `CWYD_AGENT.instructions`. Router-boundary + DRY-contract tests added; full v2 suite green (2247 passed), `pyright --strict` clean on the touched files. |
| BUG-0032 | 2026-06-14 | 2026-06-19 | backend | low | fixed | The admin prompt-override round-trip can double-wrap the guardrail. `GET /api/admin/config` returns `cwyd_agent_instructions = CWYD_AGENT.instructions`, which is the **wrapped** default (`CWYD_DEFAULT_BODY` + `CWYD_GUARDRAIL`). If the FE editor pre-populates from that GET and the operator edits and re-saves, the stored override now contains the guardrail text; at runtime the shared `resolve_cwyd_instructions` seam wraps it again via `compose_cwyd_instructions`, so `CWYD_GUARDRAIL` appears twice in the effective instructions. Latent before BUG-0031 on `langgraph` (raw injection) but present on `agent_framework`; after BUG-0031 both paths wrap, so the double-wrap is now consistent across orchestrators. Not a safety hole — the guardrail is still present (duplicated, not dropped) and the RAI write-gate still screens the prompt — hence low severity; the cost is a redundant guardrail block bloating the system prompt and a confusing authoring UX. Root cause: `GET /config` exposes the wrapped default as the editable value, so the guardrail is part of the round-tripped text. **Fixed 2026-06-19:** both admin read endpoints now expose the editable persona **body** — `GET /api/admin/config` and the env baseline of `GET /api/admin/config/effective` return `CWYD_DEFAULT_BODY` (not the guardrail-wrapped `CWYD_AGENT.instructions`); the `/config/effective` override case already surfaced the raw authored override (itself a body). The runtime seam `resolve_effective_config` is unchanged — it still appends `CWYD_GUARDRAIL` exactly once when building the effective prompt — so the editor never seeds the guardrail and a seed-edit-save round-trip can no longer bake it into the stored override. `CWYD_AGENT` is now unused in `admin.py` (import swapped to `CWYD_DEFAULT_BODY`). Verification: updated three contract tests + added `test_config_effective_default_persona_excludes_guardrail` (asserts the guardrail is absent from the editable default on both GET surfaces); admin router + service suites 133 passed; full backend suite 1070 passed / 0 failed. (The stripping-on-re-wrap alternative was rejected per the bug doc as brittle string surgery.) |
| BUG-0033 | 2026-06-14 | 2026-06-14 | backend | low | fixed | `Citation.url` serializes as the string `"None"` (not `null` / `""`) on the `agent_framework` path. Observed in a live `POST /api/conversation` capture: `{"id": "[doc1]", "title": "Benefit_Options.pdf", "url": "None", ...}`. The Foundry IQ KB annotation has no usable URL, and a Python `None` is being stringified to `"None"` somewhere on the annotation → `Citation.url` path (`citations_from_annotations` reads `url = ann.get("url", "")`, so either the SDK annotation already holds the literal string `"None"` or a `str(None)` coercion leaks through). The FE could render this as a broken `None` link. Low severity (cosmetic wire defect; no grounding/safety impact). Fix direction: coerce a missing/`None` annotation URL to `""` at the `citations_from_annotations` boundary, after confirming whether the SDK annotation `url` is itself the literal `"None"`. Found via the new integration lane while capturing the BUG-0030 citation shape. **Fixed 2026-06-14:** the annotation string fields (`file_id` / `url` / `title` / `snippet`) now pass through a `_clean_annotation_field` boundary helper in `citations.py` that coerces a Python `None` and the literal `"None"` to `""`, so the placeholder never reaches `Citation.url` (nor leaks into an `id` via the `file_id or url or title` fallback). See the BUG-0033 detail section. |
| BUG-0033 | 2026-06-14 | 2026-06-14 | backend | low | fixed | `Citation.url` serializes as the string `"None"` (not `null` / `""`) on the `agent_framework` path. Observed in a live `POST /api/conversation` capture: `{"id": "[doc1]", "title": "Benefit_Options.pdf", "url": "None", ...}`. The Foundry IQ KB annotation has no usable URL, and a Python `None` is being stringified to `"None"` somewhere on the annotation → `Citation.url` path (`citations_from_annotations` reads `url = ann.get("url", "")`, so either the SDK annotation already holds the literal string `"None"` or a `str(None)` coercion leaks through). The FE could render this as a broken `None` link. Low severity (cosmetic wire defect; no grounding/safety impact). Fix direction: coerce a missing/`None` annotation URL to `""` at the `citations_from_annotations` boundary, after confirming whether the SDK annotation `url` is itself the literal `"None"`. Found via the new integration lane while capturing the BUG-0030 citation shape. **Fixed 2026-06-14:** the annotation string fields (`file_id` / `url` / `title` / `snippet`) now pass through a `_clean_annotation_field` boundary helper in `citations.py` that coerces a Python `None` and the literal `"None"` to `""`, so the placeholder never reaches `Citation.url` (nor leaks into an `id` via the `file_id or url or title` fallback). See the BUG-0033 detail section. |
| BUG-0034 | 2026-06-14 | 2026-06-16 | functions | high | fixed | A document uploaded via the admin Add-data UI reports success (`200` + `UploadResponse`) but never appears in the documents list. **Root-caused 2026-06-16:** every PDF routes to the Document Intelligence parser, which builds its client endpoint from `settings.foundry.services_endpoint` (`AZURE_AI_SERVICES_ENDPOINT`); when that is empty `_get_client` yields the endpoint `"/"`, and the Azure SDK bearer-token policy rejects auth on the non-HTTPS URL (`ServiceRequestError: Bearer token authentication is not permitted for non-TLS protected (non-https) URLs`), so `batch_push` throws on every PDF → retries → poison queue (22 stale messages dated 2026-06-10). Upload + enqueue still return `200`, so the doc never indexes and never lists. Reproduced via the `batch_push._execute` seam; **verified the pipeline succeeds (4 chunks pushed, doc then lists) once the endpoint is present** — the host's `local.settings.json` now carries it. **(a) fail-fast DI guard + (b) `.env.sample`/`v2/.env` backfill + (d) upload-boundary `503` (honest error instead of false `200` when `AZURE_AI_SERVICES_ENDPOINT` is unset/non-HTTPS) landed 2026-06-16.** A second local-dev factor (Functions host `local.settings.json` queue name had drifted to `doc-processing-local` vs the canonical `doc-processing`) was found + folded here; the host was realigned to the canonical queue, restarted, and the 6 backend-enqueued messages drained successfully (documents list 2→5). The 22+10 stale poison messages (all referencing already-indexed docs, plus 8 unprocessable Event Grid envelopes) were operator-confirmed and cleared; all ingestion queues now empty. **Fixed 2026-06-16.** |
| BUG-0035 | 2026-06-14 | 2026-06-14 | backend | high | fixed | The `agent_framework` orchestrator set the OpenAI sampling parameter `temperature` (and `max_tokens`) on every agent run via `ChatOptions`; when the BUG-0013 reasoning path is active the same run also requests a Responses-API `reasoning` option, and the gpt-5 / o-series answer deployment (`gpt-5.1`) then rejects `temperature` with `400 Unsupported parameter: 'temperature' is not supported with this model.`, so the BUG-0013 reasoning path errors before any frame streams. Latent because the live deployment runs `langgraph` via a persisted cosmos orchestrator override, so the `agent_framework` reasoning path is never exercised by default — the new both-orchestrator live integration test caught it by forcing `agent_framework` past the override. Fixed by omitting `temperature`/`max_tokens` from `ChatOptions` when the reasoning knob is on — symmetric with the `langgraph` `reason()` path, which sends neither. The non-reasoning `agent_framework` path is unaffected (it sends `temperature=0.0` and was live-verified grounding an answer under BUG-0025). Live-verified: forcing `agent_framework` + reasoning on now streams substantive reasoning with no 400. |
| BUG-0036 | 2026-06-14 | 2026-06-14 | frontend | low | fixed | After BUG-0013 lit up substantive model reasoning, the canned "Searching the knowledge base for relevant sources…" narration (emitted upfront by `run_chat` as the during-the-wait hint per BUG-0027) stayed visible in the thinking panel *alongside* the real reasoning, because the FE concatenated every `reasoning`-channel frame into one `reasoning[]` list — so the placeholder sentence and the model's reasoning rendered together. The narration should vanish the instant real reasoning starts streaming. Fixed by marking the upfront narration frame `metadata.placeholder=true` in `run_chat`, routing placeholder-marked `reasoning` frames into a separate `reasoningPlaceholder` slot on the FE message (new `set_reasoning_placeholder` reducer action) instead of appending them to `reasoning[]`, and rendering `reasoning` when non-empty else the placeholder — so a reasoning-capable model drops the narration the moment its first native frame arrives, while a non-reasoning model keeps the narration as the sole panel content (no regression). Wire contract: the `metadata.placeholder` boolean, forwarded generically by `format_sse` for every channel. |
| BUG-0037 | 2026-06-15 | 2026-06-15 | frontend | low | fixed | The finished assistant answer has no "AI-generated content may be incorrect" disclaimer between the answer and the reference block; v1 shows one in the answer footer. Fixed by rendering a static muted disclaimer line in `MessageList` under any finished assistant answer with non-empty content, above the `CitationPanel` reference block. |
| BUG-0038 | 2026-06-15 | 2026-06-15 | frontend | low | fixed | The reasoning/thinking panel inserts an extra blank line between reasoning sections: `formatReasoning` replaces each model section title with a `\n\n` paragraph break, so consecutive reasoning bodies render as separate paragraphs with a blank line between them. Fixed by replacing each section title with a single `\n` line break and collapsing `\n{2,}` to `\n`, so bodies render as adjacent lines within one `<p>` (no blank line). |
| BUG-0039 | 2026-06-15 | 2026-06-15 | frontend | medium | fixed | Clicking a reference does not open a source-detail view. v1 renders references as bordered, clickable chips that open a right-side column showing the document title, an open-document link, and the markdown-rendered reference text. v2's `CitationPanel` only expanded each item inline to a snippet + link, with no detail column. Fixed by adding `activeCitation` + `ShowCitation`/`CloseCitation` to chat state, restyling references as clickable v1-style chips that dispatch `ShowCitation`, adding a `CitationDetailPanel` (title + open-document link + markdown-rendered snippet), and adding a third push-content grid column to `ChatPage` (driven by an inner `ChatShell` reading `activeCitation`) that the panel occupies. |
| BUG-0040 | 2026-06-15 | 2026-06-15 | frontend | medium | fixed | The reference block's "N references" toggle does not collapse the chip list. `CitationPanel.tsx` sets `hidden={!expanded}` on the chip container, but `CitationPanel.module.css` styles it `.chipList { display: flex }`; an explicit `display` overrides the user-agent `[hidden] { display: none }` rule, so the references stay visible regardless of the toggle state. The existing test asserts only the `hidden` attribute (set correctly), so jsdom could not catch the visual regression. Fixed by adding a `.chipList[hidden] { display: none }` rule so the `hidden` attribute hides the container; verified live. |
| BUG-0041 | 2026-06-15 | 2026-06-15 | frontend | low | fixed | The expanded reference chips render side by side on one wrapping row (`.chipList { display: flex; flex-wrap: wrap }`) instead of stacked one per line as in v1, which lays the reference list out as a column. Fixed by changing `.chipList` to `flex-direction: column` with `align-items: flex-start` so references stack one per line; verified live. |
| BUG-0042 | 2026-06-15 | 2026-06-15 | frontend | medium | fixed | Expanding a reference shows no link to open the document. v2 ingestion intentionally omits a `url` field from the search index (only `id`/`content`/`title`/`content_vector` are written; the blob filename survives in `title`), so `Citation.url` is always empty and the detail panel's open-document link is never rendered. The backend also exposes no file-serving endpoint, so a recovered filename had nowhere to resolve. Fixed by adding a backend `GET /api/files/{filename}` route that streams the blob inline (content type from filename, `Content-Disposition: inline`, path-traversal guards), registering it on the app, and rendering an absolute `${VITE_BACKEND_URL}/api/files/<filename>` open-document link derived from `Citation.title`; verified with backend pytest + live (`200`, `application/pdf`, inline). |
| BUG-0043 | 2026-06-15 | 2026-06-19 | backend | low | fixed | Raw native Foundry IQ Knowledge Base citation markers (`【N:M†source】`, e.g. `【6:1†source】`) leak into the reasoning-panel text. The `agent_framework` orchestrator runs `normalize_kb_citations` only over the answer string, but `_update_to_events` forwards `text_reasoning` blocks verbatim to the `reasoning` channel; when a reasoning-capable model references KB sources in its summary it emits these answer-only markers inline, and the FE reasoning panel (which has no `[docN]` rendering) shows them as garbage. They should be stripped before the `reasoning` event is emitted. **Fixed 2026-06-19:** added a `strip_kb_markers(text) -> str` helper to `tools/citations.py` (reuses `_KB_MARKER_RE`, drops every native marker and collapses the doubled horizontal whitespace it leaves, preserving newlines) and applied it to the `text_reasoning` text in `_update_to_events` so the `reasoning` channel only removes markers while the answer-side `normalize_kb_citations` keeps sole ownership of rewriting them to `[docN]`. The `langgraph` path is unaffected (it emits `[docN]`, not native markers). 5 new tests (4 `strip_kb_markers` unit + 1 orchestrator `test_run_strips_native_kb_markers_from_reasoning_events`); citations + agent_framework suites 82 passed; full backend suite 1075 passed / 0 failed. |
| BUG-0044 | 2026-06-15 | 2026-06-15 | backend | low | fixed | The conversation-route test `test_sse_emits_leading_retrieval_narration_when_search_wired` asserts the leading retrieval-narration reasoning frame carries `metadata: {}`, but `run_chat` intentionally marks that hint event `metadata={"placeholder": True}` so the frontend can replace the placeholder bubble once retrieval completes. The stale assertion fails at `HEAD`; it was unmasked when the suite was re-run during the BUG-0017 conversation-persistence wiring (the source is correct; the test expectation was never updated when the placeholder metadata was added). Fixed by updating the expected metadata to `{"placeholder": True}`. |
| BUG-0045 | 2026-06-15 | 2026-06-19 | frontend | medium | fixed | The chat page still shows a double scroll (the whole page scrolls — the app header slides off the top and the composer lifts off the bottom exposing whitespace — on top of the inner message-list scroll) after the L1 shell height-chain fix (`.appFluentRoot` height:100%, CoralShell `.column` 100vh→100%, ChatPage `.main` min-height:0) and a hard reload. Agent Playwright measurements reported `pageOverflow = 0` across empty / long-content / real-conversation / viewport-sweep scenarios, so the reproducing condition was not captured by the automation environment and the height-chain hypothesis was incomplete. **Fixed 2026-06-19 by the operator** (operator-owned per the original triage); operator confirms the double scroll is gone — only the inner message-list region scrolls now, the header and composer stay pinned. Closed. |
| BUG-0046 | 2026-06-15 | 2026-06-15 | frontend | high | fixed | The v2 SPA never resolves the signed-in user or forwards an identity header, so chat history is not isolated per user. The backend is already user-scoped (the history and conversation routes consume `UserIdDep` → the `x-ms-client-principal-id` header, and the Cosmos DB / PostgreSQL providers key every read and write on `user_id`, so a non-owned conversation already `404`s), but the frontend (a) never calls `/.auth/me` and keeps no user-id state, (b) forwards no `x-ms-client-principal-id` on any of its four `fetch` clients (`streamChat`, `conversationHistory`, `admin`, `speech`), (c) has no auth-enabled detection or blocked screen, and (d) has no default-user fallback. With the separate-origin topology (SPA → absolute `VITE_BACKEND_URL`) the platform-injected Easy Auth header never reaches the cross-origin backend call, so in production every history / conversation call returns `401` and locally every caller collapses into the single `local-dev` partition. Fix: port the v1 `getUserInfo()` flow + the reference architecture's header-forward pattern — resolve the user on load (the real id from `/.auth/me` when auth is enforced, else the all-zeros default `00000000-0000-0000-0000-000000000000`), block with an error screen when enforced but absent, and inject `x-ms-client-principal-id` on every API call; add an `auth_enforced` backend signal (folded into `GET /api/health`) plus a light principal-id well-formedness check. Security: a browser-forwarded principal id is not a trust boundary (chat partitioning only) — admin RBAC stays anchored on the backend's own server-injected Easy Auth claims. Latent (local dev unaffected; surfaces in cloud multi-user). Fixed: resolve-on-load + `userIdHeaders()` spread across every client; a **fifth** request seam (`HistoryPanel`'s inline `fetchJson`, missed by the four-client enumeration) was caught by a live request-header capture and corrected; verified live (history calls now carry the id) and by a two-user isolation integration test against real Cosmos. |
| BUG-0047 | 2026-06-16 | 2026-06-16 | backend | high | fixed | Every `/api/admin/*` call returns `401 {"detail":"Missing client principal claims; Easy Auth claims header required."}` in local dev, so the admin UI cannot load (observed at `http://localhost:5273/api/admin/status`). Regression from BUG-0046: the SPA's single modular header seam `userIdHeaders()` now forwards a default `x-ms-client-principal-id` (the all-zeros default user) on **every** API call, but the admin gate `requires_role("admin")._checker` has a local-dev bypass keyed on **both** Easy Auth headers being absent (`if not principal_id and not claims_raw`); the always-present id header defeats the bypass, so the gate falls through to the no-claims `401`. The frontend correctly does **not** forward the `x-ms-client-principal` *claims* blob — a browser-forged claims blob would let any caller assert the admin role (the documented trust boundary, see BUG-0046 Security note) — so the missing header cannot be supplied from the SPA. Fix: the local-dev bypass keys on the **absence of the claims blob** (the sole role authority), not on both headers; in `local` a missing claims blob → synthetic `local-dev` admin (regardless of the forgeable id header), in any non-local environment it still fails closed (`401`). One backend method fixes every admin endpoint at once. **Fixed 2026-06-16.** |
| BUG-0048 | 2026-06-16 | 2026-06-16 | backend | high | fixed | `DELETE /api/admin/documents/{source}` returns `503 {"detail":"Azure dependency temporarily unavailable."}` for every delete (observed deleting `Benefit_Options.pdf`). `AzureSearch.delete_by_source` builds a server-side OData `$filter` `title eq '<source>'` to collect the chunk ids, but the deployed index (`post_provision.py` `_build_chat_index`) defines `title` as a `SearchableField` — searchable but **not** filterable — so Azure Search rejects the query with `HttpResponseError: Invalid expression: 'title' is not a filterable field. Only filterable fields can be used in filter expressions.`, which the app-level handler sanitises to the `503`. The sibling `list_sources` already documents and works around the same index-schema fragility (it avoids faceting on `title` and pages client-side). Not a name-vs-id problem (a wrong identifier returns `404`, not `503`): the delete already removes **by id** — it collects chunk `id`s and calls `delete_documents` — and only the *id-discovery* step failed. Fix: page every chunk client-side selecting `id` + `title` (auto-paging, no server-side `$filter`) and match `source` by exact client-side equality, mirroring `list_sources`; this also removes the OData-injection seam a quote-bearing filename opened. No index schema / reindex / frontend change; the `DELETE /…/{source}` contract is unchanged. **Fixed 2026-06-16.** |
| BUG-0049 | 2026-06-16 | 2026-06-16 | backend | high | fixed | Uploading a `.docx` (or any Office / HTML format) indexes nothing — the file uploads, enqueues, and `batch_push` reports success, but no chunks are produced so the document never appears in search. `DocumentIntelligenceParser.parse` builds chunks exclusively from `result.pages[*].lines[*].content`, but Azure Document Intelligence treats Office (DOCX/PPTX/XLSX) and HTML inputs as "pageless": it returns the extracted text in `result.paragraphs` / `result.content` and leaves `pages[*].lines` empty. Confirmed against the live service with a sample DOCX: `len(pages)=1`, per-page `len(lines)=[0]`, but `len(paragraphs)=848`, `len(result.content)=29335`; the same probe on a PDF returns `len(pages)=4`, per-page `len(lines)=[4,6,34,25]`. The page loop therefore emits zero chunks for DOCX and the file silently fails to index (the handler logs the zero-chunk info line and returns `[]`). Fix: when the page pass yields zero chunks, fall back to one chunk per `result.paragraphs` entry (the same semantic unit `TextParser` uses), keeping `index` dense; paginated formats (PDF, images) still emit one chunk per page and never reach the fallback, so there is no double-emit. No infra / model / frontend change. **Fixed 2026-06-16.** |
| BUG-0050 | 2026-06-16 | 2026-06-16 | frontend | high | fixed | The **Admin** button never appears in the deployed app and the admin-availability probe returns `200` with HTML instead of JSON. The admin REST client (`api/admin.tsx`) is the only frontend fetch client that issues **relative** `/api/admin/*` URLs; every sibling (`streamChat`, `conversationHistory`, `HistoryPanel`, the health probe, `documentHref`) prepends `VITE_BACKEND_URL`. In the split-host deployment (frontend App Service + backend Container App on different origins) a relative `/api/admin/status` resolves against the frontend host, whose `frontend_app.py` SPA catch-all returns `index.html` (`200` + HTML) for any non-file path — so `getAdminStatus()`'s `response.json()` throws, `adminAvailable` resolves false, and the Header hides the button. Confirmed live: the frontend host returns `<!doctype html>` (`200`) while the backend host returns the JSON status (`200`) for the same path. Local dev was unaffected (the Vite proxy maps relative `/api` to the backend). Fix: add the same `backendUrl()`/`apiUrl()` base-resolution seam the sibling clients use and prepend it to all 8 admin fetch sites, so admin calls cross to the backend origin; relative behavior (and existing unit assertions) are unchanged when `VITE_BACKEND_URL` is unset. No backend / auth change — the backend already returns JSON without an auth header in this deployment (`auth_enforced:false`). **Fixed 2026-06-16.** |
| BUG-0051 | 2026-06-16 | 2026-06-16 | infra | high | fixed | Uploading a document through the (now-visible) admin UI returns `503 {"detail":"Document storage is not configured for this deployment."}`; the same gate fronts URL ingestion, reprocess-all, and file download. The backend admin endpoints gate on `StorageSettings.documents_container` **and** `doc_processing_queue` being non-empty, but the `backendContainerApp` env block in `main.bicep` never wired the storage vars — only the function app block sets `AZURE_STORAGE_ACCOUNT_NAME` / `AZURE_DOCUMENTS_CONTAINER` / `AZURE_DOC_PROCESSING_QUEUE` (and emits them as stack outputs) — so the deployed backend resolved all three to `""` and the gate tripped on every upload. Confirmed live: the backend Container App's container `env` listed none of the three storage vars. Not an RBAC gap — the shared UAMI already holds Storage Blob Data Owner + Storage Queue Data Contributor/Message Sender on the account (the function app does ingestion under the same identity). Fix: mirror the function app's three storage env entries into the `backendContainerApp` container `env` array (blob/queue endpoints are derived from the account name by `resolve_storage_endpoints`, so no explicit `AZURE_STORAGE_BLOB_ENDPOINT` is needed). A live hotfix `az containerapp update --set-env-vars` added the three vars to unblock QA; the new revision came up Healthy and an end-to-end `.txt` upload returned `200` `{…,"queued":true,"ingestion_job_id":"…"}`, proving the config gate, blob write, and queue enqueue all succeed under the UAMI. **Fixed 2026-06-16.** |
| BUG-0052 | 2026-06-16 | 2026-06-16 | infra | high | fixed | Uploading a PDF/DOCX through the (now-working, post-BUG-0051) admin UI returns `503 {"detail":"Document parsing is not configured for this deployment."}`. Two deployment gaps: (1) the BUG-0034 unit-(d) upload guard refuses a Document-Intelligence-routed file when `AZURE_AI_SERVICES_ENDPOINT` is empty/non-HTTPS, and that env var was wired into **neither** the deployed backend Container App nor the function app (same bicep-gap class as BUG-0051 — set only as outputs, never on either runtime's `env`); (2) past the guard, the shared UAMI lacked **Cognitive Services User** on the AI Services account, so `DocumentIntelligenceParser` could not authenticate to DI under managed identity. Fixed live: added `AZURE_AI_SERVICES_ENDPOINT=https://aisa-<SUFFIX>.cognitiveservices.azure.com/` to both runtimes and granted the UAMI **Cognitive Services User** scoped to the AI Services account; a PerksPlus.pdf upload then returned `200 {…,"queued":true}`. End-to-end parse verification is blocked downstream by BUG-0053 (the queued message is never consumed). Durable bicep back-port (env var on both runtimes + role assignment) noted as follow-up, not applied (bicep edits paused). **Fixed 2026-06-16.** |
| BUG-0053 | 2026-06-16 | 2026-06-16 | functions | high | fixed | Documents uploaded through the admin UI enqueue (`queued:true`) but never parse/embed/index — they never appear in `GET /api/admin/documents`. The `doc-processing` queue holds the messages indefinitely at `dequeueCount=0` (never dequeued). Root cause: the Flex Consumption function (`func-<SUFFIX>`, Linux/Python 3.11) uses per-function (target-based) scaling — `batch_push` runs only in its own `function:batch_push` scale group, which the platform **scale controller** must wake from zero on queue depth; it does not for the identity-based (`AzureWebJobsStorage__credential=managedidentity`) Storage Queue connection, so nothing ever polls the queue (the HTTP-served instances do not run `batch_push`). Everything else is provably correct: trigger metadata registered (`queueTrigger`, `queueName=%AZURE_DOC_PROCESSING_QUEUE%` → `doc-processing`, `isDisabled:null`); UAMI holds **Storage Queue Data Contributor** (⊃ reader + message processor); only the user-assigned identity, `AzureWebJobsStorage__clientId` matches it; storage fully open (`publicNetworkAccess=Enabled`, `defaultAction=Allow`, no PE/VNet); Flex scale config healthy (`maximumInstanceCount=40`); and the function **works warm** (it processed messages right after a deploy). No-cost levers that did **not** wake the group: explicit `AzureWebJobsStorage__blobServiceUri`/`__queueServiceUri`/`__tableServiceUri`; a forced `az functionapp stop`+`start` cycle (plain `restart` never cycled instances); `syncfunctiontriggers`. Compounded by BUG-0055 (App Insights empty → no scaler/host logs). Open decision: the reliable documented fix is `alwaysReady={function:batch_push:1}` (one warm instance polls the queue) which carries a small always-on cost the operator declined; remaining no-cost options are speculative (system-assigned identity + queue RBAC for the scale controller; runtime scale monitoring, documented as Premium+VNet only). **Fixed 2026-06-16:** operator approved the always-ready instance — `az functionapp scale config always-ready set … function:batch_push=1` runs the real listener continuously (the `NoOpListener` on non-allocated instances no longer applies) and drains `doc-processing` (verified 7→0, 3→0). A **separate** base64-encoding defect (BUG-0056) then blocked the downstream validation. |
| BUG-0054 | 2026-06-16 | 2026-07-02 | infra | medium | fixed | The `doc-processing-poison` queue holds 10 base64 `Microsoft.Storage.BlobCreated` Event Grid envelopes (inserted 13:46–13:48). A stray/legacy Event Grid subscription on the storage account delivers blob-created events into `doc-processing`, but `batch_push` expects CWYD ingestion envelopes (`{container, blob_filename, ingestion_job_id, …}`), not the Event Grid `BlobCreated` schema, so the events fail validation, retry, and poison. Noise/poison only — distinct messages from the CWYD envelopes, so it does **not** block ingestion — but the Event Grid → queue wiring should be removed or routed to a handler that understands the blob-event schema. The same 8-envelope artifact was noted under BUG-0034's poison cleanup. **Fix designed + locally proven 2026-06-20** ([ADR 0028](adr/0028-event-grid-single-trigger-blob-ingestion.md)): a `blob_event` queue-trigger translator turns the Event Grid `BlobCreated` envelope into a CWYD `doc-processing` message (subscription repointed to a dedicated `blob-events` queue), plus an `ingestion_trigger` flag (`direct_enqueue` default / `event_grid`) so the backend stops double-enqueuing once Event Grid owns ingestion. Validated end-to-end locally (blob → `blob-events` → `blob_event` → `doc-processing` → pgvector). **`blob_event` deployed to the cloud Function App 2026-06-24** (BUG-0080, 5 → 6 functions; `alwaysReady` instance in bicep), so the function-deploy step is **done** and the backend stays on `direct_enqueue` (flag not flipped) so ingestion keeps working. **Closed 2026-07-02** on the fresh `<AZD_ENV_NAME>` deployment (suffix `<SUFFIX>`, cosmosdb): the ingestion trigger is `event_grid` (azd env + the backend Container App carries `AZURE_INGESTION_TRIGGER=event_grid`); the live Event Grid subscription `blob-created-to-doc-processing` on system topic `evgt-<SUFFIX>` delivers `BlobCreated`+`BlobDeleted` (subject-scoped to the `documents` container) via system-assigned identity to the `blob-events` queue (not `doc-processing`); and a live end-to-end run passed — a test blob indexed (`cwyd-index` 263 → 264, sentinel hit) via Event Grid → `blob-events` → `blob_event` → `doc-processing` → `batch_push`, and deleting it removed the chunk (264 → 263) via the `BlobDeleted` branch, with **all queues (incl. both poison) at 0** throughout. No historical poison messages existed in this fresh env (the "4 historical" were in the retired deployment). The function runs as a Container App (`ca-func-<SUFFIX>`) with `minReplicas=1`, so the BUG-0053 Flex scale-from-zero risk does not apply. See the detail entry. |
| BUG-0055 | 2026-06-16 | 2026-07-02 | infra | medium | fixed | Application Insights (`appi-<SUFFIX>`) has received **zero** telemetry ever from both the function host and the backend Container App. A union query over `requests`/`traces`/`exceptions`/`dependencies`/`customMetrics` summarizing `count`/`min(timestamp)`/`max(timestamp)` returned `[0, null, null]` — total 0, no earliest, no latest. Both runtimes carry `APPLICATIONINSIGHTS_CONNECTION_STRING`, yet nothing arrives, so the OpenTelemetry / App Insights export is unwired or misconfigured at both runtimes (wrong connection string, missing exporter init, or a transport/sampling failure). Independently a defect (no production observability) and it blocks diagnosis of BUG-0053 (no scale-controller / host logs to inspect). **Fixed 2026-07-02:** the real root cause was **`disableLocalAuth: true`** on the App Insights component (both OTel exporter halves were already correctly wired) — with local auth disabled, ingestion requires an Entra token, but both `configure_azure_monitor(connection_string=...)` call sites pass no `credential=`, so every write silently 401'd. Fix (match MACAE): set `disableLocalAuth: false` (ADR-0018 Amendment 2) — ikey ingestion accepted, zero app-code change; `Monitoring Metrics Publisher` role retained for a one-line revert. Deployed via `azd provision`; verified live (deployment `Succeeded`, `disableLocalAuth` now `false`, backend serving). |
| BUG-0056 | 2026-06-16 | 2026-06-16 | functions | high | fixed | With BUG-0053's always-ready listener now draining `doc-processing`, a fresh PerksPlus.pdf upload still does not index — all 3 of its messages **poison** (`Executed Functions.batch_push (Failed, Duration=5–29ms)`, `batch_push queue message validation failed` — immediate, before any download/parse/embed). Decoding the 3 newest poison bodies shows **one is the genuine CWYD envelope** (carrying the exact upload `ingestion_job_id`, shape `{container_name, filename, ingestion_job_id, force_reindex}`) which **exactly matches** the deployed `BatchPushQueueMessage` contract — so not a schema skew. Root cause: a **queue-message base64-encoding mismatch** — the backend producer builds its `QueueClient` with no `message_encode_policy`, so `azure-storage-queue` uses `NoEncodePolicy` and sends **raw JSON** (the `queue_writer.py` docstring's "base64-encodes by default" is wrong); the Functions queue trigger only accepts raw bodies when `extensions.queues.messageEncoding=none`, present **only** in `local.settings.json` (so local works) and never propagated to `host.json` or the cloud app settings, so the Flex host uses the default `base64`, can't decode the raw JSON, and poisons every message (it base64-encodes toward the poison queue, hence peeked bodies decode to the clean envelope). Fix applied live (cloud variable, not bicep): set `AzureFunctionsJobHost__extensions__queues__messageEncoding=none` + restart; durable back-port to `host.json` / bicep noted (bicep paused). **Fixed 2026-06-16:** confirmed loaded (host `QueuesOptions.MessageEncoding=None`; the post-fix poison body is raw JSON, no longer base64) and correct — the post-fix CWYD envelope now passes validation, downloads the blob (HTTP 206, full 115 KB), and parses the PDF. The document still does not index because of a **separate** downstream embedding-deployment 404 tracked as **BUG-0057**. |
| BUG-0057 | 2026-06-16 | 2026-06-16 | functions | high | fixed | After BUG-0056's encoding fix, the post-fix CWYD envelope passes validation, downloads the blob (HTTP 206, 115 KB), and parses the PDF into chunks, then **fails at embedding** — `batch_push queue handler failed` with `openai.NotFoundError: Error code: 404` from `handler.py` `embedder.embed(chunks)` → `foundry_iq.embed` → `oai.embeddings.create(...)` (5 retries ~7 s apart, then poison). Root cause: an **embedding-deployment / endpoint mismatch**. The 1536-dim search index needs `text-embedding-3-small` (set in `AZURE_OPENAI_EMBEDDING_DEPLOYMENT`), which is deployed **only** on the classic Azure OpenAI account `oai-<DATA_SUFFIX>` (`AZURE_OPENAI_ENDPOINT`). But the Foundry IQ embedding path routes `/embeddings` to the **AI Services account** `aisa-<SUFFIX>` (`AZURE_AI_SERVICES_ENDPOINT` — the Foundry project route serves no `/embeddings`), and that account has only `text-embedding-3-large`, so the deployment is not found → 404. The deployed function build also predates the `_get_embeddings_client` account-route fix, so it may still target the project route. No pure app-setting fix exists; resolution needs `text-embedding-3-small` provisioned on `aisa-<SUFFIX>` (or the embedding endpoint repointed to the account that has it) plus a redeploy of the current function code. **Fixed 2026-06-16:** (a) provisioned `text-embedding-3-small` (1536, GlobalStandard) on `aisa-<SUFFIX>` and verified the `/openai/v1/embeddings` route returns 1536-dim vectors; (b) redeployed the current function code — the first `azd deploy function` shipped a **stale** `build-functions/` artifact (the prepackage hook did not regenerate it; tracked as BUG-0058), so the embed path still hit the project route and 404'd; after manually running `prepackage_function.py` and redeploying, the new code's `_get_embeddings_client` account-route loaded. End-to-end confirmed: PerksPlus.pdf now indexes with **4 chunks** in the active `cwyd-index` (151 docs total). |
| BUG-0058 | 2026-06-16 | 2026-07-02 | functions | medium | fixed | `azd deploy function` did not run the `prepackage` hook (`hooks.prepackage` in [v2/azure.yaml](../azure.yaml), which shells to `scripts/prepackage_function.py`), so it packaged a **stale** `v2/build-functions/` artifact dated 06-06 instead of regenerating it from current `src/`. Effect: a code fix present in `src/backend/.../foundry_iq.py` (the `_get_embeddings_client` account-route embeddings client) never reached the function host — the deploy reported SUCCESS while shipping old code, and the running function kept hitting the 404 embedding route (masking the BUG-0057 fix). Confirmed by comparing `build-functions/backend/core/providers/llm/foundry_iq.py` (line 363 `_get_openai_client`) vs `src/.../foundry_iq.py` (line 446 `_get_embeddings_client`) and the 06-06 vs 06-14 mtimes. Workaround: run `uv run python scripts/prepackage_function.py` before `azd deploy function`. Needs investigation into why azd skipped the prepackage hook on the per-service `deploy` path. **Fixed 2026-07-02 (obsolete — mechanism retired):** the stale-artifact class is now structurally impossible. The function migrated to a **Container App built from `Dockerfile.functions`** via ACR remote build (the Dockerfile COPYs `src/functions` + `src/backend` directly), so there is no staged `build-functions/` artifact and no prepackage hook at all — `build-functions/` and `scripts/prepackage_function.py` were removed, and [tests/functions/test_azure_yaml_hooks.py](../tests/functions/test_azure_yaml_hooks.py) (3 passing) now guards that the function is `host: containerapp` with **no** service- or project-level prepackage hook. Live-confirmed 2026-07-02: `azd deploy function` shipped current source — the BUG-0088 embedder/parser fixes ran in the deployed function immediately after the deploy. |
| BUG-0059 | 2026-06-16 | 2026-06-16 | infra | high | fixed | Every `agent_framework` chat 401s at KB grounding — the SSE stream emits `event: error` with `Authentication failed when connecting to the MCP server: https://srch-<DATA_SUFFIX>.search.windows.net/knowledgebases/cwyd-kb/mcp: 401 (Unauthorized)`, no `citation`/`answer` frame. **Cloud recurrence of BUG-0025.** The deployed backend Container App's `AZURE_AI_SEARCH_CONNECTION_NAME` held the bicep default `search-srch-<DATA_SUFFIX>` (category `CognitiveSearch` / bare `AAD`, no `audience`), which mints no usable token for the server-side MCP path → 401. BUG-0025 fixed the local `.env` to `cwyd-kb-mcp` but its durable bicep back-port was never completed, so `azd up` shipped the wrong value to the cloud runtime. The correct `cwyd-kb-mcp` `RemoteTool` / `ProjectManagedIdentity` connection (`audience: https://search.azure.com`, `useWorkspaceManagedIdentity: true`, `target` = the KB MCP URL) already exists, and the project MI already holds **both** `Search Index Data Reader` + `Search Service Contributor` — only the backend env var was wrong. Fixed live (cloud variable, not bicep): `az containerapp update --set-env-vars AZURE_AI_SEARCH_CONNECTION_NAME=cwyd-kb-mcp` (new revision Running). Verified end-to-end: the same question now streams `tool: knowledge_base_retrieve` → `citation [doc1] PerksPlus.pdf` → a grounded `answer`, no `error` frame. Durable bicep back-port (point `AZURE_AI_SEARCH_CONNECTION_NAME` at the RemoteTool connection + create that connection + project-MI `Search Service Contributor` in `infra/`) still pending (bicep paused). **Fixed 2026-06-16.** |
| BUG-0060 | 2026-06-17 | 2026-06-17 | infra | blocker | fixed | The first `databaseType=postgresql` `azd up` fails ARM template validation with `InvalidTemplate`: `The template reference 'avmPrivateDnsZones[7]' is not valid`. In postgresql mode `privateDnsZones` = 6 always-on zones + 1 postgres zone (indices 0–6), but the cosmos-only `aiSearch` module's private-endpoint DNS-zone reference uses the literal constant `dnsZoneIndex.search` (= 7). ARM validates copy-index references **statically** against the declared array length, **before** the module's `if (databaseType == 'cosmosdb')` condition is evaluated, so the out-of-range literal `[7]` fails — even though the search PE never deploys in postgresql mode (and even though `enablePrivateNetworking=false` empties the copy at runtime). This path had never been provisioned (all prior cloud QA was cosmos, where the array has 8 zones so index 7 is valid). Fixed in [main.bicep](../infra/main.bicep): made the search PE DNS-zone index a mode-conditional expression — `avmPrivateDnsZones[databaseType == 'cosmosdb' ? dnsZoneIndex.search : dnsZoneIndex.postgres]` — so it is never the literal `7` in postgresql mode; ARM then defers the index to runtime where it resolves to a valid in-range index (unused, because the PE is gated out). `az bicep build` clean; the subsequent `azd up` provisioned the full postgresql resource set (incl. `psql-<SUFFIX>`). **Fixed 2026-06-17.** |
| BUG-0061 | 2026-06-17 | 2026-07-03 | infra | blocker | fixed | The postgresql `azd up` (after BUG-0060) fails the **provision** step with `Managed Identity for event subscription does not have authorization to deliver to the endpoint` (Event Grid `blob-created-to-doc-processing` → `doc-processing` Storage Queue). Deploy-time chicken-and-egg: the AVM `event-grid/system-topic` module (`evgt-<SUFFIX>`) creates the system topic, its system-assigned MI, **and** the `blob-created-to-doc-processing` event subscription **inside one nested deployment**, while the role assignment that grants that MI `Storage Queue Data Message Sender` on the storage account (`eventGridQueueSenderRole`) is an **external** resource ordered *after* the module (it consumes `eventGridSystemTopic.outputs.systemAssignedMIPrincipalId`). The subscription's `deliveryWithResourceIdentity` preflight validates the MI's queue-delivery permission **at subscription-creation time, inside the module** — before the external role exists — so it 401s and fails the whole provision. (Storage has `allowSharedKeyAccess=false`, so identity-based delivery is mandatory; there is no key fallback.) Unblocked live (cloud role grant, not bicep): `az role assignment create --assignee-object-id <EG_TOPIC_MI> --role "Storage Queue Data Message Sender" --scope <storage account>`, then re-ran `azd up` — the topic's MI now passes preflight and the subscription + remaining resources provision. The random-named manual grant then collided by-content with the bicep-managed `eventGridQueueSenderRole` (deterministic GUID name) on the next provision (`RoleAssignmentExists`); resolved by deleting the manual grant and letting bicep own the role (the subscription is permanently created, so the chicken-and-egg does not recur). Durable bicep back-port (split the event subscription out of the system-topic module into a separate resource that `dependsOn` the role assignment, OR pre-create the MI + role before the module) pending — bicep edits paused. **Fixed 2026-06-17.** **Durable bicep fix implemented 2026-07-03:** the shipped standalone-subscription + `dependsOn eventGridQueueSenderRole` back-port was **insufficient** — `dependsOn` orders resource *creation*, but Azure RBAC is eventually consistent, and the **system-assigned** topic MI does not exist until the AVM module completes, so the grant was authored moments before the subscription preflight with ~0 s replication lead → the 401 recurred on the fresh eastus2 provision (DR-06). Real fix (mirrors the existing-topic reuse path): deliver as the **shared UAMI** (`id-<SUFFIX>`, exists at t=0) — the AVM `system-topic` `managedIdentities.userAssignedResourceIds` gets the UAMI, `eventGridQueueSenderRole` grants `Storage Queue Data Message Sender` to the UAMI on the storage account with **no** dependency on the topic (so ARM creates it at the start of the deployment and it replicates through the multi-minute provisioning tail before the preflight runs), and the subscription `deliveryWithResourceIdentity.identity` becomes `UserAssigned`. `az bicep build` clean. |
| BUG-0062 | 2026-06-17 | 2026-06-17 | infra | blocker | fixed | `azd deploy function` fails fast with `InaccessibleStorageException` / `BlobUploadFailedException: 403` against the Flex Consumption Function App's storage account `st<SUFFIX>`. The function uses identity-based storage (`AzureWebJobsStorage__credential=managedidentity`, no shared key), but the storage account ships `networkRuleSet.defaultAction=Deny` + `bypass=AzureServices` with **no resource-instance rule** for the Function App, and `allowSharedKeyAccess=false` (Azure Policy) leaves no key fallback — so with `enablePrivateNetworking=false` (no VNet path) the Flex Consumption platform is denied at deploy and runtime. Fixed live: `az storage account update -n st<SUFFIX> -g <RESOURCE_GROUP> --default-action Allow` (data stays Entra/RBAC-protected; only the network-ACL default flips). Durable bicep fix pending. **Fixed 2026-06-17.** |
| BUG-0063 | 2026-06-17 | 2026-06-17 | infra | blocker | fixed | The backend Container App revision fails activation (`ActivationFailed`, 0 replicas) on the first postgresql deploy, so `/api/health` times out. The FastAPI lifespan opens the asyncpg pool eagerly; `PostgresClient` authenticates with the **UAMI** managed-identity token but connects with username `AZURE_POSTGRES_ADMIN_PRINCIPAL_NAME`, which [main.bicep](../infra/main.bicep) wires (backend ~L1760, function ~L2066) to `postgresAdminPrincipalName` (the **human deployer** UPN, `<AZURE_PRINCIPAL_UPN>`). Entra-only Postgres Flexible Server rejects the login because the token subject (`id-<SUFFIX>`) ≠ the role named by the username, so the pool never opens and the lifespan raises before `yield`. The UAMI is already a postgres AAD admin under principal name `id-${solutionSuffix}` (~L1463) — the runtime env var just names the wrong admin. Fixed live: `az containerapp update -n ca-backend-<SUFFIX> -g <RESOURCE_GROUP> --set-env-vars AZURE_POSTGRES_ADMIN_PRINCIPAL_NAME=id-<SUFFIX>` → new revision boots, `/api/health` → `pass`. Durable bicep fix pending. **Fixed 2026-06-17.** |
| BUG-0064 | 2026-06-17 | 2026-06-17 | infra | high | fixed | On a postgresql deploy the backend always grounds through `agent_framework`, which is incompatible with pgvector (no Foundry IQ knowledge base) → chat fails at request time with HTTP 409 `ConfigResolutionError` (ADR 0022), regardless of `databaseType`. Two wiring faults: (a) [main.bicep](../infra/main.bicep) L1776 emits the env var `ORCHESTRATOR`, but the backend reads `CWYD_ORCHESTRATOR_NAME` (`OrchestratorSettings` `env_prefix=CWYD_ORCHESTRATOR_`, field `name`), so the bicep value is never read and the orchestrator falls to its code default `agent_framework`; (b) even with the right var name, the value is hardcoded to `agent_framework` for every database type, but pgvector mode's only coherent orchestrator is `langgraph`. Backend `/api/health` stays green (the orchestrator is built per request, not in the lifespan), so the fault only surfaces on the first chat. Fixed live: `az containerapp update -n ca-backend-<SUFFIX> -g <RESOURCE_GROUP> --set-env-vars CWYD_ORCHESTRATOR_NAME=langgraph` → new revision Running + Healthy, `/api/health` `pass`. Durable bicep fix pending (rename L1776 env var to `CWYD_ORCHESTRATOR_NAME`, value `databaseType == 'postgresql' ? 'langgraph' : 'agent_framework'`). **Fixed 2026-06-17.** |
| BUG-0065 | 2026-06-19 | 2026-06-19 | backend | high | fixed | The `langgraph` orchestrator returns no citations and the out-of-domain fallback for in-domain questions in **pgvector** mode, even when relevant chunks are indexed. Root cause: `langgraph` calls `self._search.search(query, top_k=..., use_semantic_search=...)` **without a `vector=`** ([orchestrators/langgraph.py](../src/backend/core/providers/orchestrators/langgraph.py)). `PgVector.search` ([providers/search/pgvector.py](../src/backend/core/providers/search/pgvector.py)) only runs dense cosine search when a `vector` is supplied; with none it falls back to Postgres FTS `to_tsvector('english',content) @@ plainto_tsquery('english',query)`, and `plainto_tsquery` **AND-joins every lexeme**, so a natural-language question (`"What is the Contoso remote work policy and how many days per week can I work remotely?"`) cannot be satisfied by any single small chunk → 0 rows → fallback. `AzureSearch.search` masks the same caller omission because its no-vector path is full-text **plus the Azure semantic re-ranker**, which still surfaces relevant chunks; pgvector has no semantic reranker, only AND-FTS. Confirmed live against the cloud pgvector store (6 indexed chunks, all `vector_dims=1536`): `plainto_tsquery` full question → **0**; `websearch_to_tsquery` full question → **0**; `plainto_tsquery('remote work')` → **4**. So ingestion + storage are correct; only the read-side query construction is wrong. Distinct from BUG-0028 (guardrail over-refusal — there retrieval succeeded). Fix direction: the chat retrieval path must embed the query (via the LLM/embedder seam) and pass `vector=` so `PgVector` does dense cosine retrieval (its intended mode per the provider docstring) — this also upgrades `AzureSearch` to true hybrid; keep the FTS branch only as a no-embedder fallback. Surfaced during local-against-cloud pgvector parity validation (worklog 2026-06-19). **Fixed 2026-06-19:** `LangGraphOrchestrator.run()` now embeds the latest user query via `self.llm.embed([query])` and passes `vector=` to `search(...)`, so `PgVector` runs dense cosine retrieval (and `AzureSearch` gets true hybrid scoring); the FTS branch stays only as a no-embedder fallback. Verified live against cloud pgvector — the same question now returns a grounded answer with an inline `[docN]` marker + a `Citation` (cosine score ~0.75) from the indexed policy doc; 20 `test_langgraph.py` unit tests + 174 orchestrator/pipeline/search tests green. |
| BUG-0066 | 2026-06-19 | 2026-06-19 | backend | high | fixed | The `agent_framework` orchestrator cannot ground on a **pgvector** deployment, so every chat is rejected at request time with HTTP 409 `ConfigResolutionError` (`reason=orchestrator_requires_azure_search`, ADR 0022). It retrieves only through a Foundry IQ Knowledge Base over an Azure AI Search index, and Foundry IQ has no PostgreSQL/pgvector knowledge-source type, so on pgvector it has zero retrieval and the guard blocks the pairing — violating the requirement that **both** orchestrators work on pgvector. Resolution accepted 2026-06-19: give `agent_framework` an app-side pgvector retrieval path (embed query → `search(query, vector=…)` → inject context → emit citations, reusing the BUG-0065 seam), relax the guard, and supersede ADR 0022 via ADR 0027; on pgvector `agent_framework` stops using Foundry IQ. Distinct from BUG-0064 (wrong default orchestrator). **Fixed 2026-06-19** — live-validated against the local stack on cloud pgvector: agent_framework returns 200 with a reasoning summary + pgvector dense-retrieval citations + a grounded [docN] answer; langgraph unchanged; switchable via admin. |
| BUG-0067 | 2026-06-19 | 2026-06-19 | frontend | high | fixed | The chat UI renders a **blank page** when `POST /api/conversation` returns a non-2xx status (observed: the BUG-0066 HTTP 409). A "Message failed — streamChat: SSE request failed with status 409" toast shows, then the view blanks instead of degrading to an inline error, and the structured `{error, reason}` body is never surfaced. `MessageInput` does catch the error and dispatch `set_error` (hence the toast), but the page still blanks — there is no React error boundary, and `streamChat` never parses the JSON error body. Resolution: parse the JSON error body in `streamChat`, render a handled inline error, and wrap the chat surface in an error boundary so no stream/render failure blanks the page. **Fixed 2026-06-19** — `streamChat` error-body parsing + the `ErrorBoundary` are unit-tested (566 frontend tests green); the 409 trigger itself is resolved by BUG-0066. |
| BUG-0068 | 2026-06-19 | 2026-06-19 | backend | medium | fixed | `GET /api/admin/status` reports `orchestrator_name` from the **env default** (`settings.orchestrator.name`), ignoring the persisted `RuntimeConfig` override, so after an operator saves a different orchestrator in the admin UI the status snapshot still shows the old value — even though the chat path and `GET /api/admin/config/effective` correctly apply the override. Same class as the admin config dropdown read-source defect (a read pointed at the env snapshot instead of the effective view), now on the status endpoint. Fix: `status_endpoint` overlays the override via `resolve_effective_config(settings, overrides)` (the single effective-config seam), so `/status` matches what the deployment runs immediately after a save and after a restart (the lifespan re-seeds `app.state.runtime_overrides` from the DB at boot). Persistence was never broken — the saved config is a DB row, not a file. |
| BUG-0069 | 2026-06-19 | 2026-06-22 | infra | high | fixed | A deployed **cloud** backend's `GET /api/admin/status` reports `"environment": "local"` instead of the real cloud environment (observed on a deployed Container App, alongside `app_insights_enabled: false` + `cors_origins: []`). `settings.environment` falls back to its `local` default because the environment setting is not wired onto the deployed runtime (same env-var-wiring class as BUG-0051/0052/0064). Security implication to verify: the admin auth local-dev bypass (BUG-0047) keys on `environment == "local"` — a missing Easy Auth claims blob then yields a synthetic `local-dev` admin, so a cloud runtime reporting `local` could grant admin without authentication. Fix direction: set the environment on the deployed backend (Bicep) so it reports the real environment, and confirm the admin gate fails closed in the cloud regardless. **Bicep wired 2026-06-22:** added `{ name: 'AZURE_ENVIRONMENT', value: 'production' }` to the backend Container App env block in [main.bicep](../infra/main.bicep) (with a security-rationale comment), so a deployed backend reports `production` and the local-dev admin bypass (`requires_role`, which returns the synthetic `local-dev` admin only when `environment == 'local'`) **fails closed** in the cloud; `az bicep build` clean. Status stays **open** pending cloud deploy + live verification. **Final fix (operator-directed) 2026-06-22:** the `AppSettings.environment` default stays **`local`** (dev-friendly); the fix is wiring `{ name: 'AZURE_ENVIRONMENT', value: 'production' }` onto **both** deployed runtimes in [main.bicep](../infra/main.bicep) (backend Container App + Function App), so the prod deployment flips the final config to `production` and the admin auth gate fails closed in the cloud (the local-dev bypass fires only when `environment == 'local'`). An earlier secure-by-default default-flip (`local` → `production` in [settings.py](../src/backend/core/settings.py)) was **reverted** per operator decision — keep the dev default, rely on the prod env var. `az bicep build` clean; backend **1096 passed**, pyright 0/0/0. **Marked fixed 2026-06-22** on the strength of the bicep wiring (`AZURE_ENVIRONMENT=production` on both deployed runtimes) + green build/tests; live confirmation (`/api/admin/status` → `production`; unauthenticated admin call → 401) to be observed on the next `azd deploy`. |
| BUG-0070 | 2026-06-19 | 2026-06-20 | frontend, backend | medium | fixed | The microphone (speech-to-text) button in the chat composer does not work. **Two root causes.** (1) **Frontend:** the speech REST client [api/speech.tsx](../src/frontend/src/api/speech.tsx) issued a **bare relative** `/api/speech` fetch — the only client not prepending `VITE_BACKEND_URL` — so in the deployed split-origin topology (per BUG-0050) it hit the frontend SPA catch-all (`index.html`, 200), `response.json()` threw, and the mic was disabled. (2) **Backend:** surfaced once (1) was fixed and local testing returned `502` — [core/speech.py](../src/backend/core/speech.py) tried to mint the token via the **`issueToken` STS exchange** with an AAD bearer (inherited from v1's RBAC branch), but `issueToken` is the **key**-exchange endpoint and does **not** support Entra/AAD — regional host → 400, custom-subdomain host → **401 PermissionDenied for >70 min** (not propagation; `Cognitive Services Speech User` doesn't authorize `issueToken`). The documented Entra-ID pattern is the SDK-native **`aad#{resourceId}#{aadToken}`** form (no `issueToken`). The hook, `MessageInput` wiring, RBAC, and `AZURE_SPEECH_*` backend env vars are all correct. **Fixed 2026-06-20:** (1) added the `apiUrl()` base-URL seam to `api/speech.tsx`; (2) rewrote `mint_speech_token` to return `aad#{account_resource_id}#{aad_token}` (no HTTP call); FE unchanged (its hook already passes the token to `fromAuthorizationToken`); router drops the `httpx` arm. Test-first both (FE **567 passed**; backend speech tests **7 passed**, pyright 0). **Verified end-to-end 2026-06-20:** `GET /api/speech` → 200 + `aad#` token, and the **browser mic captures and transcribes speech** in local dev — no propagation wait (Speech User authorizes recognition, where the SDK uses the token). Deployed env uses the same pattern (UAMI same role) → works on `azd deploy backend`. |
| BUG-0071 | 2026-06-19 | 2026-06-22 | frontend | low | fixed | The header brand showed a plain **"M"** glyph (off-brand, non-interactive). **Real root cause (found 2026-06-22):** Fluent v9 `Avatar` renders its `icon` slot *only when there are no initials* (`useAvatar.js`: `if (!initials && ...) { icon = ... }`), so the `name="Microsoft"` (later `name="Multi-agent"`) yielded the initial **"M"** and the `icon` logo was **never rendered**. Earlier glyph swaps (→`PeopleTeam`, →`MultiAgentLogo`) and a dev-server restart did not help because `name` kept suppressing the icon. **Fix:** removed the `name` prop from `<Avatar>` (mirrors the reference architecture, whose Avatar has no `name`) so the brand mark renders — a new [MultiAgentLogo.tsx](../src/frontend/src/components/Header/MultiAgentLogo.tsx) (the reference architecture's interlocking glyph, filled `--colorBrandForeground1`) inside `<Avatar shape="square" color="neutral">`, wrapped in a `<button data-testid="header-home" aria-label="Go to home">` → `navigate(SectionPath[Section.Chat])`. Test-first regression guard asserts the `MultiAgentLogo` svg (`viewBox="0 0 33 32"`) actually renders. Header **11 passed** + MultiAgentLogo **3 passed**, eslint clean; HMR applies it (no restart). Lesson: never pass `Avatar` a `name` when you want a custom logo to show. |
| BUG-0072 | 2026-06-19 | 2026-06-22 | frontend | low | fixed | The top-right of the header did not show the signed-in user's initials. **Fixed 2026-06-22:** added a circular **light-gray** (`color="neutral"`) Fluent `<Avatar>` after the theme button in [HeaderTools.tsx](../src/frontend/src/components/Header/HeaderTools.tsx) showing the user's **initials** (e.g. "John Doe" → "JD"), or **"G"** (name "Guest") when no user is signed in. Display-name/initials helpers (`resolveDisplayName` reads the Easy Auth `name`/`preferred_username`/email claims; `userInitials` mirrors the reference architecture's initials helper) live in a new [userIdentity.tsx](../src/frontend/src/components/Header/userIdentity.tsx) (non-component module → Fast-Refresh-clean). `userInfo` threaded through [Header.tsx](../src/frontend/src/components/Header/Header.tsx) → [App.tsx](../src/frontend/src/App.tsx) (`auth.userInfo` from `useAuth`). 9 new tests; FE suite **582 passed**, eslint clean, tsc clean. |
| BUG-0073 | 2026-06-19 | 2026-06-22 | backend | high | fixed | Deleting a document via the admin Delete Data UI did not remove **all** references — `delete_by_source` removed the index/pgvector chunks but the underlying Storage **blob** was not deleted, so the document stayed reachable via `GET /api/files/{filename}`. **Fixed 2026-06-22:** added `delete_document(filename,*,settings,credential)->bool` to [services/files.py](../src/backend/services/files.py) (`ContainerClient.delete_blob`; `ResourceNotFoundError`→`False` idempotent; other `AzureError`→log+reraise; `_validate_filename` rejects URL/path sources). Wired into [delete_document_endpoint](../src/backend/routers/admin.py) (added `SettingsDep`+`CredentialDep`): after `delete_by_source`, when `documents_container` is set, delete the blob (swallow `ValueError` for URL sources → `blob_deleted=False`); 404 only when **neither** an index chunk **nor** a blob existed. `DeleteDocumentResponse` gained `blob_deleted: bool` (default False, non-breaking). 134 admin+files tests, pyright 0/0/0, AST gates 355 passed. |
| BUG-0074 | 2026-06-19 | 2026-06-22 | backend | high | fixed | The admin **Add URL** flow did not ingest a URL's content. The old path (`ingest_url` → `add_url_handler`) ran fetch → parse → embed → write-index **inline in the backend container** (v1's `process_url_contents_directly` equivalent), which failed. **Fixed 2026-06-22 (operator-directed, v1-confirmed):** rewrote `ingest_url` to **download the URL → store it as a blob → reuse `upload_document`** (the same store→`batch_push` pipeline as file upload, mirroring v1's `download_url_and_upload_to_blob`). Added `_blob_name_for_url` (deterministic, flat, parseable name) in [ingestion.py](../src/backend/services/ingestion.py); reshaped `IngestUrlResponse` to the upload-style receipt (`url`/`filename`/`blob_path`/`ingestion_job_id`/`queued`; dropped `document_count`); FE [IngestData.tsx](../src/frontend/src/pages/admin/IngestData/IngestData.tsx) + [model](../src/frontend/src/models/admin.tsx) updated (fixes a `document_count` `TypeError` blank-page crash). **Plus v1 parser parity:** added a bs4 [HtmlParser](../src/functions/core/parsers/html_parser.py) (clean text extraction) + registered `md`/`json` on TextParser and `jpeg`/`jpg`/`png` on the DI parser, so v2 now covers all 9 v1 file types. 187 backend + 582 FE + 389 gate tests, pyright 0/0/0. |
| BUG-0075 | 2026-06-19 | 2026-06-22 | backend | low | fixed | The admin documents list shows a blank **last modified** date — `GET /api/admin/documents` returns `last_modified: null` for every source. `list_sources` does not populate `last_modified` (the search / pgvector provider returns null). Fix direction: surface a last-modified timestamp in `list_sources` (from the Storage blob `Last-Modified`, or an indexed ingest timestamp) so the column renders. **Fixed 2026-06-22 (the code landed + was verified live earlier this session; only this registry row was stale):** added a `last_modified TIMESTAMPTZ NOT NULL DEFAULT now()` column to the pgvector `documents` table — CREATE TABLE **plus** an idempotent `ALTER TABLE … ADD COLUMN IF NOT EXISTS` in `ensure_schema` so pre-existing tables backfill — set on insert and on `ON CONFLICT DO UPDATE`, and `list_sources` now selects `MAX(last_modified)` per source and emits it ISO-8601 ([pgvector.py](../src/backend/core/providers/search/pgvector.py)). Verified live: `GET /api/admin/documents` returns populated `last_modified`. Azure Search's facet path has no timestamp field, so it stays `None` there — out of scope for this pgvector deployment. |
| BUG-0076 | 2026-06-19 | 2026-06-23 | frontend | medium | fixed | The admin section is missing the dropdown of predefined **business-case prompts** (the v1 use-case presets — e.g. employee assistance, contract assistance — that let an operator pick a starting persona/prompt). Fix direction: add a business-cases prompt dropdown to the admin Configuration so the operator can select a predefined use-case prompt; pairs with the prompt-editor surface. **In progress 2026-06-22 ([ADR 0030](adr/0030-assistant-type-prompt-presets.md)):** Phase 0 (ADR) + Phase 1 (backend presets foundation — `assistant_presets.json` + `presets.py` loader with `AssistantType` enum; `CWYD_DEFAULT_BODY` moved into the JSON; adapted contract/employee bodies; populated post-answering defaults) landed (backend 1107 passed, pyright 0/0/0). Remaining: Phase 2 (config surface — `ai_assistant_type` + presets in the config response + reset), Phase 3 (frontend dropdown + tooltips), Phase 4 (verify + live). **Fixed 2026-06-23:** all phases landed — config surface (`ai_assistant_type` on `RuntimeConfig`/`AdminConfig`, `assistant_type_presets` on the effective-config response, reset restores defaults), the frontend assistant-type dropdown that loads the selected preset into the System prompt and populates post-answering defaults, and per-field hover tooltips. Verified green (backend pyright 0/0/0; frontend vitest 592 passed, tsc clean, build ok) and confirmed live by the operator (Save persona → chat round-trip → Reset). |
| BUG-0077 | 2026-06-20 | 2026-07-02 | functions | low | fixed | **Enhancement (not a defect):** auto-remove a document from the index when its blob is deleted, via the Event Grid `Microsoft.Storage.BlobDeleted` event. The B1 `blob_event` path ([ADR 0028](adr/0028-event-grid-single-trigger-blob-ingestion.md), BUG-0054) handles `BlobCreated` only, so a blob deleted directly from the `documents` container (bulk delete / Storage Explorer) leaves **stale index / pgvector chunks**. v1's `batch_push_results` handled this with a `BlobDeleted` branch (remove-from-index); v2 dropped it when porting. Fix direction: add `Microsoft.Storage.BlobDeleted` to the Event Grid subscription's `includedEventTypes`, and add a delete branch to the `blob_event` translator that maps the event `subject` → `delete_by_source` (reusing the existing `subject` parser + `ParsedBlobRef`). Complements BUG-0073 (admin delete leaves the blob) — this is the inverse direction (blob deleted → remove from index). Deferred follow-up, independent of the BUG-0054 bulk-upload work. **Phase 1 implemented + committed (`a214182`, 2026-06-22):** the `blob_event` path now classifies the event (`parse_blob_event` → `BlobEventType`) and dispatches create → ingest / delete → `delete_by_source` (handler `handle_blob_deleted`; the blueprint resolves the registry-first search provider, mirroring `batch_push`), and [main.bicep](../infra/main.bicep) adds `Microsoft.Storage.BlobDeleted` to `includedEventTypes` on **both** Event Grid subscriptions. **Phase 2 (deploy) deferred** behind BUG-0058 (prepackage) + BUG-0053 (always-ready instance for the queue trigger); status stays **open** until the function deploys and a live blob-delete is confirmed to drop its chunks. **Fixed 2026-07-02 (Phase 2 close-out):** the function redeployed today (during the BUG-0088 fix, `azd deploy function`) as a Container App with `minReplicas=1`, so the **BUG-0053** scale-from-zero blocker is moot; the **BUG-0058** prepackage blocker is also moot — the Container App function builds from source via `Dockerfile.functions` (no prepackage stage). Live-verified with a dedicated test: a `.txt` uploaded + indexed (`cwyd-index` 263 → 264, sentinel searchable), then the blob deleted **directly** from the `documents` container (the Storage-Explorer / bulk-delete scenario BUG-0077 targets) → the `BlobDeleted` event flowed `blob-events` → `blob_event` → `delete_by_source` and the chunk was removed (264 → 263, sentinel gone), zero poison. Also incidentally confirmed twice earlier the same day (the BUG-0054 and BUG-0088 blob-delete cleanups). |
| BUG-0078 | 2026-06-20 | 2026-06-20 | functions | medium | fixed | The prepackage script's `_FUNCTION_SUBPACKAGES` allow-list ([scripts/prepackage_function.py](../scripts/prepackage_function.py)) omitted the new `blob_event` blueprint, so `uv run python scripts/prepackage_function.py` staged only `add_url, batch_push, batch_start, search_skill, core` into `build-functions/` — the `blob_event` queue trigger was silently dropped from the deploy artifact. Effect: `azd deploy function` would ship a Function App **without** the `blob_event` handler while reporting SUCCESS, so the Event Grid → `blob-events` queue would never drain in cloud (the BUG-0054 path would appear deployed but be handler-less). Same "ships an incomplete artifact" class as BUG-0058 but a distinct root cause — the explicit allow-list itself was stale, not the hook firing. The list is intentionally explicit (not globbed) so a stray dir never ships; its inverse failure mode is a *new* blueprint being forgotten, which had no guard. Found while preparing the BUG-0054 cloud deploy (the staged-subpackages line printed no `blob_event`). **Fixed 2026-06-20:** added `blob_event` to `_FUNCTION_SUBPACKAGES`, and added a guard test [tests/functions/test_prepackage_subpackages.py](../tests/functions/test_prepackage_subpackages.py) asserting the allow-list equals the set of importable packages under `src/functions/` (set equality catches both a forgotten new blueprint AND a stale entry for a deleted one). Verified: prepackage now stages `blob_event/blueprint.py`; guard test green. |
| BUG-0079 | 2026-06-20 | 2026-06-22 | frontend | high | fixed | The frontend app does **not** typecheck or lint, so `npm run build` (`tsc -b && vite build`) — and therefore the `Dockerfile.frontend` production image build — fails. `npx tsc -p src/frontend` reports **TS2375** (`exactOptionalPropertyTypes`) at [src/frontend/src/api/streamChat.tsx](../src/frontend/src/api/streamChat.tsx) L160 (`{ error: string \| undefined; reason: string \| undefined }` assigned to `ConversationErrorBody`) and L261 (`{ status; reason: string \| undefined }` assigned to `StreamErrorDetail`) — the BUG-0067 error-body parsing code; `npm run lint` reports 4 errors (`@typescript-eslint/no-confusing-void-expression` in `CitationPanel.tsx` L87/L106 + `MessageList.tsx` L217; `prefer-nullish-coalescing` in `MessageList.tsx` L164). Root cause: the 2026-06-19 FE bug-fix work (BUG-0065/0066/0067) was committed without running the `tsc`/`lint` gates, which the FE CI workflow ([v2-frontend-checks.yml](../../.github/workflows/v2-frontend-checks.yml)) hard-gates — so a CI run on those paths would have failed. Surfaced 2026-06-20 while running the full FE gate suite during the test-tree relocation (ADR 0029); confirmed pre-existing and **unrelated** to that move (typescript `5.9.3` matches HEAD; `git diff` shows no `src/frontend/src` file changed by the move; the relocated Vitest suite is **566 passed**). Fix direction: make the `ConversationErrorBody` / `StreamErrorDetail` construction satisfy `exactOptionalPropertyTypes` (omit `undefined` keys rather than assigning `string \| undefined`), and resolve the 4 lint errors (brace the void-returning arrows; use `??`). Run `npx tsc -p src/frontend` + `npm run lint` green before closing. **Fixed 2026-06-22:** the 2 `TS2375` sites in [streamChat.tsx](../src/frontend/src/api/streamChat.tsx) now build `ConversationErrorBody` / `StreamErrorDetail` by **omitting** undefined keys (an absent optional property ≠ one set to `undefined`) instead of assigning `string | undefined`; the 4 lint errors fixed by bracing the three void-returning arrow shorthands ([CitationPanel.tsx](../src/frontend/src/pages/chat/components/CitationPanel/CitationPanel.tsx) L87/L106 + [MessageList.tsx](../src/frontend/src/pages/chat/components/MessageList.tsx) `onSelectCitation`) and making the reasoning guard strictly boolean (`m.reasoning !== undefined && …` so the `||` left operand is non-nullable — no `??` semantics change). `npx tsc -b` clean, `npm run lint` **0 errors** (10 pre-existing react-refresh warnings remain, non-failing), `npm run build` **succeeds**, **582 frontend tests pass**. |
| BUG-0080 | 2026-06-24 | 2026-06-24 | functions | high | fixed | The Flex Consumption function deploy never completed for ~2 days — every `func`/`azd` upload succeeded but the remote **Oryx** build sat at zero build steps and timed out; at the time this was misattributed to a "Flex build-service outage." **Real root cause:** `[project].dependencies` in [pyproject.toml](../pyproject.toml) pinned the **umbrella** meta-package `agent-framework==1.7.0`, which depends on `agent-framework-hyperlight` (env marker `python_version < "3.14"`); hyperlight requires `hyperlight-sandbox-backend-wasm<0.5,>=0.4.0`, **unresolvable** on the Functions host's **Python 3.11** runtime, so Oryx's `pip install` backtracks indefinitely and never emits a package. Local dev (Python 3.14) never reproduced it — the `< 3.14` marker excludes hyperlight there — which is why deps resolved locally but hung in cloud. The codebase only imports `agent_framework` (core) + `agent_framework_foundry`. **Fixed 2026-06-24:** repinned to `agent-framework-core==1.7.0` (kept `agent-framework-foundry==1.7.0`), proven by a clean install in the `mcr.microsoft.com/azure-functions/python:4-python3.11` container, and shipped via a pre-built `.python_packages` + `func azure functionapp publish --no-build --python` (`Skipping oryx build (remotebuild = false)`). Deploy succeeded in ~4.5 min: `blob_event` now live (6 functions), `released-package.zip` advanced 2026-06-17 → 2026-06-24, `/api/health` → `{"status":"ok"}`. The core repin also unblocks the standard remote-build path for the next `azd up`. Guard test [test_agent_framework_core_not_meta.py](../tests/functions/test_agent_framework_core_not_meta.py) asserts the umbrella never returns. Unblocks cloud verification of BUG-0054 / BUG-0055 / BUG-0058 / BUG-0077 (all were gated on this deploy). |
| BUG-0081 | 2026-06-24 | 2026-07-02 | infra | high | fixed | The **frontend never deploys via `azd deploy frontend`** — the App Service stays on the default placeholder image (`mcr.microsoft.com/appsvc/staticsite:latest`) even though `azd` reports SUCCESS. **Root cause:** `azure.yaml` declares the `frontend` service as `host: appservice` + `language: js` with a `docker:` block, but per the [azd schema](https://learn.microsoft.com/azure/developer/azure-developer-cli/azd-schema) the **`appservice` host does not support `docker`** ("Supports docker: N"; the block is only applicable to `containerapp`, `aks`, `ai.endpoint`, `azure.ai.agent`). So `azd` silently ignores the Dockerfile and **zip-deploys code** (`azd package frontend` → npm install + compress to .zip, ~18s, no image built/pushed), which is a no-op against the **container-kind** App Service that [main.bicep](../infra/main.bicep) provisions (`kind: 'app,linux,container'`, placeholder DOCKER image, UAMI for ACR pull). The two halves never agreed → the frontend has never been deployable as designed (ACR held only the backend image). **Mitigated 2026-06-24 (operator-endorsed manual container deploy):** built `docker/Dockerfile.frontend` (`--target prod`, `--build-arg VITE_BACKEND_URL=<backend-fqdn>`), pushed to ACR, `az webapp config container set` + enabled `acrUseManagedIdentityCreds` / `acrUserManagedIdentityID=<UAMI-client-id>`, restarted → the frontend now serves the real SPA (`HTTP 200`, `<title>CWYD v2</title>`, placeholder gone). **Durable fix (pending design decision, structural — Hard Rule #10):** either move the frontend to a **Container App** (like the backend — fully azd-supported container build/push) or switch the App Service to an azd **code/static deploy** model; today's manual `az webapp config container set` is reverted by the next `azd provision`. **Fixed 2026-07-02 (durable fix landed — Option A):** the frontend was migrated from an App Service to a **Container App** (`ca-frontend-<SUFFIX>`), the fully azd-supported container build/push path. Verified live: the RG has **no App Service** (`az webapp list` empty), and `ca-frontend-<SUFFIX>` runs a real ACR image (`cr<SUFFIX>` `cwyd-frontend:latest`, not the `staticsite` placeholder) deployed by `azd up`, serving the SPA (`GET /` → `200`, `<title>CWYD v2</title>`). The App Service + unsupported-`docker` mismatch that made the frontend undeployable is gone. |
| BUG-0082 | 2026-06-24 | 2026-07-03 | backend | medium | fixed | The backend Container App crash-loops (default startup probe fails, replica `Started: False`) whenever its **PostgreSQL Flexible Server is unreachable**. The FastAPI lifespan's first DB calls (`get_runtime_config` → `ensure_pool` → `ensure_schema`) have **no connect timeout**, so an unreachable server (auto-stopped Burstable instance, firewall drop) hangs the lifespan forever — the app never finishes startup and the probe restarts it indefinitely instead of failing fast or degrading. Console logs stop at uvicorn's `Waiting for application startup.` (the app's own log lines never flush — stdout is block-buffered in the container), which masks where it hangs. **Observed 2026-06-24:** the cloud backend was wedged because `psql-<SUFFIX>` had auto-stopped (Burstable, 7-day idle); `az postgres flexible-server start` + a revision restart brought it healthy (`/api/health` → `status: pass`). **Fix direction (pending):** add a bounded connect timeout to the asyncpg connect / pool acquisition and let lifespan fail fast (or mark the `database` check degraded) so a transient DB outage can't become a permanent crash-loop; add `PYTHONUNBUFFERED=1` to the backend image so startup logs flush. **Code fix landed 2026-07-03:** the bounded connect timeout shipped in [postgres.py](../src/backend/core/providers/databases/postgres.py) — `create_pool(timeout=_POOL_CONNECT_TIMEOUT_SECONDS=10.0)` with fail-fast re-raise + structured (host-only) logging; regression test `test_create_pool_uses_bounded_connect_timeout_and_fails_fast` (56 targeted tests + `pyright --strict` 0/0/0 green). `PYTHONUNBUFFERED=1` is already set in [Dockerfile.backend](../docker/Dockerfile.backend). **Live repro CONFIRMED 2026-07-03 (uksouth provision):** stopped `psql-<SUFFIX>` -> the backend crash-looped on a **bounded** cadence -- the lifespan traceback shows `_lifespan` (app.py:128 `get_runtime_config`) -> `_ensure_pool` -> `asyncpg.create_pool` -> asyncpg `compat.timeout(timeout)` -> **`TimeoutError`** -> `Application startup failed. Exiting.` (fail-fast at the 10 s bound; `CrashLoopBackOff`, restarts=5; no unbounded hang), then started the server -> the backend **recovered automatically** on the next cold-start (`/api/health` -> HTTP 200 `status: pass`, `database: pass`, ~18 s). Fix validated end-to-end; **fixed**. |
| BUG-0083 | 2026-06-24 | 2026-06-24 | frontend | low | fixed | The admin Configuration **Assistant type** dropdown renders **empty** instead of defaulting to the `default` preset. `configToForm` passed `ai_assistant_type` through verbatim, and the `select` renderer prepends any value not in its option set as a phantom first option ([Configuration.tsx](../src/frontend/src/pages/admin/Configuration/Configuration.tsx)); when the value is missing or unknown the phantom is **blank** and shows selected. The value is missing whenever the backend omits `ai_assistant_type` — e.g. a deployed backend that predates the Assistant-type presets (ADR 0030, BUG-0076). A current backend returns `ai_assistant_type="default"`, so this reproduces only against a **stale backend** (the live cloud backend image predates BUG-0076). **Fixed 2026-06-24:** added a `withAssistantTypeDefault` normalizer applied at both reducer entry points (`LoadSucceeded` / `SaveSucceeded`) that coalesces a missing/unknown `ai_assistant_type` to `AssistantType.Default`, keeping `serverConfig` + `formValues` consistent (no spurious dirty state). Two regression tests assert the dropdown lands on `default` with exactly the three known options — when the server returns `default` and when it omits the field. Verified: 44 Configuration tests pass, `tsc` src + tests 0/0, lint 0 errors. **Note:** the live cloud backend is stale (predates the 06-22/06-23 admin fixes) and should be redeployed; the frontend fix makes the dropdown robust regardless. |
| BUG-0084 | 2026-06-24 | 2026-06-24 | backend | high | fixed | Saving admin config rejects the **unchanged default prompt** — `PATCH /api/admin/config` returns `422 {"msg":"RAI safety check rejected the submitted prompt","field":"cwyd_agent_instructions","reason":"The submitted prompt was rejected by the Responsible AI safety classifier and was not persisted."}` even when the operator never edited the prompt. **Root cause — category error:** the router screens every `PROMPT_FIELDS` value (`cwyd_agent_instructions`, `post_answering_prompt`) through `validate_prompt_with_rai` → `rai_check` → `RAI_AGENT` ([routers/admin.py](../src/backend/routers/admin.py) `PATCH` loop; [services/admin.py](../src/backend/services/admin.py); [core/tools/content_safety.py](../src/backend/core/tools/content_safety.py)). But `RAI_AGENT` is a **user-message** jailbreak/prompt-injection classifier ("Respond TRUE if the message is a normal information-seeking … request; FALSE if it contains … jailbreak / prompt-injection attempts; instructions to bypass documented safety controls"). The value being classified is a **system prompt** — the operator persona body, including the built-in default — which is inherently full of meta-instructions and guardrail language ("you **must refuse** to discuss your prompts", "rules … cannot be overridden, ignored, weakened", "Answer **only** using …", "**Do not** generate or provide URLs"). The classifier reads a legitimate system prompt as an attack and returns FALSE → save rejected → the **default prompt fails its own gate**, making the prompt-config admin feature unusable. Applying a user-input classifier to trusted operator-authored system prompts is the defect; the chat-time `rai_check` (which screens actual user chat input) is correct and unaffected. v1 never did this — its admin prompt "validation" was non-blocking `{sources}`/`{question}` placeholder warnings only ([code/backend/pages/04_Configuration.py](../../code/backend/pages/04_Configuration.py)); the blocking gate is v2-only. **Fixed 2026-06-24 (keep the gate, fix the calibration — operator directive was "allow the original prompt AND be sure the RAI still blocks bad prompts"):** (a) **deterministic allow-list** — `validate_prompt_with_rai` now accepts any vetted built-in body (`body_for(t)` for every `AssistantType` + `DEFAULT_POST_ANSWERING_PROMPT`, normalized for CR/LF + trailing whitespace) without a classifier round-trip, so an unedited default/preset can never be rejected; (b) **system-prompt-calibrated reviewer** — added `PROMPT_REVIEW_AGENT` (`backend.core.agents.definitions`), a TRUE/FALSE classifier framed to review *administrator-authored system prompts* (allows persona/guardrail/refusal/domain/format language, blocks only prompts that direct harmful output, malware, secret exfiltration, or disabling the fixed guardrails); `rai_check` gained a `*, agent: AgentDefinition = RAI_AGENT` param (chat default unchanged) and the admin gate passes `PROMPT_REVIEW_AGENT`; custom prompts are still screened so a genuinely harmful operator prompt is blocked. The fixed `CWYD_GUARDRAIL` remains appended last to every override regardless. Tests: +8 (default/each-preset/default-post-answering accepted without classifier; custom prompt still screened; `rai_check` honors the `agent` arg; `PROMPT_REVIEW_AGENT` shape). Gates green: 1125 `tests/backend`, 764 shared invariant gates, pyright strict 0/0/0 on the three edited files. |
| BUG-0085 | 2026-06-25 | 2026-06-25 | infra | high | fixed | The **frontend App Service returns `503` (Application Error)** — the container exits during startup with **exit code 127** (`uvicorn: command not found`) and App Service reports `ContainerTimeout` after the 230s warm-up window. Found 2026-06-25 during cloud validation (operator hit `503` opening `https://app-frontend-<SUFFIX>.azurewebsites.net`). **Root cause:** the BUG-0081 build-from-source migration set the frontend App Service to `linuxFxVersion: PYTHON|3.11` + `appCommandLine: uvicorn frontend_app:app --host 0.0.0.0 --port 8000` and the packaging script [scripts/package_frontend.py](../scripts/package_frontend.py) stages a `requirements.txt` (`fastapi` + `uvicorn[standard]`) — but the frontend `appSettings` in [main.bicep](../infra/main.bicep) never set **`SCM_DO_BUILD_DURING_DEPLOYMENT=true`**. Without that flag App Service skips the Oryx `pip install` on zip-deploy, so `uvicorn`/`fastapi` are never installed; the start command then fails with exit 127 and the container never boots → `503`. `backend` (ACR image) and `function` (its own build path) are unaffected — only the frontend depends on the Oryx build. **Fixed 2026-06-25:** added `{ name: 'SCM_DO_BUILD_DURING_DEPLOYMENT', value: 'true' }` to the frontend `appSettings` in main.bicep (recompiled main.json), and applied the same flag to the live app (`az webapp config appsettings set`) then re-ran `azd deploy frontend` so Oryx installed the deps. Verified: the rebuilt container logged `Site startup probe succeeded` → `Site started` (the prior container failed the probe at 23.5s with exit 127), and `https://app-frontend-<SUFFIX>.azurewebsites.net` now returns `HTTP 302` to the Entra login (app up) instead of `503`. Follow-on to BUG-0081 — the build-from-source approach is correct; the missing build flag was the gap. |
| BUG-0086 | 2026-06-25 | 2026-06-25 | infra | high | fixed | The **backend Container App is down** — the latest revision shows `runningState: ActivationFailed` with 0 replicas, so every request times out (no replica becomes ready). Found 2026-06-25 (operator: "nothing is working"). **Root cause:** the FastAPI lifespan ([app.py](../src/backend/app.py) `_lifespan`) calls `database_client.get_runtime_config()` → Cosmos `read_item` at startup, but the request is rejected by the Cosmos firewall: `CosmosHttpResponseError (Forbidden) Request originated from IP <EGRESS_IP> through public internet. This is blocked by your Cosmos DB account firewall settings` → `Application startup failed. Exiting`. Cosmos is `publicNetworkAccess=Disabled` (private-endpoint-only) with **0 IP/VNet rules**, but the Container Apps environment is **not VNet-integrated** (`vnetConfiguration=null`, `enablePrivateNetworking=false`), so the backend egresses from public Azure IPs that Cosmos blocks. **Why it drifted:** the env **reuses pre-existing data resources** (`AZURE_ENV_EXISTING_COSMOS_NAME`, `..._STORAGE_NAME`, …); the Bicep Cosmos module is guarded `if (databaseType=='cosmosdb' && !useExistingCosmos)` ([main.bicep](../infra/main.bicep) L1333), so for a **reused** account it only adds the SQL db/container/role and **never manages `publicNetworkAccess`**. The reused Cosmos was left `Disabled` from a prior life, and the non-private-networking provision (which sets new accounts to `Enabled`, L1348) never re-opened it. The same drift left **Storage** private-only (breaks function ingestion); Search + OpenAI happened to be `Enabled` so they worked. **Fixed 2026-06-25:** `az cosmosdb update --public-network-access ENABLED --ip-range-filter 0.0.0.0` (the `0.0.0.0` rule = "Accept connections from within public Azure datacenters" — allows the Container Apps Azure-internal egress, still blocks the public internet), then restarted the backend revision. Verified `/api/health` → `HTTP 200 {status:pass, database=pass(cosmosdb), search=pass, foundry_iq=pass}`. **Durable fix (pending):** reused data resources aren't network-managed by the Bicep — either stop reusing (clear `AZURE_ENV_EXISTING_*` so the modules provision + manage network posture) or ensure reused accounts match the profile's posture (public + Azure-datacenters for non-VNet). Companion to the Storage instance of the same drift. |
| BUG-0087 | 2026-06-25 | 2026-06-25 | infra | medium | fixed | A **stale v1 Event Grid subscription** dead-letters a junk message on every blob upload. On the **reused** storage account's system topic (`evgt-<DATA_SUFFIX>`), a leftover subscription (`evts-<DATA_SUFFIX>`) delivered raw `Microsoft.Storage.BlobCreated`/`BlobDeleted` events **directly to the `doc-processing` queue** with plain (non-MI) delivery. `doc-processing` is consumed by `batch_push`, which expects a typed `BatchPushQueueMessage` JSON envelope — not a raw Event Grid event — so every delivered event failed to deserialize, retried 5×, and dead-lettered to `doc-processing-poison` (one junk poison message per uploaded blob). The **correct** v2 subscription (`cwyd2-blob-created-doc-processing` → `blob-events`, user-assigned MI delivery) already exists and works: the `blob_event` blueprint ([blueprint.py](../src/functions/blob_event/blueprint.py)) translates the event into a `BatchPushQueueMessage` and enqueues it to `doc-processing`. **Why it drifted:** same reused-resource cause as BUG-0086 — v2 Bicep manages the reused account's *correct* subscription but never removes a subscription left over from the account's prior life. **Fixed 2026-06-25:** `az eventgrid system-topic event-subscription delete --name evts-<DATA_SUFFIX> --system-topic-name evgt-<DATA_SUFFIX> -g <RESOURCE_GROUP>`, then cleared the 4 stale `doc-processing-poison` messages. Verified only `StorageAntimalwareSubscription` + `cwyd2-blob-created-doc-processing` remain and `doc-processing-poison` depth = 0. **Durable follow-up (not done):** reused data resources aren't fully network/config-managed by Bicep — either clear `AZURE_ENV_EXISTING_*` so the modules manage them, or document that reused accounts must match the profile's posture. |
| BUG-0088 | 2026-06-25 | 2026-07-02 | functions | high | fixed | A **`.docx` document fails to ingest** while PDFs ingest fine. Found 2026-06-25 during cloud ingestion validation: `MSFT_FY23Q4_10K.docx` (~1.1 MB, in the `documents` container) produced **0 chunks** in the index. `batch_push` received two valid `BatchPushQueueMessage` jobs for it (`{"container_name":"documents","filename":"MSFT_FY23Q4_10K.docx","force_reindex":false}`) and **both dead-lettered to `doc-processing-poison`** after the host's 5-retry policy. A PDF in the same batch (`Northwind_Standard_Benefits_Details.pdf`) ingested cleanly (+104 chunks), so the failure is specific to the `.docx` parse/embed/index path — not the queue wiring or message format. **No exception was captured** in Application Insights (`exceptions` / `AppTraces` return 0 rows): the backend reports `app_insights_enabled=false` and the function host is not emitting trace/exception telemetry to the workspace — an observability gap that hid the root cause. **Root cause: not yet determined.** Repro path: download the blob and run the v2 `.docx` parser under pytest to capture the real error, or re-trigger via `POST /api/admin/documents/reprocess`. **Root cause (confirmed 2026-07-02):** an **unbatched embedder** plus an **over-granular DI paragraph fallback**. The DOCX paragraph fallback (BUG-0049) emitted one chunk per Document Intelligence paragraph — 5047 for this 10-K — and `AzureOpenAIEmbedder.embed` sent all of them in one request, which Azure OpenAI rejects with `openai.BadRequestError: Invalid 'input': array length must be 2048 or less` → the queue handler throws → dead-letter. PDFs (~1 chunk/page) stay under the cap. The DI api-version (`2024-11-30`) + parse are correct (a local parse against the live endpoint produced 5047 chunks with no throw); the `openai.BadRequestError` is not an `AzureError`, so the embedder's narrow `except` never logged it — compounding the BUG-0055 telemetry gap. Both earlier hypotheses (stale image, DI api-version) were ruled out by the live+local evidence. **Fixed 2026-07-02 (two-part code fix + redeploy):** (1) `AzureOpenAIEmbedder.embed` now batches inputs into `_MAX_EMBED_INPUTS` (2048)-sized requests and concatenates the vectors; (2) the DI paragraph fallback groups consecutive `result.paragraphs` into `_FALLBACK_CHUNK_TARGET_CHARS` (2000)-char chunks (joined with a blank line; a single over-long paragraph stays whole), so the 10-K yields **198** chunks instead of 5047. Redeployed the function Container App (`azd deploy function`, 3m16s, no BUG-0080 hang) and live-verified: `MSFT_FY23Q4_10K.docx` ingests **198 chunks** (`cwyd-index` 263 → 461), zero poison; deleting the blob removed all 198 chunks (461 → 263) + the blob via the BlobDeleted path. 30 parser + 8 embedder tests, 1147 in the close-out sweep, pyright 0/0. **Status: fixed.** |
| BUG-0089 | 2026-06-25 | 2026-06-25 | backend | high | fixed | The **backend process reads `environment=local` despite `AZURE_ENVIRONMENT=production`** being present on the running Container App revision, which **activates the admin auth bypass** — `/api/admin/*` is reachable **unauthenticated** on the public backend FQDN. Found 2026-06-25. Evidence: `az containerapp show` confirms the traffic-serving revision (`weight:100`, created today) carries both `AZURE_ENVIRONMENT=production` **and** `AZURE_APP_INSIGHTS_CONNECTION_STRING`, yet `GET /api/health` returns `auth_enforced:false` and an **unauthenticated** `GET /api/admin/status` returns `200` with `environment:local`, `app_insights_enabled:false`. The local-dev bypass in `backend.dependencies.requires_role` returns the synthetic `local-dev` admin **only** when `AppSettings.environment == Environment.LOCAL` ([settings.py](../src/backend/core/settings.py)), so the in-process value reading `local` is what opens the gate. The same mis-read leaves `ObservabilitySettings.app_insights_connection_string` empty, so `configure_azure_monitor` is never called ([app.py](../src/backend/app.py)) — the reason BUG-0088 produced 0 telemetry. **Root cause (confirmed): a Container Apps revision-state anomaly, not a code defect.** The settings code is correct — a local repro with a clean process env returns `environment=production` + `app_insights_enabled=true`, and the deployed image (built today) contains that code (the `AZURE_ENVIRONMENT` read landed 2026-05-20). The app is in **Single**-revision mode but had **two** active revisions: the current revision (today's image, spec carries `AZURE_ENVIRONMENT=production`) was `Healthy`/`Provisioned` but **scaled to zero** (`running:Stopped`, 0 replicas), while the stale revision `0000012` (June image, created **before** the 2026-06-22 Bicep `AZURE_ENVIRONMENT` wiring, so its spec has **no** `AZURE_ENVIRONMENT`) kept a **running replica** and served all traffic as the de-facto fallback → it reads the `local` default → bypass on + telemetry off. Likely trigger: the current revision was created **before** the BUG-0086 Cosmos-firewall fix, so it crash-looped on the startup Cosmos call and never produced a serving replica; the platform kept the last-healthy old replica serving. **Fix (pending):** cut over to the current revision (now that Cosmos is reachable it should start cleanly) so it is the sole serving revision — via `azd deploy backend` for a clean single-revision cutover, or warm the current revision (`minReplicas≥1`) and verify `environment=production` **before** deactivating the stale revision. **Caveat (observed 2026-06-25):** deactivating the stale revision while the current is scaled-to-zero causes a brief outage — warm the current revision first. **Fixed (2026-06-25, Option B):** `azd deploy backend` (run from `v2/`) created a fresh revision (`azd-1782445947`) that starts cleanly now that Cosmos is reachable (the original crash-loop cause is gone), holds a **Running** replica, and is the **sole active revision** (clean Single-mode cutover; `0000012` + `azd-1782433210` deactivated). Verified: `/api/health` → `auth_enforced=true`, and an unauthenticated `GET /api/admin/status` now returns **401** (bypass closed). **Status: fixed.** |
| BUG-0090 | 2026-06-25 | 2026-07-03 | infra | high | fixed | The **production admin panel is unreachable** — `GET /api/admin/status` on the public backend FQDN returned `401 {"detail":"Missing client principal claims; Easy Auth claims header required."}` for every caller (unauthenticated *and* the signed-in SPA). Found 2026-06-25 immediately after BUG-0089 closed. **Root cause (corrected — the earlier "no Easy Auth identity source feeds the backend" analysis was a mis-diagnosis of the retired split-host App Service topology):** the 401 was the admin **role gate working as designed**. `backend.dependencies.requires_role("admin")` required the base64 `x-ms-client-principal` Easy Auth **claims** blob carrying an `admin` role, and fired only when `environment=production` **and** `require_admin_auth=true`; the SPA forwards only `x-ms-client-principal-id` (never the claims blob), so the claims check failed → 401. With the shipped defaults (`environment=local`, `require_admin_auth=false`) the admin routes were already open — the 401 surfaced only because the deployment set `AZURE_ENVIRONMENT=production` **and** `AZURE_REQUIRE_ADMIN_AUTH=true`; the endpoint was fine and the gate it carried was the defect. **Fix (delivered — marked fixed 2026-07-03; all local gates green, live deploy + `/api/admin/status` 200 check to be run at deploy time):** collapsed the backend to a single `get_user_id` dependency that validates the `x-ms-client-principal-id` header is a GUID and otherwise falls back to the anonymous default GUID `00000000-0000-0000-0000-000000000000` (never 401); swapped every `/api/admin/*` route to it; **deleted** the entire Easy Auth role gate (`requires_role`, `REQUIRE_ADMIN_USER`, `AdminUserIdDep`, the claims-blob decode + role-extraction helpers), the `require_admin_auth` setting, and the `AZURE_REQUIRE_ADMIN_AUTH` Bicep env var on `ca-backend-<SUFFIX>`. Identity enforcement, when enabled, is now an ingress/frontend concern (Easy Auth injecting/overwriting the header at the proxy), matching MACAE; the frontend already forwards the header, defaults to the guest GUID, and renders `G`, so no frontend change. **Security tradeoff:** `x-ms-client-principal-id` is client-forgeable, so admin reads **and** writes are open on the backend FQDN unless ingress-level auth is enabled — see [adr/0031-backend-admin-auth-header-only-ingress-enforced.md](adr/0031-backend-admin-auth-header-only-ingress-enforced.md) for the posture + revert path (re-add the role gate + `require_admin_auth` + the Bicep env var). **Status: open — awaiting Phase 6 live verification.** |
| BUG-0091 | 2026-06-29 | 2026-06-29 | frontend, backend | high | fixed | The `auth_enforced` health signal and the client-side `AuthBlocked` "Authentication Not Configured" screen were **removed**; the CWYD v2 SPA now resolves identity purely from Azure Easy Auth `/.auth/me` (principal → real `userId`, else the `DEFAULT_USER_ID` guest user) and never renders a blocking wall. **Root cause:** `auth_enforced` was a frontend-only UI flag — derived from the deployment's admin-auth posture and folded into `GET /api/health` (added under BUG-0046) — that made the SPA render a dead-end "Authentication Not Configured" screen whenever it read `auth_enforced=true` but `/.auth/me` returned no principal, instead of degrading to the guest user. On an open / partially-wired deploy (cf. BUG-0089's `environment` mis-read and BUG-0090's missing backend Easy Auth identity source) that wall could lock every caller out of an app that has no login configured at all. **Fix:** deleted the `auth_enforced` field from `HealthResponse` and its producer in [services/health.py](../src/backend/services/health.py); removed `AuthPhase.Blocked`, the `authEnforced` state, the `readAuthEnforced` helper, and the `AuthBlocked` component (`src/frontend/src/components/AuthBlocked/` deleted) from the SPA; the `App.tsx` bootstrap effect now calls `resolve(getUserInfo())` alone (principal → real id; none → guest) and never blocks. `require_admin_auth` (`AZURE_REQUIRE_ADMIN_AUTH`), `get_user_id`, and `requires_role` are **retained unchanged** as the orthogonal opt-in server-side admin-write gate (PD-01 → Scenario A). Supersedes the D2 / B1 / F9 / F10 / F11 portions of [frontend-user-identity-plan.md](frontend-user-identity-plan.md) (the per-user header-forwarding work from BUG-0046 is unchanged). Local gates green (backend `pytest` + `pyright --strict` 0/0/0; frontend `vitest` + `tsc` exit 0; shared AST gates); cloud verification complete 2026-06-29 (Phase 4) — live `/api/health` carries no `auth_enforced`, the deployed SPA renders no blocking wall, resolves the `DEFAULT_USER_ID` guest, and returns a grounded answer with citations. See [worklog/2026-06-29.md](worklog/2026-06-29.md). |
| BUG-0092 | 2026-06-29 | 2026-07-02 | frontend | high | fixed | The **chat History panel fails to load on the split-host cloud deploy** — opening History shows `Could not load history: Unexpected token '<', "<!doctype "... is not valid JSON`. **Root cause:** [HistoryPanel.tsx](../src/frontend/src/pages/chat/components/HistoryPanel.tsx) builds its request URL from the **build-time** `BACKEND_URL = import.meta.env.VITE_BACKEND_URL ?? ""`, which is empty in the deployed App Service build, so `GET`/`PATCH`/`DELETE` `/api/history/conversations[...]` are sent **relative** to the SPA's own App Service origin (`app-frontend-<SUFFIX>`) instead of the backend Container App. The App Service returns its SPA `index.html` (`<!doctype html>`) for the unknown `/api/*` path and `resp.json()` throws on the HTML body. Chat streaming and the health probe work because they resolve the origin at **runtime** via `loadRuntimeConfig()` → `getBackendUrl()` (the `/config` seam in [App.tsx](../src/frontend/src/App.tsx)); `HistoryPanel` never adopted that seam, and the transcript-rehydrate client [conversationHistory.tsx](../src/frontend/src/api/conversationHistory.tsx) already does. **Fix:** route `HistoryPanel`'s `buildUrl` through the runtime `getBackendUrl()` seam from [api/runtimeConfig.tsx](../src/frontend/src/api/runtimeConfig.tsx) like the rest of the SPA, dropping the build-time `VITE_BACKEND_URL` read. Observed 2026-06-29 during the Phase 4 cloud verification of BUG-0091; unrelated to the `auth_enforced` removal. **Fixed 2026-07-02:** `HistoryPanel.buildUrl` now calls the runtime `getBackendUrl()` seam at request time (the import-time `BACKEND_URL` constant removed), so history requests target the `/config`-resolved backend origin in the cloud and stay relative for the local Vite proxy. tsc clean + vitest green (added a runtime-`/config` prefix test). See [worklog/2026-07-02.md](worklog/2026-07-02.md). **Status: fixed.** |
| BUG-0093 | 2026-07-02 | 2026-07-02 | infra | blocker | fixed | The default public-profile `azd up` deploys a **non-functional backend**: the backend Container App crashes on startup with `azure.cosmos.exceptions.CosmosHttpResponseError: (Forbidden) Request originated from IP <CONTAINER_APP_EGRESS_IP> through public internet ... blocked by your Cosmos DB account firewall settings` → `ERROR: Application startup failed. Exiting.`, so `/api/health` never responds (curl times out after 90s, 0 bytes; the revision sits Healthy but `Running: Activating`). **Root cause:** the deployed **Cosmos DB** (`cosno-<SUFFIX>`) and **Storage** (`st<SUFFIX>`) accounts come up with `publicNetworkAccess: Disabled` even though `enablePrivateNetworking=false` (the default public profile has no VNet / private endpoints), so the public-egress Container Apps cannot reach them. The bicep *intends* `publicNetworkAccess: enablePrivateNetworking ? 'Disabled' : 'Enabled'` on both (Cosmos `networkRestrictions.publicNetworkAccess` + Storage `publicNetworkAccess`), and Search + AI Services correctly come up `Enabled`, but the AVM modules `avm/res/document-db/database-account:0.19.0` and `avm/res/storage/storage-account:0.32.0` apply `Disabled` regardless of the passed value. Distinct from BUG-0062 (which fixed Storage `networkAcls.defaultAction` Deny→Allow; this is the separate `publicNetworkAccess` switch, which BUG-0062 did not cover). **Fix:** force `publicNetworkAccess: Enabled` on Cosmos + Storage in the non-private-networking profile (verify the AVM param path, bump/override the module, or re-assert via post-provision). Immediate unblock (live hotfix): `az cosmosdb update -g <RESOURCE_GROUP> -n cosno-<SUFFIX> --public-network-access ENABLED` + `az storage account update -g <RESOURCE_GROUP> -n st<SUFFIX> --public-network-access Enabled`. Found 2026-07-02 during live review of the first `azd up` on the new tenant. **Hotfix APPLIED 2026-07-02:** set Cosmos + Storage `publicNetworkAccess` to `Enabled` via `az`, restarted the backend revision → `GET /api/health` + `/api/health/ready` return `200 {"status":"pass"}` (foundry_iq / database / search all pass) and the frontend serves `200`. The **durable bicep/AVM fix is still pending**, so a fresh `azd up` would re-introduce the lockout. **Durable fix (MACAE parity) 2026-07-02:** the bicep already matches MACAE (same `document-db` / `storage-account` AVM modules + `enablePrivateNetworking ? 'Disabled' : 'Enabled'` gating), so -- mirroring MACAE's `selecting_team_config_and_data.sh` `az` toggle -- [post_provision.py](../scripts/post_provision.py) now re-asserts `publicNetworkAccess: Enabled` on Cosmos + Storage after every provision via `_ensure_public_network_access` (runs before the backend deploys; no-op under private networking, where private endpoints cover access; data-plane auth stays RBAC-only, so opening the network exposes no key surface). Verified live end-to-end 2026-07-02: the re-assert ran and `az` reports Cosmos + Storage `publicNetworkAccess: Enabled`; backend `/api/health` + `/api/health/ready` `200 {"status":"pass"}`, grounded chat returns a `[doc1]` citation. 7 unit tests + AST gates green. See [worklog/2026-07-02.md](worklog/2026-07-02.md). **Status: fixed.** |
| BUG-0094 | 2026-07-02 | 2026-07-02 | frontend | medium | fixed | Inline document-citation markers render as **raw literal text** in the reasoning ("Thought process") panel instead of the compact superscript format used in the final answer -- the thinking text shows `doc[6]`, `[doc4]`, `docs[3] and [9]` as plain text. **Root cause:** [MessageList.tsx](../src/frontend/src/pages/chat/components/MessageList.tsx) renders the reasoning body via `<MarkdownContent content={formatReasoning(m.reasoning)} />` **without** the `enableSupersub` prop and **without** running `parseAnswer`, whereas the answer body renders `<MarkdownContent content={parsed.markdownText} enableSupersub />` which rewrites `[docN]` markers into visual `<sup>` superscripts (`parseAnswer` + `remark-supersub`, BUG-0016). So the reasoning channel keeps the model's raw inline references as plain text. The reasoning markers use the model's natural `doc[N]` / `docs[N]` / bare `[N]` phrasing (distinct from the answer's canonical `[docN]`), so the fix must normalize that shape before applying the superscript treatment. Distinct from BUG-0043 (which stripped native KB `【4:0†source】`-style markers from reasoning). Found 2026-07-02 on the live deployment. **Fix:** added a pure `superscriptReasoningCitations` helper in [reasoningText.tsx](../src/frontend/src/pages/chat/components/reasoningText.tsx) that normalizes the reasoning marker family (`[docN]`, `doc[N]`, `docs[N]`, bare `[N]`; 1-3 digits, so a 4-digit `[2026]` stays prose) into ` ^N^ ` `remark-supersub` tokens with the number verbatim (the reasoning panel is chain-of-thought, not a clickable citation surface, so no renumbering against `citations`). [MessageList.tsx](../src/frontend/src/pages/chat/components/MessageList.tsx) composes it over `formatReasoning` and sets `enableSupersub` on the reasoning `MarkdownContent`, so the panel shows the same tiny superscript numbers as the answer body. **Fixed 2026-07-02:** `npx tsc -b` clean + full frontend vitest green (45 files / 607 tests; +9 helper cases, +1 MessageList render test). See [worklog/2026-07-02.md](worklog/2026-07-02.md). **Status: fixed.** |
| BUG-0095 | 2026-07-02 | 2026-07-02 | frontend | high | fixed | The **"Open document" link in the citation detail panel (right column) is wrong and does not open the PDF** on the deployed app (observed opening `Northwind_Standard_Benefits_Details.pdf`). **Root cause:** [documentHref.tsx](../src/frontend/src/pages/chat/components/CitationDetailPanel/documentHref.tsx) builds the file route as `${VITE_BACKEND_URL}/api/files/<name>` reading the **build-time** `import.meta.env.VITE_BACKEND_URL`, which is **empty** in the deployed frontend Container App build (the containerization dropped the `VITE_BACKEND_URL` build-arg in favour of the runtime `/config` -> `getBackendUrl()` seam). So the link resolves **relative** to the frontend origin (`/api/files/<name>` -> the SPA catch-all returns `index.html`, not the PDF). Same class as BUG-0050 / BUG-0070 / BUG-0092 (build-time `VITE_BACKEND_URL` empty on the split-host deploy). Found 2026-07-02 on the live deployment. **Fix** (operator: open the document like CWYD v1): route `deriveDocumentHref` / `backendUrl()` through the **runtime** `getBackendUrl()` seam ([api/runtimeConfig.tsx](../src/frontend/src/api/runtimeConfig.tsx)) like the rest of the SPA, instead of the build-time `import.meta.env.VITE_BACKEND_URL`. **Fixed 2026-07-02:** `documentHref.tsx` now builds the file route from `getBackendUrl()` (the local `backendUrl()` reader removed), so the "Open document" link targets the `/config`-resolved backend origin in the cloud. tsc clean + vitest green (added a runtime-`/config` prefix test). See [worklog/2026-07-02.md](worklog/2026-07-02.md). **Status: fixed.** |
| BUG-0096 | 2026-07-03 | 2026-07-03 | functions | high | fixed | `resolve_search_provider` cleaned up + re-raised on resolution failure with `except BaseException`, which violates the exception-handling policy (the `test_no_silent_excepts` gate bans `except BaseException` because it also catches `KeyboardInterrupt`/`SystemExit`) and left `pytest tests` red. Pre-existing — committed with the v2 root reorg (`ad591ecd`), unrelated to the path migration; surfaced during the migration's Phase 8 validation. **Fixed 2026-07-03:** replaced the `try/except BaseException: <aclose provider, then pool>; raise` block with an equivalent `try/finally` guarded by a `resolved` success flag, so cleanup still fires on any early exit (error or cancellation) in provider-before-pool order and the in-flight exception still propagates. The dedicated cleanup tests (`test_ensure_schema_failure_closes_provider_and_pool_and_reraises`, `test_ensure_schema_failure_on_azure_search_closes_provider_only`) pass unchanged and the gate goes green. See [worklog/2026-07-03.md](worklog/2026-07-03.md). **Status: fixed.** |

## Details

### BUG-0001 — Embeddings routed to the project endpoint and missing `dimensions`

Area: backend. Severity: blocker. Status: fixed (found and fixed 2026-06-10).

Symptom: the `batch_push` ingestion pipeline could not produce a single embedded chunk. `FoundryIQ.embed()` returned `404`.

Root cause: `FoundryIQ.embed()` reused the project-scoped OpenAI client that is correct for chat and agents. The Foundry **project** route exposes no `embeddings` path, so every embeddings request returned `404`. The call also never passed `dimensions`, so a `text-embedding-3-large` deployment would emit 3072-dimension vectors against the 1536-dimension `content_vector` index field.

Fix: a new `FoundryIQ._get_embeddings_client()` targets the **account** endpoint (`AZURE_AI_SERVICES_ENDPOINT` plus `/openai/v1`), and `embed()` now passes `dimensions=settings.openai.embedding_dimensions`.

Why it was not caught earlier: `test_foundry_iq.py` fully mocks the OpenAI client, which hid the project-versus-account routing distinction.

References: [worklog/2026-06-10.md](worklog/2026-06-10.md); [development_plan.md](development_plan.md) §0.1 `INGEST-EMBED-DOCKEY`.

### BUG-0002 — Parser chunk id is an illegal Azure AI Search document key

Area: backend. Severity: blocker. Status: fixed (found and fixed 2026-06-10).

Symptom: after embeddings were fixed, the Azure AI Search push step failed with `InvalidDocumentKey`, so no document reached the index.

Root cause: parsers minted `Chunk.id` as `f"{source}__{index}"`. When `source` is a filename, its extension dot makes the key illegal. Azure AI Search document keys allow only letters, digits, `_`, `-`, and `=`.

Fix: a new Stable Core helper `BaseParser.make_chunk_id(source, index)` hashes the readable `f"{source}__{index}"` through SHA-256 into a key-safe hex digest (mirrors the v1 `source_document.py` hashing precedent). Both parsers call it. The readable name survives on `Chunk.source` and the Search `title` field, and the read-side `_to_result` already treats `id` as opaque.

Why it was not caught earlier: the parser tests asserted the raw `id` literal, which baked the invalid key charset into the expectation. A new `test_base.py` (5 tests) now asserts key safety with a charset regex against the helper contract instead of a magic string.

References: [worklog/2026-06-10.md](worklog/2026-06-10.md); [development_plan.md](development_plan.md) §0.1 `INGEST-EMBED-DOCKEY`.

### BUG-0003 — Admin documents endpoint returns 503 (`list_sources` throws an `AzureError` on the live index)

Area: backend. Severity: high. Status: fixed (found 2026-06-11; fixed 2026-06-14, verified live).

Symptom: `GET /api/admin/documents` returns `503 Service Unavailable` (body `{"detail":"Azure dependency temporarily unavailable."}`), so the admin Delete data grid cannot list indexed sources. Reproduced locally 2026-06-11 against the live cloud data plane **with search fully configured** (endpoint, index, and knowledge base all set, and the `search` readiness check passing) — the listing still 503s.

Root cause: `list_sources()` in `backend/core/providers/search/azure_search.py` issues `client.search(search_text="*", facets=["title,count:10000,sort:value"], top=0)` to bucket chunks by their `title` field, then reads `paged.get_facets()`. The configured index schema does not mark `title` as `facetable`, so Azure AI Search rejects the request with `HttpResponseError` — `(OperationNotAllowed) The field 'title' has not been marked as facetable in the schema` (`Code: FieldNotFacetable`). The SDK-boundary handler in `backend/routers/admin.py` maps that `AzureError` to `HTTPException(503)` per the resilience contract. The failure is independent of endpoint configuration; it fires whenever the index used by the deployment lacks a facetable `title` field. The earlier hypothesis (empty endpoint → `search_provider` is `None` → 503) is a separate latent pass-through path, not the cause of the observed 503.

Proposed fix (direction, to be settled in the fix turn): prefer a code-side fix in `list_sources()` so the endpoint is robust to existing index schemas — derive the source listing without faceting `title` (for example facet on a field declared `facetable`, or page documents and aggregate distinct sources client-side). Optionally, also mark the source-grouping field `facetable` in the index schema. Separately, decide whether the `search_provider is None` pass-through state should degrade gracefully (empty `ListDocumentsResponse` + a "search not configured" signal) instead of a hard `503`, and track that as its own concern.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md), [worklog/2026-06-14.md](worklog/2026-06-14.md).

Fix (2026-06-14, verified live): rewrote `list_sources()` in `backend/core/providers/search/azure_search.py` to stop faceting `title`. It now issues `client.search(search_text="*", select=["title"])`, pages the result set (the same async-iterator pattern `delete_by_source` uses), tallies a per-source chunk count into a `dict[str, int]` client-side, and returns `SourceListing`s sorted alphabetically by source. The `try/except AzureError` + structured `logger.exception` + re-raise contract is retained, so a genuine search outage still surfaces as 503 upstream. Tests in `tests/backend/core/providers/search/test_azure_search.py` were rewritten from the facet harness to the paging harness (29/29 green; `pyright --strict` 0/0/0).

Live confirmation: a throwaway probe ran the credential-selection path the backend lifespan uses (`select_default` → `AzureCliCredential` locally) against the live index. The new `list_sources()` returned 2 real sources; the original raw facet query (`facets=["title,count:10000,sort:value"]`) raised `HttpResponseError` 400 `(FieldNotFacetable) The field 'title' has not been marked as facetable in the schema`, confirming the recorded root cause. The earlier mock-only attempt appeared not to clear the live 503 only because the running `uvicorn` had been started **without** `--reload` and was still serving the pre-fix code (the stale worker's command line carried no `--reload` flag). After stopping that process and starting a fresh backend with the patched code, `GET /api/admin/documents` returns `200` with the source listing (`total: 2`). The separate latent `search_provider is None` pass-through (empty endpoint → hard 503) is unchanged and stays tracked as its own concern.

### BUG-0004 — Orchestrator field is free text instead of a dropdown

Area: frontend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: on the admin Configuration page the Orchestrator field is a single-line text input, so an operator can type an invalid key and save it.

Root cause: `orchestrator_name` is declared as a text field in the `FIELD_SPECS` array in `frontend/src/pages/admin/Configuration/Configuration.tsx`; there is no select control bound to the known orchestrator keys.

Proposed fix: render a dropdown sourced from the first-party orchestrator keys `langgraph` and `agent_framework` (the `OrchestratorName` enum in `backend/core/settings.py`, registered in `backend/core/providers/orchestrators/registry.py`). Decide in the fix turn whether to keep a free-text escape hatch for third-party registered keys, since the settings type is widened to `OrchestratorName | str`.

Fix: added an `OrchestratorName` closed-set map (`langgraph`, `agent_framework`) to `frontend/src/models/admin.tsx` mirroring the backend StrEnum, extended `FieldSpec` with a `select` kind plus an `options` list, and rendered the Orchestrator field as a Fluent UI `Select` dropdown. Escape-hatch decision: no free-text input is kept, but if the running config already holds a key outside the first-party set (allowed by the widened `OrchestratorName | str` backend type), that current value is added as an extra option so it stays selectable and is never silently dropped. The wire field `orchestrator_name` stays a plain `string`, and `validateField` now treats `select` as a closed choice. Covered by the Configuration Vitest suite (`src/tests/frontend/pages/admin/Configuration/Configuration.test.tsx`); full frontend suite green (371 tests).

Follow-up defect (persistence): after the dropdown shipped, selecting `agent_framework` and saving reported success but the field reverted to `langgraph` on reload. Root cause: the admin client `getAdminConfig()` in `frontend/src/api/admin.tsx` read `GET /api/admin/config` — the plain env-default snapshot that intentionally ignores overrides — instead of `GET /api/admin/config/effective`, the endpoint that overlays the persisted `RuntimeConfig` from `app.state.runtime_overrides`. The `PATCH /api/admin/config` write persisted and live-reloaded the override correctly; only the read-back used the wrong source, so every override field (the orchestrator was just the visible one) reverted on reload. Fix: pointed `getAdminConfig()` at `/api/admin/config/effective` and unwrapped the `values` payload, and added an `EffectiveAdminConfig` wire type to `frontend/src/models/admin.tsx` mirroring the backend model. The backend env-vs-effective endpoint split is intentional (backend tests pin `/api/admin/config` as the env-only snapshot), so no backend change was made. Covered by the admin client Vitest suite (`src/tests/frontend/api/admin.test.tsx`, `getAdminConfig` block now asserts the effective endpoint and the unwrapped override-resolved value); full frontend suite green (371 tests).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0005 — Admin configuration labels show internal config-key names

Area: frontend. Severity: low. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: every field label on the admin Configuration page appends the raw config key in parentheses, for example "Orchestrator (orchestrator_name)" and "Content safety (content_safety_enabled)".

Root cause: the label rendering in `frontend/src/pages/admin/Configuration/Configuration.tsx` appends each field spec's key to its human-readable label.

Proposed fix: render the human-readable label only and drop the parenthetical config-key suffix.

Fix: removed the `<span>` that rendered `({spec.key})` from the field label so it shows only the human-readable `spec.label`, and deleted the now-dead `.fieldName` CSS class. Added a test asserting rendered labels contain the human text and never the parenthetical config key.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0006 — Content Safety defaults to disabled

Area: backend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: Content Safety starts disabled by default, so the input pre-filter does not run unless an operator opts in.

Root cause: `ContentSafetySettings.enabled` defaults to `False` in `backend/core/settings.py`, a deliberate opt-in. Production infrastructure already sets `AZURE_CONTENT_SAFETY_ENABLED` to `true` in Bicep, and the lifespan activates the client only when both `enabled` is true and an endpoint is set; the shipped `.env.sample` leaves it `false`.

Proposed fix: change the default to `True`. This is safe because the lifespan gates the client on both `enabled` and a configured endpoint — with no endpoint the client stays `None` and the guard is inert. Confirm the intent in the fix turn before flipping the default.

Fix: flipped `ContentSafetySettings.enabled` to default `True` (secure-by-default) and set `.env.sample` `AZURE_CONTENT_SAFETY_ENABLED=true` (chosen with the operator). The lifespan gate still requires both `enabled` and a non-empty endpoint, so the new default is inert until `AZURE_CONTENT_SAFETY_ENDPOINT` is set; production (which already provisions the endpoint via Bicep) is unchanged. Updated the settings docstring, the `.env.sample` comment, and the two backend tests that pinned the old `False` default (`test_content_safety_settings_defaults_when_unset` now asserts `True`; `test_init_content_safety_client_returns_none_when_disabled` now opts out explicitly).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0007 — Default agent instructions do not carry the v1 default prompt text

Area: backend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: the v2 default system prompt does not reflect the vetted default answering prompt that v1 ships.

Root cause: the v1 default prompt text lives in `code/backend/batch/utilities/helpers/config/default.json` (the `answering_system_prompt` and `answering_user_prompt` keys). v2 defines its built-in instructions as `CWYD_AGENT` in `backend/core/agents/definitions.py` and never ported the v1 default text. The functional impact is that the prior minimal prompt never told the model the `[docN]` inline citation format, yet the v2 citation pipeline depends on it: the langgraph orchestrator injects a `Sources:` system message, the model is expected to emit `[docN]` markers, and `filter_to_referenced` (`backend/core/tools/citations.py`) extracts those markers into `citation` SSE events. Without the format instruction, citations were unreliable.

Fix: ported v1's `answering_system_prompt` into `CWYD_AGENT.instructions` as a strict near-verbatim port (Option A, chosen with the operator) -- kept the two code-generation lines and the "private model trained by Open AI" line, and reproduced v1's exact text (including its sub-bullet indentation and its original wording quirks) so the prompt stays the vetted, shipped text. The **only** adaptation was dropping v1's stale knowledge-cutoff line ("Your internal knowledge ... only current until ... 2021 ... The current date will be provided in the system message."), because v2 injects no current date. The v1 per-business-unit assistant-type system (Default / Contract assistant / Employee assistant) and the separate `answering_user_prompt` user-turn template remain out of scope. Updated the test (renamed `test_cwyd_agent_grounds_in_knowledge_base` -> `test_cwyd_agent_carries_vetted_v1_default_prompt`) to assert the ported content: grounds in "retrieved documents", carries the `[doc+index]` citation format, preserves the out-of-domain refusal string, and confirms the 2021 line is gone. `name`/`tools` assertions unchanged. 173 backend tests pass (agents + admin + orchestrators).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0008 — Separate Prompt editor page should fold into Configuration

Area: frontend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: v2 exposes a dedicated Prompt editor page, whereas v1 edits all prompts inline on a single Configuration page.

Root cause: the prompt-editor route was added during the `#35d` admin merge. `frontend/src/pages/admin/PromptEditor/PromptEditor.tsx` is wired through `frontend/src/models/sections.tsx` (the `AdminPrompt` section and its `/admin/prompt` route), `frontend/src/App.tsx` (the route element), `frontend/src/pages/admin/AdminLayout.tsx`, and the header nav — built as v1 parity, but v1 has no separate editor.

Fix: adopted the v1 structure — `cwyd_agent_instructions` is now a multi-line `kind: "text"` field with `allowEmpty: true` at the top of `FIELD_SPECS` on the Configuration page, rendered through the existing `<Textarea>` branch exactly like `post_answering_prompt`. It is added to `RAI_GUARDED_FIELDS`, so a 422 RAI rejection surfaces inline on the field row. v1 has no per-field "Reset to default" button; clearing the textarea and saving sends an empty string, which `_resolve_definition` (`backend/core/providers/agents/base.py`) already treats identically to `null` (both fall back to the built-in `CWYD_AGENT` default), so reset-to-default is preserved without extra `null` plumbing. Removed the standalone page and all its wiring: deleted `frontend/src/pages/admin/PromptEditor/` (page + CSS) and `tests/frontend/pages/admin/PromptEditor/`, dropped the `AdminPrompt` member + its `SectionPath` entry from `sections.tsx`, removed the `admin-subnav-prompt` nav item from `AdminLayout.tsx`, and removed the `PromptEditor` import + `/admin/prompt` route from `App.tsx`. The folded coverage lives in `tests/frontend/pages/admin/Configuration/Configuration.test.tsx` (textarea render, empty-allowed, PATCH, inline RAI rejection + clear). The wire models already declared the field across `AdminConfig` / `RuntimeConfig` / `AdminConfigPatch`, so no model or backend change was needed.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0009 — Log level field is free text instead of a dropdown

Area: frontend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: on the admin Configuration page the Log level field is a single-line text input, so an operator can type an arbitrary string and save it.

Root cause: `log_level` is declared with `kind: "text"` in the `FIELD_SPECS` array in `frontend/src/pages/admin/Configuration/Configuration.tsx`, so it renders through the text `<Input>` branch. The field hint even enumerates the closed set (`DEBUG`, `INFO`, `WARNING`, `ERROR`), but no select control is bound to it — the page imports only `Button`, `Input`, `Switch`, and `Textarea` from `@fluentui/react-components`.

Fix: rendered `log_level` as a dropdown over the closed set, reusing the same `kind: "select"` machinery as the orchestrator field (BUG-0004). Added a `LogLevel` closed-set const (`Debug`/`Info`/`Warning`/`Error` → `DEBUG`/`INFO`/`WARNING`/`ERROR`) to `frontend/src/models/admin.tsx`, mirroring the existing `OrchestratorName` `as const` + literal-union pattern; the wire field stays a plain `string` because the backend stores `log_level` as a free-form logging level name (`settings.observability.log_level: str = "INFO"`, no backend enum). Switched the `log_level` `FieldSpec` to `kind: "select"` with `options: [LogLevel.Debug, LogLevel.Info, LogLevel.Warning, LogLevel.Error]` and imported `LogLevel`. No change was needed to the select render branch, `selectOptions` derivation, `validateField` (select branch already present), `computePatch` (already sends `log_level` as a string), or `configToForm`. The pre-existing "empty text fields" validation test used `log_level` as its example non-empty text field; since `log_level` is now a select (and after BUG-0008 no `kind: "text"` field is non-`allowEmpty`), that test was retargeted to assert the select-cleared path (`must be selected`). Added two tests: a dropdown-render assertion (`SELECT` tag, options `["DEBUG","INFO","WARNING","ERROR"]`, value `INFO`) and a PATCH assertion (`patchAdminConfig` called with `{ log_level: "DEBUG" }`). Frontend suite green at 370 tests (31 files).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); related BUG-0004.

### BUG-0010 — Numeric config fields should validate numeric entry

Area: frontend. Severity: low. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom (as reported): numeric configuration values should have frontend validation that enforces a numeric entry.

Resolution: confirmed already-implemented and closed with a regression test; no production change was needed. The admin Configuration page is the only numeric-entry surface in the entire v2 frontend (a workspace search for `type="number"` / numeric input returns a single hit, in `frontend/src/pages/admin/Configuration/Configuration.tsx`). There the numeric fields (`openai_temperature`, `openai_max_tokens`, `search_top_k`) render through the `kind === "number"` branch as `<Input type="number">` with `step`, `min`, and `max`; `handleNumberChange` coerces an empty or non-parseable entry to `NaN`; `validateField` returns "must be a number" for a `NaN` or non-number value and enforces the min/max bounds; the error renders inline as `config-field-error-<key>` and `anyFieldInvalid` disables Save. The existing Vitest suite already covered the max-bound path (`openai_temperature` = `5` → "2 or less" + Save disabled) and the numeric PATCH delta, but had no test for the literal symptom (a non-numeric entry). Added one regression test — emptying `openai_max_tokens` surfaces "must be a number" and disables Save — pinning the enforce-numeric-entry behavior the bug asked for. Frontend suite green at 371 tests (31 files).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0011 — Authored prompt could supersede the system guardrail prompt

Area: backend. Severity: high. Status: fixed (found 2026-06-11, fixed 2026-06-11).

Symptom: an authored agent prompt could override the system's master ("uber") guardrail prompt — the authored text fully replaced the built-in safety, out-of-domain, and citation rules instead of being anchored to a fixed guardrail.

Root cause: `backend/core/providers/agents/base.py::_resolve_definition` applied the configured `cwyd_agent_instructions` with `definition.model_copy(update={"instructions": text})`, so the authored text fully **replaced** the `CWYD_AGENT` system instructions. Only `RAI_AGENT` and future safety surfaces were override-shielded (`if definition.name != CWYD_AGENT.name: return definition`), so an authored `CWYD_AGENT` prompt could supersede the intended system instructions.

Fix: extracted the non-negotiable safety / out-of-domain / citation rules into a single `CWYD_GUARDRAIL` constant in `backend/core/agents/definitions.py` and added `compose_cwyd_instructions(body)`, which appends the guardrail once, after `body`, so the fixed rules have last-word precedence (strongest position for resisting an override appearing in the body). Both the built-in default (`CWYD_AGENT.instructions = compose_cwyd_instructions(CWYD_DEFAULT_BODY)`) and the operator override path (`_resolve_definition` now returns `compose_cwyd_instructions(text)`) route through the same guardrail, so an authored prompt customizes the persona ahead of the guardrail but cannot supersede the fixed rules. To keep the prompt centralized and free of duplication, `CWYD_DEFAULT_BODY` was deduped against the guardrail (Tidy): every rule the guardrail owns (safety, the out-of-domain refusal message, the `[doc+index]` citation mechanics, the no-URLs rule, refuse-to-discuss-prompts, greetings-without-docs) was removed from the body, which now keeps only the unique persona / capabilities / formatting guidance and the in/out-of-domain decision methodology, deferring to the guardrail "below" for the rules it owns. Each rule therefore appears exactly once — no duplication or triplication. Regression coverage: `tests/backend/core/agents/test_definitions.py` (guardrail content + guardrail appended exactly once + default is guardrail-suffixed + each owned rule stated once) and `tests/backend/core/providers/agents/test_base.py` (a hostile override still resolves to instructions carrying the guardrail once, appended last).

Notes on the originally-recorded second facet: (a) RAI validation of the authored `cwyd_agent_instructions` on save was found to be **already in place** — `cwyd_agent_instructions` is in both the backend `PROMPT_FIELDS` (`backend/models/admin.py`) and the frontend `RAI_GUARDED_FIELDS`, so the PATCH handler RAI-screens it before persisting (wired when the prompt editor was folded into Configuration). The original root-cause note was stale on this point. (b) Whether end-user chat input should also be RAI-classified (in addition to the existing `ContentSafetyGuard` screen in `backend/app.py`) is a separate, larger design decision — it adds a Foundry round-trip to every chat turn — so it is intentionally **out of scope** here and is not bundled into this fix.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); related BUG-0006, BUG-0007.

### BUG-0012 — Robot avatar and Thinking panel are not aligned

Area: frontend. Severity: low. Status: fixed (found 2026-06-11; fixed 2026-06-11; refined 2026-06-11; operator-confirmed live 2026-06-14).

Symptom: in the chat transcript the assistant robot avatar icon and the Thinking (reasoning) panel do not line up.

Root cause: in `frontend/src/pages/chat/components/MessageList.module.css` the reasoning `<details>` panel (`.reasoning`) carried `margin: 0 0 0 36px`, indenting it into the bubble column (28px avatar + 8px gap = 36px gutter). The assistant avatar (`Bot20Regular`, inside `.row`) and the `<CitationPanel>` `<section>` rendered below it (`.section`, `margin-left: 0`) both sit at the list-item left edge, so the Thinking panel was the odd one out — offset right from the avatar and from the citation panel. (The originally recorded hypothesis was inverted: the panel was already offset to the bubble column, not starting at the list-item edge, so the original "align with the bubble column" proposal would have been a no-op.)

Fix: set `.reasoning { margin: 0 }` so the panel's left edge is flush with the list-item edge, lining up with the avatar and with the citation panel below it. CSS-only — no JSX change; `.row`, the reasoning `<details>`, and `<CitationPanel>` were already direct `<li>` children, so zeroing the offset gives all three a shared left origin. Added a structural regression test (`renders the reasoning panel and citation panel as list-item siblings of the message row`) asserting both decorations render as direct children of the message `<li>`. Vitest runs with `css: false`, so the margin value itself is not observable in jsdom; the test pins the DOM sibling contract the alignment depends on (re-nesting the panel inside `.row` would fail it).

Refinement (2026-06-11): the left-edge fix gave the three elements a shared left origin but kept the avatar on its own line above the Thinking panel — the avatar lives in `.row`, while the reasoning `<details>` rendered as a sibling `<li>` child below it. The operator asked for the avatar to sit on the *same horizontal line* as the Thinking panel. For an assistant message the reasoning `<details>`, the answer bubble, and the `<CitationPanel>` now stack in a vertical `.content` column rendered *inside* `.row` beside the avatar, so the avatar lines up with the column's first item (the Thinking panel while streaming, else the answer); `flex: 1 1 auto; min-width: 0` moved off the assistant `.bubble` onto `.content`. The user-message branch is unchanged. The regression test was retargeted from the sibling contract to the same-row contract — `renders the reasoning panel and citation panel inside the same row as the avatar` asserts both panels share the `.row` that holds the avatar and are no longer direct `<li>` children.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0013 — Thinking/reasoning feed never shows

Area: backend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-14).

Symptom: the Thinking/reasoning feed is never visible in the chat UI.

Root cause: the frontend reasoning panel in `frontend/src/pages/chat/components/MessageList.tsx` renders only while `m.streaming === true` or when `m.reasoning` has content, and its body is `m.reasoning.join("")`. The SSE handling in the chat input flow appends `reasoning` frames to `m.reasoning`, so the frontend is wired correctly — but the backend orchestrator emits no `reasoning`-channel SSE frames at runtime (the default `langgraph` orchestrator does not surface o-series reasoning), so `m.reasoning` stays empty and the panel collapses to nothing once streaming ends.

Proposed fix (direction): emit `reasoning`-channel `OrchestratorEvent`s from the orchestrator when a reasoning summary is available, so the existing frontend panel renders. Confirm in the fix turn which orchestrator(s) should surface reasoning and the source of the reasoning text.

Update (2026-06-14): the related BUG-0027 fix now emits an upfront `reasoning` frame ("Searching the knowledge base…") in the shared `run_chat` pipeline before the orchestrator runs, so the panel is no longer always empty — it shows that one canned narration during the retrieval wait. But no **substantive** model reasoning content is ever emitted: neither orchestrator surfaces an o-series reasoning summary or step-by-step thinking, so the panel shows only the canned narration, not the actual thinking process. The operator confirmed exactly this today while verifying BUG-0012 (alignment fixed; thinking-process *content* still missing). This bug remains the home for surfacing real reasoning content.

Confirmed root cause (2026-06-14, code read): the reasoning machinery is fully built end-to-end and correct — `FoundryIQ.reason()` uses the Responses API with `reasoning={"effort": "medium", "summary": "auto"}` and emits `reasoning`-channel events for `response.reasoning_summary_text.delta`; the `langgraph` orchestrator passes reasoning events through verbatim; the `agent_framework` orchestrator maps `text_reasoning` content blocks to `reasoning` events; the FE panel renders them. The reason no substantive content appears is that **the answer model never produces a reasoning summary on either live path**: (1) `langgraph` — `LangGraphOrchestrator.run()` calls `self.llm.complete()`, whose router delegates to `reason()` only when the chosen deployment equals `settings.openai.reasoning_deployment`; the answer always uses `gpt_deployment` (gpt-5.1), so `complete()` always routes to `chat()` (Chat Completions), which emits only `answer`. The `reason()` path (Responses API, summary deltas) is wired but never invoked for answering — the reasoning deployment is used only for KB query-planning. (2) `agent_framework` (the live default in cosmosdb mode) — `CWYD_AGENT` runs on `gpt_deployment` via the server-side Responses run, but the agent definition sets no reasoning effort/summary knob, so the run emits no `text_reasoning` summary blocks. Surfacing real reasoning is therefore a design decision (which path, and whether to change the answer model/API), not a mechanical one-method fix — pending operator direction.

Fix (2026-06-14): the answer deployment's reasoning capability is auto-detected — `BaseLLMProvider.supports_reasoning(deployment)` runs a cached one-shot Responses-API probe (a `reasoning` request against the chat deployment, classified by the 400 the Responses API returns for non-reasoning models) and both orchestrators consult it before answering. When the deployment supports reasoning: (1) `langgraph` — `FoundryIQ.complete()` routes to `reason()` (Responses API, `reasoning={"effort":"medium","summary":"auto"}`), which now builds its `input` via the new `_to_responses_input` helper that emits explicitly `message`-typed items (`{"type":"message","role":…,"content":…}`); the bare `{role,content}` dicts it sent before were rejected by the Responses API with `400 … param=input[N] … invalid_value` (empty `type`). (2) `agent_framework` — `run()` sets the Responses `reasoning` option on the agent run and, in reasoning mode, omits the `temperature`/`max_tokens` sampling knobs (see BUG-0035). Both paths emit substantive model reasoning on the existing `reasoning` SSE channel, which the FE panel already renders; the canned KB-search narration (BUG-0027) still leads during the retrieval wait. An earlier interim fix used an opt-in `AZURE_OPENAI_REASONING_SUMMARY` env flag (default off); it was removed the same day because it defaulted off and hid a capability the shipped `gpt-5.1` answer model has — auto-detection always lights up reasoning when the model supports it, with no operator action. Verified live against the deployment with a parametrized `tests/integration` test that forces each orchestrator past the persisted cosmos `langgraph` override: both `langgraph` and `agent_framework` stream non-empty, non-canned reasoning content (the live run also caught BUG-0035, the `agent_framework` temperature 400).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md), [worklog/2026-06-14.md](worklog/2026-06-14.md); related BUG-0026, BUG-0027, BUG-0035.

### BUG-0014 — Assistant response markdown is not converted to HTML

Area: frontend. Severity: medium. Status: fixed (2026-06-14, found 2026-06-11).

Symptom: assistant answers display as raw markdown (literal `**bold**`, list markers, headings, fenced code, tables) instead of rendered HTML.

Root cause: `frontend/src/pages/chat/components/answerTokens.tsx` (`renderAnswerTokens`) only tokenizes `[docN]` citation markers and otherwise pushes raw text slices as plain strings; it performs no markdown-to-HTML conversion, and the v2 frontend has no markdown renderer dependency. v1 renders answers through `react-markdown` (`code/frontend/src/components/Answer/Answer.tsx`).

Resolution (2026-06-14): added `frontend/src/pages/chat/components/MarkdownContent.tsx` — a reusable `{ content, className }` renderer wrapping `react-markdown` + `remark-gfm` (GFM tables/strikethrough) with a link override (`target=_blank` + `rel=noreferrer`) and **no** `rehype-raw`, so embedded raw HTML is escaped rather than mounted (XSS-safe). `MessageList` now renders both the assistant answer and the reasoning-panel body through `MarkdownContent` (the reasoning body still flows through `formatReasoning` first); user messages stay plain text. Scope decision (operator-approved Option A): the answer renders as pure markdown, so inline `[docN]` markers now appear as literal text — the clickable-citation behavior previously wired via `renderAnswerTokens` is intentionally deferred to BUG-0016 (which reworks inline references into v1-style superscript links). `renderAnswerTokens` and its tests are retained for that rework, and the panel stays manually expandable in the interim. Covered by `MarkdownContent.test.tsx` (9 cases incl. the raw-`<script>`-not-mounted XSS guard) and the updated `MessageList` answer-rendering tests; full FE suite 402/402, both FE workspaces typecheck clean.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); [worklog/2026-06-14.md](worklog/2026-06-14.md); related BUG-0016.

### BUG-0015 — Citation/reference block should look like v1

Area: frontend. Severity: low. Status: fixed (found 2026-06-11, fixed 2026-06-15).

Symptom: the reference block shown under an assistant answer does not match the v1 appearance.

Root cause: the v2 reference block is rendered by `frontend/src/pages/chat/components/CitationPanel/CitationPanel.tsx` with its own styling, which differs from the v1 citation presentation (`code/frontend/src/components/Answer/Answer.tsx` plus `Answer.module.css` `.citationContainer` and `.citation`).

Resolution (2026-06-15, operator-approved scope — reference-block restyle only; the v1 right-side citation detail column is out of scope): reworked `CitationPanel` into the v1 reference block — a collapsible toggle labeled `N references` / `1 reference` (singular at one) with a chevron (`ChevronRight16Regular` / `ChevronDown16Regular`), collapsed by default, over a numbered citation list. Each item shows a circular brand-colored number chip plus the citation title (falling back to `Citation N` when the title is empty); the score badge was dropped. Items still expand individually to a snippet and an XSS-safe `Open source` link (`target=_blank` + `rel=noopener noreferrer`). The inner list is mounted-but-`hidden` while collapsed (not conditionally unmounted), and a focused citation auto-opens the toggle and expands the matching item. `MessageList` now computes `parseAnswer(m.content, m.citations)` once and passes the renumbered referenced-citation subset to `CitationPanel` (falling back to `m.citations` when the answer has no markers), so the chip numbers stay coherent with the BUG-0016 answer superscripts and only cited sources appear. Covered by the rewritten `CitationPanel.test.tsx` (17 cases) and a new `MessageList` coherence test (out-of-order `[doc2] [doc1]` → chips numbered to match the renumbered superscripts); full FE suite 420/420, both FE workspaces typecheck clean.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0014, BUG-0016.

### BUG-0016 — Inline references should render like v1

Area: frontend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-15).

Symptom: inline citation markers inside the answer render as literal `[docN]` text; v1 shows them as compact superscript citation links.

Root cause: the answer body rendered the wire string as-is through `MarkdownContent`, so the `[docN]` markers the model emits stayed as literal bracket text. (The earlier `answerTokens.tsx` renderer had shown each marker as `<button>{raw}</button>` — still the literal bracket text — before BUG-0014 routed the answer through the markdown panel.)

Resolution: added `frontend/src/pages/chat/components/parseAnswer.tsx`, a pure transform that rewrites each in-bounds `[docN]` marker into a `^K^` superscript token, deduplicating repeated sources and renumbering them `1..K` in first-appearance order, collapsing consecutive duplicates, and leaving out-of-bounds markers verbatim. `MarkdownContent` gained an opt-in `enableSupersub` prop that adds `remark-supersub` to the remark pipeline (no `rehype-raw`, so rendering stays XSS-safe), turning `^K^` into `<sup>` nodes. `MessageList` routes the assistant answer body through `parseAnswer(...).markdownText` with `enableSupersub`; the superscripts are visual-only (v1-exact — the reference block in BUG-0015 is the click target). `CitationPanel` still consumes the unmodified `m.citations`; user messages and citation-less assistant answers keep `[docN]` verbatim. Covered by `parseAnswer.test.tsx` (13 cases) and two new `MarkdownContent.test.tsx` supersub cases; full FE suite 417/417, both FE workspaces typecheck clean. The now-unused `renderAnswerTokens` / `answerTokens.tsx` (+ test) are retained pending a separate removal decision.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0014, BUG-0015.

### BUG-0017 — New conversation is not saved to history

Area: backend. Severity: high. Status: fixed (found 2026-06-11, fixed 2026-06-15).

Symptom: when a user starts a conversation by sending a message, the conversation does not appear in the left-hand chat history column.

Root cause: `POST /api/conversation` in `backend/routers/conversation.py` runs the orchestrator and streams the answer but performs no chat-history writes. History persistence is entirely client-driven through the separate `/api/history/*` endpoints — the only create path is the explicit New chat button in `HistoryPanel` (`handleNew` → `POST /api/history/conversations`). So a conversation begun by simply typing a first message is never persisted server-side and never appears in the history list.

Proposed fix (direction): persist the conversation and its turns as part of the conversation flow — either server-side in the conversation router or by having the chat flow create and persist a conversation on the first message. Settle the exact persistence trigger and ownership in the fix turn.

Progress (2026-06-15): Phase 1 (backend persistence) complete and green. `POST /api/conversation` now persists every completed turn server-side via `services.conversation.persist_turn` — the user message, the assistant answer, and the answer's grounding citations (stored in the assistant message's `metadata["citations"]`) — keyed by the Easy Auth principal id, in both buffered and streaming modes. A new thread is titled with the first question; the streaming path returns the resolved id on a terminal `conversation` SSE control frame. Units U1 / U2a / U2b / U3a–U3d landed test-first with an end-to-end live integration test (`test_conversation_persists_citations_to_history_live`, green against the live env). **Stays open**: the user-visible fix needs Phase 2 (frontend) — `ChatContext` conversation-id tracking, parsing the terminal `conversation` frame, load-on-click rehydration in `HistoryPanel`, and the new-chat / hidden-db-name UX (folds BUG-0018 / BUG-0019). BUG-0017 flips to fixed when Phase 1 + 2 both land.

Resolution (2026-06-15): Phase 2 (frontend bridge) is now complete and green, so BUG-0017 is fixed. The frontend half landed test-first: `ChatContext` tracks the `conversationId` (U4); `streamChat` parses the terminal `conversation` SSE control frame and surfaces the resolved id (U5); `MessageInput` sends the tracked id and folds the resolved id back into context on the next turn (U6); selecting a history row fetches the stored conversation detail and rehydrates the full transcript plus persisted citations (U7a / U7b); the New chat button was removed from the history column in favor of the existing header affordance (U9, BUG-0019); and the raw backend db-name display was removed from the history header (U10, BUG-0018). With Phase 1 (backend persistence, validated by the live integration test) and Phase 2 both landed, a conversation begun by simply sending a first message is persisted server-side and appears in the left history column. Follow-on work tracked separately and **not** part of this bug's symptom: Phase 3 (rename validation + RAI screening, U11–U12) and Phase 4 (cloud identity / per-user history isolation, U13–U15).

References: [worklog/2026-06-11.md](worklog/2026-06-11.md), [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0018, BUG-0019.

### BUG-0018 — Chat history column shows the backend database name

Area: frontend. Severity: low. Status: fixed (found 2026-06-11, fixed 2026-06-15).

Symptom: the chat history left column header shows `backend: <db_type>` — the configured chat-history database discriminator.

Root cause: `frontend/src/pages/chat/components/HistoryPanel.tsx` renders `<p data-testid="history-db-type">backend: {status.db_type}</p>` from the `/api/history/status` response in the panel header.

Fix (2026-06-15): removed the `history-db-type` display from the panel header. Since the panel fetched `/api/history/status` solely to surface that discriminator (it never branched on it — Hard Rule #4), the now-orphaned status fetch, the `status` React state, and the `HistoryStatus` interface were removed too; the panel now mounts by loading only `/api/history/conversations`. Test-first: the mount test was updated to assert the panel loads the conversations list *without* calling `/api/history/status`, plus a negative test that `history-db-type` is never rendered.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md), [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0017.

### BUG-0019 — Remove the New chat section from the history column

Area: frontend. Severity: low. Status: fixed (found 2026-06-11, fixed 2026-06-15).

Symptom: the chat history left column contains a New chat section that should be removed.

Root cause: `frontend/src/pages/chat/components/HistoryPanel.tsx` renders a New chat button (`data-testid="history-new"`) wired to `handleNew` (`POST /api/history/conversations`). Related New-chat wiring exists outside the panel — the header `HeaderTools` `onNewChat` control and the `newChatNonce` flow in `App.tsx`.

Fix (2026-06-15): removed the `history-new` button and the `handleNew` callback from `HistoryPanel`, along with the now-unused `Add16Regular` icon import. Scope decision: the header New-chat affordance (`HeaderTools` `data-testid="header-new-chat"` → `App.tsx` `newChatNonce` → `<ChatPage key={newChatNonce} />` remount) is the working way to start a fresh chat — it remounts `ChatPage`, re-initializing `ChatContext` to `initialChatState` (empty transcript, `conversationId: null`). Because the bug is scoped to the history left column and removing the header control too would leave no way to start a new chat, the header affordance and `newChatNonce` wiring are intentionally kept. Test-first: the obsolete "creates a new conversation" `HistoryPanel` test was replaced with a negative assertion that the `history-new` button is not rendered.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md); [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0017.

### BUG-0020 — KB MCP connect is unauthenticated (401 on chat)

Area: backend. Severity: high. Status: fixed (found and fixed 2026-06-11).

Symptom: sending a chat message on a Foundry IQ deployment failed with `Message failed ('Failed to enter context manager.', ExceptionGroup('unhandled errors in a TaskGroup', [HTTPStatusError("Client error '401 Unauthorized' for url 'https://srch-<SUFFIX>.search.windows.net/knowledgebases/cwyd-kb/mcp?api-version=2025-11-01-preview'")]))`.

Root cause: the `agent_framework` orchestrator (`backend/core/providers/orchestrators/agent_framework.py`) attached the Search data-plane bearer to the Knowledge Base MCP tool through the SDK's `header_provider` hook only. The Agent Framework SDK applies that hook exclusively inside `MCPStreamableHTTPTool.call_tool()` (it sets a `contextvars.ContextVar` that an httpx request event hook reads). The MCP session-initialize connect handshake runs earlier — when the tool's async context manager is entered (`MCPTool.__aenter__` → `connect()`), before any tool call — so the connect request carried no `Authorization` header. Azure AI Search rejected the unauthenticated connect with `401`, and the SDK re-raised it as `ToolExecutionException("Failed to enter context manager.")`. The `/health/ready` search probe stayed green because it exercises the normal data-plane index credential, a different surface from the KB MCP connect.

Fix: `run()` now constructs an `httpx.AsyncClient` carrying the same bearer as a default `Authorization` header and passes it to `_build_kb_tool(..., http_client=...)`, which forwards it to `MCPStreamableHTTPTool(http_client=...)`. httpx applies default headers to every request, so the connect handshake is authenticated as well as tool calls. The `header_provider` hook is retained (belt-and-suspenders for tool calls; the same bearer merges idempotently). Because the MCP streamable-HTTP transport never closes a caller-supplied client (`mcp.client.streamable_http.streamable_http_client` only manages clients it creates), `run()` owns the client lifecycle and closes it in a `finally` that covers both the streaming path and an agent-construction failure. Cross-checked against the read-only reference architecture, whose one bearer-attached MCP client path sets `headers["Authorization"]` as a default header on the httpx client (`src/backend/v4/common/services/mcp_service.py`), confirming the connection-level default-header approach.

Why it was not caught earlier: this was the first live end-to-end exercise of the agent path against a real Search service; orchestrator unit tests fully fake the FoundryAgent and never enter the MCP tool's context manager, so the connect handshake was never exercised. New tests assert the KB tool is built with a default-`Authorization`-header client and that the client is closed after a normal run and after an agent-construction failure.

References: [worklog/2026-06-11.md](worklog/2026-06-11.md).

### BUG-0021 — `agent_framework` chat 404s: persistent-assistant vs name-referenced Foundry agent mismatch

Area: backend. Severity: blocker. Status: fixed (found 2026-06-11, fixed 2026-06-12).

Symptom: every `POST /api/conversation` on the default `agent_framework` orchestrator returns `500` (buffered) / an `error` SSE frame (streaming). Backend log: `ChatClientException: ... 404 - Agent 'cwyd' not found`. The failure surfaces in the chat-completion stream (`agent_framework_openai/_chat_client.py`), i.e. *after* `_prepare_run_context` — so it is downstream of, and distinct from, the BUG-0020 MCP-connect 401 (which is fixed).

Root cause: two different Foundry agent surfaces are crossed.

- Provisioning (`BaseAgentsProvider.get_or_create_agent` → `FoundryAgentsProvider`) calls `azure.ai.agents.aio.AgentsClient.create_agent(...)`, which creates an **Assistants-API persistent agent** identified by an `asst_` id. Confirmed live: the Foundry project holds exactly one agent — an `asst_`-prefixed id with `name='cwyd'` and `model='gpt-5.1'`, i.e. an Assistants-API persistent agent.
- Invocation (`AgentFrameworkOrchestrator` → `agent_framework_foundry.FoundryAgent(agent_name="cwyd", allow_preview=True)`) references the agent through the **Responses API by name** (`get_openai_client(agent_name=...)` / `agent_reference {"name": "cwyd"}`). That name-reference resolves a Foundry *named/prompt* agent resource, not an Assistants-API `asst_` persistent agent, so Foundry returns 404.
- The conversation router compounds the gap: it calls `get_or_create_agent(CWYD_AGENT, db)` but **discards the returned `asst_` id** and passes the friendly `CWYD_AGENT.name` ("cwyd") to the orchestrator.

Contributing context: the project endpoint was recently switched to a different Foundry account/project (per `.env`). In the new project only the persistent-assistant create path has run, so no name-referenceable agent resource exists.

Fix direction (recommended — structural, Hard Rule #10, needs operator consent): align the two halves of the decoupled-hybrid (dev_plan §0.1 `B1-MAF-MISLABEL` / `B-IMPL-AUDIT`) on the **name-referenced Prompt Agent** resource type. Switch `FoundryAgentsProvider`'s create path from `azure.ai.agents.AgentsClient.create_agent` (which mints an Assistants-API `asst_` agent) to the Foundry **Prompt Agent** path (`PromptAgentDefinition` + `create_version` on the projects SDK), so a name(+version)-resolvable `cwyd` agent exists for the orchestrator's `FoundryAgent(agent_name=..., agent_version=...)` invocation. This is exactly the alternative the dev_plan **B-Spike** flagged (`PromptAgentDefinition` + `create_version` vs `AgentsClient.create_agent`) but never switched.

Rejected alternatives: (a) invoke the existing `asst_` agent by id — the OSS `FoundryAgent` exposes no `agent_id` parameter (name + optional version only, for Prompt/Hosted agents) and no `agent_framework` persistent-agent (Azure AI Agent Service threads/runs) client is installed; (b) toggling `allow_preview` does not change the resource type the reference resolves — both code paths reference a Prompt/Hosted agent by name.

Why it was not caught earlier: `test_agent_framework.py` injects a fake agent factory, so the persistent-assistant-vs-name-reference distinction is never exercised against the live Responses API. The "Thinking" panel appears as soon as the SSE stream opens, so a screenshot showing "Thinking" does not prove the agent actually ran.

Resolution (2026-06-12, [ADR 0025](adr/0025-foundry-prompt-agent-ga-pattern.md)): fixed via the Foundry **Prompt Agent GA pattern** — exactly the recommended direction above. `FoundryAgentsProvider.get_or_create_agent` now builds a `PromptAgentDefinition` and calls `AIProjectClient.agents.create_version(agent_name=...)` (the `asst_` `AgentsClient.create_agent` path is removed), persisting and returning the agent **name**; a cold start validates the name with `agents.get` (`ResourceNotFoundError` → recreate) and a concurrent-create `409` re-reads and reuses the existing agent. Invocation no longer uses the by-name `FoundryAgent` Responses path: the orchestrator composes a client-side `agent_framework.Agent` over `FoundryChatClient` through the shared `build_agent` seam and drives it as an async context manager. KB grounding moved server-side (`MCPTool(project_connection_id=...)`), which also retired the BUG-0020 client-side bearer / `httpx` machinery. Both halves now reference one name-addressed Prompt Agent, so the resource-type mismatch cannot recur. Landed across units U1–U8 + U10 (RAI migrated to the same seam in U8); ADR 0025 carries the full decision record and the multi-agent coordinator readiness note.

References: [adr/0025-foundry-prompt-agent-ga-pattern.md](adr/0025-foundry-prompt-agent-ga-pattern.md); [worklog/2026-06-11.md](worklog/2026-06-11.md); [worklog/2026-06-12.md](worklog/2026-06-12.md).

### BUG-0022 — Operator-authored system prompt dropped on the `agent_framework` chat path

Area: backend. Severity: medium. Status: fixed (found 2026-06-11, fixed 2026-06-12).

Symptom: surfaced by audit during the BUG-0021 fix planning (GAP-2), not a separate live repro — BUG-0021's `404` masked the chat path entirely. On the default `agent_framework` orchestrator, an operator-authored instruction override (the admin `cwyd_agent_instructions` in `RuntimeConfig`) could fail to reach the model: the saved prompt would silently not take effect, while the same override applied correctly on the `langgraph` path.

Root cause: the conversation router threads the effective instructions to every orchestrator uniformly as `system_prompt=effective.cwyd_agent_instructions`. `langgraph` consumes it (injects it as the leading system message). The `agent_framework` orchestrator swallowed `system_prompt` via `**_extras` and invoked the agent **by name** (`FoundryAgent(agent_name="cwyd")`), which resolves the server-side agent's instructions — baked once at create time and cached by identity ([ADR 0008](adr/0008-lazy-foundry-agent-bootstrap.md)). So neither the per-request effective instructions nor a later admin prompt edit propagated to the default path; the operator prompt lever worked only on `langgraph`.

Fix: the GA pattern ([ADR 0025](adr/0025-foundry-prompt-agent-ga-pattern.md), unit U5) makes instruction resolution a per-request provider concern. `FoundryAgentsProvider.build_agent` calls `_resolve_definition(definition)` — which reads the current `RuntimeConfig` override (`overrides.cwyd_agent_instructions`, `CWYD_AGENT` only; `RAI_AGENT` is intentionally not operator-editable so the classifier prompt cannot be weakened) and wraps it via `compose_cwyd_instructions` so the fixed `CWYD_GUARDRAIL` keeps last-word precedence (the BUG-0011 invariant) — then composes the client-side `agent_framework.Agent(instructions=resolved.instructions)`. Because the runtime `Agent` is built with the resolved instructions on every request, the authored prompt now applies on the `agent_framework` path too. The orchestrator still swallows the router's `system_prompt` kwarg by design — instructions are resolved in the provider, not threaded by the orchestrator — so that kwarg is intentionally inert on this path.

References: [adr/0025-foundry-prompt-agent-ga-pattern.md](adr/0025-foundry-prompt-agent-ga-pattern.md); [worklog/2026-06-12.md](worklog/2026-06-12.md).

### BUG-0023 — Knowledge-base seed wires the o-series reasoning model; Foundry IQ rejects it

Area: infra. Severity: high. Status: fixed (found and fixed 2026-06-12).

Symptom: `post_provision` (the azd `postprovision` hook) fails when seeding the Foundry IQ knowledge base in `cosmosdb` / AzureSearch mode. The knowledge *source* PUT succeeds, then the knowledge *base* PUT returns `400 Bad Request`: `Unsupported model type in Knowledge Base Model Configuration` -- the API accepts only chat models from a fixed allow-list (the gpt-5.x family, e.g. gpt-5, gpt-5-mini, gpt-5-nano, gpt-5.1, gpt-5.2, gpt-5.4, gpt-5.4-mini, gpt-5.4-nano, gpt-5.5). So the knowledge base is never created and the `agent_framework` orchestrator has nothing to ground against.

Root cause: `_ensure_knowledge_base` sourced the knowledge-base query-planning model from the **reasoning** deployment (`AZURE_OPENAI_REASONING_DEPLOYMENT`, an o-series model such as `o4-mini`). The Foundry IQ knowledge-base model configuration only accepts **chat** models from a fixed allow-list (the 400 lists chat-model families such as the gpt-5.x family); o-series reasoning models are not accepted, so the PUT 400s. This affected every deployment, not just one environment — no path through the seed would pass with the reasoning deployment.

Why it was not caught earlier: the unit tests for `_build_knowledge_base_seed` / `_ensure_knowledge_base` used `"gpt-5"` as the model name (a value that happens to be on the allow-list), so they asserted the seed *shape* but never exercised the real reasoning-deployment value. The seed had also never been run end-to-end against a live knowledge-base API on the current project.

Fix: source the knowledge-base query-planning model from the **chat** deployment (`AZURE_OPENAI_GPT_DEPLOYMENT`, e.g. `gpt-5.1`), with an optional `AZURE_OPENAI_GPT_MODEL_NAME` override (the deployment id doubles as the model name when unset, matching how Bicep names the deployment after `gptModelName`). Renamed the `_build_knowledge_base_seed` parameters `reasoning_deployment` / `reasoning_model_name` to `query_planning_deployment` / `query_planning_model_name`, changed the guard + error message to require `AZURE_OPENAI_GPT_DEPLOYMENT`, and updated the comments / docstrings to record that Foundry IQ rejects o-series models for the KB model. Tests now set both the chat and reasoning deployments and assert the knowledge base uses the chat one (regression guard). Verified live: re-running the seed reports `knowledge base 'cwyd-kb' ready`.

References: [worklog/2026-06-12.md](worklog/2026-06-12.md).

### BUG-0024 — Search service identity lacks OpenAI User, so the knowledge-base query-planning model call 401s

Area: infra. Severity: high. Status: fixed (found and fixed 2026-06-12).

Symptom: on the `agent_framework` / Foundry IQ path (`cosmosdb` mode), a chat such as "tell me about employee benefits" returns the out-of-domain fallback (`The requested information is not available in the retrieved data...`) with **no `tool`, `reasoning`, or `citation` SSE frames** — even though the chat index contains relevant content (a direct index search for "employee benefits" returns `Benefit_Options.pdf` and `employee_handbook.pdf`). The backend logs no error: the chat request returns `200` and the agent simply produces the grounding refusal.

Root cause: the Foundry IQ knowledge base cannot call its query-planning chat model. Calling the knowledge-base retrieve endpoint directly reproduces the failure server-side: `Could not complete model action. The model endpoint returned status code '401' (Unauthorized). The principal <search-service-MI-principal-id> lacks the required data action Microsoft.CognitiveServices/accounts/OpenAI/deployments/chat/completions/action`. The knowledge-base model configuration has `authIdentity: null`, which means the knowledge base authenticates the model call with the **search service's system-assigned managed identity**. That identity has **no role assignments** on the AI Services account (`az role assignment list --assignee <search-MI> --scope <ai-services-account>` returns `[]`), so the `chat/completions` data action is denied and the model call 401s. The knowledge base then returns no grounded results, and the agent (correctly, given empty grounding) answers with the out-of-domain fallback. This is environment-independent: the v2 Bicep (`infra/main.bicep`) grants `Cognitive Services OpenAI User` (`5e0bd9bd-7b93-4f28-af87-19fc36ad61bd`) and `Azure AI User` to the **UAMI** on the AI Services account, but never grants any inference role to the **search service identity**, so every Foundry IQ deployment hits this once the knowledge base is exercised.

Why it was not caught earlier: this is the first end-to-end exercise of knowledge-base retrieval against a live AI Services account on the current project. The earlier `agent_framework` bugs (`BUG-0020` MCP-connect `401`, `BUG-0021` agent-resource `404`) blocked the path upstream of retrieval, and orchestrator unit tests fake the agent + knowledge base entirely, so the knowledge-base → model auth hop was never exercised. The model 401 happens server-side inside Foundry IQ and is not surfaced as a backend error — the agent receives empty grounding and refuses, which looks like a prompt over-refusal but is not.

Fix direction: grant the search service's system-assigned managed identity the `Cognitive Services OpenAI User` role (`5e0bd9bd-7b93-4f28-af87-19fc36ad61bd`) on the AI Services account — both live (`az role assignment create`) to unblock the running environment, and durably in `infra/main.bicep` (add a role assignment with the search service principal id as assignee on the AI Services account, mirroring the existing UAMI grants) so every `azd up` wires it. The reuse-existing-OpenAI path needs the same grant against the reused account. Alternative (not chosen): set the knowledge-base model `authIdentity` to a user-assigned identity that already holds the role — rejected because the search service has no suitable user-assigned identity attached, so the role grant on the system-assigned identity is the smaller, standard change.

Resolution (2026-06-12): granted the search service system-assigned MI `Cognitive Services OpenAI User` on the AI Services account, both live (to unblock the running environment) and durably in `infra/main.bicep` (`searchOpenAiUserOnFoundry`, with `searchOpenAiUserOnReusedOpenAi` covering the reuse-existing-OpenAI path). Verified live: a direct knowledge-base `/retrieve` now returns grounded `Benefit_Options.pdf` content, so the inner KB → query-planning-model call no longer 401s. Note: this resolves only the *inner* (KB → model) hop; the end-to-end agent chat remains blocked by BUG-0025 — the *outer* agent → KB-MCP-endpoint hop 401s under the project connection identity.

References: [worklog/2026-06-12.md](worklog/2026-06-12.md); related BUG-0020, BUG-0021, BUG-0023, BUG-0025.

### BUG-0025 — Server-side KB MCP call 401s under the project search connection identity

Area: infra. Severity: high. Status: fixed (found 2026-06-12, fixed 2026-06-12).

Symptom: on the `agent_framework` / Foundry IQ path (`cosmosdb` mode), after BUG-0024's search-MI → OpenAI User grant made the knowledge base's *inner* query-planning model call succeed (a direct REST `/retrieve` with an operator token now returns grounded `Benefit_Options.pdf` content), the end-to-end chat through the backend still returns the out-of-domain fallback with **no `tool`, `reasoning`, or `citation` SSE frames** and **no backend error** (`200 OK`). A standalone orchestrator diagnostic that reproduces the production wiring (credential → FoundryIQ llm → Foundry agents provider → cosmosdb client → `agent_framework` orchestrator) with `openai` + `agent_framework` DEBUG logging exposes the real failure in the Responses-API event stream:

```text
McpListTools(server_label='cwyd-kb', tools=[], type='mcp_list_tools', error=None)
ResponseMcpListToolsInProgressEvent(...)
ChatClientException / openai.APIError: Authentication failed when connecting to the MCP server:
  https://srch-<SUFFIX>.search.windows.net:443/knowledgebases/cwyd-kb/mcp:
  Response status code does not indicate success: 401 (Unauthorized). Verify your authentication headers.
```

The Responses API attempts `mcp_list_tools` against the KB MCP endpoint, gets `401`, and the agent ends up with an empty tool list (`tools=[]`) → no grounding → the out-of-domain refusal. In the live backend the orchestrator's `run()` catches only `AzureError`, so this `ChatClientException` / `openai.APIError` does not surface as a clean SSE `error` frame — it manifests as the refusal answer.

Root cause: the KB retrieval tool is attached **server-side**. `AgentFrameworkOrchestrator._build_kb_tool()` returns `MCPTool(server_url=…/knowledgebases/cwyd-kb/mcp, require_approval="never", allowed_tools=["knowledge_base_retrieve"], project_connection_id=<search connection>)`, serialized via `.as_dict()` into the agent's `extra_tools`. The Responses API therefore opens the MCP connection **itself**, under the *project search connection's* identity (`search-srch-<SUFFIX>`, category `CognitiveSearch`, `authType AAD`, no explicit managed-identity binding — the live connection reports `useWorkspaceManagedIdentity: false`, and `ai-project-search-connection.bicep` creates it with bare `authType: 'AAD'`). That identity is not accepted by the search MCP endpoint: the search service runs `aadOrApiKey` auth with `aadAuthFailureMode: http401WithBearerChallenge`, where a *missing/invalid* token returns `401` (authentication) and an *under-privileged* token returns `403` (authorization). The `401` therefore means no valid bearer reaches the endpoint — the runtime is not presenting the project managed identity's token. The **project** MI *does* hold `Search Index Data Reader` on the search service (sufficient per the Foundry RBAC "retrieve from a knowledge base" row, and assigned 7 days earlier so propagation is ruled out), but it is not the identity the server-side MCP path presents.

This is distinct from BUG-0020 and BUG-0024:

- BUG-0020 fixed the **SDK-side** (client-side) MCP connect via the Agent Framework `header_provider` / default-`Authorization` httpx client, minting the bearer from the *backend's* own credential. The GA pattern (ADR 0025, BUG-0021/0022) moved MCP execution **server-side** (`project_connection_id`), so that hook no longer runs — the connect is now authenticated by Foundry under the connection identity, not by our process.
- BUG-0024 was the KB's **inner** call (knowledge base → query-planning model), authenticated by the *search service* MI. BUG-0025 is the **outer** call (agent → KB MCP endpoint), authenticated by the *project connection* identity. BUG-0024's verification used a direct `/retrieve` with an operator token for the outer call, so it exercised only the inner hop and never surfaced this outer-hop `401`.

Why it was not caught earlier: orchestrator unit tests fake the agent + Responses API entirely, so the server-side `mcp_list_tools` connect is never exercised; this is the first end-to-end agent run against the live KB MCP endpoint with the project connection identity, and the failure is swallowed (the agent refuses rather than erroring).

Root cause confirmed against the read-only reference architecture (whose server-side KB MCP grounding works): CWYD points the `MCPTool` at the **wrong category of connection**. The KB MCP tool's `project_connection_id` must reference a dedicated **`RemoteTool` / `ProjectManagedIdentity`** connection that targets the KB MCP URL and pins the token audience — not the general-purpose `CognitiveSearch` / `AAD` connection CWYD reuses. The reference architecture creates one connection per KB at post-deploy via `PUT …/connections/{kb}-mcp?api-version=2025-04-01-preview` with this payload:

```json
{ "properties": {
    "category": "RemoteTool",
    "target": "https://srch-<SUFFIX>.search.windows.net/knowledgebases/<kb>/mcp?api-version=2025-11-01-preview",
    "authType": "ProjectManagedIdentity",
    "useWorkspaceManagedIdentity": true,
    "isSharedToAll": true,
    "audience": "https://search.azure.com",
    "metadata": { "ApiType": "Azure" } } }
```

The explicit `audience: https://search.azure.com` is the linchpin: it makes Agent Service mint a **project-MI** token for the **search** audience and present it to the MCP endpoint. CWYD's `CognitiveSearch` / bare-`AAD` connection (`useWorkspaceManagedIdentity: false`, no `audience`) mints no usable token for the generic server-side MCP path, so the endpoint sees no valid bearer → `401`, `mcp_list_tools` returns `tools=[]`, and the agent refuses. The reference architecture keeps a `CognitiveSearch` connection too, but only as the *base* search connection — and even that sets `useWorkspaceManagedIdentity: true` (`modules/aifp-connections.bicep`); the KB tool uses the separate `RemoteTool` connection. The reference architecture also grants the project MI **both** `Search Index Data Reader` **and** `Search Service Contributor` on the search service (CWYD granted only Reader), and grants the search MI `Cognitive Services OpenAI User` on the AI Services account — identical to CWYD's BUG-0024 grant, which is the part CWYD already matches.

Fix (applied + verified live 2026-06-12, operator-approved): (1) created a `cwyd-kb-mcp` `RemoteTool` / `ProjectManagedIdentity` connection on the project using the reference architecture's payload (`category: RemoteTool`, `authType: ProjectManagedIdentity`, `useWorkspaceManagedIdentity: true`, `isSharedToAll: true`, `audience: https://search.azure.com`, `target` = the `cwyd-kb` MCP URL, `metadata.ApiType: Azure`) — GET-verified all six properties; (2) pointed `AZURE_AI_SEARCH_CONNECTION_NAME` at `cwyd-kb-mcp` (the gitignored `.env`); (3) granted the project MI `Search Service Contributor` on the search service so it now holds **both** `Search Index Data Reader` and `Search Service Contributor` (matching the reference architecture). Verified by re-running the standalone orchestrator diagnostic against the live environment: the Responses-API stream now shows `mcp_call_arguments.delta` (real `knowledge_base_retrieve` queries) followed by `response.mcp_call.completed` (no `error`), and the orchestrator emits two `citation` frames plus a grounded `answer` about Contoso Electronics benefits (Northwind Health Plus / Standard) with `【N:1†source】` citation markers — **no `401`, no `tools=[]`, no out-of-domain fallback**. This confirms the `agent_framework`/Foundry IQ retrieval path now grounds. **End-to-end verified:** a first live `/api/conversation` test still returned the fallback — not a BUG-0025 regression, but because the persisted `RuntimeConfig` pinned `orchestrator_name='langgraph'`, which `resolve_effective_config` overlays *on top of* the env's `CWYD_ORCHESTRATOR_NAME=agent_framework`, so the backend dispatched to `langgraph` (client-side search + strict-prompt over-refusal) and never exercised the fixed path. After flipping the override to `agent_framework` via `PATCH /api/admin/config` (live-reload, no restart), the same query returned a **4608-byte grounded answer** with two `mcp://searchindex/…` `citation` frames and `【6:0†source】` markers about the Northwind Health Plus / Standard plans — so the conversation regression is **fixed end-to-end through the real request path**. The earlier Option B test (granting the account/workspace MI `Search Index Data Reader`) is confirmed **not** the fix — the bare-`AAD` connection with `useWorkspaceManagedIdentity: false` never presents that identity (re-tested `401` ~5 min post-grant) — and that spurious grant is being reverted. Still to do (tracked): make the connection + project-MI `Search Service Contributor` durable in `infra/` (a Bicep `RemoteTool` connection resource — CWYD's KB name `cwyd-kb` is static, unlike the reference architecture's dynamic content-pack KBs, so a seed script is unnecessary). Separately consider broadening the orchestrator's `run()` except so an MCP `ChatClientException` / `openai.APIError` surfaces as a clean SSE `error` frame (tracked separately; Hard Rule #12).

References: [worklog/2026-06-12.md](worklog/2026-06-12.md); related BUG-0020, BUG-0024; [adr/0025-foundry-prompt-agent-ga-pattern.md](adr/0025-foundry-prompt-agent-ga-pattern.md).

### BUG-0026 — agent_framework KB narration keys on `function_call`, but server-side MCP surfaces as `mcp_server_tool_call`

Area: backend. Severity: medium. Status: fixed (found and fixed 2026-06-12).

Symptom: on the `agent_framework` / Foundry IQ path the grounded SSE stream carries only `citation` + `answer` frames — the FE "thinking" panel stays empty even though a knowledge-base retrieval clearly ran (citations are present). The earlier `e65bf826` "Narrate KB search before tool events" change was meant to populate the panel but has no visible effect on the live server-side path.

Root cause: `AgentFrameworkOrchestrator._update_to_events` maps each streamed content block to SSE events. The KB-search reasoning narration (`_KB_SEARCH_NARRATION`) and the `tool` event are emitted only inside the `ctype == "function_call"` branch. After the GA-FULL migration (`8f83ad69`, "Use build_agent seam and server-side KB tool") KB retrieval is attached as a server-side `MCPTool(project_connection_id=…)` and runs inside the Responses API. The `agent_framework_openai` chat client maps that server-side call to a `Content.from_mcp_server_tool_call(...)` block whose `.type` is **`mcp_server_tool_call`** (tool name on `.tool_name`, not `.name`) — not `function_call`. The orchestrator has no branch for that content type, so it falls through: no narration, no tool event. The narration added later (`e65bf826`) inherited the same `function_call`-only condition, so it never fired on the live server-side path. This is the concrete mechanism behind BUG-0013's empty-panel symptom for the `agent_framework` + KB path.

Fix: broaden the `function_call` branch in `_update_to_events` to also handle `mcp_server_tool_call`, resolving the tool name from either `.name` (client-side function tools) or `.tool_name` (server-side MCP). Both paths then drive the same `_KB_SEARCH_NARRATION` reasoning event + `tool` event, so the FE thinking panel populates whenever KB retrieval runs, regardless of which retrieval transport executed.

References: [worklog/2026-06-12.md](worklog/2026-06-12.md); BUG-0013 (umbrella symptom); BUG-0025 (the infra fix that made server-side retrieval actually run, which exposed this). SDK evidence: `agent_framework_openai/_chat_client.py` streaming `response.output_item.added` → `case "mcp_call"` → `Content.from_mcp_server_tool_call(...)`; `agent_framework/_types.py` content-type literal `"mcp_server_tool_call"`.

### BUG-0027 — KB-search narration shows only at the end of the wait, not during it (and not at all on `langgraph`)

Area: backend. Severity: medium. Status: fixed (found 2026-06-12; U1 shared-pipeline seam + U1b router gating landed test-first; U2 `agent_framework` dedup landed + fixed 2026-06-14).

Symptom: while the assistant is working, the FE "thinking" panel shows only the "Thinking" label and animated dots. The actual reasoning narration ("Searching the knowledge base for relevant sources…") never appears *during* the multi-second wait — on the `agent_framework` / Foundry IQ path it flashes in for an instant at the very end (immediately before the `answer` arrives and the panel collapses), and on the `langgraph` path it never appears at all. The user sees no indication of *what* the model is doing while they wait.

Root cause: BUG-0026 correctly made the narration reach the stream, but it is emitted **too late to function as a during-the-wait indicator**. On the `agent_framework` path the narration + `tool` event are produced inside `_update_to_events` when the `mcp_server_tool_call` content block surfaces — and Foundry IQ runs KB retrieval **server-side**, so that block does not surface until retrieval has already completed. The whole `reasoning → tool → citation → answer` sequence therefore arrives in a tight burst at the end; during the actual multi-second wait no `reasoning` frame has been emitted yet, so `m.reasoning` is empty and the panel renders only dots, then everything floods in and the `answer` immediately collapses the panel. On the `langgraph` path retrieval runs in-code at the top of `run()` and blocks before any event is yielded, so it emits nothing upfront either. The FE reasoning pipeline (streamChat → MessageInput → ChatContext → MessageList) is correctly wired and renders `m.reasoning` in the force-open panel while streaming — the defect is backend **timing + coverage**, not a frontend bug.

Fix: emit the narration **upfront in the shared `run_chat` pipeline** — after the content-safety / RAI guards pass and *before* the orchestrator runs — via a new `retrieval_hint: str | None` parameter carrying the canonical `KB_SEARCH_NARRATION` string. The router passes the hint only when a knowledge source is configured for the active mode. Because the seam is in the orchestrator-agnostic pipeline, every current and future orchestrator inherits the during-the-wait narration with zero per-provider code; an orchestrator that emits its own native reasoning simply streams more frames after it. The now-redundant late per-call narration in `agent_framework._update_to_events` is then removed (the `tool` event stays). `sse_stream` is a true async generator, so the upfront frame reaches the browser as soon as it is yielded — i.e. at the start of the wait. Landed test-first across three units: U1 (the `run_chat` seam + `retrieval_hint` parameter + `KB_SEARCH_NARRATION` constant + 5 pipeline tests), U1b (router gating -- `retrieval_hint = KB_SEARCH_NARRATION if search is not None else None` in `conversation.py`, passed only when a search backend is wired), and U2 (2026-06-14 -- removed the now-redundant late per-call narration block from `agent_framework._update_to_events`, keeping the raw `tool` event; dropped the now-unused `_KB_SEARCH_NARRATION` orchestrator constant). The two former "narrates" orchestrator tests now assert that a KB retrieval `function_call` / `mcp_server_tool_call` yields only the `tool` event with **no** `reasoning` frame, and the upstream during-the-wait narration stays covered by the `run_chat` pipeline tests. Net after U2: the `agent_framework` path no longer double-narrates -- the single during-the-wait `reasoning` frame is emitted upfront by `run_chat`, and the orchestrator streams only its native `tool` / `citation` / `answer` frames afterward.

References: [worklog/2026-06-12.md](worklog/2026-06-12.md); BUG-0026 (made the narration reach the stream — this fixes *when* it is emitted); BUG-0013 (umbrella reasoning-feed symptom). Seam: `run_chat(..., retrieval_hint=...)` in `core/pipelines/chat.py` emits a leading `reasoning` frame; constant `KB_SEARCH_NARRATION` lives there and the router imports it.

### BUG-0028 — `langgraph` over-refuses in-domain queries (strict CWYD guardrail → gpt-5.1 fallback)

Area: backend. Severity: high. Status: fixed (found 2026-06-12; root cause confirmed by live prompt-variant probes; prompt-softening fix landed + live-verified 2026-06-14).

Symptom: with the active orchestrator set to `langgraph`, a normal in-domain question ("tell me about employee benefits") returns the out-of-domain fallback ("The requested information is not available in the retrieved data. Please try another query or topic.") — a single `answer` frame, no `tool` / `reasoning` / `citation` frames, HTTP 200, no backend error. The same deployment and corpus ground correctly on the `agent_framework` path.

Root cause: the `langgraph` orchestrator does raw chat completion — it retrieves with client-side Azure AI Search, injects the results as a `[docN]` "Sources:" system message, and streams the chat model with the shared CWYD system prompt (`compose_cwyd_instructions(CWYD_DEFAULT_BODY)`). That prompt ends with the non-negotiable `CWYD_GUARDRAIL`, whose grounding clause — "if the retrieved documents do not contain enough information to answer the query, or if no documents are retrieved, your only response is [fallback]" — makes `gpt-5.1` read "enough information" ultra-conservatively and refuse relevant-but-broad queries. Retrieval and message structure are ruled out: direct index search returns 5 relevant hits, and a minimal prompt + the same injected sources grounds reliably. `agent_framework` survives the identical prompt because Foundry IQ executes retrieval server-side and presents the KB results as authoritative tool outputs, which the agent runtime is tuned to ground on; the raw chat-completion path obeys the strict prompt literally.

Confirmation (five live guardrail-variant probes, temp 0.0 + default): the strict prompt refuses the benefits query 8/8 (temperature is not the lever); softening the guardrail grounds most in-domain queries (health plans, handbook, "tell me about the company") while all out-of-domain / creative requests still refuse — but the single broad "tell me about employee benefits" overview sits exactly on gpt-5.1's grounding decision boundary for this small corpus (non-deterministic even at temp 0.0), and a naively permissive guardrail regresses the BUG-0011 grounding guarantee (leaks ungrounded creative content such as "write me a poem about dragons"). So there is a genuine tension: permissive enough to ground broad in-domain queries vs. strict enough to refuse ungrounded creative / out-of-domain requests.

Impact by mode: in `cosmosdb` mode the intended path is `agent_framework` (Foundry IQ), which grounds correctly end-to-end, so `langgraph` is a legacy / fallback path there. In `postgresql` mode `langgraph` is the only supported orchestrator (no pgvector knowledge source for Foundry IQ — ADR 0021 / 0022), so this over-refusal blocks answering entirely for every postgres deployment. This is the "Phase 3 (prompt softening)" work already tracked in the worklog.

Secondary finding (separate code defect): `LLMProvider.complete()` calls `self.chat(messages, deployment=...)` without threading `temperature` / `max_tokens`, so the configured `openai_temperature` (0.0) and `openai_max_tokens` are ignored on the `langgraph` path — the model runs at its default temperature, contributing to the boundary non-determinism. Threading them was attempted as a follow-on unit but **reverted**: passing `max_tokens` made every `langgraph` chat 500 on `gpt-5.1`, which rejects `max_tokens` in favor of `max_completion_tokens` (tracked as BUG-0029). The threading is deferred behind that fix.

Fix (landed 2026-06-14, with operator consent per Hard Rule #10 — edits the non-negotiable, BUG-0011-protected shared CWYD safety prompt): softened the `CWYD_GUARDRAIL` grounding clause and replaced the `CWYD_DEFAULT_BODY` strict v1 in/out-of-domain section ("read sentence by sentence", "only when … enough information", "think twice", "you cannot decide") with a relevance-based "## On answering from the retrieved documents:" section. The new guardrail directive is positive and relevance-anchored — *"When the retrieved documents contain information relevant to the user's question, you **must** answer using that information and cite each claim. Do not refuse just because the documents are brief, fragmentary, or do not cover every detail … Only when none of the retrieved documents are relevant … your only response is [fallback]"* — and explicitly bans ungrounded creative content (*"Never produce creative content (such as stories, poems, jokes, or song lyrics) unless the retrieved documents contain it"*) so the BUG-0011 grounding guarantee is preserved. The exact fixed fallback string, the `[doc+index]` citation format, the no-URLs rule, and the `compose_cwyd_instructions` guardrail-wrapping (BUG-0011 invariant) are all unchanged. Landed test-first: four new invariant assertions in `test_definitions.py` (relevance-based grounding directive present, strict "enough information" wording absent, creative-content ban present, strict v1 body drag dropped) plus the existing fallback/citation/wrapping assertions stay green (16/16). Live-verified on `langgraph` + `gpt-5.1` + `cwyd-index`: "tell me about employee benefits" grounded 3/3 (the prior on-the-boundary query), "what health plans are available?" grounded, "what is the capital of France?" and "write me a poem about dragons" both returned the fixed fallback. Both orchestrators share the prompt, so `agent_framework` inherits the softer grounding while keeping the same safety ban.

References: [worklog/2026-06-12.md](worklog/2026-06-12.md) (the "Conversation regression" + operator-decision sections, "Phase 3 prompt softening"); [worklog/2026-06-14.md](worklog/2026-06-14.md) (the realized fix + BUG-0029 discovery); BUG-0011 (the guardrail wrapping invariant the fix preserves); BUG-0029 (the `max_tokens` → `max_completion_tokens` defect that deferred the secondary temperature threading); ADR 0021 / ADR 0022 (the db_type ↔ orchestrator coupling that locks postgres mode to `langgraph`).

### BUG-0029 — `FoundryIQ.chat()` sends `max_tokens`, rejected by `gpt-5.1` (`max_completion_tokens` required)

Area: backend. Severity: medium. Status: open (found 2026-06-14 while attempting the BUG-0028 secondary temperature/`max_tokens` threading).

Symptom: threading the configured `openai_max_tokens` through `BaseLLMProvider.complete()` into `FoundryIQ.chat()` made every `langgraph` conversation return `500 Internal Server Error`. Backend log: `RuntimeError: Error code: 400 - {'error': {'message': "Unsupported parameter: 'max_tokens' is not supported with this model. Use 'max_completion_tokens' instead.", 'type': 'invalid_request_error', 'param': 'max_tokens', 'code': 'unsupported_parameter'}}`.

Root cause: `FoundryIQ.chat()` / `chat_stream()` forward the sampling cap as the legacy Chat Completions `max_tokens` parameter. Current Azure OpenAI chat models (`gpt-5.1`, gpt-5 family, o-series) removed `max_tokens` in favor of `max_completion_tokens`; sending `max_tokens` is a hard `400`. The defect is **latent** in the shipped code because `complete()` (the only `langgraph` chat entry point) does not pass `max_tokens`, so the parameter is never populated on that path — it only fires when a caller explicitly threads `max_tokens` (as the BUG-0028 secondary fix did). That threading was reverted, so the live tree is back to green.

Fix (separate unit, not yet landed): in `FoundryIQ.chat()` and `chat_stream()` send `max_completion_tokens` instead of `max_tokens` (the forward-compatible name accepted by current chat models). Update `test_chat_passes_temperature_and_max_tokens` accordingly. Then re-attempt the BUG-0028 secondary improvement (thread `settings.openai.temperature` / `max_tokens` through `complete()`), but first confirm `gpt-5.1` accepts a custom `temperature` — gpt-5 reasoning models constrain `temperature` to the default, so the chat path may need to omit it for those deployments.

References: BUG-0028 (the deferred secondary temperature threading that surfaced this); [worklog/2026-06-14.md](worklog/2026-06-14.md).

### BUG-0030 — citation shape diverges per orchestrator (`agent_framework` native `【N:M†source】` + raw `mcp://` id vs `langgraph` `[docN]` + filename)

Area: backend. Severity: high. Status: fixed (found 2026-06-14 by side-by-side comparison of the two orchestrators answering "tell me about employee benefits"; fixed 2026-06-15 via fallback (b) — app-side by-key citation enrichment + a null-url mapping fix).

Symptom: the same question against the same corpus produces two different citation shapes depending on the active orchestrator.

* `langgraph` answer: inline `[doc1]` markers; **Sources** block lists `Benefit_Options.pdf` (the friendly filename).
* `agent_framework` answer: inline `【6:0†source】` markers (OpenAI Responses native annotation format); **Sources** block lists `mcp://searchindex/<doc-key>` (the raw search-index document key, no filename, with an `[Open source](mcp://…)` link that is not user-meaningful).

Root cause: the two paths ground and cite through entirely different mechanisms, even though they share the same system instructions.

* `langgraph` ([langgraph.py](../../src/backend/core/providers/orchestrators/langgraph.py)) retrieves client-side via `BaseSearch.search`, injects the hits as a numbered `Sources:\n[doc1]: <content>` system message (`format_sources_block`), and the shared `CWYD_GUARDRAIL` instructs the model to cite with `[docN]`. After the answer streams, `filter_to_referenced` maps each referenced `[docN]` marker to a `Citation(id="[doc1]", title=<src.title>=Benefit_Options.pdf, url=<src.url>)`. The `[docN]` contract is fully honored, and the title is the friendly filename the search provider returns.
* `agent_framework` ([agent_framework.py](../../src/backend/core/providers/orchestrators/agent_framework.py)) grounds via the server-side Foundry IQ Knowledge Base MCP tool. The Responses model performs retrieval through that tool and emits its **own native** citation annotations — inline `【N:M†source】` markers plus `Content.annotations` (`type == "citation"`) whose `file_id` / `url` is the raw search-index document key (`mcp://searchindex/<doc-key>`), not the filename. `citations_from_annotations` ([citations.py](../../src/backend/core/tools/citations.py)) sets `id = file_id or url or title`, so the raw key flows straight through; the `[docN]` rule in the shared prompt is effectively overridden by the KB tool's annotation contract and never produces `[docN]` markers on this path.

So the system **instructions** are identical (`compose_cwyd_instructions(...)` for both — confirmed: `langgraph` threads `system_prompt=effective.cwyd_agent_instructions`; `agent_framework` applies the same via `build_agent` → `_resolve_definition`), but the **effective citation behavior** diverges because grounding is in-prompt (`[docN]`) for one and server-side-tool (native annotations + document keys) for the other. This is a backend defect, distinct from the FE-rendering bugs BUG-0014 (markdown→HTML), BUG-0015 (block styling), and BUG-0016 (`[docN]` superscript) — all three assume a `[docN]` marker is already present, which is exactly what the `agent_framework` path fails to produce.

Fix direction (not yet landed; needs operator direction): normalize the `agent_framework` output to the v1 citation shape so both orchestrators emit one `Citation` shape and one inline-marker convention. That means, on the `agent_framework` path: (a) rewrite the inline native `【N:M†source】` markers in the answer text to sequential `[docN]` markers; (b) build the parallel `Citation` list with `id="[docN]"` and a friendly `title` resolved from the annotation `title` (or a KB-metadata lookup keyed by the document key) instead of passing the raw `mcp://searchindex/<doc-key>` through. Open question for the fix: confirm what the Foundry IQ KB annotation actually carries for `title` / display name (it may already include the filename), which decides whether a metadata lookup is needed or the title is already present and just dropped today.

References: BUG-0026 (the related `mcp_server_tool_call` content-shape difference on the same `agent_framework` path); BUG-0016 (the FE `[docN]` superscript rendering that depends on this backend normalization); BUG-0011 (the shared-prompt guardrail wrapping that both paths preserve **for the default prompt only** — see BUG-0031 for the override-case gap); [worklog/2026-06-14.md](worklog/2026-06-14.md).

**Update 2026-06-14 (live integration-lane capture):** this does **not** reproduce on the current live `agent_framework` deployment. A real `POST /api/conversation` ("What health plans are available?") returned inline `[doc1]` markers in the answer text and a structured `Citation(id="[doc1]", title="Benefit_Options.pdf", snippet=<doc text>)` — the normalized v1 shape on both the inline-marker and `Citation` fronts. This answers the open question above: the Foundry IQ KB annotation now carries `file_id="[doc1]"` **and** a friendly filename `title`, so `citations_from_annotations` (`id = file_id or url or title`) passes the normalized `[doc1]` + filename shape straight through with no code change. The new integration lane asserts this as a live regression guard (`tests/integration/test_conversation_live.py::test_in_domain_citation_ids_use_normalized_docn_shape`): if Foundry IQ / the SDK regress to native `【N:M†source】` markers or raw `mcp://searchindex/<doc-key>` ids, that test fails. No code fix landed — the normalization is Foundry-IQ-side and may be KB-config-dependent — so the table-row status stays **open** pending operator confirmation to close. (A separate minor wire defect surfaced in the same capture: `Citation.url` serialized as the string `"None"` — see BUG-0033.)

**Update 2026-06-14 (second live capture — SUPERSEDES the note above):** the divergence **does** reproduce. A fresh A0 capture (same query, throwaway integration-lane probe forcing the `agent_framework` orchestrator) returned the **un-normalized** shape on both fronts: the answer text literally contains native `【6:1†source】` / `【6:0†source】` markers, and `citations_from_annotations` returned `id == title == url == mcp://searchindex/<doc-key>`, `snippet == ""`, `score == null`. The two captures disagree because the friendly `[doc1]` + filename output is **Foundry-IQ-KB-config-dependent**; the live KB now emits the native shape, so a code-side normalizer is required (the earlier "no code change" conclusion was an artifact of a transient KB config). The integration guard `test_in_domain_citation_ids_use_normalized_docn_shape` correctly fails against this deployment — it is catching the real regression.

Three findings shape the fix:

* **Mapping is offset-anchored, not `N:M`.** Each annotation carries `annotated_regions = [{type: "text_span", start_index, end_index}]` — the exact character span of one native marker in the answer text (verified: first span `78..90` lands on the first 12-char `【6:1†source】`, offsets align with the buffered answer 1:1). `citations_from_annotations` already groups annotations by source id in first-occurrence order and aggregates every span under `Citation.metadata["annotated_regions"]`. The `N:M` inside the marker is misleading — `N` is constant and `M` is the KB's internal source index, **inverted** from citation order here (`M=1`→Citation[0], `M=0`→Citation[1]). The normalizer maps by the **citation grouping order** (Citation[0]→`[doc1]`, Citation[1]→`[doc2]`) and rewrites each citation's spans to its `[docN]`.
* **Friendly title + snippet are NOT in the annotation.** The full `raw_representation` is `{type: "url_citation", start_index, end_index, title: "mcp://searchindex/<doc-key>", url: "mcp://searchindex/<doc-key>"}`; `additional_properties` is only `{annotation_index: <n>}`. No filename, no chunk text anywhere. So title/snippet recovery cannot read annotation fields — it needs either a Foundry IQ KB citation-field-mapping change (infra) or a secondary Azure Search lookup keyed by the document key (code), or it accepts a degraded `[docN]` id with no friendly title/snippet. This fork is surfaced to the operator and gates the right-panel snippet, **not** the marker normalization.
* **The normalizer is independent of that fork.** Rewriting `【N:M†source】` → `[docN]` and renumbering citation ids to `[docN]` is fully determined by `annotated_regions` + grouping order, so it lands first as a pure unit; title/snippet recovery follows once the fork is decided.

Fix landing: a pure `normalize_kb_citations(answer, citations)` in [citations.py](../../src/backend/core/tools/citations.py) — rewrite each `annotated_regions` span to the citation's 1-based `[docN]`, renumber ids to `[docN]`, strip any residual native marker — then wire it into `agent_framework.run` after `citations_from_annotations`. Status stays **open** until the normalizer + wire-in land and the live guard passes.

**Update 2026-06-14 (option (a) seed change — KB citation source-data fields):** the marker normalizer (`normalize_kb_citations`) has landed and the live guard passes on the marker/id front. For the remaining title/snippet recovery fork the operator chose **(a) KB citation-field-mapping config** (over the app-side secondary lookup (b) or the degraded `[docN]`-only (c)), because a Foundry-IQ-side mapping keeps recovery out of every orchestrator's code (Hard Rule #20). Implemented in the `post_provision` knowledge-source seed: `_build_knowledge_base_seed` now sets `searchIndexParameters.sourceDataFields = [{"name": "title"}, {"name": "url"}, {"name": "content"}]` (Azure AI Search KB REST `2025-11-01-preview`, "request additional fields for referenced source data"), so the KB returns the friendly filename (`title`), document `url`, and chunk `content` snippet as citation source data instead of only the raw `mcp://searchindex/<doc-key>`. Module constant `KNOWLEDGE_SOURCE_SOURCE_DATA_FIELDS`; test `test_build_knowledge_base_seed_shape` updated (18/18). **Live verification still required before close:** a re-seed (`azd hooks run postprovision` / `python -m scripts.post_provision`) + a fresh A0 capture must confirm the Responses model's `url_citation` annotation now carries a friendly `title` / snippet. If the annotation `title` still shows `mcp://searchindex/<doc-key>` after the re-seed (the knob may only enrich the tool's source-data payload, not the emitted annotation), a follow-up code unit in `citations_from_annotations` reads the source-data fields, or falls back to (b)/(c). Status stays **open** until the live capture confirms friendly title/snippet on the served `Citation`.

**Live verification 2026-06-14 (re-seed + A0 capture) — option (a) is INSUFFICIENT.** Re-seeded the live KB (`python -m scripts.post_provision` → `knowledge base 'cwyd-kb' ready`), then captured a fresh `agent_framework` A0 run. The answer marker/id parity holds (inline `[doc1]`, no native `【N:M†source】` leak), but the served `Citation` still carries `title` = `url` = the raw `mcp://searchindex/<doc-key>` with an empty `snippet`. The `sourceDataFields` knob enriches the KB tool's *referenced source data* but does **not** surface on the Responses `url_citation` annotation `citations_from_annotations` reads. The seed change stays in (correct KB config) but does not close this bug. **Fallback (b) now proven viable:** a read-only `get_document(key=<doc-key>)` against the chat index resolved both captured doc-keys to real documents (`id` == the `<doc-key>` hash, `title` == the sample filename, `content` == the chunk snippet), so a secondary Search lookup by doc-key is deterministic. Decision pending operator: implement (b) (app-side enrichment after `citations_from_annotations`, honoring Hard Rule #20 R2) or accept (c) degraded `[docN]`-only. Status stays **open**.

**Fixed 2026-06-15 (fallback (b) implemented + live-confirmed).** The operator chose **(b)** — an app-side secondary Azure AI Search lookup keyed by the KB document id — over the degraded `[docN]`-only (c). Landed as three test-first units, all honoring Hard Rule #20 R2 (citation shaping stays in [citations.py](../../src/backend/core/tools/citations.py), provider-free):

* **`enrich_kb_citations(citations, fetch_document)`** — for each citation whose `mcp://searchindex/<key>` id (read from `metadata["source_id"]`) resolves, backfills `title`/`snippet`/`url` from the fetched document; preserves `id`/`score`/`metadata` (including the original `source_id`). `fetch_document` is an **injected** `Callable[[str], Awaitable[SearchResult | None]]`, so `citations.py` imports no Search provider — the single response-format point.
* **`BaseSearch.get_document_by_key(key)`** with an `AzureSearch` override (`client.get_document(key=…, selected_fields=["id","content","title","url"])`): `ResourceNotFoundError` → `None` (soft miss), other `AzureError` → log + re-raise (Hard Rule #14); a by-key fetch carries no relevance `score`.
* **Wire-in** in `agent_framework.run` after `normalize_kb_citations`: best-effort `await enrich_kb_citations(citations, self._search.get_document_by_key)`, guarded by `try/except AzureError` → `logger.warning` + degrade to the raw-id citation (never drops it). The orchestrator gained a keyword-only `search: BaseSearch | None`, threaded by the router from the active Search provider.
* **Null-url fix** in `AzureSearch._to_result`: `url=str(doc.get("url", ""))` produced the string `"None"` for a present-but-`null` `url` field (the `""` default only fires when the key is *absent*), which the enrichment newly exercised. Changed to `url=str(doc.get("url") or "")`. This is a **second** `str(None)`→`"None"` site, distinct from BUG-0033's `citations_from_annotations` boundary fix, and also cleans the `langgraph` retrieval path for url-less documents.

Live A0 capture (real `agent_framework` deployment, same query) now returns the normalized v1 shape end-to-end: inline `[doc1]`, `Citation(id="[doc1]", title="Benefit_Options.pdf", snippet=<chunk text>, url="")`, no native `【N:M†source】` markers, `metadata["source_id"]` retaining the raw `mcp://searchindex/<key>` for traceability. See [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0031 — Authored-prompt guardrail wrapping applied only on `agent_framework`, not `langgraph`

Area: backend. Severity: medium. Status: fixed (found 2026-06-14 while tracing whether the two orchestrators share instruction-building code, in response to the parity question behind BUG-0030; fixed 2026-06-14).

Observed: instruction composition is two parallel code paths, not one shared seam. They converge only when no operator override is set.

* Default (no `cwyd_agent_instructions` override): both paths resolve to `CWYD_AGENT.instructions` = `compose_cwyd_instructions(CWYD_DEFAULT_BODY)` — identical, guardrail-wrapped text. No divergence.
* With an operator override: the paths diverge.
  * `agent_framework`: `build_agent` → `_resolve_definition` ([base.py](../../src/backend/core/providers/agents/base.py)) calls `definition.model_copy(update={"instructions": compose_cwyd_instructions(text)})`, so the authored text is bookended by the fixed `CWYD_GUARDRAIL`.
  * `langgraph`: the router threads `system_prompt=effective.cwyd_agent_instructions` and `LangGraphOrchestrator.run` ([langgraph.py](../../src/backend/core/providers/orchestrators/langgraph.py)) injects it verbatim as a `ChatRole.SYSTEM` message — no `compose_cwyd_instructions` call anywhere on this path.

Root cause: the stored override is raw (un-wrapped). The PATCH handler ([admin.py](../../src/backend/routers/admin.py)) screens the prompt through the RAI gate (`PROMPT_FIELDS`) but stores `payload[field]` un-wrapped; `resolve_effective_config` ([services/admin.py](../../src/backend/services/admin.py)) overlays that raw value onto `effective.cwyd_agent_instructions`. The guardrail wrap exists only at agent_framework read-time (`_resolve_definition`) and in the default constant — there is no shared composition step, so anything that consumes `effective.cwyd_agent_instructions` directly (langgraph) gets the un-wrapped persona. (That the override is stored raw is confirmed by the fact that `_resolve_definition` wraps at read-time; if storage were already wrapped, agent_framework would double-wrap.)

Impact / reachability: only reachable when an operator saves a custom `cwyd_agent_instructions` override AND the active orchestrator is `langgraph` (the sole path in `postgresql` / pgvector mode, and a selectable option in `azure_search` mode). The malicious-prompt vector that motivated BUG-0011 is NOT reintroduced — the RAI gate still rejects unsafe authored prompts at write time. What is lost on `langgraph` is the non-safety guardrail behavior the authored persona is supposed to be bookended by: the out-of-domain refusal directive, the `[docN]` citation instruction, and the fixed fallback string. With a custom persona the langgraph orchestrator effectively runs persona-only, diverging from agent_framework. This also means BUG-0030's note that "both paths preserve" the BUG-0011 wrapping is accurate only for the default prompt.

Fix (landed 2026-06-14): instruction composition is now a single shared seam both consumers call — `resolve_cwyd_instructions(override_text: str | None) -> str` in [definitions.py](../../src/backend/core/agents/definitions.py). It returns `compose_cwyd_instructions(override_text)` when the override is non-blank and `compose_cwyd_instructions(CWYD_DEFAULT_BODY)` (= `CWYD_AGENT.instructions`) when it is `None` / blank, so a blank-clear falls back to the wrapped default. Both consumers route the **raw** stored override through this one seam, each wrapping it exactly once: `resolve_effective_config` ([services/admin.py](../../src/backend/services/admin.py)) re-resolves `values["cwyd_agent_instructions"]` through the seam after the overlay loop (this is the value `langgraph` injects as its SYSTEM message), and `_resolve_definition` ([base.py](../../src/backend/core/providers/agents/base.py)) calls the same seam instead of an inline `compose_cwyd_instructions`. There is no double-wrap because each consumer wraps the raw override (`overrides.cwyd_agent_instructions`), never the already-wrapped effective value. The three previous composition references (default constant + `_resolve_definition` + the absent langgraph wrap) collapse to one definition. Tests: `test_definitions.py` pins the seam's None/blank/override behavior and that `resolve_cwyd_instructions(None) == CWYD_AGENT.instructions`; `test_services_admin.py` asserts the effective override is guardrail-wrapped (count == 1, body leads) and a blank override falls back to the default; `test_base.py` asserts the agent_framework path equals `resolve_cwyd_instructions(override)`; `test_conversation.py` asserts the router forwards a guardrail-wrapped `system_prompt` (the langgraph-boundary regression). Full v2 suite green (2247 passed, 1 skipped); `pyright --strict` clean on the three touched source files. Implemented as Option (a) from the original fix direction (wrap in `resolve_effective_config`) but keeping `_resolve_definition`'s wrap routed through the same seam rather than dropping it, so both paths share one composition function.

References: BUG-0011 (the original guardrail-wrapping fix this completes for the langgraph path); BUG-0030 (the citation-mechanism divergence found in the same parity investigation — same shared-seam fix family); BUG-0022 (the prior agent_framework override-handling fix); [worklog/2026-06-14.md](worklog/2026-06-14.md).

### BUG-0032 — Admin prompt-override round-trip can double-wrap the guardrail

Area: backend. Severity: low. Status: fixed (found 2026-06-14 during the BUG-0031 shared-seam fix; fixed 2026-06-19).

Observed: the effective CWYD instructions can carry the fixed `CWYD_GUARDRAIL` block twice when an operator saves a custom prompt that was seeded from the admin GET.

Mechanism:

1. `GET /api/admin/config` ([admin.py](../../src/backend/routers/admin.py)) returns `cwyd_agent_instructions = CWYD_AGENT.instructions`, which is the **wrapped** default — `compose_cwyd_instructions(CWYD_DEFAULT_BODY)` = body + `CWYD_GUARDRAIL`.
2. If the FE admin editor pre-populates the field from that GET, the operator is editing text that already contains the guardrail.
3. On `PATCH /api/admin/config` the RAI gate screens the prompt (passes) and stores it raw — the stored override now contains the guardrail.
4. At runtime the shared `resolve_cwyd_instructions` seam (BUG-0031) wraps the raw override via `compose_cwyd_instructions`, appending `CWYD_GUARDRAIL` a second time. The effective instructions contain two guardrail blocks.

Impact / reachability: only when an operator saves a custom `cwyd_agent_instructions` that includes the guardrail text (the common path if the editor seeds from the wrapped default). Not a safety regression — the guardrail is duplicated, never dropped, and the RAI write-gate still screens authored prompts — so severity is low. The cost is a bloated system prompt (the non-negotiable rules repeated) and a confusing authoring experience (the operator sees and must scroll past the fixed guardrail). Before BUG-0031 the duplication occurred only on `agent_framework` (`_resolve_definition` wrapped) while `langgraph` injected the operator's copy verbatim; after BUG-0031 both paths wrap, making the double-wrap consistent across orchestrators.

Fix (landed 2026-06-19): make the persona **body** the unit of storage and editing. Both admin read endpoints now expose the editable body instead of the wrapped runtime prompt — `GET /api/admin/config` ([config_endpoint](../../src/backend/routers/admin.py)) and the env baseline of `GET /api/admin/config/effective` ([config_effective_endpoint](../../src/backend/routers/admin.py)) return `CWYD_DEFAULT_BODY`, not `CWYD_AGENT.instructions`. The `/config/effective` override case already surfaced the raw authored override (itself a body), so it was correct. The runtime seam `resolve_effective_config` ([admin.py](../../src/backend/services/admin.py)) is **unchanged** — it still appends `CWYD_GUARDRAIL` exactly once via `resolve_cwyd_instructions` when building the effective prompt. So the editor never shows or round-trips the guardrail, the operator edits only the body, and the guardrail is applied once at runtime where it belongs. The `cwyd_agent_instructions` field on `AdminConfig` stays a single field whose value differs by surface by design (editable body on the admin reads, wrapped effective prompt on the runtime resolve); the divergence is documented in the two endpoint comments. `CWYD_AGENT` became unused in `admin.py` and its import was swapped to `CWYD_DEFAULT_BODY`. The alternative (strip a leading/trailing guardrail before re-wrapping) was rejected as brittle string surgery.

Verification: three existing contract tests updated to expect `CWYD_DEFAULT_BODY` (`test_config_surfaces_cwyd_agent_instructions_default`, `test_config_effective_returns_env_defaults_when_no_overrides`, `test_config_effective_overlays_cwyd_agent_instructions_override` — the last keeps the override-case assertion against the raw operator string); one new regression `test_config_effective_default_persona_excludes_guardrail` asserts the guardrail is absent from the editable default on both GET surfaces; the no-override env baseline tests assert `CWYD_GUARDRAIL not in instructions`. Admin router + service suites 133 passed; full backend suite 1070 passed / 0 failed. (Unrelated pre-existing tracked failure `ROUTERS-NON-ROUTE-CONTENT-DEBT` on `_DI_PARSER_PROBE_KEY` in the router-purity gate is from the BUG-0034 fix, not this change — confirmed by diff.)


References: BUG-0031 (the shared composition seam that made the double-wrap consistent and surfaced this round-trip); BUG-0011 (the original guardrail-wrapping invariant); [worklog/2026-06-14.md](worklog/2026-06-14.md).

### BUG-0033 — `Citation.url` serializes as the string `"None"` on the `agent_framework` path

Area: backend. Severity: low. Status: fixed (found 2026-06-14 via the live integration lane while capturing the BUG-0030 citation shape; fixed 2026-06-14).

Symptom: a live `POST /api/conversation` capture returned a citation whose `url` was the literal string `"None"` rather than `null` / `""`: `{"id": "[doc1]", "title": "Benefit_Options.pdf", "url": "None", ...}`. The Foundry IQ KB annotation carried no usable link, yet a `"None"` placeholder reached the wire, which the FE could render as a broken `None` link.

Root cause: `citations_from_annotations` ([citations.py](../../src/backend/core/tools/citations.py)) read each annotation field verbatim (`url = ann.get("url", "")`) and built the `Citation` from those raw values. When the KB source has no URL, the SDK surfaces the field as a Python `None` — or, once stringified upstream, the literal text `"None"` — and that value flowed straight into `Citation.url`. The same un-coerced read also fed the identity fallback `id = file_id or url or title`, so a `"None"` `file_id` could have become the citation id.

Fix (landed 2026-06-14): a single boundary helper `_clean_annotation_field(value: object) -> str` coerces both a Python `None` and the literal string `"None"` to `""`. The annotation adapter routes its four user-visible string reads — `file_id`, `url`, `title`, `snippet` — through it before assembling the `Citation`, so the placeholder is treated as absent: it never reaches `Citation.url`, and a `"None"` `file_id` / `url` / `title` no longer becomes an id (the fallback skips empties and the annotation is dropped when no real identity remains). The `langgraph` path is unaffected — it builds citations from `SearchResult` fields, not annotations.

Verification: `tests/backend/core/tools/test_citations.py` adds coverage for a `"None"`-string url → `""`, a Python `None` url → `""`, a `"None"` `file_id` falling back to the url for the id (and omitted from metadata), and an annotation whose every identity field is `"None"` being dropped. Full file green (22 passed); `pyright --strict` clean on `citations.py`; the shared AST gates pass (926).

References: BUG-0030 (the citation-shape parity investigation that surfaced this in the same live capture); BUG-0016 / BUG-0015 (the FE citation rendering that consumes this `Citation` shape); [worklog/2026-06-14.md](worklog/2026-06-14.md).

### BUG-0034 — Uploaded document reports success but never appears in the documents list

Area: functions. Severity: high. Status: fixed (found 2026-06-14, fixed 2026-06-16).

Symptom: an operator uploads a document through the admin Add-data UI; the request returns `200` with an `UploadResponse` (the UI shows success), but the document never appears in the admin documents list (the Delete-data grid), so it cannot be managed or — by implication — used for grounding.

Root cause (diagnosed 2026-06-16): the upload endpoint (`POST /api/admin/documents` → `backend.services.ingestion.upload_document`) is a two-step "store blob + enqueue" operation — it writes the blob and enqueues a `BatchPushQueueMessage`, then returns `200`; indexing happens asynchronously in the Functions `batch_push` queue trigger (download → parse → embed → push to Azure AI Search). The break is in the **parse** step for PDFs: `DocumentIntelligenceParser` builds its client endpoint from `settings.foundry.services_endpoint` (`AZURE_AI_SERVICES_ENDPOINT`, prefix `AZURE_AI_`). When that setting is empty, `_get_client` computes `endpoint = f"{''.rstrip('/')}/"` → `"/"`, a URL with no `https` scheme; the Azure SDK's `BearerTokenCredentialPolicy` then refuses to attach the managed-identity token and raises `azure.core.exceptions.ServiceRequestError: Bearer token authentication is not permitted for non-TLS protected (non-https) URLs` before any request leaves the process. `batch_push` therefore throws on every PDF, the message is retried and dead-lettered, and the doc never reaches the index — but the upload + enqueue already returned `200`, so the UI reports success. `list_sources` reads distinct `title` values from the index, so an unindexed doc never lists.

Diagnostic trace (2026-06-16): (1) wiring confirmed identical between backend-enqueue and Functions-trigger (queue name, storage account, container all match; identity-based auth, not Azurite). (2) Queue state: main queue empty, **poison queue held 22 messages** (14 `.pdf` + 8 extension-less, all dated 2026-06-10) with valid envelopes (`filename` / `container_name` / `ingestion_job_id` present) — so the failure is downstream of message validation, in parse/embed/push. (3) Running the exact `functions.batch_push.blueprint._execute` seam in-process against `Benefit_Options.pdf` reproduced the failure: `ServiceRequestError` in `DocumentIntelligenceParser.parse` → `begin_analyze_document`, because `services_endpoint` resolved to `""` (the `v2/.env` the script loads is missing `AZURE_AI_SERVICES_ENDPOINT`). (4) Re-running the same seam with the Functions host's `local.settings.json` values loaded (which **do** carry an `https` `AZURE_AI_SERVICES_ENDPOINT`) **succeeded — 4 chunks pushed** — and `Benefit_Options.pdf` then appeared in `GET /api/admin/documents`. So the code + host config are now correct; the 22 poison messages are stale failures from before the endpoint was added to the host config on/after 2026-06-10.

Classification: primarily a **configuration gap** (empty `AZURE_AI_SERVICES_ENDPOINT`) compounded by a **code-hardening gap** — an empty/non-HTTPS endpoint fails with a cryptic SDK auth error and silently poisons every PDF while the UI shows success, with no fail-fast validation and no surfacing of the ingestion failure to the operator.

Fix plan (separate units, test-first): (a) **fail-fast guard** — validate `services_endpoint` is non-empty and `https`-schemed at `DocumentIntelligenceParser` construction (or in `FoundrySettings`), raising a clear, actionable error instead of the opaque SDK "non-TLS" message; this is the real code fix and prevents silent PDF-poisoning. (b) **`v2/.env` backfill** — add `AZURE_AI_SERVICES_ENDPOINT` (the backend's synchronous `ingest_url` path and the ingestion scripts read `.env` and still break without it; only the Functions host's `local.settings.json` was repaired). (c) **operational cleanup** — redrive the 22 poison messages (or `POST /api/admin/documents/reprocess`) now that config is correct. (d) **upload-boundary fail-fast** — surface the misconfig as an honest error at the HTTP boundary: `POST /api/admin/documents` returns `503` (not a green `200`) when a Document-Intelligence-routed file (PDF / DOCX) is uploaded while `AZURE_AI_SERVICES_ENDPOINT` is unset or non-HTTPS, so the operator is told the deployment cannot index the file instead of seeing a false success.

Fix progress: **all units (a)–(d) landed 2026-06-16.** (a) `DocumentIntelligenceParser._get_client` now raises an actionable `ValueError` naming `AZURE_AI_SERVICES_ENDPOINT` when `services_endpoint` is empty or not `https`-schemed, before constructing the SDK client (after the injected-client seam, so client-injecting tests are unaffected); five new tests in `v2/tests/functions/core/parsers/test_document_intelligence_parser.py` (empty, three non-HTTPS forms, injected-seam bypass), `pyright --strict` `0/0/0`. (b) the tracked template `v2/.env.sample` was missing `AZURE_AI_SERVICES_ENDPOINT` entirely (the source of the gap — every `v2/.env` copied from it inherited the omission); added the documented key to the sample and backfilled the real value into the gitignored `v2/.env` from the azd env store, so backend `AppSettings.foundry.services_endpoint` now resolves to an `https` URL. (d) the upload endpoint (`upload_document_endpoint` in [admin.py](../../src/backend/routers/admin.py)) now fails fast with `503` when a Document-Intelligence-routed file is uploaded while `services_endpoint` is empty/non-HTTPS — a registry-identity probe (`registry.get(ext) is registry.get("pdf")`) scopes the guard to DI-routed extensions so non-DI uploads (txt / md / html) are never over-blocked; two new tests in `v2/tests/backend/test_admin.py` (DI file + blank endpoint → `503`; txt + blank endpoint → `200`), full admin suite 111 passed, `pyright` `0/0/0`. This converts the BUG-0034 false-`200` into an honest error at the boundary the operator sees. A **second, local-dev contributing factor** surfaced during the (c) drain: the Functions host's gitignored `local.settings.json` had drifted to queue `doc-processing-local` while the azd store + backend `v2/.env` use the canonical `doc-processing`, so the local host listened on a different queue than the backend enqueues to and never consumed the messages — not a shipped defect (azd wires both tiers from one env), fixed by aligning the local value, restarting the host on the canonical queue, and draining it: all 6 backend-enqueued messages processed successfully (DI parse → embed → push, 22–27 s each) and the documents list grew from 2 to 5 distinct sources (`PerksPlus.pdf`, `role_library.pdf`, and the Woodgrove cyber-risk policy newly indexed), with the canonical queue returning to empty. A non-destructive peek confirmed every PDF the poison referenced was already indexed and that 8 of the 22 canonical-poison messages were base64 Event Grid blob-event envelopes that `batch_push` cannot process; redriving would therefore re-index present docs and re-poison the envelopes, so (operator-confirmed) both poison queues were cleared instead (22→0 and 10→0). All four ingestion queues are now empty and the pipeline indexes uploads end-to-end. **Fixed 2026-06-16.** See [bug-0034-diagnosis.md](bug-0034-diagnosis.md) for the full narrative.

Adjacent observations (not part of this defect): the in-process `_execute` run logged `aiohttp` "Unclosed client session / Unclosed connector" warnings — the DI client / embedder is not closed on the `batch_push` path (low-severity resource leak, candidate for a separate bug). Separately, the locally-running Functions host (restarted after an overnight DNS/credential crash) was not draining the main queue during the trace (a queued message sat undrained) — a local-dev-session listener artifact cleared by a clean host restart, not a code defect.

References: BUG-0003 (the same `list_sources` documents list — healthy after its facet→paging fix; this defect is upstream of it, in ingestion); [worklog/2026-06-16.md](worklog/2026-06-16.md), [worklog/2026-06-14.md](worklog/2026-06-14.md).

### BUG-0035 — `agent_framework` sends unsupported `temperature` to the reasoning model (400)

Area: backend. Severity: high. Status: fixed (found 2026-06-14, fixed 2026-06-14).

Symptom: on the BUG-0013 reasoning path (the answer deployment auto-detected as reasoning-capable), forcing the `agent_framework` orchestrator live fails the agent run with `400 - Unsupported parameter: 'temperature' is not supported with this model.`, so the conversation stream emits an `error` frame instead of reasoning + answer.

Root cause: `AgentFrameworkOrchestrator.run()` built `ChatOptions(temperature=…, max_tokens=…)` for every run and passed it to `agent.run(...)`. When the run also requests a Responses-API `reasoning` option, the gpt-5 / o-series answer deployment (`gpt-5.1`) switches to reasoning-model semantics and rejects the `temperature` sampling parameter. The `langgraph` reasoning path never hit this because its `reason()` sends neither `temperature` nor `max_tokens`. The whole `agent_framework` reasoning path was never exercised live before because the deployment runs `langgraph` via a persisted cosmos orchestrator override (`resolve_effective_config` overlays it on the `agent_framework` env default), so only the new both-orchestrator integration test — which sets `app.state.runtime_overrides = RuntimeConfig(orchestrator_name="agent_framework")` after lifespan boot — drove the path and surfaced the 400.

Fix: branch `run()` on the auto-detected reasoning capability (`llm.supports_reasoning()`) — in reasoning mode build a bare `ChatOptions()` and set only the `reasoning` option (omit `temperature`/`max_tokens`), symmetric with the `langgraph` `reason()` path; in non-reasoning mode keep `ChatOptions(temperature, max_tokens)` unchanged. The non-reasoning `agent_framework` path is unaffected — it sends `temperature=0.0` and was live-verified grounding an answer under BUG-0025, so this is strictly a reasoning-mode combination defect, not a broader temperature problem.

Verification: the parametrized `tests/integration/test_conversation_live.py::test_reasoning_feed_streams_substantive_model_reasoning[agent_framework]` now passes live (substantive reasoning streams, no 400); the `langgraph` parameter also passes. Unit coverage: `test_run_requests_reasoning_summary_when_model_supports_it` asserts the `reasoning` option is present and `temperature`/`max_tokens` are absent; `test_run_omits_reasoning_option_when_model_lacks_reasoning` and the default-sampling tests assert the non-reasoning path is unchanged.

References: [worklog/2026-06-14.md](worklog/2026-06-14.md); related BUG-0013 (the reasoning feed this unblocks), BUG-0025 (prior `agent_framework` non-reasoning live verification), BUG-0029 (the sibling `max_tokens`→`max_completion_tokens` chat-path parameter defect).

### BUG-0036 — canned KB-search narration persists alongside real model reasoning

Area: frontend. Severity: low. Status: fixed (found 2026-06-14, fixed 2026-06-14).

Symptom: once BUG-0013 made the answer model stream substantive reasoning, the thinking panel showed the canned "Searching the knowledge base for relevant sources…" sentence *and* the real reasoning together — the placeholder line was never replaced when genuine reasoning began.

Root cause: the during-the-wait narration (added in BUG-0027, emitted upfront by the shared `run_chat` pipeline on the `reasoning` channel) and the orchestrator's own native reasoning frames are both `reasoning`-channel `OrchestratorEvent`s. The FE `append_reasoning` reducer pushed every `reasoning` frame into a single `reasoning[]` list, and `MessageList` joined the whole list, so the placeholder and the model reasoning concatenated. SSE is append-only, so the already-displayed narration could not be removed without FE state support.

Fix: mark the transient narration so the client can drop it. (1) Backend — `run_chat` emits the `retrieval_hint` frame with `metadata={"placeholder": True}`; `format_sse` already forwards `metadata` for every channel, so it reaches the FE as `ev.metadata.placeholder === true`. (2) FE model — `ChatMessage` gains a `reasoningPlaceholder?: string` slot, held apart from `reasoning[]`. (3) FE reducer — a new `set_reasoning_placeholder` action sets (never appends) the placeholder. (4) FE handler — `MessageInput` routes a `reasoning` frame with `metadata.placeholder === true` to `set_reasoning_placeholder`, all other `reasoning` frames to `append_reasoning`. (5) FE renderer — `MessageList` shows the panel when streaming, or when `reasoning` is non-empty, or when a non-empty placeholder exists, and renders `reasoning.join("")` when non-empty else `reasoningPlaceholder` — so the first real reasoning frame supersedes the placeholder, and a non-reasoning model keeps the placeholder as the sole content (no regression).

Verification: `tests/backend/core/pipelines/test_chat.py` asserts the narration frame carries `metadata == {"placeholder": True}` and the orchestrator's native reasoning frame does not (18/18). FE Vitest: `ChatContext.test.tsx` (placeholder set without touching `reasoning`, replace-not-append), `MessageInput.test.tsx` (placeholder frame → `reasoningPlaceholder`, subsequent real frame → `reasoning[]`), `MessageList.test.tsx` (placeholder shown when no reasoning yet; real reasoning drops the placeholder) — 87/87 across the three suites. `tsc --noEmit` clean on the frontend source + test workspaces; `pyright` 0/0/0 on `chat.py`.

References: [worklog/2026-06-14.md](worklog/2026-06-14.md); related BUG-0013 (the reasoning content this polishes), BUG-0027 (the upfront narration whose lifecycle this completes).

### BUG-0037 — finished answer missing the "AI-generated content may be incorrect" disclaimer

Area: frontend. Severity: low. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: a finished assistant answer renders the answer body and then the reference block with nothing between them; v1 shows a small muted "AI-generated content may be incorrect" disclaimer in the answer footer.

Root cause: `MessageList` never rendered a disclaimer element; the assistant content column stacked only the answer `MarkdownContent` and the `CitationPanel`.

Fix: `MessageList` now renders a static `<p>` (`AI-generated content may be incorrect`, `data-testid="answer-disclaimer-<id>"`) between the answer body and the `CitationPanel`, gated on `m.streaming !== true && m.content.length > 0` so it appears only under a finished, non-empty answer — not while streaming, not on user messages, not on empty error-only messages. The `.disclaimer` style is small and muted (`fontSizeBase200` / `colorNeutralForeground3`) to mirror the v1 answer footer.

Verification: `MessageList.test.tsx` — disclaimer present under a finished assistant answer; absent on a user message; absent while streaming; absent on an empty error-only message (33/33).

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0038 — extra blank line between reasoning sections in the thinking panel

Area: frontend. Severity: low. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: the thinking/reasoning panel renders consecutive reasoning sections separated by a blank line (an extra paragraph break), making the panel taller and looser than intended.

Root cause: `formatReasoning` (reasoningText.tsx) replaces each model bold section title (`SECTION_TITLE`) with a `"\n\n"` paragraph break and then collapses `\n{3,}` to `\n\n`. Rendered through the markdown panel, each `\n\n` becomes a separate `<p>`, so sequential reasoning bodies show a blank line between them.

Fix: changed the section-title replacement to a single `"\n"` line break and the run-collapse to `\n{2,}` → `"\n"`, so adjacent reasoning bodies render on consecutive lines within one `<p>` (a soft break shown via the panel's `white-space: pre-wrap`) with no empty paragraph between them. Updated `reasoningText.test.tsx` (multi-section expectations now `\n`) and the `MessageList.test.tsx` reasoning assertion (one `<p>` with a softbreak instead of two paragraphs).

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0039 — references do not open a source-detail column on click

Area: frontend. Severity: medium. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: in v1, references render as bordered, clickable chips (a number box + the document filename); clicking a chip opens a right-side column showing the document title, a link to open the document, and the reference text (with markdown handled). In v2, the `CitationPanel` renders a "N references" toggle over a numbered list whose items expand inline to a snippet + an "Open source" link — there is no right-side detail column, and the chat layout has no place for one.

Root cause: the v2 `CitationPanel` was scoped (BUG-0015) to the reference-block restyle only; the right-side citation detail column was explicitly deferred as a separate structural change. There is no `activeCitation` state, no `CitationDetailPanel` component, and `ChatPage` is a two-column grid.

Fix (multi-unit): added `activeCitation` + `ShowCitation` / `CloseCitation` to chat state (models/chat.tsx, ChatContext.tsx); added a pure `formatCitationLabel` middle-truncation helper; restyled the references as clickable v1-style chips (number box + truncated filename) that dispatch `ShowCitation` (CitationPanel.tsx + .module.css; MessageList.tsx wires `onSelectCitation`); added a `CitationDetailPanel` that renders the active citation's title, an open-document link, and the markdown-rendered reference text (XSS-safe `MarkdownContent`, no rehype-raw), with a dismiss control dispatching `CloseCitation`; and added a third push-content grid column to `ChatPage` driven by an inner `ChatShell` component that reads `activeCitation` and toggles `data-citation-open` (chat `.main` is `1fr`, so it narrows when the column opens). Structural sign-off for the third column obtained from the operator (push-content column, v1 parity).

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0040 — reference toggle does not collapse the chip list

Area: frontend. Severity: medium. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: the "N references" toggle under an answer never hides the reference chips. The list stays visible whether the toggle reads as expanded or collapsed, so the collapse affordance appears broken and the references always show.

Root cause: `CitationPanel.tsx` correctly sets the `hidden` attribute on the chip-list container (`hidden={!expanded}`), but `CitationPanel.module.css` styles that container with `.chipList { display: flex }`. An explicit `display` declaration overrides the user-agent `[hidden] { display: none }` rule (lowest possible priority), so the element stays laid out even when `hidden` is present. The existing `CitationPanel.test.tsx` asserts only the `hidden` attribute (set correctly), and jsdom does not apply CSS-module styles, so the visual regression was invisible to the test.

Planned fix: add a `.chipList[hidden] { display: none }` rule so the `hidden` attribute actually hides the container. Verified live (CSS-only change; jsdom cannot assert computed layout).

Fix: added a `.chipList[hidden] { display: none }` rule so the `hidden` attribute actually hides the container. Verified live (CSS-only change; jsdom cannot assert computed layout).

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0041 — reference chips render side by side instead of stacked

Area: frontend. Severity: low. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: the expanded references render as chips laid out side by side on the same line, wrapping to the next line only when they run out of horizontal room. v1 lists each reference on its own line (a vertical stack).

Root cause: `CitationPanel.module.css` styles the chip container as a wrapping row (`.chipList { display: flex; flex-wrap: wrap }`); v1's reference list is a column.

Fix: changed `.chipList` to a column layout (`flex-direction: column`, `align-items: flex-start`) and let each chip size to its content, so references stack one per line. Verified live.

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0042 — expanded reference has no link to open the document

Area: frontend (with a backend dependency). Severity: medium. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: opening a reference in the citation detail column shows the title and the reference text but no "Open document" link, so there is no way to view the underlying file (for example a sample PDF).

Root cause: the v2 ingestion pipeline intentionally omits a `url` field from the search index — `SearchDocument` writes only `id`, `content`, `title`, and `content_vector`, and the blob filename survives only in `title` (`title = chunk.source`). The index `url` column is therefore never populated, so `SearchResult.url` and in turn `Citation.url` are always the empty string, and `CitationDetailPanel` gates the open-document link on a non-empty `url`. The backend also exposes no file-serving endpoint (routers: health, conversation, history, speech, admin), so even a recovered filename had nowhere to resolve. v1 served documents through a Flask `/api/files/<filename>` proxy that streamed the blob from the documents container with path-traversal and content-type guards.

Fix (multi-unit): recover the filename from `Citation.title`; add a backend `GET /api/files/{filename}` route that streams the blob from the documents container via the existing storage helpers, sets the content type from the filename and `Content-Disposition: inline` (so the browser renders the PDF or native format in a new tab), and rejects path traversal / double-encoding; register the router on the app; and render an absolute `${VITE_BACKEND_URL}/api/files/<filename>` open-document link in `CitationDetailPanel` (the cloud frontend is a static-only app on a separate origin, so the new-tab navigation needs the absolute backend URL). Verified with backend pytest + a live probe (`200`, `Content-Type: application/pdf`, `Content-Disposition: inline`, 544 KB body) plus browser confirmation of the toggle-collapse, vertical-stack, and open-document-in-new-tab behaviors.

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0043 — raw KB citation markers leak into the reasoning panel

Area: backend. Severity: low. Status: fixed (found 2026-06-15, fixed 2026-06-19).

Symptom: the reasoning/thinking panel renders raw native citation markers of the form `【message_idx:search_idx†source_name】` (for example `【6:1†source】`) inline in the reasoning text. These full-width-bracket markers are an internal citation encoding, not human-readable content, so they appear as garbage in the panel and need to be removed.

Root cause: the `agent_framework` orchestrator grounds through the server-side Foundry IQ Knowledge Base, whose model emits native `【N:M†source】` markers. Those markers are normalized away from the **answer** string by `normalize_kb_citations` (which rewrites attributed markers to the shared `[docN]` shape and strips any residual marker via `_KB_MARKER_RE`), but that normalization runs only over the answer. In `_update_to_events`, `text_reasoning` content blocks are forwarded **verbatim** to the `reasoning` channel with no marker stripping. When a reasoning-capable model (gpt-5 / o-series) produces a reasoning summary that references KB sources, it emits the same `【N:M†source】` markers inline in that summary, and they reach the frontend reasoning panel unmodified. The panel has no `[docN]` citation rendering, so the markers show as raw text. The `langgraph` path is unaffected (it emits `[docN]` markers, not native ones, and its reasoning summaries carry no KB markers).

Fix (landed 2026-06-19): added a `strip_kb_markers(text) -> str` helper to [tools/citations.py](../../src/backend/core/tools/citations.py) that removes every `_KB_MARKER_RE` match and collapses the doubled horizontal whitespace the removal leaves behind (`[ \t]{2,}` → single space), preserving newlines so a multi-line reasoning summary keeps its structure. `_update_to_events` ([agent_framework.py](../../src/backend/core/providers/orchestrators/agent_framework.py)) now wraps the `text_reasoning` text in `strip_kb_markers` before emitting the `reasoning` event, so the answer-side `normalize_kb_citations` keeps sole ownership of rewriting markers to `[docN]` while the reasoning channel only drops them. The `langgraph` path is untouched (it emits `[docN]`, not native markers). The helper is exported via `__all__`.

Verification: four `strip_kb_markers` unit tests in `tests/backend/core/tools/test_citations.py` (removes-and-collapses, multiple markers, preserves newlines, no-op without markers) + one orchestrator test `test_run_strips_native_kb_markers_from_reasoning_events` asserting a reasoning frame containing `【6:1†source】` is emitted clean and no native marker leaks onto any channel. Citations + agent_framework suites 82 passed; full backend suite 1075 passed / 0 failed; changed files 0 type/lint errors. (The unrelated pre-existing `ROUTERS-NON-ROUTE-CONTENT-DEBT` shared-gate red on `_DI_PARSER_PROBE_KEY` in `admin.py` is from the BUG-0034 fix, not this change.)

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0044 — stale conversation-route test asserts narration metadata `{}` (source emits `{"placeholder": True}`)

Area: backend. Severity: low. Status: fixed (found and fixed 2026-06-15 while wiring conversation persistence for BUG-0017).

Symptom: `tests/backend/test_conversation.py::test_sse_emits_leading_retrieval_narration_when_search_wired` fails at `HEAD`: `{'content': '…', 'metadata': {'placeholder': True}} != {'content': '…', 'metadata': {}}`. The failure was latent because the conversation-route suite was not re-run between the source change and the BUG-0017 wiring.

Root cause: BUG-0036 added `metadata={"placeholder": True}` to the leading retrieval-narration `reasoning` event that `run_chat` emits (so the frontend can replace the transient placeholder bubble once real reasoning streams). The narration test's expected frame was never updated to match — it still asserts `metadata: {}`. The source is correct (the placeholder marker is required by BUG-0036's FE replace-not-append behavior); the test expectation is stale.

Fix: updated the expected metadata in `test_sse_emits_leading_retrieval_narration_when_search_wired` to `{"placeholder": True}` so the test matches the shipped `run_chat` behavior; added a one-line comment noting the placeholder is the BUG-0036 frontend-replacement marker. No source change. Full conversation-route + services suites and the four shared AST gates pass (509 passed, 1 skipped).

References: [worklog/2026-06-15.md](worklog/2026-06-15.md); related BUG-0036 (the source change that introduced the placeholder metadata), BUG-0017 (the persistence wiring whose suite run unmasked the stale assertion).

### BUG-0045 — chat page still has a double scroll after the L1 height-chain fix

Area: frontend. Severity: medium. Status: fixed (found 2026-06-15, fixed 2026-06-19 by the operator).

Symptom: on the v2 chat page the whole page scrolls in addition to the inner message-list scroll (a "double scroll"). When the page scrolls, the app header slides off the top of the viewport and the message composer lifts off the bottom, exposing empty whitespace below it. The operator reports this is still present after the L1 fix and a hard reload.

Root cause (under investigation): the L1 fix targeted the shell height chain — it gave the Fluent provider wrapper a height (`.appFluentRoot { height: 100% }` on the `FluentThemeBridge` provider), changed CoralShell `.column` from `height: 100vh` to `height: 100%`, and added `min-height: 0` to ChatPage `.main` — on the hypothesis that `100vh` (which includes the strip under a horizontal scrollbar) combined with X-overflow from wide markdown tables was producing vertical page overflow. After the fix, agent Playwright measurements reported `document.scrollingElement.scrollHeight - clientHeight === 0` (no page overflow) across an empty chat, 2000px of injected content, a real four-message conversation, and a viewport sweep (1360×600 → 1280×1024) — only the inner `.scroll` region scrolled. Despite those measurements the operator still observes the double scroll in their own browser, so the reproducing condition is not captured by the automation environment. Candidates not yet ruled out: a specific browser/OS scrollbar mode or zoom level, a particular conversation/content state, or a layout path the headless viewport does not exercise. The original height-chain hypothesis is therefore incomplete.

Status: fixed 2026-06-19. The operator took ownership of the fix (per the original triage) and confirms the double scroll is gone — only the inner `.scroll` message-list region scrolls now; the app header stays pinned to the top and the composer to the bottom, with no page-level overflow. The L1 height-chain edits remain in place (they are independently correct and pass their `FluentThemeBridge` vitest) and are documented in the worklog's L1 and live-verification sections. Closed on operator confirmation.

References: [worklog/2026-06-15.md](worklog/2026-06-15.md).

### BUG-0046 — frontend never resolves the user or forwards an identity header

Area: frontend. Severity: high. Status: fixed (found 2026-06-15, fixed 2026-06-15).

Symptom: chat history is not isolated per user. The v2 SPA never resolves the signed-in user, keeps no user-id state, and forwards no `x-ms-client-principal-id` header on any of its four `fetch` clients (`streamChat`, `conversationHistory`, `admin`, `speech`). There is no detection of whether login is enforced, no error screen when login is enforced but no user is present, and no default-user fallback when login is not enforced.

Root cause: the backend is already user-scoped — the history and conversation routes consume `UserIdDep` (which reads `x-ms-client-principal-id`), and the Cosmos DB / PostgreSQL providers key every read and write on `user_id`, so a non-owned conversation already resolves to `404` on get / delete / rename. But the frontend was never wired to supply that identity. With the separate-origin topology (the SPA reaches the backend at an absolute `VITE_BACKEND_URL`), the Easy Auth header that the platform injects on the frontend origin does not travel to the cross-origin backend call, so without manual forwarding the backend sees no principal: in `production` mode `get_user_id` returns `401` on every history / conversation call, and in `local` mode every caller collapses into the single `local-dev` partition. Net: per-user isolation is non-functional even though the server-side enforcement is correct. Latent in local single-user development; surfaces in a cloud multi-user deployment.

Fix direction: port the v1 `getUserInfo()` flow and the reference architecture's header-forward pattern. On load, read an `auth_enforced` signal (folded into `GET /api/health`) and call `/.auth/me`; when login is enforced, resolve the real user from the `objectidentifier` claim and block with an error screen if it is absent; when login is not enforced, use the all-zeros default user `00000000-0000-0000-0000-000000000000`. Inject `x-ms-client-principal-id` on every API call via a single `userIdHeaders()` helper spread into each `fetch`. Add the backend `auth_enforced` signal and a light principal-id well-formedness check in `get_user_id`. The full unit checklist lives in [frontend-user-identity-plan.md](frontend-user-identity-plan.md).

Security: a browser-forwarded `x-ms-client-principal-id` is not a trust boundary — it is acceptable for chat-history partitioning only. Admin RBAC (`requires_role("admin")`) stays anchored on the backend's own server-injected Easy Auth claims (`x-ms-client-principal`); the browser must not forward the claims blob to satisfy a role check, and "valid" for a forwarded id means well-formed, not authentic. (The BUG-0046 id-forwarding change later regressed the admin local-dev bypass — see BUG-0047.)

Verification finding (2026-06-15): the original fix enumerated **four** `fetch` clients (`streamChat`, `conversationHistory`, `admin`, `speech`) and spread `userIdHeaders()` into each, but a live browser request-header capture showed `GET /api/history/conversations` still going out with **no** `x-ms-client-principal-id` while `GET /api/admin/status` carried the default id correctly. Root cause of the miss: `HistoryPanel.tsx` is a **fifth** request seam — it does not use any `api/*.tsx` client; it has its own inline `fetchJson` helper that built headers from scratch (`Content-Type` only), so the panel's list / rename / delete calls bypassed the shared identity seam. This is the primary history surface, so the core requirement ("the chat history call must be a query by the user id" / "delete must belong to the same user") was unmet from the browser even though the per-client unit tests passed. Fixed by spreading `userIdHeaders()` into `HistoryPanel`'s `fetchJson` (smallest change — no refactor to route the panel through a shared client). Re-verified live: `/api/history/conversations` now carries the default id. Regression guard added in `HistoryPanel.test.tsx` (the fetch harness now captures the forwarded principal and asserts it rides the list, rename, and delete calls). Caught only because the verification step captured **real** browser request headers rather than trusting the mocked per-client suites — the mocks asserted the clients that were wired, not the seam that was missed.

References: [worklog/2026-06-15.md](worklog/2026-06-15.md), [frontend-user-identity-plan.md](frontend-user-identity-plan.md).

### BUG-0047 — admin panel 401s in local dev (claims-absent bypass regression)

Area: backend. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: every `/api/admin/*` request (`GET /api/admin/status`, `/config`, `/documents`, …) returns `401 {"detail":"Missing client principal claims; Easy Auth claims header required."}` in local development, so the admin UI cannot load. Observed at `http://localhost:5273/api/admin/status` → `401`.

Root cause: a regression introduced by the BUG-0046 identity-forwarding fix. The SPA's single modular header seam, `userIdHeaders()` in `api/auth.tsx`, now spreads `x-ms-client-principal-id` (the resolved user, or the all-zeros default `00000000-0000-0000-0000-000000000000` when auth is not enforced) onto **every** API call — including all admin calls. The backend admin gate `requires_role("admin")._checker` (in `dependencies.py`) has a local-dev bypass that returned the synthetic `local-dev` admin **only when both** Easy Auth headers were absent (`if not principal_id and not claims_raw`). Because the id header is now always present, that condition is never true locally, so the gate falls through to the next branch (`if not claims_raw` → `401`). The bypass guard was written against the pre-BUG-0046 world where the SPA sent no headers at all.

Why the frontend is NOT the fix: the missing header is the `x-ms-client-principal` **claims** blob, which carries the role claims the gate evaluates. That blob must stay server-injected by Easy Auth and must never be forged or forwarded by the browser — a browser-supplied claims blob would let any caller assert the admin role. The frontend's modular seam already forwards everything it legitimately can (the partition id); supplying the claims blob from the SPA is the documented anti-pattern (see the BUG-0046 Security note). The correct, modular fix is the single backend conditional, which fixes all admin endpoints simultaneously.

Fix: the local-dev bypass now keys on the **absence of the claims blob** (the sole authority for the role check), not on both headers being absent. When `x-ms-client-principal` is absent: in `local` the gate returns `local-dev` (the admin panel is exercisable end-to-end without forging claims, regardless of whether the forgeable id header rode along); in any non-local environment it raises `401` and fails closed (the id header alone can never satisfy a role gate). Authenticated callers (claims present) are unchanged — decode, check the role, `403` on absence. Regression test `test_requires_role_falls_back_to_local_dev_when_id_present_no_claims_in_local` locks the id-present + claims-absent + `local` → `local-dev` path; the existing production no-claims `401` test continues to guard the fail-closed boundary.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0048 — admin document delete 503s (`title` not filterable in the index)

Area: backend. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: `DELETE /api/admin/documents/{source}` returns `503 {"detail":"Azure dependency temporarily unavailable."}` for every delete. Observed deleting `Benefit_Options.pdf` from the admin documents list (`DELETE http://localhost:5273/api/admin/documents/Benefit_Options.pdf` → `503`).

Root cause: `AzureSearch.delete_by_source` resolved the chunk ids to delete by issuing a server-side OData filter — `client.search(search_text="*", filter="title eq '<source>'", select=["id"])` — but the deployed index schema (`scripts/post_provision.py` `_build_chat_index`) defines `title` as a `SearchableField`, which is **searchable but not filterable**. Azure Search therefore rejects the query with `azure.core.exceptions.HttpResponseError: Invalid expression: 'title' is not a filterable field. Only filterable fields can be used in filter expressions. Parameter name: $filter`. That `AzureError` propagates to the app-level handler in `backend.app`, which sanitises every `AzureError` to `503 "Azure dependency temporarily unavailable."` (Hard Rule #14, no SDK detail leaked). The sibling `list_sources` method in the same provider already documents and works around the identical fragility for the *facetable* attribute (it avoids faceting on `title` and pages client-side), so filtering was the one path that still assumed an index capability the schema does not grant.

Why it is not a name-vs-id problem: a wrong identifier returns `404` (the `deleted == 0` branch), not `503`. The delete already removes **by id** — it collects the chunk `id`s and calls `client.delete_documents(documents=[{"id": ...}])`. Only the *id-discovery* step failed. The admin documents list surfaces *sources* (filenames / URLs), not per-chunk ids, and one logical document is N chunks each with its own search-doc `id`, so the `DELETE /…/{source}` contract is correct; the frontend has no per-chunk id to send.

Fix: resolve source → ids client-side, mirroring `list_sources`. `delete_by_source` now pages every chunk (`client.search(search_text="*", select=["id", "title"])`, auto-paging, no `top` cap, no server-side `$filter`), keeps the `id`s whose `title` exactly equals the requested source, then deletes them in ≤1000-id batches as before. Because the user-supplied filename is no longer interpolated into an OData expression, the OData-injection seam that the previous `title eq '<source>'` filter opened is closed by construction (matching is plain Python equality). No index-schema change, no reindex, no frontend change; works against the already-deployed index. Tests updated to assert the client-side contract (no `filter` kwarg; `select == ["id", "title"]`; non-matching titles are not deleted; a mixed-source scan deletes only exact-title matches).

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0049 — DOCX (and Office / HTML) uploads index nothing (`pages[].lines` empty)

Area: backend. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: uploading a `.docx` file appears to succeed (upload + enqueue + `batch_push` all report success), but the document never appears in the search index and cannot be queried. No error surfaces to the operator — the file silently fails to index.

Root cause: `DocumentIntelligenceParser.parse` ([azure/functions/core/parsers/document_intelligence_parser.py](../src/functions/core/parsers/document_intelligence_parser.py)) builds chunks exclusively from `result.pages[*].lines[*].content`. Azure Document Intelligence's `prebuilt-layout` model handles paginated formats (PDF, images) by returning `pages[*].lines`, but it treats Microsoft Office (DOCX/PPTX/XLSX) and HTML inputs as **"pageless"**: it returns the extracted text in `result.paragraphs` (and the flat `result.content`) and leaves `pages[*].lines` empty. Confirmed against the live service: a sample DOCX returned `len(pages)=1`, per-page `len(lines)=[0]`, `len(paragraphs)=848`, `len(result.content)=29335`, while a sample PDF returned `len(pages)=4`, per-page `len(lines)=[4,6,34,25]`. So the page loop produces **zero** chunks for DOCX; the handler `batch_push_handler` then logs its zero-chunk info line and returns `[]` without ever embedding or indexing, and the upload boundary already returned `200` — hence the silent false success.

Why one chunk per `result.content` is the wrong fix: the `batch_push` handler embeds each `Chunk.content` directly — there is no downstream re-chunker (v2 removed the chunker primitive; parsers own chunking per format). A single 29 KB chunk would push the embedding deployment toward its token ceiling and produce one undifferentiated vector with poor retrieval granularity. The codebase already has the right precedent: `TextParser` chunks plain text **one chunk per paragraph**, and Document Intelligence already returns the DOCX text pre-split into 848 semantic `paragraphs`.

Fix: add a paragraph fallback. After the existing page pass, when it produced **zero** chunks, emit one chunk per `result.paragraphs` entry (stripping empties, keeping `index` dense for stable `make_chunk_id`s) — the same semantic unit `TextParser` uses. The fallback fires only when the page pass yielded nothing, so paginated formats (PDF, images) keep their one-chunk-per-page shape and never double-emit (a PDF returns both `pages[].lines` and `paragraphs`, but its page chunks suppress the fallback). No new chunker abstraction, no infra / model / frontend change; works against the already-deployed index. Verified live: the sample DOCX now produces 848 paragraph chunks and indexes end-to-end.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0050 — Admin button missing in the deployed app (relative admin URLs hit the SPA catch-all)

Area: frontend. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: in the deployed app the **Admin** button never appears in the chat header, and probing the admin-availability endpoint returns `200` with an HTML body instead of JSON. Chat works normally.

Root cause: the admin REST client ([admin.tsx](../src/frontend/src/api/admin.tsx)) is the **only** frontend fetch client that issues **relative** `/api/admin/*` URLs. Every sibling client prepends `VITE_BACKEND_URL`: `streamChat.tsx`, `conversationHistory.tsx`, `HistoryPanel.tsx`, the `App.tsx` health probe, and `documentHref.tsx`. The deployed topology is split-host — the frontend is an App Service container and the backend is a separate Container App on a different origin — and `VITE_BACKEND_URL` (baked into the bundle at build time) is what every client uses to cross to the backend. Because the admin client omitted that base, a relative `/api/admin/status` resolved against the **frontend** origin, and the frontend's SPA server ([frontend_app.py](../src/frontend/frontend_app.py)) has a catch-all `@app.get("/{full_path:path}")` that returns `index.html` for any path that is not an on-disk file. So the admin probe received `index.html` (HTTP `200`, `Content-Type: text/html`); `getAdminStatus()` then called `response.json()`, which threw on the HTML body; the `App.tsx` availability effect caught the throw and set `adminAvailable=false`; and the `Header` renders the Admin button only when `adminAvailable===true`. Confirmed live: `GET <frontend-host>/api/admin/status` returned `200` `<!doctype html>…`, while `GET <backend-host>/api/admin/status` returned `200` `{"orchestrator_name":…}` for the same path, with or without an identity header (this deployment has `auth_enforced:false`, so the admin gate does not block the status read). Local dev was unaffected because Vite proxies relative `/api` to the backend.

Why this is not an auth or backend bug: the backend admin status endpoint already returns `200` JSON in this deployment regardless of the `x-ms-client-principal` header, so once the request reaches the backend origin the button resolves. The only defect is client-side URL construction.

Fix: add the same `backendUrl()` (reads `VITE_BACKEND_URL` at call time, empty when unset) + `apiUrl(path)` (joins the base with the API path, trailing slash trimmed) seam the sibling clients already use, and prepend `apiUrl(...)` to all 8 admin fetch sites (`getAdminStatus`, `addDocumentUrl`, `uploadDocument`, `reprocessAll`, `listDocuments`, `deleteDocument`, `getAdminConfig`, `patchAdminConfig`; `resetAdminConfig` delegates to `patchAdminConfig`). When `VITE_BACKEND_URL` is unset the base is empty, so the relative-URL behavior (and the existing unit assertions) are unchanged; when set, admin calls target the backend origin like every other client. A new unit test asserts `getAdminStatus` prepends the base when the env var is set. No backend / infra / auth change. The deployed frontend image must be rebuilt (the base is baked at build time) and the App Service container repointed for QA to receive the fix.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0051 — Admin document upload 503s (backend container missing storage env vars)

Area: infra. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: uploading a document from the (now-visible, post-BUG-0050) admin UI returns `503 {"detail":"Document storage is not configured for this deployment."}`. The same gate fronts URL ingestion, reprocess-all, and the file-download route.

Root cause: the backend admin endpoints ([admin.py](../src/backend/routers/admin.py)) gate document upload / URL ingest / reprocess on `settings.storage.documents_container` **and** `settings.storage.doc_processing_queue` being non-empty, and `GET /api/files/{filename}` ([files.py](../src/backend/services/files.py)) needs the documents container; `StorageSettings` ([settings.py](../src/backend/core/settings.py), `env_prefix="AZURE_"`) defaults every storage field to `""`. The `backendContainerApp` module in [main.bicep](../infra/main.bicep) never wired the storage env vars into its container `env` array — `AZURE_STORAGE_ACCOUNT_NAME` / `AZURE_DOCUMENTS_CONTAINER` / `AZURE_DOC_PROCESSING_QUEUE` were set **only** on the function app block (and emitted as stack outputs), so the deployed backend resolved all three to empty and the gate tripped on every upload. Confirmed live: the backend Container App's container `env` listed none of the three storage vars.

Why this is not an RBAC bug: the shared user-assigned managed identity already holds Storage Blob Data Owner, Storage Blob Data Contributor, Storage Queue Data Contributor, and Storage Queue Data Message Sender on the storage account (the function app performs ingestion blob + queue I/O under the same identity). Once the env vars are present, the backend writes and enqueues with no permission change.

Fix: add the same three storage env entries the function app uses (`AZURE_STORAGE_ACCOUNT_NAME` = `effectiveStorageName`, `AZURE_DOCUMENTS_CONTAINER` = `documentsContainerName`, `AZURE_DOC_PROCESSING_QUEUE` = `docProcessingQueueName`) to the `backendContainerApp` container `env` array. The blob and queue endpoints are derived from the account name by `resolve_storage_endpoints` ([storage_endpoints.py](../src/functions/core/storage_endpoints.py)) — it builds `https://<account>.blob.core.windows.net` and swaps `.blob.` → `.queue.` — so no explicit `AZURE_STORAGE_BLOB_ENDPOINT` var is required (matching the function app, which also omits it). `bicep build` passes with no new errors.

Deploy: a live hotfix `az containerapp update --set-env-vars AZURE_STORAGE_ACCOUNT_NAME=<account> AZURE_DOCUMENTS_CONTAINER=documents AZURE_DOC_PROCESSING_QUEUE=doc-processing` added the three vars to the deployed backend (additive, reversible) to unblock QA immediately; the new revision came up Healthy and an end-to-end `.txt` upload returned `200` `{"blob_path":"documents/…","ingestion_job_id":"…","queued":true}`, proving the config gate, blob write, and queue enqueue all succeed under the UAMI. The smoke-test blob was deleted afterward. The bicep edit makes the wiring durable across re-provision.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0052 — Deployed parsing 503 (backend + function missing AI Services endpoint; UAMI lacks DI role)

Area: infra. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: with the admin UI working and storage wired (post-BUG-0051), uploading a PDF/DOCX to `POST /api/admin/documents` on the deployed backend returns `503 {"detail":"Document parsing is not configured for this deployment."}`.

Root cause: two deployment gaps. (1) The BUG-0034 unit-(d) upload guard ([admin.py](../src/backend/routers/admin.py)) refuses a Document-Intelligence-routed file when `AZURE_AI_SERVICES_ENDPOINT` is empty or non-HTTPS — and the deployed **backend** Container App and the **function** app were both missing that env var (the same class of bicep gap as BUG-0051: the var was emitted only as a stack output, never wired into either runtime's `env` array). (2) Even past the guard, the shared UAMI lacked **Cognitive Services User** on the AI Services account, so `DocumentIntelligenceParser` ([document_intelligence_parser.py](../src/functions/core/parsers/document_intelligence_parser.py)) could not authenticate to Document Intelligence (`begin_analyze_document`) under managed identity.

Fix (live): added `AZURE_AI_SERVICES_ENDPOINT=https://aisa-<SUFFIX>.cognitiveservices.azure.com/` to both the backend Container App (`az containerapp update --set-env-vars`) and the function app (`az functionapp config appsettings set`), and granted the UAMI **Cognitive Services User** scoped to the AI Services account (`az role assignment create`). After the backend revision cycled, a PerksPlus.pdf upload returned `200 {…,"queued":true,"ingestion_job_id":"…"}` — the guard passes and the file enqueues. End-to-end parse verification is blocked downstream by BUG-0053 (the queued message is never consumed, so the DI parse under the new role has not yet executed in the deployed environment).

Durable follow-up: both the env var (backend + function `env`) and the RBAC role assignment should be added to `main.bicep` so the wiring survives re-provision — noted as follow-up, not applied this turn (further bicep edits paused by the operator).

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0053 — batch_push queue trigger never fires on Flex Consumption (scale-from-zero, identity-based queue connection)

Area: functions. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: documents uploaded through the admin UI enqueue successfully (`queued:true`) but never parse, embed, or index — they never appear in `GET /api/admin/documents`. The `doc-processing` queue holds the messages indefinitely at `dequeueCount=0` (never dequeued); PerksPlus.pdf sat unprocessed for over an hour.

Root cause (diagnosed): the function app (`func-<SUFFIX>`, Flex Consumption, Linux/Python 3.11) uses **per-function (target-based) scaling** — each non-HTTP trigger gets its own scale group. The `batch_push` queue trigger runs only in the `function:batch_push` group, which the platform **scale controller** must wake from zero based on queue depth. The HTTP-served instances (health/admin, woken by direct HTTP) do **not** run `batch_push`. The scale controller is not waking the `function:batch_push` group for the identity-based (`AzureWebJobsStorage__credential=managedidentity`) Storage Queue connection, so nothing ever polls `doc-processing`.

Everything else is provably correct: the trigger metadata is registered (`bindings:[{type:queueTrigger, queueName:%AZURE_DOC_PROCESSING_QUEUE%, connection:AzureWebJobsStorage}], isDisabled:null`); the queue name resolves to `doc-processing` (where the messages are); RBAC is sufficient (the UAMI holds **Storage Queue Data Contributor**, which ⊃ the doc-required Storage Queue Data Reader + Storage Queue Data Message Processor); the app has **only** the user-assigned identity and `AzureWebJobsStorage__clientId` matches it; storage networking is fully open (`publicNetworkAccess=Enabled`, `defaultAction=Allow`, no private endpoints, no VNet integration); and the Flex scale config is healthy (`maximumInstanceCount=40`, target-based defaults, identity-based deployment storage via the UAMI). Decisively, the function **works when an instance is warm** — right after a deploy a warm `batch_push` instance processed messages — so the binding, auth, and code are all correct; the failure is purely scale-from-zero.

No-cost levers tried, none of which woke the group (the queue stayed at 7 messages, `dq=0`, after all four): (a) added explicit `AzureWebJobsStorage__blobServiceUri` / `__queueServiceUri` / `__tableServiceUri` — the documented way for the scale controller to resolve identity-based endpoints, since the `__accountName` shorthand is host-runtime-only; (b) forced a full `az functionapp stop` + `start` cycle (plain `az functionapp restart` never cycled the running instances); (c) `syncfunctiontriggers` to force the platform to re-read triggers + scale config; (d) enabled a **system-assigned identity** on the app (alongside the UAMI) and granted it **Storage Queue Data Contributor** + **Storage Blob Data Owner** scoped to the storage account — on the theory the scale controller might poll under the system-assigned identity — then cycled + re-synced. The queue still did not drain ~15 min after the grant.

Definitive evidence (from `FunctionAppLogs` in the `log-<SUFFIX>` Log Analytics workspace, which — unlike App Insights, BUG-0055 — *is* populated via the app's `allLogs` diagnostic setting): every host start logs `batch_push` with a `Microsoft.Azure.WebJobs.Script.Description.FunctionGroupListenerDecorator+NoOpListener`. The `NoOpListener` **is** Flex Consumption's per-function-scaling mechanism — on instances **not** allocated to the `function:batch_push` scale group (the HTTP/health instances woken by direct HTTP), the real queue listener is deliberately replaced by a no-op that never polls. Across the entire 24h window **no** instance ever started the real `batch_push` listener, which proves the scale controller never allocated a `function:batch_push` instance and the queue poll never happens from zero. This matches the Flex Consumption troubleshooting doc's "trigger registration succeeded but the listener didn't enable" failure class exactly.

Telemetry path: App Insights is empty (BUG-0055), but the host *does* emit platform logs to the `log-<SUFFIX>` Log Analytics workspace (≈500 `Information` rows / 24h) via its `allLogs` diagnostic setting — that path supplied the `NoOpListener` evidence above. The scale controller's own decision trace (why it computes desired=0 for the identity-based queue) is **not** in `FunctionAppLogs`; it would require the App Insights scale-controller traces, which are missing.

Resolution: no-cost levers were exhausted (the speculative system-assigned-identity route (d) was tried and failed; runtime scale monitoring is Premium + VNet only, N/A to this non-VNet Flex app). The only no-regression fix is `alwaysReady = {function:batch_push: 1}` — one always-warm instance runs the real listener instead of the `NoOpListener` and polls the queue. The operator approved the small always-on cost, applied live (cloud variable, not bicep): `az functionapp scale config always-ready set -g <RESOURCE_GROUP> -n func-<SUFFIX> --settings function:batch_push=1`. The always-ready instance immediately ran the real listener and drained `doc-processing` (verified 7→0, then 3→0 on a fresh upload). The identity-based / no-secret design is preserved (switching `AzureWebJobsStorage` to a connection string was rejected). Durable back-port: add `alwaysReady` to the function app's bicep `functionAppConfig.scaleAndConcurrency` (follow-up; bicep edits paused). Note: with the listener fixed, ingestion still failed at message validation — a **separate** base64-encoding defect tracked as **BUG-0056**.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0054 — Event Grid blob-created events poison the doc-processing queue (schema mismatch)

Area: infra. Severity: medium. Status: fixed — all four close-out steps satisfied and cloud-validated on the fresh `<AZD_ENV_NAME>` deployment (found 2026-06-16, fix designed + locally validated 2026-06-20, `blob_event` deployed 2026-06-24, closed out + cloud-validated 2026-07-02; see the Resolution note below).

Symptom: the `doc-processing-poison` queue holds 10 base64 `Microsoft.Storage.BlobCreated` Event Grid envelopes (inserted 13:46–13:48).

Root cause: a stray/legacy Event Grid subscription on the storage account delivers blob-created events into `doc-processing`, but `batch_push` expects CWYD ingestion envelopes (`{container, blob_filename, ingestion_job_id, …}`), not the Event Grid `Microsoft.Storage.BlobCreated` schema. The events fail validation, retry, and land in poison. This is noise/poison accumulation — the events are **distinct** messages from the CWYD envelopes, so it does **not** block ingestion — but it pollutes the poison queue and indicates a stray Event Grid → queue wiring that should be removed or pointed at a handler that understands the blob-event schema. The same 8-envelope artifact was noted under BUG-0034's poison cleanup.

Fix design ([ADR 0028](adr/0028-event-grid-single-trigger-blob-ingestion.md)): a `blob_event` queue-trigger blueprint (`src/functions/blob_event/`) translates the raw Event Grid `BlobCreated` envelope into the CWYD `doc-processing` message, so the schema mismatch can never poison `doc-processing`. The Event Grid subscription is repointed off `doc-processing` onto a dedicated `blob-events` queue (with `blob-events-poison`) that only `blob_event` drains. A new `StorageSettings.ingestion_trigger` setting (`direct_enqueue` | `event_grid`; env `AZURE_INGESTION_TRIGGER`; bicep param `ingestionTrigger`; default `direct_enqueue`) lets the backend stop double-enqueuing once Event Grid owns ingestion — under `event_grid` the admin upload stores the blob and lets Event Grid drive the pipeline; under `direct_enqueue` (default) the backend enqueues to `doc-processing` as before.

Locally validated end-to-end (2026-06-20): with the local Functions host running `blob_event` against cloud storage, a blob dropped into the `documents` container flowed Event Grid → `blob-events` → `blob_event` → `doc-processing` → `batch_push` → pgvector, and the document appeared in `GET /api/admin/documents`. Test artifacts (blob, index entry, poison) cleaned up afterward.

Cloud deploy deferred on 2026-06-20, completed 2026-06-24: the operator initially parked the cutover — `azd deploy function` uploaded the package but exceeded azd's 20-minute wait, attributed at the time to "Flex Consumption remote-build slowness" (later root-caused as the `agent-framework` umbrella / hyperlight Python-3.11 resolver hang, BUG-0080). As of that 2026-06-20 decision the cloud Function App still served only the prior five functions (`add_url`, `batch_push`, `batch_start`, `health`, `search_skill`) and `blob_event` was **not** yet registered (a forced `syncfunctiontriggers` confirmed nothing new had deployed). The cutover was later completed under **BUG-0080** — see the close-out note below. Separately, `blob_event` is a queue trigger, so in Flex it carries the same **BUG-0053** scale-from-zero risk `batch_push` had — cloud `blob_event` reliability is coupled to that always-ready config.

Live state: the backend stays on `direct_enqueue` (`AZURE_ENV_INGESTION_TRIGGER` is unset → the default), so admin uploads keep working via the direct enqueue path. The env flag is **not yet** flipped to `event_grid` — that flip is the first close-out step below, deliberately held until after the `blob_event` deploy (which landed 2026-06-24) so the cloud never sits with the backend enqueue removed and no `blob_event` draining `blob-events`. The committed `infra/main.bicep` points the Event Grid subscription at `blob-events`; confirming the **live** subscription matches (delivers to `blob-events`, not `doc-processing`) is a close-out verification step below. A prepackage gap surfaced during this work (the staged artifact omitted `blob_event`) and was fixed under BUG-0078 with a guard test.

Close-out (BUG-0054 stays **open** until all four pass): `blob_event` is already deployed to the cloud Function App (2026-06-24, BUG-0080 — the app is at 6 functions), and its `alwaysReady` instance for `function:blob_event` is in `infra/main.bicep` (added 2026-06-29, guarded by `tests/infra/test_main_bicep.py::test_function_app_keeps_blob_event_always_ready`), so the function-deploy step is **done**. The remaining operator-driven steps are: (1) flip the ingestion trigger — `azd env set AZURE_ENV_INGESTION_TRIGGER event_grid` then `azd provision` — to turn off the backend double-enqueue and let Event Grid drive ingestion; (2) verify the **live** Event Grid system-topic subscription delivers blob-created events to the `blob-events` queue (not `doc-processing`); (3) re-validate end-to-end in the cloud (blob create + delete) — the BUG-0058 cloud-verify gate, with the delete path covered by BUG-0077; (4) drain the 4 historical `doc-processing-poison` messages left from the original stray-subscription wiring.

Resolution (2026-07-02): the close-out completed against the fresh `<AZD_ENV_NAME>` deployment (suffix `<SUFFIX>`, cosmosdb, function hosted as the `ca-func-<SUFFIX>` Container App). (1) Ingestion trigger — the azd env carries `AZURE_ENV_INGESTION_TRIGGER=event_grid` / `AZURE_INGESTION_TRIGGER=event_grid`, and the deployed backend Container App (`ca-backend-<SUFFIX>`) carries `AZURE_INGESTION_TRIGGER=event_grid`, so the backend stores the blob and lets Event Grid drive ingestion (no double-enqueue). (2) Live Event Grid wiring — the sole storage system topic `evgt-<SUFFIX>` has one CWYD subscription `blob-created-to-doc-processing` that delivers `Microsoft.Storage.BlobCreated`+`BlobDeleted` (subject `subjectBeginsWith=/blobServices/default/containers/documents/`) via `deliveryWithResourceIdentity` (system-assigned) to the `blob-events` queue — not `doc-processing`; the only other subscription is the built-in `StorageAntimalwareSubscription`. No stray direct-to-`doc-processing` subscription exists. (3) Cloud end-to-end — a uniquely-worded test blob uploaded to `documents` flowed Event Grid → `blob-events` (observed depth 1 → 0) → `blob_event` → `doc-processing` → `batch_push` → embed → `cwyd-index` (263 → 264; a search for the sentinel phrase returned the `bug0054-e2e-test.txt` chunk) in ~65 s; deleting the blob fired `BlobDeleted` → `blob-events` → `blob_event` → `delete_by_source`, removing the chunk (264 → 263, sentinel → 0) in ~21 s. Both poison queues stayed at 0 the entire run, proving the schema-mismatch poisoning can no longer occur. (4) Poison drain — N/A: all queues (`doc-processing`, `doc-processing-poison`, `blob-events`, `blob-events-poison`, `add-url-poison`) were already at 0 in this fresh env; the "4 historical" poison messages were in the retired deployment. The BlobDeleted half also confirms the BUG-0077 delete-branch live.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md), [worklog/2026-06-20.md](worklog/2026-06-20.md), [worklog/2026-06-24.md](worklog/2026-06-24.md), [worklog/2026-07-02.md](worklog/2026-07-02.md); [ADR 0028](adr/0028-event-grid-single-trigger-blob-ingestion.md); related BUG-0053 (scale-from-zero), BUG-0058 (stale deploy artifact / cloud-verify gate), BUG-0078 (prepackage allow-list), BUG-0077 (BlobDeleted follow-up — its delete-branch is live-confirmed here), BUG-0080 (the 2026-06-24 `agent-framework-core` repin that unblocked the `blob_event` deploy).

### BUG-0055 — Application Insights receives zero telemetry from the function + backend

Area: infra. Severity: medium. Status: fixed (found 2026-06-16, fixed 2026-07-02).

Symptom: `appi-<SUFFIX>` has never received any telemetry. A union query over `requests` / `traces` / `exceptions` / `dependencies` / `customMetrics` summarizing `count` / `min(timestamp)` / `max(timestamp)` returned `[0, null, null]` — total 0, no earliest, no latest.

Root cause (narrowed): the function's `APPLICATIONINSIGHTS_CONNECTION_STRING` was verified to **match** the `appi-<SUFFIX>` resource exactly (same ingestion endpoint + instrumentation key), so it is **not** a connection-string mismatch. Separately, the host **does** emit platform logs to the `log-<SUFFIX>` Log Analytics workspace via its `allLogs` diagnostic setting (≈500 `Information` rows / 24h in `FunctionAppLogs`), so telemetry generation works and the gap is specifically the **App Insights ingestion path** — the SDK/OTel application telemetry (`requests` / `traces` / `dependencies`) is not being exported, while the platform resource-log path is. Narrowed cause: the OTel → App Insights exporter is not initialized/exporting in either runtime (the `FunctionAppLogs` resource-log path is unaffected because it is wired by the diagnostic setting, not the SDK). Still independently a defect (no application-level observability); the `FunctionAppLogs` path is a working substitute for host-log diagnosis in the meantime (it supplied the BUG-0053 `NoOpListener` evidence).

Backend half — root cause confirmed + fixed (2026-06-23): the backend ACA container is a plain container with no host-level App Insights agent, so its Python lifespan (`backend/app.py`) only emits telemetry when it calls `configure_azure_monitor(connection_string=...)`, and that connection string comes from `ObservabilitySettings.app_insights_connection_string`. `ObservabilitySettings` sets `env_prefix="AZURE_"`, so the field reads **`AZURE_APP_INSIGHTS_CONNECTION_STRING`** (a deliberate decision — CU-002b/CU-007, enforced by two `test_app_lifespan.py` tests that intentionally ignore the standard name). But [main.bicep](../infra/main.bicep) wired the backend container with the **standard** `APPLICATIONINSIGHTS_CONNECTION_STRING` (the name the Function host needs), so the typed setting stayed empty and `configure_azure_monitor` never fired in the cloud — zero backend telemetry. Local dev worked only because `v2/.env` uses the `AZURE_`-prefixed name. Fix (operator chose the Bicep path over reversing the code decision): rename the **backend** container's env var to `AZURE_APP_INSIGHTS_CONNECTION_STRING` (the Function App keeps `APPLICATIONINSIGHTS_CONNECTION_STRING`, which its host reads natively); the `test_appinsights_connection_string_bound_to_workload` drift-guard is now parametrized per workload, and [ADR 0018](adr/0018-monitoring-default-on-and-appi-rbac.md) carries Amendment 1. Durable in Bicep — takes effect on the next `azd provision`; not yet cloud-verified (deploy blocked by the platform Flex build outage, see worklog 2026-06-23).

Function half — worker OTel wired (2026-06-23): added `configure_telemetry()` in `src/functions/core/telemetry.py`, called once at module load from `function_app.py` (kept out of the host entry module so it stays a thin blueprint-registration surface with no logic), which reads the host-provided `APPLICATIONINSIGHTS_CONNECTION_STRING` and calls `configure_azure_monitor(connection_string=...)` so the Python worker's application telemetry (logs / traces / dependencies) exports to App Insights; absent (local dev / monitoring off) it no-ops. Two `tests/functions/core/test_telemetry.py` tests cover the configured + no-op paths. Note: host-level `requests` (invocation) telemetry is emitted by the Functions **host**, not the worker — if those stay absent after a deploy, enabling host OpenTelemetry (`host.json` `telemetryMode`) is a separate follow-up. Both halves are now code/config-complete but **not yet cloud-verified** (deploy blocked by the Flex build outage); keep open until App Insights shows backend + function telemetry post-deploy.

Real root cause + fix (2026-07-02): both exporter halves above were correctly wired — the actual blocker was the App Insights component's **`disableLocalAuth: true`** (the ADR-0018 WAF posture). With local auth disabled, the ingestion data plane requires a Microsoft Entra token, but both `configure_azure_monitor(connection_string=...)` call sites (backend `app.py` lifespan + functions `core/telemetry.py` worker) pass **no `credential=`**, so every ingestion request silently returned 401 — telemetry looked wired while nothing ever landed. This is why the connection string matched and the exporters were initialized yet the union query stayed `[0, null, null]`. Fix (match MACAE, whose App Insights `avm/res/insights/component` omits the flag and ingests via instrumentation key): set **`disableLocalAuth: false`** on the App Insights module so the existing connection-string-only exporters ingest via instrumentation key — **no application-code change**. Recorded as [ADR 0018](adr/0018-monitoring-default-on-and-appi-rbac.md) Amendment 2; the `Monitoring Metrics Publisher` role is retained (now unused) for a one-line revert to Entra-only ingestion (flip back to `true` + pass a sync `ManagedIdentityCredential`). Guarded test-first by `test_application_insights_enables_local_auth` in `tests/infra/test_main_bicep.py` (43 infra + env-ID-gate tests green; `az bicep build` clean). Deployed via `azd provision` (2026-07-02): the ARM deployment reported **`Succeeded`**, the live App Insights resource now reports **`disableLocalAuth: false`**, and the backend Container App is confirmed serving requests (telemetry primed with test requests; the silent-401 blocker that produced zero telemetry is removed, so ikey ingestion is now accepted). Delivered via the research → plan → implement → review workflow ([research](../../.copilot-tracking/research/2026-07-02/bug-0055-appinsights-zero-telemetry-research.md), [plan](../../.copilot-tracking/plans/2026-07-02/bug-0055-appinsights-local-auth-plan.instructions.md)).

References: [worklog/2026-06-16.md](worklog/2026-06-16.md), [worklog/2026-06-23.md](worklog/2026-06-23.md); [ADR 0018](adr/0018-monitoring-default-on-and-appi-rbac.md).

### BUG-0056 — CWYD ingestion envelopes poison in the cloud (queue messageEncoding base64 mismatch)

Area: functions. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: with BUG-0053's always-ready listener now draining `doc-processing`, a fresh PerksPlus.pdf admin upload still does **not** index — the document never appears in `GET /api/admin/documents`. The upload returns `200 {…,"queued":true,"ingestion_job_id":"…"}` and three messages appear on `doc-processing`, but the listener dequeues them and all three **poison** (poison depth +3). `FunctionAppLogs` shows `Executed Functions.batch_push (Failed, Duration=5–29ms)` with `batch_push queue message validation failed` — the failure is immediate (single-digit ms), before any blob download / parse / embed, i.e. at message validation, not in the pipeline.

Decoding the three newest poison bodies (each base64) shows the real shape: two are duplicate Event Grid `BlobCreated` envelopes (BUG-0054 noise) and **one is the genuine CWYD envelope** carrying the exact upload `ingestion_job_id` — `{"container_name":"documents","filename":"PerksPlus.pdf","ingestion_job_id":"…","force_reindex":false}`. That envelope **exactly matches** the current `BatchPushQueueMessage` contract (`container_name`, `filename`, `ingestion_job_id`, `force_reindex`; `frozen=True, extra="forbid"`), and the deployed package is from the same day with the only-ever contract — so this is **not** a schema/field-name skew.

Root cause: a **queue-message base64-encoding mismatch** between producer and consumer. The backend producer (`backend/services/ingestion.py` → `functions/batch_start/queue_writer.enqueue_push_message`) builds its `QueueClient` (`functions/core/storage_clients.py`) with **no `message_encode_policy`**, so the modern `azure-storage-queue` SDK uses `NoEncodePolicy` and sends the body as **raw JSON** (the `queue_writer.py` docstring's claim that the SDK base64-encodes by default is factually wrong). The Azure Functions queue trigger only accepts raw bodies when `extensions.queues.messageEncoding=none`; that setting exists **only** in `functions/local.settings.json` (so local dev works — confirmed by BUG-0034's successful local drain) and was **never** propagated to `host.json` or the cloud function app settings. The cloud Flex host therefore falls back to the Storage-Queue extension default `messageEncoding=base64`, cannot decode the raw-JSON body, and poisons every message after 5 retries. This reconciles all evidence: the host **base64-encodes on the way to the poison queue**, which is why every peeked poison body is base64 that decodes to a clean JSON envelope; Event Grid events poison for an independent reason (BUG-0054 schema mismatch) under either encoding.

Confirmed absent in the cloud: `az functionapp config appsettings list` returned no `messageEncoding` / `JobHost__extensions__queues` override (only `AzureWebJobsStorage__queueServiceUri`).

Fix (applied live, cloud variable — not bicep, not code): `az functionapp config appsettings set -g <RESOURCE_GROUP> -n func-<SUFFIX> --settings "AzureFunctionsJobHost__extensions__queues__messageEncoding=none"` (verified stored value `none`), then a forced restart so the `JobHost` setting loads at host startup. This matches the producer's raw-JSON output and the local configuration. Durable back-port — done: `host.json` now carries `extensions.queues.messageEncoding = none`, so every deploy ships the setting and the host reads it at startup, making the live `az`-applied app-setting override redundant on the next deploy. No bicep function-app-setting parity is needed because the package-level `host.json` is authoritative.

Resolution (confirmed 2026-06-16): the host loaded the setting (startup `QueuesOptions { … MessageEncoding: None }`) and the poison body for the post-fix upload is **raw JSON** (no longer base64), proving the host now reads/writes raw. The post-fix CWYD envelope passes validation, acquires its managed-identity token, downloads the blob (HTTP 206, full 115 KB), and parses the PDF — so the encoding defect is resolved. The document still does not index because embedding then fails with a 404; that downstream failure is a separate defect tracked as **BUG-0057**.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0057 — cloud ingestion embedding call returns 404 (embedding-deployment / endpoint mismatch)

Area: functions. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: after BUG-0056's `messageEncoding=none` fix, a fresh PerksPlus.pdf upload's CWYD envelope is now accepted by the queue trigger, but the document still does **not** index. `FunctionAppLogs` shows the envelope passes validation, acquires a managed-identity token, and downloads the blob (`Response status: 206 … Content-Length: 115310`, the full file), then fails with `batch_push queue handler failed` at ERROR level — retried 5 times ~7 s apart (01:16:58 → 01:17:26 UTC) before poisoning. The traceback bottoms out at `handler.py` `embedding_results = await embedder.embed(chunks)` → `embedders/azure_openai.py` `provider.embed(...)` → `llm/foundry_iq.py` `oai.embeddings.create(...)` → `openai.NotFoundError: Error code: 404`. So the failure is **after** blob download + PDF parse, at the embedding step (not validation, not storage).

Root cause: an embedding-deployment / endpoint mismatch across the two OpenAI-capable accounts in this environment. The search index `content_vector` field is **1536-dim**, which matches `text-embedding-3-small` (the value of `AZURE_OPENAI_EMBEDDING_DEPLOYMENT`) — `text-embedding-3-large` is 3072-dim — so the configured *model* is correct for the index. But the two accounts host different embedding deployments: `oai-<DATA_SUFFIX>` (`AZURE_OPENAI_ENDPOINT`) has **`text-embedding-3-small`** (the configured one); `aisa-<SUFFIX>` (`AZURE_AI_SERVICES_ENDPOINT`) has only **`text-embedding-3-large`**. The Foundry IQ provider routes embeddings to the **AI Services account** (`_get_embeddings_client` overrides `base_url` to `AZURE_AI_SERVICES_ENDPOINT` + `/openai/v1`, because the Foundry *project* route serves no `/embeddings`), so it asks `aisa-<SUFFIX>` for `text-embedding-3-small`, which is not there → 404. The **deployed** function build is also older than the local tree (deployed `foundry_iq.py` has `embeddings.create` at line 365 vs local 449 and lacks `_get_embeddings_client`), so it likely still routes embeddings to the Foundry project route, which 404s on `/embeddings` regardless of deployment name. No integrated vectorizer is configured on the index, so the function must compute embeddings itself — this path is unavoidable.

Why no pure cloud-variable fix exists: the embeddings `base_url` is derived from `AZURE_AI_SERVICES_ENDPOINT` (local code) or the fixed Foundry project route (deployed code), never from `AZURE_OPENAI_ENDPOINT` where `text-embedding-3-small` lives. Repointing `AZURE_AI_SERVICES_ENDPOINT` at the classic OpenAI account would also redirect other AI Services calls (e.g. Document Intelligence parsing), so it is not a safe lever.

Candidate fixes (operator decision required — all exceed the cloud-variable scope): (1) provision `text-embedding-3-small` (1536, GlobalStandard) on `aisa-<SUFFIX>` so the code's account-route finds it, **and** redeploy the current function code so embeddings use `_get_embeddings_client`'s account route rather than the project route — cleanest, aligns model + index + endpoint; (2) redeploy current code and route embeddings to `oai-<DATA_SUFFIX>` (where `text-embedding-3-small` already lives) via a code change that reads `AZURE_OPENAI_ENDPOINT` for embeddings (reusing `AZURE_AI_SERVICES_ENDPOINT` for that is unsafe); (3) standardize on `text-embedding-3-large` (already on `aisa-<SUFFIX>`) with `dimensions=1536` and re-embed everything — most disruptive (mixes vector spaces with the existing 1536-dim 3-small docs), not recommended.

Durable back-port: the bicep should deploy the embedding model on the **same** account the embedding code targets, and the function code should be redeployed so it carries the account-route embeddings client (bicep + redeploy paused per operator).

Resolution (confirmed 2026-06-16, fix path 1): (a) provisioned `text-embedding-3-small` (model version 1, 1536-dim, GlobalStandard, capacity 100) on `aisa-<SUFFIX>` — the AI Services account the embed path targets via `_get_embeddings_client` — and verified `POST https://aisa-<SUFFIX>.cognitiveservices.azure.com/openai/v1/embeddings` returns a 1536-dim vector for `text-embedding-3-small`. (b) Redeployed the current function code so `embed` uses `_get_embeddings_client` (account route `…cognitiveservices.azure.com/openai/v1`) instead of the legacy project route. The first `azd deploy function` shipped a stale `build-functions/` artifact (dated 06-06; the prepackage hook did not run — see BUG-0058), so logs still showed `POST https://aisa-<SUFFIX>.services.ai.azure.com/api/projects/proj-<SUFFIX>/openai/v1/embeddings → 404` and a traceback at the old `foundry_iq.py` line 365. After manually running `uv run python scripts/prepackage_function.py` (which regenerated `build-functions/` from current `src/`, putting `embed` → `_get_embeddings_client` at line 446) and redeploying, a fresh PerksPlus.pdf upload processed cleanly: validation → managed-identity token → blob download → PDF parse → embed → push. End-to-end verified — the backend `admin/documents` endpoint lists PerksPlus.pdf with **4 chunks**, and a direct query of the active `cwyd-index` (151 docs) returns 4 `title='PerksPlus.pdf'` chunks. (Note: `index-<OLD_SUFFIX>` is a legacy v1 index with blob-URL `source` values; the active v2 index is `cwyd-index`, keyed on `title`/`url`/`content_vector`.)

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0058 — `azd deploy function` ships a stale build-functions artifact (prepackage hook skipped)

Area: functions. Severity: medium. Status: fixed (found 2026-06-16; obsolete/fixed 2026-07-02 — the prepackage/build-functions mechanism was retired when the function migrated to a Container App).

Symptom: while fixing BUG-0057, two consecutive `azd deploy function` runs reported `SUCCESS` yet the function host kept running **old** code — the embedding call still hit the legacy Foundry project route (`…services.ai.azure.com/api/projects/<project>/openai/v1/embeddings → 404`) with a traceback at `foundry_iq.py` line 365, even though the local source had been fixed (account-route `_get_embeddings_client`, `embed` at line 446) days earlier.

Root cause: `azd deploy function` packaged `v2/build-functions/` **without** running the `prepackage` hook that is supposed to regenerate it. `hooks.prepackage` in [v2/azure.yaml](../azure.yaml) shells to `scripts/prepackage_function.py`, which wipes and rebuilds `build-functions/` from current `src/functions/` + `src/backend/`. The artifact on disk was dated **2026-06-06** (`build-functions/backend/core/providers/llm/foundry_iq.py` had `_get_openai_client` at line 363 and no `_get_embeddings_client`), while `src/.../foundry_iq.py` was dated **2026-06-14** (with the fix). The mtimes were unchanged after `azd deploy`, confirming the hook never fired. The deploy therefore zipped and shipped the stale 06-06 tree.

Fix / workaround: run `uv run python scripts/prepackage_function.py` to regenerate `build-functions/` from current source, then `azd deploy function`. After doing so, the staged artifact carried `embed` → `_get_embeddings_client` (line 446) and the redeploy resolved BUG-0057.

Root cause confirmed + fixed (2026-06-23): the azd schema (`schemas/v1.0/azure.yaml.json`) settles the open question. Project-level `hooks:` are *command*-scoped — a `prepackage` there "Runs before the `package` command" only, so a targeted `azd deploy function` (which packages just that one service) never fires it. Service-level hooks (`services.<name>.hooks`) are the per-service lifecycle: a service `prepackage` "Runs before the service is deployment package is created", which **includes** `azd deploy <service>`. Fix: moved the `prepackage` hook from project-scoped `hooks.prepackage` to `services.function.hooks.prepackage` in [azure.yaml](../azure.yaml). The run path is now service-relative (`../scripts/prepackage-function.{sh,ps1}`); the Python script already resolves every path from its own location (`Path(__file__).resolve().parents[1]`), so the hook cwd is irrelevant. A drift-guard ([tests/functions/test_azure_yaml_hooks.py](../tests/functions/test_azure_yaml_hooks.py)) asserts the hook is service-scoped and that no project-level prepackage hook returns. Verified locally: 293 `tests/functions` pass, and the prepackage script still stages all six subpackages incl. `blob_event`. The manual `uv run python scripts/prepackage_function.py` workaround is no longer required once this ships. Status stays **open** only until a live `azd deploy function` is confirmed to regenerate `build-functions/` (gated on the same blocked Flex deploy).

Resolution (2026-07-02): the 2026-06-23 service-scoped-hook fix is **superseded** — the function was migrated to a **Container App built from `docker/Dockerfile.functions`** (ACR remote build), which reproduces the Functions deploy layout by copying current `src/functions` + `src/backend` into the image. That structurally eliminates the defect: there is **no** staged `build-functions/` artifact and **no** prepackage hook to skip (both `build-functions/` and `scripts/prepackage_function.py` were removed). The drift-guard [tests/functions/test_azure_yaml_hooks.py](../tests/functions/test_azure_yaml_hooks.py) was rewritten accordingly — 3 passing tests assert the function is `host: containerapp` with `Dockerfile.functions` and **no** service- or project-level prepackage hook. Live-confirmed 2026-07-02: `azd deploy function` (3m16s) shipped current source and the BUG-0088 embedder count-batching + DI paragraph-grouping fixes were verified running in the deployed function immediately after (a `.docx` that previously dead-lettered ingested 198 grouped chunks). A stale-artifact deploy can no longer occur.

### BUG-0059 — agent_framework KB MCP grounding 401s in cloud (backend env var points at the wrong connection; cloud recurrence of BUG-0025)

Area: infra. Severity: high. Status: fixed (found 2026-06-16, fixed 2026-06-16).

Symptom: every question asked through the deployed frontend returns an SSE `event: error` immediately after the `reasoning` placeholder — `("<class 'agent_framework_foundry._chat_client.FoundryChatClient'> service failed to complete the prompt: Authentication failed when connecting to the MCP server: https://srch-<DATA_SUFFIX>.search.windows.net:443/knowledgebases/cwyd-kb/mcp: Response status code does not indicate success: 401 (Unauthorized).. Verify your authentication headers.", APIError(...))`. No `citation` or `answer` frame is emitted; the agent cannot list or call the KB retrieval tool.

Root cause: a **cloud recurrence of BUG-0025** caused by an incomplete durable back-port. The `agent_framework` orchestrator builds the KB `MCPTool` with `project_connection_id = AZURE_AI_SEARCH_CONNECTION_NAME`, and the Responses API opens the MCP connection **server-side under that connection's identity**. The deployed backend Container App carried the bicep default `AZURE_AI_SEARCH_CONNECTION_NAME=search-srch-<DATA_SUFFIX>` — the general-purpose `CognitiveSearch` / bare-`AAD` connection (`useWorkspaceManagedIdentity: false`, no `audience`), which mints no usable bearer for the generic server-side MCP path, so the KB endpoint sees no valid token → 401. BUG-0025 already diagnosed this exact failure and fixed it by (1) creating a dedicated `cwyd-kb-mcp` `RemoteTool` / `ProjectManagedIdentity` connection (`audience: https://search.azure.com`, `useWorkspaceManagedIdentity: true`, `target` = the KB MCP URL) and (2) pointing `AZURE_AI_SEARCH_CONNECTION_NAME` at it — but only in the gitignored `.env`. The durable bicep back-port (`main.bicep` wires `AZURE_AI_SEARCH_CONNECTION_NAME` to `aiProjectSearchConnection.outputs.name`, i.e. `search-srch-<DATA_SUFFIX>`) was tracked as "still to do" and never landed, so the cloud `azd up` shipped the wrong connection name to the backend runtime.

Pre-fix state verified (everything already correct except the env var): the `cwyd-kb-mcp` connection exists on `proj-<SUFFIX>` with `category=RemoteTool`, `authType=ProjectManagedIdentity`, `audience=https://search.azure.com`, `useWorkspaceManagedIdentity=true`, `target=https://srch-<DATA_SUFFIX>.search.windows.net/knowledgebases/cwyd-kb/mcp?api-version=2025-11-01-preview`; the project system-assigned MI holds **both** `Search Index Data Reader` and `Search Service Contributor` scoped to the search service. The only wrong value was the backend `AZURE_AI_SEARCH_CONNECTION_NAME` (`search-srch-<DATA_SUFFIX>` instead of `cwyd-kb-mcp`).

Fix (applied live, cloud variable — not bicep): `az containerapp update -g <RESOURCE_GROUP> -n ca-backend-<SUFFIX> --set-env-vars "AZURE_AI_SEARCH_CONNECTION_NAME=cwyd-kb-mcp"`, which rolled a new revision (`runningState=Running`). No connection or RBAC change was needed (both already in place from BUG-0025).

Resolution (confirmed 2026-06-16): re-asking the same question against the live backend `/api/conversation` (SSE) now streams `reasoning` → `tool: knowledge_base_retrieve` (the MCP call, no 401) → `citation` `[doc1] PerksPlus.pdf` (with the full snippet) → a grounded `answer` citing `[doc1]`, and closes with `conversation` — **no `error` frame**. This also confirms the freshly-indexed PerksPlus.pdf (BUG-0057) is retrievable through the KB.

Durable back-port (pending, bicep paused): change `infra/main.bicep` so `AZURE_AI_SEARCH_CONNECTION_NAME` resolves to the KB MCP `RemoteTool` connection rather than the `CognitiveSearch` connection, and create that `cwyd-kb-mcp` connection + the project-MI `Search Service Contributor` assignment durably in `infra/` (a Bicep `RemoteTool` connection resource — CWYD's KB name `cwyd-kb` is static, so no seed script is needed). Until that lands, every `azd up` re-introduces this 401 and the env var must be re-flipped post-deploy.

References: [worklog/2026-06-16.md](worklog/2026-06-16.md); origin BUG-0025; related BUG-0024, BUG-0026; [adr/0025-foundry-prompt-agent-ga-pattern.md](adr/0025-foundry-prompt-agent-ga-pattern.md).

References: [worklog/2026-06-16.md](worklog/2026-06-16.md).

### BUG-0060 — first postgresql `azd up` fails ARM validation (`avmPrivateDnsZones[7]` out of range)

Area: infra. Severity: blocker. Status: fixed (found and fixed 2026-06-17).

Symptom: the first ever `databaseType=postgresql` `azd up` fails at ARM template validation, before any service-resource is created, with `RESPONSE 400 … ERROR CODE: InvalidTemplate … "The template reference 'avmPrivateDnsZones[7]' is not valid: could not find template resource or resource copy with this name."`. Infra never provisions. The empty `rg-<SUFFIX>` is created (azd makes the resource group before submitting the deployment), but nothing else.

Root cause: in [main.bicep](../infra/main.bicep) the private-DNS-zone list is mode-dependent — `privateDnsZones = concat(alwaysOnPrivateDnsZones, databaseType == 'postgresql' ? postgresModePrivateDnsZones : cosmosModePrivateDnsZones)`. In **postgresql** mode that array is 6 always-on zones + 1 postgres zone = **7 elements (indices 0–6)**; in **cosmosdb** mode it is 6 + 2 (documents + search) = **8 elements (indices 0–7)**. The cosmos-only `aiSearch` module (`if (databaseType == 'cosmosdb' && !useExistingSearch)`) declares a private endpoint whose DNS-zone reference used the literal constant `dnsZoneIndex.search` (= 7). ARM validates copy-index references **statically against the declared copy length**, *before* the module's own `if` condition is evaluated — so in postgresql mode the literal `[7]` is out of range and the whole template is rejected, even though the search PE never deploys in that mode (and even though `enablePrivateNetworking=false` empties the `avmPrivateDnsZones` copy at runtime). The path had never been exercised: all prior cloud QA was cosmos, where index 7 is valid, so the latent defect first surfaced on the postgresql deploy. The pre-existing code comment on `dnsZoneIndex` assumed module-level `databaseType` gating protects the index reference; that assumption does not hold for ARM's static copy-index validation.

Fix: make the search PE DNS-zone index a **mode-conditional expression** so it is never the literal `7` in postgresql mode — `avmPrivateDnsZones[databaseType == 'cosmosdb' ? dnsZoneIndex.search : dnsZoneIndex.postgres]!.outputs.resourceId`. Because the index is now a runtime `if(...)` expression rather than a compile-time constant, ARM defers it past static validation and evaluates it at deployment expansion: in cosmosdb mode it resolves to 7 (the search zone, unchanged behavior); in postgresql mode it resolves to 6 (the postgres zone — a valid in-range index whose value is never used, since the search PE is gated out and the copy is empty when private networking is off). No behavior change for cosmos deploys. `az bicep build main.bicep` clean; the subsequent `azd up` passed validation and provisioned the full postgresql resource set (Container Apps Environment, App Service plans, Storage, AI Services, Container Registry, Foundry + project + gpt-5.1 / o4-mini / text-embedding-3-large deployments, and `psql-<SUFFIX>` PostgreSQL Flexible Server).

References: [worklog/2026-06-17.md](worklog/2026-06-17.md).

### BUG-0061 — postgresql `azd up` provision fails: Event Grid subscription MI not authorized to deliver to the queue (deploy-time chicken-and-egg)

Area: infra. Severity: blocker. Status: fixed (found 2026-06-17; live-unblocked 2026-06-17; durable bicep fix 2026-07-03).

Symptom: after BUG-0060, the postgresql `azd up` provisions all the way through the backend Container App, the Function App, and the PostgreSQL Flexible Server, then fails the **provision** step. azd prints the headline `ERROR: A resource with this name already exists or is in a conflicting state.` followed by `Deployment Error Details: Managed Identity Authorization Error: Managed Identity for <sub>:rg-<SUFFIX>:<eg-mi>:blob-created-to-doc-processing does not have permission to perform appropriate action on the destination resource. Managed identity for event subscription does not have authorization to deliver to the endpoint. Visit aka.ms/egmsivalidation for more details.` The three services (backend / frontend / function) are still mid-packaging when the provision aborts.

Root cause: a deploy-time ordering chicken-and-egg in the Event Grid wiring of [main.bicep](../infra/main.bicep) (not mode-specific, but first hit on this fresh deploy). The AVM `event-grid/system-topic:0.6.4` module (`evgt-<SUFFIX>`) creates, **in one nested deployment**, the system topic, its **system-assigned** managed identity, **and** the `blob-created-to-doc-processing` event subscription. That subscription uses `deliveryWithResourceIdentity` (mandatory because the storage account sets `allowSharedKeyAccess=false`, so Event Grid → Storage Queue delivery cannot fall back to a shared key and must authenticate as the topic's MI). The role assignment that grants that MI `Storage Queue Data Message Sender` on the storage account — `eventGridQueueSenderRole` — is an **external** resource that consumes the module output `eventGridSystemTopic.outputs.systemAssignedMIPrincipalId`, so ARM orders it **after** the module completes. But Event Grid validates the delivery identity's queue-delivery permission **synchronously, at subscription-creation time, inside the module** — before the external role assignment can run. With no role yet, the preflight returns 401 and the nested module deployment fails, which means the module never produces its outputs, which means `eventGridQueueSenderRole` is never created either: the role can structurally never exist before the subscription it is meant to authorize.

Why a plain retry is not reliable: on a clean retry the module re-runs and re-attempts the subscription, whose preflight still finds no role (the external role assignment is still ordered after the module that keeps failing), so the failure can repeat deterministically. The robust break is to make the role exist **before** the subscription preflight.

Fix (applied live, cloud role grant — not bicep): granted the existing topic's system-assigned MI the queue-sender role out of band, then retried `azd up`.

1. `az eventgrid system-topic show -g rg-<SUFFIX> -n evgt-<SUFFIX> --query identity.principalId` → the topic's MI object id (the topic + MI survive the failed provision).
2. `az role assignment create --assignee-object-id <eg-mi> --assignee-principal-type ServicePrincipal --role "Storage Queue Data Message Sender" --scope <storage account resource id>`.
3. Re-ran `azd up` — idempotent, fast-forwards past the already-provisioned resources; the subscription preflight now finds the role and the subscription + remaining resources (incl. the frontend App Service) provision.

Follow-on conflict (and its fix): the out-of-band `az role assignment create` mints the assignment under a **random GUID name**, but the bicep `eventGridQueueSenderRole` resource names its assignment **deterministically** (`guid(storageAccount.id, eventGridSystemTopicName, storageQueueDataMessageSenderRoleId)`). Azure rejects a second role assignment for the same `(principal, role, scope)` triple even under a different name, so the next `azd up` provision failed with `RoleAssignmentExists: The role assignment already exists` when bicep tried to create its deterministically-named twin of the manual grant. Resolution: because the event subscription is now **permanently created** (the chicken-and-egg only bites at first subscription *creation*, not on idempotent re-evaluation), the manual grant is no longer needed to unblock the subscription — so delete it and let bicep own the role under its own name. `az role assignment delete --ids <storage account id>/providers/Microsoft.Authorization/roleAssignments/<random-guid>`, then re-ran `azd up`; bicep created `eventGridQueueSenderRole` cleanly (no conflict) and the subscription update no-op'd without re-preflighting. **Lesson: when hand-granting a role that bicep also manages, either skip the manual grant and just retry `azd up` once the topic MI exists, or create the manual assignment under bicep's deterministic `guid(...)` name so the bicep create is idempotent — a random-named grant guarantees a follow-on `RoleAssignmentExists`.**

Durable back-port (implemented 2026-07-03): the fix took two stages. **Stage 1 (insufficient):** lift the `blob-created-to-doc-processing` subscription out of the AVM system-topic module into a standalone `Microsoft.EventGrid/systemTopics/eventSubscriptions` resource that `dependsOn` `eventGridQueueSenderRole`. This ordered resource *creation* correctly but still 401'd on a fresh provision because (i) Azure RBAC is **eventually consistent** — `dependsOn` guarantees the role-assignment resource is *created* before the subscription, not that the grant has *replicated* to the Storage data plane that Event Grid's synchronous delivery-authorization preflight consults; and (ii) the topic used a **system-assigned** MI that does not exist until the AVM module completes, so the grant could only be authored moments before the subscription, giving replication ~0 s of lead time. The recurrence was reproduced on the fresh eastus2 postgres provision (DR-06, [worklog/2026-07-03.md](worklog/2026-07-03.md)). **Stage 2 (durable):** switch the new-topic delivery from the topic's system-assigned MI to the **shared UAMI** (`id-<SUFFIX>`), which exists from t=0 — exactly what the `useExistingEventGridTopic` reuse path already does. Three coordinated edits in [main.bicep](../infra/main.bicep): (a) the AVM `event-grid/system-topic` module gets `managedIdentities.userAssignedResourceIds: [ userAssignedIdentity.outputs.resourceId ]` instead of `systemAssigned: true`; (b) `eventGridQueueSenderRole` grants `Storage Queue Data Message Sender` to the **UAMI principal** on the storage account with **no dependency on the topic**, so ARM creates it at the very start of the deployment and it replicates during the multi-minute provisioning tail, well before the subscription preflight runs; (c) `blobCreatedSubscription.deliveryWithResourceIdentity.identity` becomes `{ type: 'UserAssigned', userAssignedIdentity: userAssignedIdentity.outputs.resourceId }`. The grant now shares its deterministic `guid(storageAccount.id, userAssignedIdentity.name, roleId)` name with the reuse path's `existingQueueMessageSenderRole` (mutually exclusive gates, so at most one deploys). `az bicep build` clean (only pre-existing BCP081/BCP334 warnings elsewhere). Rejected alternatives: a `deploymentScripts` grant-then-wait resource (needs an `allowSharedKeyAccess=true` backing storage account, conflicting with ADR 0002's storage hardening) and moving the subscription into an azd `postprovision` hook (MACAE's grant-then-use pattern, kept as a backstop) both proved unnecessary once the grant is UAMI-based and topic-independent.

References: [worklog/2026-06-17.md](worklog/2026-06-17.md); related BUG-0054 (the same Event Grid subscription's blob-created envelopes poisoning `doc-processing` at runtime).

### BUG-0062 — Flex Consumption function storage 403: storage firewall denies the platform when private networking is off

Area: infra. Severity: blocker. Status: fixed (found and fixed 2026-06-17).

Symptom: `azd deploy function` fails fast with `InaccessibleStorageException` / `BlobUploadFailedException: ... 403`. The deploy never reaches the host; the Function App cannot read or write its own deployment/content storage account.

Root cause: the Function App is Flex Consumption and uses **identity-based** storage access (`AzureWebJobsStorage__credential=managedidentity`, `AzureWebJobsStorage__clientId=<UAMI client id>` — no shared key). The storage account `st<SUFFIX>` is created with `networkRuleSet.defaultAction=Deny`, `bypass=AzureServices`, and **no resource-instance rule** binding the Function App, while `allowSharedKeyAccess=false` is enforced by Azure Policy (no key fallback). With `enablePrivateNetworking=false` there is no VNet/private-endpoint path either, so the Flex Consumption platform — which reaches storage from the platform network, not as an "Azure service" the `bypass` covers for this operation — is denied at both deploy and runtime. (Server-side exception names confirm the 403 is platform-side, not the azd client.) This path had never been exercised: prior cloud QA used cosmos with a differently-configured storage posture.

Fix (applied live, storage network rule — not bicep): `az storage account update -n st<SUFFIX> -g <RESOURCE_GROUP> --default-action Allow`. Data stays protected — `allowSharedKeyAccess=false` is unchanged, so every access still requires an Entra token + RBAC; only the network ACL default flips from Deny to Allow. The function then deployed cleanly. Granting the deployer `Storage Blob Data Contributor` and adding the deployer workstation IP to the firewall did **not** help (the blocked principal is the platform, not the client); updating azd `1.25.5 → 1.25.6` was also a red herring.

Durable bicep fix pending: for the no-private-networking profile, either set the Function App storage `networkRuleSet.defaultAction=Allow`, or add a `networkRuleSet` **resource-instance rule** granting the `Microsoft.Web/sites` Function App access while keeping `defaultAction=Deny`. (When `enablePrivateNetworking=true` the private-endpoint path covers this and the Deny default is correct.)

References: [worklog/2026-06-17.md](worklog/2026-06-17.md); related BUG-0061 (the same fresh postgres `azd up`).

### BUG-0063 — backend Container App fails activation: postgres connect username is the deployer, not the UAMI

Area: infra. Severity: blocker. Status: fixed (found and fixed 2026-06-17).

Symptom: after the services deploy reports `SUCCESS`, the backend `/api/health` times out. The backend Container App has two active revisions — the provision-time placeholder (`Activating`, never ready) and the azd image revision in `ActivationFailed` with **0 replicas**. The container starts and then exits during the FastAPI lifespan; with no Log Analytics workspace wired (`ENABLE_MONITORING=false`, log destination `azure-monitor` with `logAnalyticsConfiguration=null`), the crash is not in any queryable log — it was diagnosed by reading the lifespan + `PostgresClient` source.

Root cause: the lifespan eagerly opens the asyncpg pool (`get_runtime_config()` → `ensure_pool()` → `ensure_schema()`). `PostgresClient._ensure_pool()` authenticates with an **AAD token minted for the UAMI** (`AZURE_CLIENT_ID` = the UAMI client id) but connects with `user = AZURE_POSTGRES_ADMIN_PRINCIPAL_NAME`, which [main.bicep](../infra/main.bicep) wires (backend ~L1760, function ~L2066) to `postgresAdminPrincipalName` — the **human deployer's** principal name (`<AZURE_PRINCIPAL_UPN>`). Postgres Flexible Server (Entra-only auth, passwords disabled) validates that the token subject matches the role named by the username; the UAMI token subject (`id-<SUFFIX>`) ≠ the deployer role, so the login is rejected, the pool never opens, and the lifespan raises before `yield` → the revision fails activation. The UAMI is already registered as a postgres AAD admin under principal name `id-${solutionSuffix}` (~L1463); the runtime env var simply names the wrong one of the two admins.

Fix (applied live, container env override — not bicep): `az containerapp update -n ca-backend-<SUFFIX> -g <RESOURCE_GROUP> --set-env-vars AZURE_POSTGRES_ADMIN_PRINCIPAL_NAME=id-<SUFFIX>`. The new revision boots (`Application startup complete`), and `GET /api/health` returns `{"status":"pass", checks: foundry_iq=pass, database=pass (db_type=postgresql), search=skip}`.

Durable bicep fix pending: the runtime `AZURE_POSTGRES_ADMIN_PRINCIPAL_NAME` env var for the backend (~L1760) and the function (~L2066) must be `'id-${solutionSuffix}'` (the UAMI's postgres principal) rather than `postgresAdminPrincipalName`; the deployer admin registration stays only for human/local DDL. Note: a `--set-env-vars` override is reverted by the next `azd deploy` (which re-applies the bicep env), so the durable fix is required before the next deploy or the backend re-breaks.

References: [worklog/2026-06-17.md](worklog/2026-06-17.md).

### BUG-0064 — postgresql deploy grounds through agent_framework (wrong orchestrator env var + pgvector-incompatible default)

Area: infra. Severity: high. Status: fixed (found and fixed 2026-06-17).

Symptom: the backend `/api/health` passes, but any chat on the postgresql deployment fails at request time with HTTP 409 `ConfigResolutionError` (ADR 0022) — `agent_framework` grounds on a Foundry IQ knowledge base, which exists only in cosmosdb mode; pgvector has no KB.

Root cause: two independent faults select the wrong orchestrator. (a) [main.bicep](../infra/main.bicep) L1776 emits the env var `ORCHESTRATOR`, but the backend reads `CWYD_ORCHESTRATOR_NAME` — `OrchestratorSettings` uses `env_prefix="CWYD_ORCHESTRATOR_"` over field `name`. The bicep value is therefore never read, and the orchestrator falls to its code default, `agent_framework`. (b) Even with the correct var name, the bicep hardcodes `agent_framework` for every `databaseType`; pgvector mode has no KB, so the only coherent orchestrator is `langgraph`. The mismatch does not crash the lifespan (the orchestrator is constructed per request, not at startup), so health stays green and the fault only appears on the first chat.

Fix (applied live, container env override — not bicep): `az containerapp update -n ca-backend-<SUFFIX> -g <RESOURCE_GROUP> --set-env-vars CWYD_ORCHESTRATOR_NAME=langgraph`. New revision Running + Healthy; `/api/health` still `pass`.

Durable bicep fix pending: rename the L1776 env var from `ORCHESTRATOR` to `CWYD_ORCHESTRATOR_NAME`, and set its value mode-conditionally — `databaseType == 'postgresql' ? 'langgraph' : 'agent_framework'` — so each database type gets its coherent orchestrator. As with the other live overrides, a `--set-env-vars` value is reverted by the next `azd deploy`, so the durable fix is required before redeploying.

References: [worklog/2026-06-17.md](worklog/2026-06-17.md).

### BUG-0066 — agent_framework cannot ground on pgvector (Foundry IQ has no pgvector source; ADR 0022 guard returns 409)

Area: backend. Severity: high. Status: fixed (found + fixed 2026-06-19).

Symptom: on a pgvector deployment, selecting the `agent_framework` orchestrator (env `CWYD_ORCHESTRATOR_NAME=agent_framework` or an admin override) makes every `POST /api/conversation` fail with HTTP 409 and the body `{"error": "Orchestrator 'agent_framework' grounds on a Foundry IQ Knowledge Base over an Azure AI Search index, but this deployment uses pgvector, which has no Knowledge Base source. Set CWYD_ORCHESTRATOR_NAME=langgraph for pgvector deployments.", "reason": "orchestrator_requires_azure_search"}`. No answer is produced.

Root cause: `agent_framework` retrieves only through a Foundry IQ Knowledge Base, reached by a server-side MCP tool that targets an Azure AI Search index — `AgentFrameworkOrchestrator._build_kb_tool()` ([orchestrators/agent_framework.py](../src/backend/core/providers/orchestrators/agent_framework.py)) builds a `…/knowledgebases/<kb>/mcp` URL against the Search endpoint and returns `None` when the Search endpoint / knowledge-base name / connection name are empty (the pgvector case). Foundry IQ has no PostgreSQL/pgvector knowledge-source type (its sources are searchIndex / blob / Azure SQL / file / OneLake / SharePoint / Fabric / MCP-server / Web), so a pgvector deployment has no KB, the agent is built with no retrieval tool, and it would refuse every question as out-of-domain. ADR 0022's guard in `resolve_effective_config` ([services/admin.py](../src/backend/services/admin.py)) detects the incoherent `index_store == pgvector` + `orchestrator == agent_framework` pairing and raises `ConfigResolutionError(reason="orchestrator_requires_azure_search")`, which the app maps to HTTP 409 — turning a silent "always refuses" into an explicit, explainable error. The guard works as designed; the design itself conflicts with the product requirement that both orchestrators serve pgvector. Distinct from BUG-0064 (there the deploy merely defaulted to the wrong orchestrator; here the orchestrator is architecturally unable to ground on pgvector).

Resolution (accepted 2026-06-19; test-first, multi-unit): give `agent_framework` an app-side pgvector retrieval path so it can ground without Foundry IQ. When `_build_kb_tool()` returns `None` and `index_store == pgvector`, `run()` embeds the user query through the LLM/embedder seam and calls `self._search.search(query, vector=…)` — the same retrieval path `langgraph` uses (fixed in BUG-0065) — injects the retrieved chunks as grounding context into the user turn (the Agents Responses thread drops system messages, so grounding cannot ride a system message), and emits `citation` events through the shared formatter in [tools/citations.py](../src/backend/core/tools/citations.py) (Hard Rule #20). The pgvector search provider is already injected on the orchestrator (`self._search`, used today for citation enrichment), so no new wiring is required beyond threading the `top_k` / semantic-search knobs into the constructor. Relax the guard in `resolve_effective_config` to allow `pgvector + agent_framework` and flip its tests; supersede ADR 0022's hard-block rule with ADR 0027 (records that `agent_framework` on pgvector does app-side RAG with no Foundry IQ; Azure AI Search deployments keep grounding through Foundry IQ unchanged). Consequence to accept: on pgvector, `agent_framework` does not use Foundry IQ — that is impossible because Foundry IQ cannot index pgvector — it does app-side retrieval like `langgraph`, differing only in how it drives the model.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md); [adr/0022-config-resolution-error-on-incompatible-overrides.md](adr/0022-config-resolution-error-on-incompatible-overrides.md); related BUG-0064, BUG-0065; companion frontend defect BUG-0067 (the blank page on the 409 this raises).

### BUG-0067 — chat UI renders a blank page on a non-2xx /api/conversation response (unhandled SSE error)

Area: frontend. Severity: high. Status: fixed (found + fixed 2026-06-19).

Symptom: when `POST /api/conversation` returns a non-2xx status (observed: the BUG-0066 HTTP 409), the chat page goes blank. A transient toast "Message failed — streamChat: SSE request failed with status 409" appears, then the chat surface renders empty instead of showing the error inline. Browser console: `POST .../api/conversation 409 (Conflict)`, initiated from `streamChatOnce @ streamChat.tsx:183`, propagating `streamChat.tsx:141` → `MessageInput.tsx:171` → `MessageInput.tsx:272`.

Root cause: the SSE client `streamChat` ([api/streamChat.tsx](../src/frontend/src/api/streamChat.tsx)) throws when the response status is not OK (`NonRetryableStreamError` for a 4xx) carrying only the string `streamChat: SSE request failed with status <n>`; it never reads the structured `{error, reason}` body. `MessageInput.handleSubmit` ([pages/chat/components/MessageInput.tsx](../src/frontend/src/pages/chat/components/MessageInput.tsx)) does catch the throw and dispatch `set_error` (which is why the toast appears), so the rejection itself is handled — but the page still blanks because the frontend has no React error boundary (no `ErrorBoundary` / `componentDidCatch` / `getDerivedStateFromError` anywhere under `src/frontend/src`), so a render-time throw triggered by the resulting error / empty-assistant state unmounts the whole React tree to a blank page. The backend's actionable message is also lost because the JSON body is never parsed. The defect reproduces on any non-2xx (`4xx` / `5xx`) from `/api/conversation`, not only the BUG-0066 409.

Resolution (planned; test-first): in `streamChat`, on a non-OK status read and parse the JSON error body (best-effort) and reject with a typed error carrying `{error, reason, status}`, keeping the existing retry classification. In the chat component, render that as a handled inline error (error bubble / banner) showing the backend `error` message — never blank the page. Add a React error boundary around the chat surface so any future stream- or render-time failure degrades to a recoverable error UI instead of an empty page. Vitest: assert that a mocked 409 response surfaces an inline error and leaves the conversation / messages rendered.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md); companion backend defect BUG-0066 (the 409 this fails to handle).

### BUG-0068 — GET /api/admin/status reports the env orchestrator, not the saved override

Area: backend. Severity: medium. Status: fixed (found + fixed 2026-06-19).

Symptom: an operator switches the orchestrator (e.g. langgraph → agent_framework) in the admin UI and saves; the save succeeds and the chat path runs the new orchestrator, but `GET /api/admin/status` keeps reporting `"orchestrator_name": "langgraph"`. The operator concludes the save did not apply.

Root cause: `status_endpoint` ([routers/admin.py](../src/backend/routers/admin.py)) returned `orchestrator_name=settings.orchestrator.name` — the env / code default from `AppSettings` — and never read the persisted `RuntimeConfig` override on `app.state.runtime_overrides`. Every other consumer applies the override: `PATCH /api/admin/config` persists it to the database and live-reloads `app.state.runtime_overrides`; the lifespan re-seeds that attribute from the database at startup; `GET /api/admin/config/effective` overlays it; and the chat path resolves it through `resolve_effective_config`. Only the status snapshot read the raw setting. This is the same class as the prior admin config dropdown read-source defect (the client read `/api/admin/config`, the plain env snapshot, instead of `/api/admin/config/effective`), but on the status endpoint. Persistence was never broken — the saved config is a `RuntimeConfig` row in the (cloud) database, not a file, and it already survives restarts via the lifespan seed.

Fix: `status_endpoint` now takes `RuntimeOverridesDep` and reports `resolve_effective_config(settings, overrides).orchestrator_name` — the single effective-config seam shared with `/config/effective` and the chat path — so the status snapshot matches what the deployment actually runs, both immediately after a save and after a restart (the lifespan reloads the override from the database). The remaining status fields are infra / env settings that are not admin-overridable, so they continue to surface from `settings` directly.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md); observed while live-testing BUG-0066 (the agent_framework-on-pgvector feature).

### BUG-0069 — cloud /api/admin/status reports `environment: "local"`

Area: infra. Severity: high. Status: open (found 2026-06-19).

Symptom: a deployed cloud backend's `GET /api/admin/status` returns `"environment": "local"` instead of the real cloud environment. Observed on a deployed Container App backend, alongside `app_insights_enabled: false` and `cors_origins: []` — consistent with an incomplete deployed env-var set.

Likely cause (to verify): `settings.environment` defaults to `local` and the env var that sets it is not wired onto the deployed backend runtime — the same env-var-wiring class as BUG-0051 / BUG-0052 / BUG-0064 (vars set as Bicep outputs but never on the container `env`).

Security implication (to verify): the admin auth gate's local-dev bypass (BUG-0047) keys on `environment == "local"` — in `local`, a missing Easy Auth claims blob yields a synthetic `local-dev` admin. A cloud runtime that reports `environment == "local"` could therefore grant admin access without authentication. Confirm whether the deployed admin gate actually fails closed independent of the environment string before adjusting severity.

Fix direction: wire the environment setting on the deployed backend (Bicep) so it reports the real environment (e.g. `production`); confirm the admin gate fails closed in the cloud regardless of the environment value.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md); related BUG-0047 (admin local-dev bypass), BUG-0051/0052/0064 (deployed env-var wiring gaps).

### BUG-0070 — chat microphone (speech-to-text) does not work

Area: frontend + backend. Severity: medium. Status: fixed (found 2026-06-19, fixed 2026-06-20 — **two** root causes).

Symptom: clicking the microphone button in the chat composer does not capture or transcribe speech into the message input.

**Root cause 1 (frontend — split-origin fetch).** The speech REST client [api/speech.tsx](../src/frontend/src/api/speech.tsx) issued a **bare relative** `/api/speech` fetch (`const SPEECH_URL = "/api/speech"` → `fetch(SPEECH_URL, …)`) — the **only** frontend fetch client that did not prepend `VITE_BACKEND_URL` (every sibling routes through a `backendUrl()` / `apiUrl()` / `conversationUrl()` seam). In the deployed **split-origin** topology (frontend App Service ≠ backend Container App, per BUG-0050) the relative URL resolves against the *frontend* host's SPA catch-all → `index.html` (`200`) → `getSpeechConfig()`'s `response.json()` throws → `useSpeechRecognition.start()` sets `error` → `MessageInput`'s `micDisabled` disables the button. Local dev was unaffected ([vite.config.ts](../src/frontend/vite.config.ts) proxies `/api` → backend). Same class as BUG-0050; got the BUG-0046 `userIdHeaders()` treatment but missed the BUG-0050 base-URL fix.

**Root cause 2 (backend — wrong Speech AAD pattern).** Surfaced during local end-to-end testing once root cause 1 was fixed: the backend returned `502 Speech token mint failed`. [core/speech.py](../src/backend/core/speech.py) tried to mint the token via the **`/sts/v1.0/issueToken` STS exchange** with an AAD bearer (a pattern inherited from v1's RBAC branch). Two layers of wrongness: (a) it first used the **regional** host (`{region}.api.cognitive.microsoft.com`), which **does not support Entra/AAD auth at all** → **400**; (b) even repointed to the account's **custom-subdomain** host, the `issueToken` exchange returned **401 PermissionDenied** and **stayed 401 for >70 minutes** — far past any RBAC propagation — because **`issueToken` is the *key*-exchange endpoint and `Cognitive Services Speech User` does not authorize it.** Microsoft's docs confirm: `issueToken` is documented only with `Ocp-Apim-Subscription-Key`; the **Entra-ID** path is **not** `issueToken` — it is the SDK-native authorization-token form **`aad#{resourceId}#{aadToken}`** passed to `SpeechConfig.fromAuthorizationToken(token, region)`, where the SDK exchanges the AAD token with the Speech service itself. v1's `issueToken`-RBAC branch was almost certainly dead code (v1 shipped on keys); v2 revived a non-working path.

Not broken (verified): the hook [useSpeechRecognition.tsx](../src/frontend/src/hooks/useSpeechRecognition.tsx) (token → `SpeechConfig.fromAuthorizationToken` → continuous recognition); the [MessageInput.tsx](../src/frontend/src/pages/chat/components/MessageInput.tsx) wiring (`toggleMic` → `start`/`stop`, transcript reflected into the draft); the AAD token acquisition; and the deployment wiring — Bicep provisions `spch-<SUFFIX>` with `customSubDomainName` + `disableLocalAuth: true`, grants the UAMI **Cognitive Services Speech User** (`f2dc8367-…`), and wires `AZURE_SPEECH_SERVICE_NAME` / `_SERVICE_REGION` / `_ACCOUNT_RESOURCE_ID` onto the **backend** container env. No Bicep change is needed — the `account_resource_id` the `aad#` form needs is already wired.

Fix (2026-06-20):
- **Frontend (root cause 1):** added the `backendUrl()` / `apiUrl()` base-URL seam to `api/speech.tsx` and routed the fetch through `apiUrl(SPEECH_URL)`. Test-first `VITE_BACKEND_URL`-prepend Vitest case (RED→GREEN) in [tests/frontend/api/speech.test.tsx](../tests/frontend/api/speech.test.tsx). FE suite **567 passed**.
- **Backend (root cause 2):** rewrote `mint_speech_token` to return the documented Entra-ID form **`aad#{account_resource_id}#{aad_token}`** (no `issueToken` / HTTP call); the FE is unchanged (its hook already passes the token to `fromAuthorizationToken`). The router drops the `httpx` error arm — only `AzureError` (AAD acquisition) can fail now. Test-first: reworked `test_speech_helper.py` to assert the `aad#…` return + the cognitive-services AAD scope (RED→GREEN) and removed the obsolete HTTP/`issueToken` tests; `tests/backend/core/test_speech_helper.py` + `tests/backend/routers/test_speech.py` **7 passed**, pyright `src/backend/core/speech.py` + `routers/speech.py` 0/0/0. No Bicep / FE / hook change. *(An earlier attempt — flipping the `issueToken` host regional → custom-subdomain — was superseded once the >70-min 401 proved `issueToken`-with-AAD is unsupported.)*

Verification (end-to-end, 2026-06-20): backend confirmed live — `GET /api/speech` returns **200** with an `aad#…` token (`region=swedencentral`, length ≈ 2870) — **and the browser microphone captures and transcribes speech** in local dev (user-confirmed). Works with the **already-granted** `Cognitive Services Speech User` role and **no propagation wait** — that role authorizes Speech *recognition* (where the SDK actually uses the token), which had long since taken effect; only the unsupported `issueToken` action was being denied. The **deployed** env uses the same pattern (the UAMI holds the same role) and works on a code-only `azd deploy backend`.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md), [worklog/2026-06-20.md](worklog/2026-06-20.md); related BUG-0050 (relative URL — root cause 1 class), BUG-0046 (fetch-seam enumeration); v1 precedent in `code/create_app.py` `/api/speech` + the v1 Bicep `AZURE_SPEECH_REGION_ENDPOINT: speechService.outputs.endpoint`.

### BUG-0071 — replace the "M" header logo with a multi-agent-style icon and make it click-to-home

Area: frontend. Severity: low. Status: fixed (found 2026-06-19, fixed 2026-06-20).

Symptom: the header brand showed a plain **"M"** glyph (off-brand, non-interactive) instead of a real logo.

Root detail (corrected 2026-06-22): [Header.tsx](../src/frontend/src/components/Header/Header.tsx) rendered the brand inside a Fluent `<Avatar shape="square" name="Microsoft" icon={<MsftColorLogo/>}>`. **Fluent v9 `Avatar` renders its `icon` slot *only when there are no initials*** — see [useAvatar.js](../../node_modules/@fluentui/react-avatar/lib/components/Avatar/useAvatar.js) `if (!initials && (!image || imageHidden)) { icon = ... }`. Because `name="Microsoft"` yields the initial **"M"**, the `icon` (the 4-square `MsftColorLogo`) was **never rendered** — the Avatar showed "M". So the brand was *never* the 4-square logo; the `name` prop silently suppressed the icon. The first fix attempts (swap `MsftColorLogo`→`PeopleTeam`, then →`MultiAgentLogo`, keeping `name="Multi-agent"`) did not help — the Avatar kept showing the "M" of "Multi-agent". A misdiagnosis ("stale dev server") and a dev-server restart were tried and were **not** the cause. The brand also carries an `<h1>` title beside the logo, so only the logo (not the whole brand block) can be the click target (an `<h1>` can't nest in a `<button>`).

Fix (2026-06-20 → corrected 2026-06-22):
- **The decisive fix: removed the `name` prop from the `<Avatar>`** so Fluent renders the `icon` slot. With no `name` there are no initials, so the brand mark (`<MultiAgentLogo size={20} />`, the reference architecture's interlocking glyph adapted from the reference architecture's logo component, filled with `--colorBrandForeground1`) renders inside `<Avatar shape="square" color="neutral">`. This mirrors the reference architecture's header, whose Avatar has **no `name`**.
- Added [MultiAgentLogo.tsx](../src/frontend/src/components/Header/MultiAgentLogo.tsx) + [MultiAgentLogo.test.tsx](../tests/frontend/components/MultiAgentLogo.test.tsx).
- Wrapped **only the logo** in a `<button type="button" data-testid="header-home" aria-label="Go to home">` calling a new optional `onNavigateHome` prop; added a `.brandButton` CSS reset. [App.tsx](../src/frontend/src/App.tsx) passes `onNavigateHome={() => void navigate(SectionPath[Section.Chat])}` — the `/` chat root.
- Test-first regression guard in [Header.test.tsx](../tests/frontend/components/Header.test.tsx): asserts the `MultiAgentLogo` svg (`viewBox="0 0 33 32"`) actually renders — this fails whenever a `name` prop re-suppresses the icon.

Verification: Header tests **11 passed** (incl. the icon-renders regression test), MultiAgentLogo tests **3 passed**, `eslint` clean on the touched source files. The fix is an edit to an existing file, so Vite HMR applies it without a dev-server restart. The now-orphaned `MsftColorLogo.tsx` is left in place (not deleted). **Lesson: Fluent `Avatar` suppresses `icon` whenever `name` yields initials — never pass `name` when you want a custom logo to show.**

References: [worklog/2026-06-19.md](worklog/2026-06-19.md), [worklog/2026-06-20.md](worklog/2026-06-20.md).

### BUG-0072 — show signed-in user initials in the top-right

Area: frontend. Severity: low. Status: fixed (found 2026-06-19, fixed 2026-06-22).

Symptom: the top-right of the header did not show the signed-in user's initials.

Fix (2026-06-22): added a user badge after the theme button in [HeaderTools.tsx](../src/frontend/src/components/Header/HeaderTools.tsx) — a circular **light-gray** (`color="neutral"`) Fluent `<Avatar size={28}>` rendering the user's **initials** when signed in (e.g. "John Doe" → "JD"), or **"G"** (name "Guest") when no user is signed in (auth not enforced / local dev). The display-name + initials helpers live in a new [userIdentity.tsx](../src/frontend/src/components/Header/userIdentity.tsx) module (kept non-component so `HeaderTools` stays React-Fast-Refresh-clean): `resolveDisplayName(userInfo)` reads the Easy Auth claims (`name` → `preferred_username` → email → "Guest"), and `userInitials(name)` mirrors the reference architecture's initials helper (first letters of the first two name parts; parentheticals stripped). The resolved `userInfo` is threaded from `useAuth` through [Header.tsx](../src/frontend/src/components/Header/Header.tsx) → [App.tsx](../src/frontend/src/App.tsx) (`userInfo={auth.userInfo}`). Reviewed the reference architecture's login-button pattern (used `color="neutral"` for the requested light gray instead of the reference architecture's `colorful`).

Verification: test-first — 9 new cases in [HeaderTools.test.tsx](../tests/frontend/components/HeaderTools.test.tsx) (guest avatar shows "G", signed-in shows "JD", plus `resolveDisplayName`/`userInitials` units). FE suite **582 passed**, eslint clean on the changed files, tsc clean (only the pre-existing BUG-0079 `streamChat` errors remain).

References: [worklog/2026-06-22.md](worklog/2026-06-22.md), [worklog/2026-06-19.md](worklog/2026-06-19.md); related BUG-0046 (user resolution on load), BUG-0071 (the Fluent `Avatar` initials/icon precedence — here initials are the desired output).

### BUG-0073 — deleting a document does not remove all references; deleted documents stay accessible

Area: backend. Severity: high. Status: fixed (found 2026-06-19, fixed 2026-06-22).

Symptom: after deleting a document via the admin Delete Data UI, the user could still access the deleted document through `GET /api/files/{filename}` (and the source blob remained in the documents container).

Root cause: [delete_document_endpoint](../src/backend/routers/admin.py) called **only** `search.delete_by_source(source)`, which removes the index chunks ([pgvector.py](../src/backend/core/providers/search/pgvector.py) `DELETE … WHERE title=$1` / [azure_search.py](../src/backend/core/providers/search/azure_search.py) `delete_documents`). Nothing deleted the Storage blob, so [download_document](../src/backend/services/files.py) kept streaming it. In this deployment the index **is** pgvector (one store), so `delete_by_source` already clears it; the gap was the blob.

Fix (2026-06-22):
- **Unit A** — added `delete_document(filename, *, settings, credential) -> bool` to [services/files.py](../src/backend/services/files.py), mirroring `download_document`: builds a `ContainerClient` and calls `delete_blob`; `ResourceNotFoundError` → `False` (already-absent is an idempotent no-op); any other `AzureError` → log + re-raise (Hard Rule #14); `_validate_filename` rejects URL/path-typed sources (`ValueError`) before any SDK call.
- **Unit B** — wired it into the endpoint: added `SettingsDep` + `CredentialDep`; after `delete_by_source`, when `documents_container` is configured, delete the blob (swallowing `ValueError` for URL-typed sources → `blob_deleted=False`). The endpoint now 404s only when **neither** an index chunk **nor** a source blob existed. `DeleteDocumentResponse` gained `blob_deleted: bool` (default `False`, non-breaking for the FE OpenAPI client).

Verification: test-first across both units — 4 service tests in [test_services_files.py](../tests/backend/test_services_files.py) (delete→True, missing→False no-log, `AzureError`→log+reraise, URL source→`ValueError`) and 7 endpoint tests in [test_admin.py](../tests/backend/test_admin.py) (200 w/ blob, 404 nothing, 200 blob-only, URL-skip, no-container-skip, 503, prod-auth 401). 134 admin + files-service tests passed, pyright on the three changed source files **0/0/0**, AST gates **355 passed**.

References: [worklog/2026-06-22.md](worklog/2026-06-22.md), [worklog/2026-06-19.md](worklog/2026-06-19.md); related BUG-0048 (delete-by-source id-discovery fix), BUG-0042 (the `/api/files` download route), BUG-0077 (the inverse — auto-de-index on `BlobDeleted`).

### BUG-0074 — admin Add URL ingestion does not work

Area: backend. Severity: high. Status: fixed (found 2026-06-19, fixed 2026-06-22).

Symptom: submitting a URL through the admin Add Data (URL) flow did not ingest the URL's content — it never appeared in the documents list and was not retrievable; the admin page also blanked with a `TypeError`.

Root cause: the admin route (`ingest_url` → `add_url_handler`) ran fetch → parse → embed → write-index **inline in the backend container** — v1's `process_url_contents_directly` equivalent — which failed. (v1 also had the operator-requested path, `download_url_and_upload_to_blob`: download the page, store it as a blob, let the blob-triggered pipeline index it.)

Fix (2026-06-22), operator-directed and v1-confirmed — make a URL ingest like an uploaded file:
- **`ingest_url`** rewritten to `fetch_url(url)` → bytes → `_blob_name_for_url(url)` → `upload_document(...)` ([ingestion.py](../src/backend/services/ingestion.py)), so the URL flows through the identical store→`batch_push` pipeline as file upload (enqueued under `DIRECT_ENQUEUE`; Event-Grid-driven otherwise). `_blob_name_for_url` derives a deterministic, flat, parseable blob filename. The inline `add_url_handler`/embedder path was dropped from the backend route (the Functions `add_url` blueprint keeps its own copy, untouched).
- **`IngestUrlResponse`** reshaped to the upload-style receipt (`url`, `filename`, `blob_path`, `ingestion_job_id`, `queued`; dropped `document_count` — indexing is async, same as file upload). The route now guards on documents-container/queue config (503) instead of search.
- **Frontend** ([model](../src/frontend/src/models/admin.tsx) + [IngestData.tsx](../src/frontend/src/pages/admin/IngestData/IngestData.tsx)) updated to the new shape — fixes a `response.document_count.toString()` `TypeError` that blanked the admin page on success.
- **v1 parser parity** (surfaced because ext-less web pages were stored as `.txt` and the raw HTML was indexed): v1 supports 9 file types (PDF, JPEG, JPG, PNG, TXT, HTML, MD, DOCX, JSON) but v2 registered only 3 (pdf/docx/txt). Added a bs4 [HtmlParser](../src/functions/core/parsers/html_parser.py) (strips script/style → clean text, mirroring v1's BeautifulSoup extraction); registered `md`/`json` on [TextParser](../src/functions/core/parsers/text_parser.py) and `jpeg`/`jpg`/`png` on the [DI parser](../src/functions/core/parsers/document_intelligence_parser.py) (image OCR). `_blob_name_for_url` now defaults web pages to `.html`. `beautifulsoup4` was already a declared-but-unused dependency, so no new package.

Verification: test-first across all units — 187 backend/parser + ingestion + admin tests, **582** FE tests, **389** AST gates, pyright **0/0/0**. New [test_supported_extensions.py](../tests/functions/core/parsers/test_supported_extensions.py) guards the full v1 9-type set against regression. Local stack restarted (backend auto-reloaded; Functions host restarted so the new parsers load).

References: [worklog/2026-06-22.md](worklog/2026-06-22.md); related the `add_url` Functions blueprint (#42), BUG-0073 (the delete counterpart). v1 precedent: `code/backend/batch/add_url_embeddings.py` (`download_url_and_upload_to_blob`) + `docs/supported_file_types.md`.

### BUG-0075 — documents list "last modified" date is blank

Area: backend. Severity: low. Status: open (found 2026-06-19).

Symptom: the admin documents list shows a blank "last modified" date — `GET /api/admin/documents` returns `last_modified: null` for every source.

Likely cause (to verify): `list_sources` does not populate `last_modified`; the search / pgvector provider returns null for the field.

Fix direction: surface a last-modified timestamp in `list_sources` — from the Storage blob's `Last-Modified` (or an indexed ingest timestamp) — so the column renders.

References: [worklog/2026-06-19.md](worklog/2026-06-19.md).

### BUG-0076 — admin section missing the business-cases prompt dropdown

Area: frontend. Severity: medium. Status: fixed (found 2026-06-19, resolved 2026-06-23).

Symptom: the admin Configuration is missing the dropdown of predefined business-case prompts (the v1 use-case presets — e.g. employee assistance, contract assistance — that let an operator pick a starting persona/prompt).

Desired behavior: add a business-cases prompt dropdown to the admin Configuration so the operator can select a predefined use-case prompt; pairs with the existing prompt-editor surface.

Resolution (2026-06-23, [ADR 0030](adr/0030-assistant-type-prompt-presets.md)): added an `ai_assistant_type` dropdown (default / contract assistant / employee assistant) to the admin Configuration. Backend — `assistant_presets.json` + `presets.py` loader (`AssistantType` enum; `CWYD_DEFAULT_BODY` sourced from the JSON), `ai_assistant_type` on `RuntimeConfig`/`AdminConfig`, and `assistant_type_presets` on the effective-config response; reset restores the defaults. Frontend — selecting a type loads its preset into the System prompt and populates the post-answering defaults, with per-field hover tooltips. Verified green (backend pyright 0/0/0; frontend vitest 592 passed, tsc clean, build ok) and confirmed live by the operator (Save persona → chat round-trip → Reset).

References: [worklog/2026-06-19.md](worklog/2026-06-19.md), [worklog/2026-06-22.md](worklog/2026-06-22.md), [worklog/2026-06-23.md](worklog/2026-06-23.md), [adr/0030-assistant-type-prompt-presets.md](adr/0030-assistant-type-prompt-presets.md).

### BUG-0080 — function deploy hangs on the Agent Framework umbrella package (unresolvable hyperlight on Python 3.11)

Area: functions. Severity: high. Status: fixed (found and fixed 2026-06-24).

Symptom: for ~2 days every attempt to deploy the `function` service to the Flex Consumption host (`func-<SUFFIX>`) appeared to hang. `func azure functionapp publish ... --build remote` (and `azd deploy function`) uploaded the package and the deployment slot was granted, then the **Oryx build step sat at zero build steps** and timed out; `released-package.zip` stayed dated 2026-06-17 and the app kept serving the prior 5 functions (no `blob_event`). The kudu deployment ledger accumulated several "in-progress" builds that never reaped. This was **misdiagnosed at the time as a platform "Flex build-service outage"** (worklog 2026-06-23 / 2026-06-24); retries only added more stuck builds to the queue.

Root cause: a **dependency-resolution conflict that only manifests on Python 3.11**. `[project].dependencies` in [pyproject.toml](../pyproject.toml) pinned the **umbrella** meta-package `agent-framework==1.7.0`. The umbrella depends on `agent-framework-hyperlight` under the environment marker `python_version < "3.14"`, and hyperlight requires `hyperlight-sandbox-backend-wasm<0.5,>=0.4.0` — a distribution pip cannot resolve, so the resolver backtracks across every hyperlight release indefinitely and the build never produces a package. The Functions host runs **Python 3.11**, so the marker is active and hyperlight is pulled; **local dev runs Python 3.14**, where the marker excludes hyperlight, so the same `pyproject.toml` resolved cleanly locally and the conflict was invisible until the cloud build. The codebase only imports `agent_framework` (core runtime primitives) and `agent_framework_foundry` (Foundry hosted-agent client) — the umbrella's ~20 extra provider packages (anthropic, bedrock, redis, qdrant, copilotstudio, hyperlight, …) are unused.

Diagnosis: reproduced the cloud failure deterministically by running `pip install -r requirements.txt --target ...` inside the exact Functions runtime image `mcr.microsoft.com/azure-functions/python:4-python3.11`. With the umbrella pinned, pip emitted the hyperlight / `hyperlight-sandbox-backend-wasm` backtracking wall and failed — the same hang Oryx hit in cloud. Swapping the single dependency line to `agent-framework-core==1.7.0` made the same install **succeed cleanly** (no hyperlight; ~100 deps incl. `agent-framework-core` + `agent-framework-foundry` + `agent-framework-openai`).

Fix (2026-06-24): repinned `[project].dependencies` from `agent-framework==1.7.0` to `agent-framework-core==1.7.0` (kept `agent-framework-foundry==1.7.0`) in [pyproject.toml](../pyproject.toml), with a comment naming the hyperlight / Python-3.11 constraint. Shipped the proven package by (a) building deps into `build-functions/.python_packages/lib/site-packages` inside the 3.11 container and (b) `func azure functionapp publish func-<SUFFIX> --no-build --python`, which skips Oryx (`[Kudu-OryxBuildStep] Skipping oryx build (remotebuild = false)`). The deploy completed in ~4.5 min; `blob_event` is now live (6 functions), `released-package.zip` advanced to 2026-06-24, host status `Running`, `/api/health` → `{"status":"ok"}`. Because the generated `requirements.txt` is copied verbatim from `[project].dependencies`, the repin also fixes the **standard** remote-build path — the next `azd up` (Oryx) resolves cleanly. A regression guard ([test_agent_framework_core_not_meta.py](../tests/functions/test_agent_framework_core_not_meta.py)) asserts the dependency set pins `agent-framework-core` and never the bare `agent-framework` umbrella. Local gates after the repin: 295 `tests/functions`, 1117 `tests/backend`, 1012 `tests/shared` green; the agent_framework orchestrator suite (114) green on core-only.

Lesson: an env-marker-gated transitive (`python_version < "3.14"`) makes a dependency conflict **invisible on a newer local interpreter** and fatal only on the older deploy runtime — always reproduce a deploy-build hang in the **target runtime image**, and prefer the narrow distribution (`-core`) over an umbrella meta-package that pulls providers the code never imports. Do not defend a "platform outage" diagnosis when the hang sits consistently at the dependency-install step.

References: [worklog/2026-06-24.md](worklog/2026-06-24.md), [worklog/2026-06-23.md](worklog/2026-06-23.md); related BUG-0058 (prepackage / stale-artifact — a distinct deploy-artifact defect), BUG-0054 / BUG-0055 / BUG-0077 (cloud verification was gated on this deploy).

### BUG-0096 — `resolve_search_provider` used a banned `except BaseException` cleanup

**Area:** functions · **Severity:** high · **Status:** fixed

**Symptom.** `pytest tests` failed one case: `tests/test_no_silent_excepts.py::test_no_silent_excepts[src\functions\core\search_resolution.py]` reported `line 92: except BaseException`. The `test_no_silent_excepts` gate bans `except BaseException` because catching `BaseException` also traps `KeyboardInterrupt` and `SystemExit`, which is always wrong. Surfaced 2026-07-03 during the v2 root-reorg path-migration's Phase 8 validation.

**Root cause.** In [search_resolution.py](../src/functions/core/search_resolution.py), `resolve_search_provider` wrapped pool acquisition, provider construction, and `ensure_schema` in `try: … except BaseException: <aclose provider, then pool>; raise` to guarantee cleanup on any failure path (including cancellation) before re-raising. The intent was correct, but `except BaseException` is the banned form: the idiomatic way to run cleanup on any exit is `try/finally`, not a broad `BaseException` catch. This is **pre-existing** — the file was committed with the v2 root reorg (`ad591ecd`) and predates that move; it is unrelated to the path migration and was merely revealed when the migration's Phase 8 ran the full suite.

**Fix (2026-07-03).** Replaced the `except BaseException` block with an equivalent `try/finally` guarded by a `resolved` success flag:

- the success path sets `resolved = True` and returns `ResolvedSearch` inside the `try`;
- the `finally` closes the provider (then the pool) only when `resolved` is `False`, preserving the provider-before-pool ordering so the SDK client is released over the connection it was layered on;
- an in-flight exception (ordinary error or `CancelledError`) still propagates through `finally` to the trigger-level decorators.

Behavior is identical to the original on every path; the two dedicated cleanup tests — `test_ensure_schema_failure_closes_provider_and_pool_and_reraises` and `test_ensure_schema_failure_on_azure_search_closes_provider_only` in [test_search_resolution.py](../tests/functions/core/test_search_resolution.py) — pass unchanged, and the gate goes green (`131 passed` for the two suites; full `pytest tests` green).

**References:** [worklog/2026-07-03.md](worklog/2026-07-03.md); [exception_handling_policy.md](exception_handling_policy.md) (the cross-cutting rule the gate enforces).
