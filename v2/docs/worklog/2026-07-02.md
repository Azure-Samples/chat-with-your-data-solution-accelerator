---
title: CWYD v2 — Worklog 2026-07-02
description: First azd up on the new tenant provisioned + deployed all three services successfully, but live review found the backend crashes on startup because Cosmos DB (and Storage) come up with publicNetworkAccess Disabled in the default public profile (BUG-0093).
author: CWYD Engineering
ms.date: 2026-07-02
topic: reference
keywords: worklog, azd up, deploy, BUG-0093, BUG-0054, BUG-0090, admin auth, ADR 0031, cosmos firewall, publicNetworkAccess, event grid, blob-events, ingestion, container apps, review, v2
estimated_reading_time: 4
---

## Summary

Completed the first `azd up` of CWYD v2 on the new tenant/subscription. Provisioning + build/push/deploy of all three container services **succeeded**, and the post-provision index + knowledge base seeded. But a live review of the running deployment found the **backend Container App crashes on startup**: it reads a Cosmos DB container during the FastAPI lifespan and gets `(Forbidden) ... blocked by your Cosmos DB account firewall`, so `/api/health` never responds. Root cause is that Cosmos DB + Storage come up with `publicNetworkAccess: Disabled` in the default public profile — logged as [BUG-0093](../bugs.md).

## Planned

- Repoint the azd env to the new subscription, verify quota, run the first `azd up`.
- Review the live deployment (health + grounding).

## Done

- Repointed the azd env to the new dev subscription (cleared the stale old-tenant subscription/tenant); confirmed `azd auth login` works for the new account (`az login` is not available on it, so the deploy is azd-driven).
- Verified model quota in `eastus2`: `gpt-5.1` GlobalStandard 0/1000, `text-embedding-3-large` Standard 0/350 — ample. Subscription state **Enabled**.
- `azd provision --preview` validated the binding; then `azd up` (run from the **v2** sync terminal — a fresh async terminal picks up the **root v1** `azure.yaml` and fails; the `--cwd` flag did **not** redirect azd, so the project must be the shell cwd).
- **`azd up` succeeded:** provisioned Log Analytics, ACR, App Insights, Speech, Storage, Foundry + project, **gpt-5.1** + embedding deployments, Content Safety, Container Apps Environment, Cosmos DB, Search, the Foundry↔Search connection, and all three Container Apps (`ca-backend`, `ca-frontend`, `ca-func`). Post-provision created the `cwyd-index` search index + `cwyd-kb` knowledge base + KB-MCP connection. Endpoints emitted.
- **Live review found the backend down:** `/api/health` times out (0 bytes, 90s). Diagnosed via `az containerapp logs`: uvicorn starts, hangs at "Waiting for application startup", then `CosmosHttpResponseError: (Forbidden) ... blocked by your Cosmos DB account firewall` → `Application startup failed. Exiting.`. Confirmed the deployed Cosmos + Storage have `publicNetworkAccess: Disabled` (Search + AI Services are `Enabled`) despite the public profile. → [BUG-0093](../bugs.md).
- **Hotfix + recovery:** set Cosmos + Storage `publicNetworkAccess` to `Enabled`, restarted the backend revision → `/api/health` + `/api/health/ready` return `200 {"status":"pass"}` (foundry_iq / database / search all pass); frontend serves `200`.
- **Grounding validated:** re-ran the postdeploy seed non-interactively (`AZURE_ENV_SAMPLE_DATA=default`) — uploaded 6 benefits PDFs, index reached 15 docs (PASS). `POST /api/conversation` ("What does the Northwind Health Plus plan cover?") returned a grounded answer with a `[doc1]` citation to `Benefit_Options.pdf`. The deployment is functional end-to-end.
- **Closed the live-deploy review's two remaining follow-ups** (`.copilot-tracking/reviews/2026-07-02/v2-first-azd-up-deploy-live-review.md`). (1) **Embedding model** — confirmed `text-embedding-3-small` is intended (pinned via `AZURE_ENV_EMBEDDING_MODEL_NAME` in the azd env, native 1536-dim matching `cwyd-index`, grounding verified); no infra change (OBS-01 resolved). (2) **Smoke-test conversation** — deleted the grounding-test conversation (`"What does the Northwind Health Plus plan cover?"`, user `local-dev`) + its 2 messages from the Cosmos `conversations` container. A second guest test conversation (`"tell me about employee benefits"`) remains, flagged for the operator. All review findings are now closed.
- **Closed out BUG-0054 (Event Grid single-trigger ingestion) on the live deployment.** Verified the ingestion trigger is `event_grid` (azd env + the `ca-backend-<SUFFIX>` container carries `AZURE_INGESTION_TRIGGER=event_grid`); confirmed the sole storage system topic `evgt-<SUFFIX>` routes `BlobCreated`+`BlobDeleted` (subject-scoped to `documents`) via system-assigned identity to the `blob-events` queue (not `doc-processing`), alongside only the built-in antimalware subscription. Ran a live end-to-end test: uploaded a uniquely-worded blob → Event Grid → `blob-events` (1 → 0) → `blob_event` → `doc-processing` → `batch_push` → `cwyd-index` (263 → 264, sentinel-phrase search returned the test chunk); then deleted the blob → `BlobDeleted` → `blob_event` → `delete_by_source` (264 → 263, sentinel gone). Both poison queues stayed at 0 throughout; no historical poison existed in this fresh env. The `BlobDeleted` half also live-confirms the BUG-0077 delete branch. Cleaned up the local temp file (the blob + index chunk were removed by the delete test itself).
- **Scrubbed pre-existing env-specific identifiers from tracked files (Hard Rule #18 / ADR-0019).** A repo-wide audit (triggered by the env-ID review finding) found real deployment values in **12 tracked files** — a retired resource suffix, an older v1-style suffix, the azd env name, the resource group, embedded FQDNs, the deployer UPN, and leaked subscription names. Genericized all to placeholders (`<SUFFIX>` / `<OLD_SUFFIX>` / `<AZD_ENV_NAME>` / `<RESOURCE_GROUP>` / `<AZURE_PRINCIPAL_UPN>` / `<AZURE_SUBSCRIPTION_NAME>`), preserving the deliberate old-vs-current suffix contrast in the older cleanup narrative. Built-in role-definition GUIDs and region names were correctly left (ADR-0019 carve-outs). Post-scrub repo-wide grep is clean of every token class.
- **Added an automated env-ID gate (FU-02) so ADR-0019 is enforced, not just disciplined.** New `v2/tests/shared/test_no_env_specific_content.py` reads the developer's gitignored azd `.env`, derives a denylist of the real env-specific values (subscription / tenant / RG / env name / resource-name vars + a derived suffix — 10 entries on the current env; no secret is hard-coded), and asserts no git-tracked file contains any of them. It runs where leaks originate (the dev machine) and skips cleanly with no local env; the helper logic is unit-tested so it stays covered in CI. The live gate passes against the now-clean tree; full `tests/shared` suite green (1039 passed).
- **Diagnosed + fixed BUG-0088 (`.docx` dead-letters at ingestion) — root cause is an unbatched embedder, code fix landed (redeploy pending).** The confirm-first live probe uploaded `MSFT_FY23Q4_10K.docx`; it dead-lettered after 5 retries. Local probes against the live DI endpoint proved current-source parse = **5047 chunks** (paragraph fallback works, no throw) and that `AzureOpenAIEmbedder.embed` raises `openai.BadRequestError: array length must be 2048 or less` on the 5047-input request. So the DOCX paragraph fallback (BUG-0049) produces thousands of chunks and the embedder sends them in one over-cap request; PDFs (~1 chunk/page) stay under the cap. Ruled out the DI api-version hypothesis (deployed uses the correct `2024-11-30` default) and the stale-image hypothesis (fallback is present). **Fix:** batch `AzureOpenAIEmbedder.embed` into 2048-input requests + concatenate (8 embedder tests pass, pyright 0/0). The live deployment needs a function redeploy to pick it up.
- **BUG-0088 fully fixed + live-verified (redeploy done).** Added a second fix so the DI paragraph fallback groups consecutive paragraphs into ~2000-char chunks (a 10-K yields ~198 not 5047), then `azd deploy function` (3m16s, no BUG-0080 hang) shipped both fixes to the function Container App. Live: `MSFT_FY23Q4_10K.docx` now ingests **198 chunks** (`cwyd-index` 263 → 461), zero poison; deleting the blob removed all 198 chunks (→ 263) + the blob via the BlobDeleted path. Close-out sweep 1147 passed. BUG-0088 marked **fixed**.
- **Closed BUG-0077 (auto-remove index chunks on `BlobDeleted`).** Its Phase 1 code shipped 2026-06-22; Phase 2 (deploy + live-verify) was deferred behind BUG-0058 (prepackage) + BUG-0053 (scale-from-zero) — both now moot (the function is a Container App built from source with `minReplicas=1`, redeployed today). Dedicated live test: a `.txt` indexed, then the blob deleted directly → `BlobDeleted` → `blob_event` → `delete_by_source` removed the chunk (264 → 263), zero poison. Marked **fixed**.
- **Closed BUG-0058 (azd deploy function ships a stale build-functions artifact) as obsolete/fixed.** The prepackage/build-functions mechanism was retired when the function migrated to a Container App built from `Dockerfile.functions` (ACR remote build copies current `src/` into the image) — no staged artifact, no prepackage hook. `build-functions/` + `scripts/prepackage_function.py` are gone; `tests/functions/test_azure_yaml_hooks.py` (3 passing) guards `host: containerapp` + no prepackage hook. Today's `azd deploy function` live-confirmed current source ships (the BUG-0088 fixes ran in the deployed function). A stale-artifact deploy is now structurally impossible.
- **Fixed BUG-0055 (App Insights zero telemetry) via the research → plan → implement → review workflow.** Corrected root cause: both OTel exporter halves were already wired — the actual blocker was the App Insights component's **`disableLocalAuth: true`**. With local auth disabled, ingestion needs an Entra token, but both `configure_azure_monitor(connection_string=...)` sites pass no `credential=`, so every write silently 401'd (why the connection string matched + exporters initialized yet the union query stayed `[0, null, null]`). Checked MACAE per the user's request: its App Insights `avm/res/insights/component` **omits** `disableLocalAuth` (defaults `false`) and ingests via instrumentation key with no credential — so the fix is to match that. **Fix:** `disableLocalAuth: false` on the App Insights module ([ADR 0018](../adr/0018-monitoring-default-on-and-appi-rbac.md) Amendment 2), **no app-code change**; `Monitoring Metrics Publisher` role retained (now unused) for a one-line revert. Test-first guard `test_application_insights_enables_local_auth`; 43 infra + env-ID-gate tests green; `az bicep build` exit 0; RPI review clean (one minor ADR-accuracy nuance fixed in-review). **Deployed via `azd provision`** (the azd client got accidentally interrupted by a status query, but the ARM deployment ran server-side to `Succeeded`); verified live: the App Insights resource now reports **`disableLocalAuth: false`**, and the backend Container App is serving requests (telemetry primed with test hits; the silent-401 blocker is removed). Marked **fixed**. Open bugs → 3.
- **Closed BUG-0081 (frontend never deploys via `azd deploy frontend`) as fixed — durable fix landed (Option A).** Reassessing the remaining open bugs against the current topology, the frontend was migrated from an App Service to a **Container App** (`ca-frontend-<SUFFIX>`) — exactly BUG-0081's proposed durable fix. Verified live: the RG has **no App Service** (`az webapp list` empty), all three services are Container Apps (`ca-backend`/`ca-frontend`/`ca-func`), `ca-frontend` runs a real ACR image (`cwyd-frontend:latest`, not the `staticsite` placeholder) deployed by `azd up`, and serves the SPA (`GET /` → `200`, `<title>CWYD v2</title>`). The App Service + unsupported-`docker` mismatch is gone. Open bugs → 2 (BUG-0082, BUG-0090). Note: BUG-0090's analysis references the old frontend App Service Easy Auth — now stale (frontend is a Container App); its core defect (backend has no Easy Auth identity source) needs re-verification against the new topology.
- **Corrected BUG-0090's root cause + documented the delivered admin-auth fix (Phase 5 documentation; live verification pending Phase 6).** The earlier "no Easy Auth identity source feeds the backend" analysis was a mis-diagnosis of the retired split-host App Service topology. **Real root cause:** the `401` was the admin **role gate working as designed** — `backend.dependencies.requires_role("admin")` required the base64 `x-ms-client-principal` claims blob carrying an `admin` role and fired only when `environment=production` **and** `require_admin_auth=true`; the SPA forwards only `x-ms-client-principal-id` (never the claims blob), so the claims check failed → 401 (with the shipped `local` / `false` defaults the admin routes were already open). **Delivered fix (Phases 1-4):** collapsed the backend to one `get_user_id` dependency that validates the `x-ms-client-principal-id` header is a GUID and otherwise falls back to the anonymous default GUID `00000000-0000-0000-0000-000000000000` (never 401); swapped every `/api/admin/*` route to it; **deleted** the entire Easy Auth role gate (`requires_role`, `REQUIRE_ADMIN_USER`, `AdminUserIdDep`, the claims-blob decode + role-extraction helpers), the `require_admin_auth` setting, and the `AZURE_REQUIRE_ADMIN_AUTH` Bicep env var. **Security tradeoff (stated):** `x-ms-client-principal-id` is client-forgeable, so admin reads **and** writes are open on the backend FQDN unless ingress-level auth is enabled — real authentication, when wanted, is now an ingress/frontend concern (Easy Auth injecting/overwriting the header at the proxy), matching MACAE. Rewrote the BUG-0090 registry row (status stays **open** pending Phase 6 live verification) and authored [ADR 0031](../adr/0031-backend-admin-auth-header-only-ingress-enforced.md) (posture + security tradeoff + revert path). **Frontend no-op verification (read-only, zero edits):** confirmed the SPA already forwards `x-ms-client-principal-id` on every request (`api/auth.tsx` `userIdHeaders()`), defaults to the guest GUID `00000000-0000-0000-0000-000000000000` (`auth.tsx` `DEFAULT_USER_ID`), and renders `G` for the guest badge (`components/Header/userIdentity.tsx` `userInitials`) — so no frontend change is needed.

## Bugs

- [BUG-0054](../bugs.md) (**fixed**, medium, infra) -- Event Grid single-trigger blob ingestion (ADR 0028). All four close-out steps satisfied + cloud-validated on the fresh `<AZD_ENV_NAME>` deployment: (1) ingestion trigger flipped to `event_grid` on the backend runtime; (2) the live Event Grid subscription `blob-created-to-doc-processing` delivers `BlobCreated`+`BlobDeleted` to the `blob-events` queue via system-assigned identity (not `doc-processing`); (3) end-to-end create (`cwyd-index` 263 -> 264, indexed) + delete (264 -> 263, removed) both passed with all queues at 0 (no poison); (4) poison-drain N/A (0 messages in this fresh env). The function is a Container App (`ca-func-<SUFFIX>`, `minReplicas=1`) so the BUG-0053 Flex scale-from-zero risk does not apply. Also live-confirms the BUG-0077 `BlobDeleted` delete branch.
- [BUG-0093](../bugs.md) (**fixed**, blocker, infra) -- default public-profile `azd up` yields a non-functional backend: Cosmos DB + Storage come up `publicNetworkAccess: Disabled` (AVM modules ignore the `enablePrivateNetworking ? 'Disabled' : 'Enabled'` intent), so the public-egress backend can't reach Cosmos and crashes on startup. **Live hotfix applied 2026-07-02** (Cosmos + Storage -> `Enabled`, backend restarted -> health `200 pass`); **durable fix landed 2026-07-02** -- `post_provision.py` `_ensure_public_network_access` re-asserts `Enabled` on every provision (MACAE `selecting_team_config_and_data.sh` parity), verified live (`az` reports both `Enabled`); 7 unit tests + AST gates green.
- [BUG-0094](../bugs.md) (**fixed**, medium, frontend) -- inline `doc[N]` citation markers rendered as raw text in the reasoning ("Thought process") panel instead of the answer's superscript format. **Fixed:** new pure `superscriptReasoningCitations` helper (`reasoningText.tsx`) normalizes the reasoning marker family (`[docN]`/`doc[N]`/`docs[N]`/bare `[N]`, 1-3 digits) into ` ^N^ ` supersub tokens (verbatim number); `MessageList` composes it over `formatReasoning` and enables `enableSupersub` on the reasoning `MarkdownContent`. tsc clean, full frontend vitest green (45 files / 607 tests; +9 helper cases, +1 render test).
- [BUG-0095](../bugs.md) (**fixed**, high, frontend) + [BUG-0092](../bugs.md) (**fixed**, high, frontend) -- same root cause (a component reading the build-time `VITE_BACKEND_URL`, empty on the split-host frontend). **Fixed together:** `documentHref.tsx` (the "Open document" link) and `HistoryPanel.tsx` (`buildUrl`) now read the runtime `getBackendUrl()` seam (the `/config` origin in cloud; Vite proxy locally) like the other SPA clients. tsc clean, full vitest green (2 new runtime-`/config` tests).
- [BUG-0090](../bugs.md) (**open**, high, infra) -- production admin panel returned `401` on `/api/admin/status`. **Root cause corrected:** the `401` was the admin role gate (`requires_role("admin")`) working as designed — it required the base64 `x-ms-client-principal` claims blob (which the SPA never sends) and fired only when `environment=production` + `require_admin_auth=true`; the prior "no Easy Auth identity source" analysis was a mis-diagnosis of the retired split-host App Service topology. **Fix delivered (Phases 1-4):** one `get_user_id` dependency (GUID-validate the header, else the default guest GUID, never 401) on every `/api/admin/*` route; deleted the role gate + `require_admin_auth` + `AZURE_REQUIRE_ADMIN_AUTH`. Enforcement is now ingress-level (the forgeable header is trusted; real auth is Easy Auth at the proxy), matching MACAE -- see [ADR 0031](../adr/0031-backend-admin-auth-header-only-ingress-enforced.md). Frontend already compliant (header forwarded, guest-GUID default, `G` initial) so no frontend change. **Status stays open -- live verification pending Phase 6.**

## Decisions

- **DEP-03:** Killed the parked `azd up` process — it had finished provision + deploy and was blocked at the interactive postdeploy "sample-data" menu. The seed can be re-run non-interactively (`--set default` / `AZURE_ENV_SAMPLE_DATA=default`) after the backend is unblocked.
- **OBS-01:** The embedding deployment came up as **`text-embedding-3-small`** (1536-dim index), not `text-embedding-3-large` as the research assumed. It deployed fine and the index matches (1536); flagged for confirmation that this is the intended model in `main.parameters.json`. **Resolved 2026-07-02:** confirmed intended — `-small` is deliberately pinned via `AZURE_ENV_EMBEDDING_MODEL_NAME=text-embedding-3-small` in the azd env (overrides the bicep `-large` default), native 1536-dim, grounding verified; kept as-is, no infra change.

## Next steps

1. Unblock the backend (BUG-0093): live hotfix `az cosmosdb update --public-network-access ENABLED` + `az storage account update --public-network-access Enabled`, then restart/scale the backend revision and re-check `/api/health`.
2. Durable fix (BUG-0093): make Cosmos + Storage honor `publicNetworkAccess: Enabled` in the non-private-networking profile (AVM module param/version or post-provision re-assert), then re-deploy.
3. Re-run the sample-data seed non-interactively; run the grounding smoke test (benefits question → citation).
4. Confirm the `text-embedding-3-small` vs `-large` intent.
