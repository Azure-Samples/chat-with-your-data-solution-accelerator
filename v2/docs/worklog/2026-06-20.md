---
title: CWYD v2 — Worklog 2026-06-20
description: Planned and started the BUG-0054 fix — make the deployed Event Grid system topic actually work so bulk-dropped blobs auto-ingest. Researched MACAE (no Event Grid; provision-time indexing) and CWYD v1 (HTTP-driven, no Event Grid), confirmed the v2 subscription is an un-ported handler-less addition, and decided on B2 + 2a (an Event Grid trigger function as the single ingestion trigger). Wrote ADR 0028 and landed U1 (the Event Grid subject parser).
author: CWYD Engineering
ms.date: 2026-06-20
topic: how-to
keywords: worklog, event grid, blob created, bulk upload, ingestion, batch_push, doc-processing, single trigger, ADR 0028, BUG-0054, system topic, event_grid_trigger
estimated_reading_time: 4
---

## Summary

Took [BUG-0054](../bugs.md) from "stray poison noise" to a scoped feature build. The deployed Event Grid system topic (`evgt-<SUFFIX>`) forwards `Microsoft.Storage.BlobCreated` events into the `doc-processing` queue, but the only consumer (`batch_push`) validates CWYD ingestion envelopes, not `BlobCreated` events — so every event poisons. Research confirmed there is **no reference pattern to copy**: MACAE has no Event Grid ingestion (it batch-indexes the `data/` datasets at provision time via a script), and CWYD v1 is HTTP-driven (`BatchStartProcessing` → queue → `batch_push_results`), which v2 already ports as `batch_start` → `doc-processing` → `batch_push`. The v2 Event Grid subscription is therefore an un-ported, handler-less addition. The operator confirmed they **bulk-upload by dropping files directly into the `documents` container**, so the Event Grid path is genuinely needed — it just never had its translator built.

Decision (operator-approved): **B2 + 2a** — build the missing handler as an Event Grid *trigger* function (sidesteps the BUG-0056 queue-encoding conflict), and make Event Grid the **single** blob-ingestion trigger (admin upload writes the blob only; `batch_start` stays as reprocess-all). Documented in [ADR 0028](../adr/0028-event-grid-single-trigger-blob-ingestion.md).

## Done

* **Researched + planned the BUG-0054 fix (not a delete).** Confirmed via the bicep that the Event Grid subscription is deployed by `main.bicep` (not stray/legacy); mapped the full v2 ingestion pipeline (`batch_start` HTTP producer → `doc-processing` → `batch_push` queue trigger; `BatchPushQueueMessage` envelope; raw-JSON producer + host `messageEncoding=none` from BUG-0056); checked MACAE (no Event Grid — provision-time `index_datasets.py`) and v1 (HTTP-driven, no Event Grid). Recorded the design fork and the chosen B2 + 2a in the session plan.
* **Wrote [ADR 0028](../adr/0028-event-grid-single-trigger-blob-ingestion.md)** documenting the decision: Event Grid as the single trigger for blob ingestion; an Event Grid *trigger* function (not a queue destination) to avoid the BUG-0056 encoding trap; repoint the bicep subscription `StorageQueue` → `AzureFunction` (delete nothing); rollout order that keeps ingestion working at every step. Captures consequences, the rejected alternatives (B1 dedicated queue, C MACAE-style provision indexing, 2c idempotent double, delete), and the BUG-0053 / BUG-0056 / BUG-0058 follow-ups.
* **U1 — Event Grid subject parser.** New `blob_event` blueprint package + `event_parser.py`: `parse_blob_created_subject(subject)` extracts `(container_name, filename)` from the `/blobServices/default/containers/<c>/blobs/<b>` subject into a frozen `ParsedBlobRef`, returning `None` for non-blob / container-level / malformed / blank subjects so the caller skips them; virtual-directory blob paths (embedded `/`) are captured whole. 11 unit tests in `tests/functions/blob_event/test_event_parser.py` — all green; new files pass the shared AST gates (marker-only `__init__`, imports-at-top, no process narrative, typed return); the only shared-gate red is the pre-existing `ROUTERS-NON-ROUTE-CONTENT-DEBT` on `admin.py` (BUG-0034), unrelated.
* **U2 — `blob_event` handler.** `blob_event_handler(subject, queue_client)` parses the subject (skipping non-blob / malformed subjects → `None`), builds a `BatchPushQueueMessage` (fresh `ingestion_job_id`, `force_reindex=False`), and enqueues it onto `doc-processing` via the existing `enqueue_push_message` producer — reusing producer + consumer rather than duplicating ingestion. Returns the enqueued envelope (or `None` on skip) so the blueprint can log the job id. 4 unit tests (mock queue: enqueues for a blob subject, captures virtual-directory paths, skips non-blob subjects without enqueueing, mints distinct job ids) — `tests/functions/blob_event` 15 green; 0 type/lint errors.
* **U3 — `log_event_grid_errors` observability decorator.** The Event Grid sibling of `log_queue_errors` in `functions/core/exception_mapping.py` (the module's design is one dedicated decorator per trigger type, not a polymorphic one). Logs `AzureError` / generic exceptions with structured `extra={operation, trigger:"event_grid"}` then **re-raises** so the Functions runtime fails the invocation and Event Grid retries delivery per the subscription's retry policy. No `ValidationError` arm — the event subject is parsed defensively (skip on bad input), not Pydantic-validated like the queue envelope. 4 new tests (passthrough, AzureError, unexpected, name/doc preserved) — `test_exception_mapping.py` 18 green; 0 type/lint errors.
* **U4 — `blob_event` Event Grid trigger blueprint + registration.** `@bp.event_grid_trigger` → `_execute` resolves the credential (registry, Hard Rule #4) + opens the `doc-processing` queue client **directly** (an Event Grid trigger has no queue output binding, mirroring how `batch_push._execute` opens its `ContainerClient` directly) → calls `blob_event_handler(event.subject, queue_client)` under `log_event_grid_errors`. Registered in `function_app.py` (import + `register_functions`, alphabetical slot). 4 tests (happy-path dispatch, AzureError + generic re-raise/log wiring with `trigger="event_grid"`, route registered on the app) — `tests/functions/blob_event` **19 green**. Full functions suite + shared gates: **1221 passed**, only the pre-existing `ROUTERS-NON-ROUTE-CONTENT-DEBT` (admin.py, BUG-0034) red. **The Functions tier (U1–U4) is complete** — parser → handler → decorator → blueprint; `batch_push` untouched. Live validation follows the bicep repoint (U5).
* **Pivoted B2 → B1 (operator-confirmed) + corrected ADR 0028.** While writing the U5 bicep, found B2's `AzureFunction` destination won't deploy cleanly with `azd`: (1) it uses the function *access key* (no managed-identity delivery — deviates from ADR 0002); (2) it needs the function to **exist** at subscription-creation so Event Grid can fetch its key, which fails `azd`'s provision-before-deploy order. Pivoted to **B1**: Event Grid → a dedicated `blob-events` Storage Queue (managed-identity delivery, exists at provision, pure bicep) → a `blob_event` *queue* trigger that translates the event → enqueues to `doc-processing` → `batch_push` (unchanged). The encoding worry that originally rejected B1 was unfounded — Event Grid writes **raw JSON** to a Storage Queue (why `messageEncoding=none` is the standard for EG→queue→Functions, which we already run), so no encoding change. Rewrote ADR 0028's Decision / Consequences / Alternatives (B2 is now the rejected alternative with the azd-ordering + function-key reasons). The single-trigger architecture (2a) is unchanged; **U1 parser + U2 handler are reused as-is**.
* **B1 Unit A — Event Grid event-body → subject extractor.** Added `subject_from_event_message(raw)` to `event_parser.py`: JSON-decodes the raw Event Grid message body (raw JSON first, base64 fallback — EG's queue encoding is external), accepts a single-event array, returns the top-level `subject` string or `None` (malformed / no string subject → skip). 7 new tests — `test_event_parser.py` **18 green**; 0 type/lint errors.
* **B1 Unit B — reworked the blueprint to a queue trigger.** `blob_event/blueprint.py` is now `@bp.queue_trigger` on `blob-events` (literal queue name — a fixed infra constant; avoids a host app-setting binding dependency in every `local.settings.json`) wrapped by the existing `log_queue_errors`. `_execute(msg, settings)` extracts the subject via `subject_from_event_message` (skipping a malformed message before any client opens), opens the `doc-processing` queue client, and calls `blob_event_handler`. `function_app.py` registration is unchanged (same `bp`). Reworked the blueprint test (queue-message fakes carrying a raw Event Grid event body; decorator wiring asserts `trigger="queue"`) + added a real-`_execute` translate-and-enqueue test (stubbed credential + `QueueClient` — asserts one CWYD envelope lands on `doc-processing`) and a skip-malformed test. `tests/functions/blob_event` **28 green**.
* **B1 Unit C — removed the dead `log_event_grid_errors` decorator.** The queue trigger uses the existing `log_queue_errors`, so the U3 decorator + `_EventGridHandler` alias + its 4 tests were removed (throwaway from the pre-pivot design, caught before it spread). Full functions suite + shared gates: **1226 passed**, only the pre-existing `ROUTERS-NON-ROUTE-CONTENT-DEBT` (admin.py, BUG-0034) red. **The Functions tier is now fully on B1** (queue trigger); `batch_push` untouched.
* **U5 (bicep) — repointed Event Grid to the `blob-events` queue.** Added a `blobEventsQueueName = 'blob-events'` var + `blob-events` (+ `blob-events-poison`) queues to **both** the new-storage `queueServices` array and the existing-storage queue resources. Repointed **both** subscriptions' StorageQueue destination `doc-processing` → `blob-events` (the AVM-module `blob-created-to-doc-processing` and the v1-reuse `cwyd2-blob-created-doc-processing`), keeping managed-identity delivery + the account-scoped sender role, and updated the existing subscription's queue `dependsOn`. **Subscription names retained** (not renamed to `…-blob-events`) deliberately: under azd's incremental deployment a rename would *orphan* the prior subscription, leaving it to keep delivering to `doc-processing` — an in-place destination change avoids that. `az bicep build` → **EXIT 0, no errors** (14 pre-existing warnings). **The full path is now wired:** EG `BlobCreated` → `blob-events` → `blob_event` trigger → `doc-processing` → `batch_push`.
* **Corrected an ADR 0028 factual error about v1.** Earlier research wrongly concluded "v1 is HTTP-driven, no Event Grid" (the grep silently excluded `code/`). v1 **does** use Event Grid: `code/backend/batch/batch_push_results.py` is a *polymorphic* queue trigger that branches on `eventType` and handles `Microsoft.Storage.BlobCreated` / `BlobDeleted` events directly on `doc-processing` (filename from `data.url`) alongside the legacy envelope. v2 deliberately does **not** copy that loose-dict consumer (no-imitate-v1); B1's typed translator keeps `batch_push` single-schema. ADR 0028 Context updated. v1's **BlobDeleted → remove-from-index** branch is a worthwhile follow-up feature (separate from BUG-0054).
* **Resolved the admin-documents 500 — operational, not a code bug.** `GET /api/admin/documents` returned `{"detail":"Internal server error."}` because Postgres `psql-<SUFFIX>` had **auto-stopped** on inactivity. A connection-level failure isn't caught by pgvector `list_sources`'s `asyncpg.PostgresError` handler (that catches server-side *query* errors), so it surfaced through the app-level handler as a generic sanitized 500 — the endpoint failed loud + logged structured context, which is correct behavior for a downed dependency. Started the server (`az postgres flexible-server start`); `health/ready` → 200, `GET /api/admin/documents` → 200 with the two existing sources. No code change. (This Flex server auto-stops every idle period — `az postgres flexible-server start -g <RESOURCE_GROUP> -n psql-<SUFFIX>` at session start.)
* **Discovered two gates that turn a blind U6 into new ingestion bugs.** Confirmed against the live Function App that the `blob_event` blueprint is **not yet deployed** (the app has only `add_url`, `batch_push`, `batch_start`, `health`, `search_skill`) — so Event Grid fills `blob-events` but nothing drains it, which is why the test blob `eg-test.txt` never appeared in the index. Also confirmed there is **no Event Grid locally** (`blob_event` is a queue trigger on `blob-events`, and nothing feeds that queue in dev). A naive "remove the backend enqueue" U6 would therefore break ingestion in **both** places (undeployed cloud + all local dev).
* **U6 — conditional enqueue (config-flag, not blind removal).** Added an `IngestionTrigger` `StrEnum` (`direct_enqueue` | `event_grid`; a hard `StrEnum` with no `str` arm — env discriminator, no registry dispatch, Hard Rule #11) + `StorageSettings.ingestion_trigger` (env `AZURE_INGESTION_TRIGGER`, default `direct_enqueue` so local dev + every existing deploy keep the backend-side enqueue). `upload_document` now enqueues the `BatchPushQueueMessage` **only** when the trigger is `DIRECT_ENQUEUE`; under `EVENT_GRID` it writes the blob only and returns `queued=False` (Event Grid → `blob-events` → `blob_event` owns the push, so no double-ingest). Updated the `UploadResponse` docstring to match. 3 new settings tests (default, env override, rejects-unknown) + 1 new ingestion test (EG mode writes blob, no enqueue, `queued=False`). Affected suites + shared gates: **1175 passed**, only the pre-existing `ROUTERS-NON-ROUTE-CONTENT-DEBT` (admin.py, BUG-0034) red. The cloud deploy will set `AZURE_INGESTION_TRIGGER=event_grid` **after** the Event Grid path is validated live.
* **Live-validated the Event Grid path end-to-end (local host against cloud storage).** The local func host (`local.settings.json` already pointed at `st<SUFFIX>` with identity-based bindings + `doc-processing-local` override) has `blob_event` registered and drains the real EG-fed `blob-events` queue. Confirmed the live subscription `blob-created-to-doc-processing` delivers to `blob-events` (StorageQueue, Succeeded). Baseline 2 indexed docs → dropped a fresh `eg-live-test.txt` into the cloud `documents` container → it ingested through the full chain (EG → `blob-events` → `blob_event` translate → `doc-processing-local` → `batch_push` parse/embed/index → pgvector) and appeared in the admin documents list as `eg-live-test.txt (chunks=1)` with `blob-events-poison=0`. **The Event Grid architecture is proven.** (2 stale `doc-processing-local-poison` messages remain from the earlier `eg-test.txt` attempts while Postgres was auto-stopped — local-queue-only, do not affect cloud; cleanup pending.) Remaining for cloud cutover: deploy `blob_event` to the cloud Function App (`azd deploy`, `prepackage` first per BUG-0058), wire `AZURE_INGESTION_TRIGGER` into the backend Container App (U6-infra bicep param), then flip it to `event_grid` and close BUG-0054 (U7).
* **Cleaned up all test artifacts** (per the "always clean up before the next step" rule). Deleted the `eg-live-test.txt` index entry via `DELETE /api/admin/documents/{source}` (`deleted=1`), deleted both test blobs (`eg-live-test.txt`, `eg-test.txt`) from the `documents` container, cleared the 2 stale `doc-processing-local-poison` messages, and removed the scratch temp files (`eg-diag*.txt`, `repoint-blob-events.bicep`). Verified `doc_total=2` (back to the two real samples) and `doc-processing-local-poison=0`. Confirmed the subscription filters to `Microsoft.Storage.BlobCreated` only, so the blob deletes fired no phantom events.
* **U6-infra — wired `AZURE_INGESTION_TRIGGER` into the backend Container App (bicep param).** Added an `@allowed(['direct_enqueue','event_grid'])` `ingestionTrigger` param (default `direct_enqueue`) in `main.bicep`, bound it as `AZURE_INGESTION_TRIGGER` on the backend container env (not the Functions app — only the backend's `upload_document` reads it), added a matching `output AZURE_INGESTION_TRIGGER`, and mapped it in both `main.parameters.json` + `main.waf.parameters.json` as `${AZURE_ENV_INGESTION_TRIGGER=direct_enqueue}` so an operator flips it with `azd env set AZURE_ENV_INGESTION_TRIGGER event_grid`. `az bicep build` → **exit 0**; both params files valid JSON. Default keeps current behavior (backend enqueues); the flip to `event_grid` happens **after** the `blob_event` Function is deployed + validated in cloud.
* **[BUG-0078](../bugs.md) — prepackage allow-list omitted `blob_event`.** Found while preparing the cloud deploy: `scripts/prepackage_function.py`'s explicit `_FUNCTION_SUBPACKAGES` tuple never listed `blob_event`, so `prepackage` staged only `add_url, batch_push, batch_start, search_skill, core` — the `blob_event` queue trigger would have shipped silently absent (same "incomplete artifact" class as BUG-0058, distinct root cause). Fixed by adding `blob_event` to the tuple + a guard test (`tests/functions/test_prepackage_subpackages.py`) asserting the allow-list equals the importable packages under `src/functions/`. Re-ran `prepackage` → `build-functions/functions/blob_event/blueprint.py` staged; guard green; **1230 passed** (only the pre-existing BUG-0034 `ROUTERS-NON-ROUTE-CONTENT-DEBT` red).
* **Cloud `blob_event` deploy stalled → deferred (operator decision).** `azd deploy function --no-prompt` packaged + uploaded the artifact (`Deploying (Uploading deployment package) [7s]`) but then exceeded azd's 20-minute wait timeout (`exceeded the azd wait timeout (20m0s)` → exit 1) — Flex Consumption remote-build slowness, same env friction as BUG-0058 / BUG-0062. Post-run checks: `az functionapp function list` still shows only the prior five functions (`add_url`, `batch_push`, `batch_start`, `health`, `search_skill`) — **no `blob_event`** — and a forced `syncfunctiontriggers` returned `{}` with no new function, confirming the running package predates the change. Beyond the deploy friction, `blob_event` is a queue trigger so it also carries the **BUG-0053** scale-from-zero risk (needs an `alwaysReady` instance to wake in Flex). The operator chose to **defer** the cloud cutover. Left the live state coherent: backend stays on `direct_enqueue` (`AZURE_ENV_INGESTION_TRIGGER` is **unset** → default — verified), so admin uploads keep working; the env flag was deliberately **not** flipped to `event_grid` (which would remove the backend enqueue with no cloud drainer); the Event Grid subscription stays pointed at `blob-events` (matching the committed bicep). All code/infra/test work remains green and committed-ready. Updated [BUG-0054](../bugs.md) with the implemented-but-deferred status + the resume steps.

## Decisions

* **B2 over B1.** An Event Grid *trigger* function deserializes the event outside the queue, so `doc-processing` stays single-schema + single-encoding and `batch_push` is untouched. B1 (dedicated `blob-events` queue) would force standardizing base64 everywhere (touching the working `batch_start` path) because the host `messageEncoding` is shared by all queue triggers.
* **2a — Event Grid is the single ingestion trigger.** Cleanest, no double-processing, no dedup hacks. Admin upload reduces to write-the-blob-only; `batch_start` stays as the explicit reprocess-all. Rollout lands the handler + bicep first (brief harmless double) and removes the backend enqueue last.
* **Delete nothing** — the subscription is repointed into a working configuration, per the operator directive.

## Next

The Event Grid architecture is **built and locally proven**; the cloud cutover is **parked** (operator decision — the Flex deploy stalled and the cloud queue trigger is coupled to BUG-0053). To resume:

1. **Deploy `blob_event` to the cloud Function App.** Run `uv run python scripts/prepackage_function.py` first (BUG-0058 — and `blob_event` is now in the allow-list per BUG-0078), then retry `azd deploy function --timeout 2400` (or diagnose the Flex remote-build hang directly). Confirm `az functionapp function list` shows `blob_event`.
2. **Add an `alwaysReady` instance for `function:blob_event`** (per BUG-0053) so the queue trigger wakes from zero in Flex; ensure the `messageEncoding=none` host setting (BUG-0056) persists so `blob_event` reads the raw Event Grid body.
3. **Validate in the cloud** — stop the local func host (so it doesn't drain `blob-events` first), drop a test blob into `documents`, confirm it ingests via the cloud `blob_event` → `doc-processing` → `batch_push` → pgvector; clean up the test artifacts.
4. **Flip off the backend double-enqueue** — `azd env set AZURE_ENV_INGESTION_TRIGGER event_grid` + `azd provision`; re-validate that admin uploads still index (now via Event Grid only).
5. **U7** — flip [BUG-0054](../bugs.md) → fixed; update env/docs.

Follow-ups: BUG-0077 (BlobDeleted → remove-from-index, the inverse direction) remains a separate deferred feature.

## Frontend Vitest tree relocated to `v2/tests/frontend/` (ADR 0029)

Separate task this day. Repo-owner decision to make the frontend test tree **symmetric** with the backend tree (revisiting [ADR 0020](../adr/0020-frontend-tests-under-src-tests-frontend.md)'s recorded alternative #2). Wrote [ADR 0029](../adr/0029-frontend-tests-symmetric-under-tests-frontend.md) (supersedes ADR 0020 + transitively ADR 0012) and executed the move:

* **Moved the `cwyd-frontend-tests` Vitest package** `v2/src/tests/frontend/` → `v2/tests/frontend/` (44 `*.test.tsx` + configs), and the Python ASGI test `test_frontend_app.py` → `v2/tests/frontend_app/` so each test dir is single-toolchain. No `v2/src/tests/` remains. (First attempt botched — a `git mv` into a non-existent target dir failed, then `Remove-Item` deleted the working-tree files; recovered intact from HEAD via `git checkout`, then redid the move with `Move-Item` + pre-created dirs.)
* **Re-pathed** the test package's `@` alias `../../frontend/src` → `../../src/frontend/src` in `vitest.config.ts` + `tsconfig.json` (alias *target* `v2/src/frontend/src` unchanged → zero test-file import edits); updated the npm-workspace member path in `package.json` (`src/tests/frontend` → `tests/frontend`; package name unchanged); regenerated `package-lock.json` cleanly (a first in-place `npm install` left an `extraneous` stale entry, so the lockfile + `node_modules` were regenerated from scratch — typescript stayed `5.9.3`, matching HEAD, so no dependency drift).
* **Updated** CI `v2-frontend-checks.yml` (path filters + `tsc -p tests/frontend` step), the `Dockerfile.frontend` comment (image copies only `src/frontend` → unaffected), the instruction files (`v2-frontend`, `v2-tests` `applyTo`, Hard Rule #11), the frontend `eslint.config.js` comment, and the ADR index (added the missing 0027/0028 rows + the new 0029 row).
* **Verified:** Vitest **566 passed / 44 files** from the new layout; relocated pytest **4 passed**; the relocated test tree typechecks with no test-tree errors.
* **Pre-existing breakage surfaced (out of scope, NOT caused by this move):** `tsc -p src/frontend` + `npm run lint` are red on **app-source** files — `src/frontend/src/api/streamChat.tsx` (`exactOptionalPropertyTypes` TS2375, the BUG-0067 error-body code), `CitationPanel.tsx` + `MessageList.tsx` (lint). Confirmed pre-existing: typescript `5.9.3` matches HEAD and `git diff` shows **no** `src/frontend/src` file changed by the move. These FE gates were red before the relocation (the 2026-06-19 FE bug fixes were committed without running `tsc`/`lint`). Recommend a separate gate-green pass (and likely a `BUG-####` for the app-source type/lint failures). *(Logged as [BUG-0079](../bugs.md).)*

## BUG‑0070 fixed — chat microphone (speech‑to‑text)

First of the 70→79 bug sweep (analysis‑first). **Root cause:** the speech REST client [api/speech.tsx](../src/frontend/src/api/speech.tsx) was the **only** fetch client issuing a bare relative `/api/speech` (no `VITE_BACKEND_URL` prepend). In the deployed split‑origin topology (frontend App Service ≠ backend Container App, per BUG‑0050) it resolves against the frontend host's SPA catch‑all → `index.html` (200) → `response.json()` throws → `useSpeechRecognition.start()` sets `error` → mic disabled, no transcription. Local dev worked because the Vite proxy maps `/api` → backend. Everything else (the hook, the `MessageInput` wiring, the backend AAD token mint, the Speech account + `Cognitive Services Speech User` RBAC + `AZURE_SPEECH_*` backend env vars) is correctly wired — verified.

* **Fix:** added the `backendUrl()` / `apiUrl()` seam to `api/speech.tsx` (mirroring `admin.tsx`) and routed the fetch through `apiUrl(SPEECH_URL)`. Test‑first: added a `VITE_BACKEND_URL`‑prepend Vitest case (RED on the relative URL → GREEN after the seam) + `vi.unstubAllEnvs()` cleanup.
* **Scope audit:** checked every `fetch(` site under `src/frontend/src/` — speech was the **only** remaining bare‑relative `/api` seam (the 6th of the BUG‑0046/0050 family; `auth.tsx`'s `/.auth/me` is correctly same‑origin). No 7th.
* **Verified:** Vitest **567 passed** (566 + the new case, no regression); `eslint` + `tsc` clean on the two touched files (the only `tsc` red is the pre‑existing BUG‑0079 `streamChat.tsx`, unrelated). No backend / infra / hook change **for root cause 1**. (Root cause 2 below adds the backend change.)
* **Root cause 2 found (backend) — during local end‑to‑end testing.** After the FE fix, local `/api/speech` returned `503 "not configured"` (the local `v2/.env` predated the `AZURE_SPEECH_*` vars — populated from `azd env get-values`; region was empty), then `502 token mint failed`. Researched it thoroughly (analysis‑first, no rush): [core/speech.py](../src/backend/core/speech.py) `_token_mint_url` minted the AAD token against the **regional** STS host (`{region}.api.cognitive.microsoft.com`), but Microsoft docs are explicit — *"regional endpoints do not support Microsoft Entra authentication"*; AAD requires the account's **custom‑subdomain** host. With `disableLocalAuth: true` (keyless), the regional host **400s** the AAD bearer → mapped to `502`. **Regression from v1**, whose `/api/speech` RBAC path used `speechService.outputs.endpoint` (the custom‑domain host) — v2 hardcoded the regional host. Confirmed empirically against the live account: regional+AAD → **400**, custom‑subdomain+AAD → **401** (auth‑checked; the `x-ms-cognitiveservices-resource-id` header is irrelevant).
* **Backend fix:** changed `_token_mint_url` regional → custom‑subdomain (`https://{service_name}.cognitiveservices.azure.com/sts/v1.0/issueToken`), derived from the already‑wired `AZURE_SPEECH_SERVICE_NAME` (the custom subdomain == the account name in Bicep → **no Bicep change**). Test‑first: the `test_speech_helper.py` URL assertion flipped regional → custom‑subdomain (RED→GREEN); speech helper + router tests **11 passed**, pyright `core/speech.py` 0/0/0.
* **Verified Bicep populates the var:** `AZURE_SPEECH_SERVICE_NAME` lives inside the **backend** container app's `env: union([...])` array (`= speechService.outputs.name`, unconditional module), so every `azd provision` sets a real value — not the BUG‑0051/0052 output‑only class.
* **Local confirmation pending RBAC propagation:** the custom‑domain `issueToken` still returns `401 PermissionDenied` for the dev's `az` identity; verified the "Cognitive Services Speech User" grant IS present at the correct principal + scope, so this is pure Cognitive Services **data‑plane RBAC propagation** (minutes), not a defect. The **deployed** env (UAMI role long propagated) works as soon as the code ships (`azd deploy backend`). Restarted the local backend with the fixed code so the mic works the moment propagation completes.
* Both root causes now fixed in code; [BUG‑0070](../bugs.md) documents both under one entry (no separate bug, per the one‑symptom‑one‑entry decision).
* **Pivot — the custom‑subdomain `issueToken` fix (above) was WRONG; switched to the documented `aad#` pattern (operator‑approved).** The custom‑domain `issueToken` stayed **401 for >70 minutes** (role granted 21:28 UTC, still 401 at 22:41 UTC — verified at correct principal+scope), which is far past propagation → **`issueToken` is the key‑exchange endpoint and `Cognitive Services Speech User` doesn't authorize it.** Microsoft's docs confirm the Entra‑ID Speech pattern is **not** `issueToken` — it's the SDK‑native `aad#{resourceId}#{aadToken}` form passed to `fromAuthorizationToken`; the SDK exchanges the AAD token with the Speech service itself. v1's `issueToken`‑RBAC branch was almost certainly dead code (v1 shipped on keys); v2 revived a non‑working path.
* **Fix B (final):** rewrote `mint_speech_token` to return `aad#{account_resource_id}#{aad_token}` — no `issueToken` / HTTP call; the router drops the `httpx` error arm (only `AzureError` from AAD acquisition can fail). The **FE is unchanged** (its hook already passes the token to `fromAuthorizationToken`). Test‑first: reworked `test_speech_helper.py` to assert the `aad#…` return + the cognitive‑services AAD scope, removed the obsolete HTTP/`issueToken` tests; `test_speech_helper.py` + `test_speech.py` **7 passed**, pyright `core/speech.py` + `routers/speech.py` 0/0/0.
* **Confirmed live:** restarted the backend and `GET /api/speech` returns **200** with an `aad#` token (`region=swedencentral`, len ≈ 2870) — **no propagation wait** (Speech User authorizes *recognition*, which had long taken effect; only the unsupported `issueToken` action was denied). Final browser‑mic confirmation is the SDK validating the token; deployed env uses the same pattern (UAMI same role) → works on `azd deploy backend`. (Lesson: the `aad#` pattern is the only keyless browser‑Speech AAD path; `issueToken` requires a key.)
* **Closed (user‑confirmed):** the operator clicked the mic in the browser and it **captured and transcribed speech** end‑to‑end. [BUG‑0070](../bugs.md) marked verified/closed (both root causes). Next in the sweep: BUG‑0071.

## BUG‑0071 fixed — multi‑agent header logo + click‑to‑home

Second of the 70→79 sweep (analysis‑first). **Root:** [Header.tsx](../src/frontend/src/components/Header/Header.tsx) rendered the static Microsoft 4‑square [MsftColorLogo.tsx](../src/frontend/src/components/Header/MsftColorLogo.tsx) inside a non‑interactive `<Avatar>` — off‑brand for a multi‑agent product and not clickable. The brand also carries an `<h1>` title beside the logo, so the *whole* brand block can't be one button (an `<h1>` can't nest inside `<button>`).

* **Fix:** swapped the glyph to the Fluent `PeopleTeam20Filled` multi‑agent icon inside `<Avatar shape="square" color="brand" name="Multi-agent">`, and wrapped **only the logo** in a `<button data-testid="header-home" aria-label="Go to home">` that calls a new optional `onNavigateHome` prop. Added a `.brandButton` CSS reset (no border/background, pointer cursor, `:focus-visible` ring). [App.tsx](../src/frontend/src/App.tsx) wires `onNavigateHome={() => void navigate(SectionPath[Section.Chat])}` → the `/` chat root.
* **Icon correction (operator feedback):** the generic `PeopleTeam` icon wasn't the multi‑agent product's mark. Researched MACAE's actual header brand → it renders the **Contoso interlocking glyph** (`commonComponents/imports/ContosoLogo`, filled `--colorBrandForeground1`), not the Microsoft 4‑square. Added a new [MultiAgentLogo.tsx](../src/frontend/src/components/Header/MultiAgentLogo.tsx) with that SVG (adapted from MACAE) + [MultiAgentLogo.test.tsx](../tests/frontend/components/MultiAgentLogo.test.tsx), set the Avatar to `color="neutral"` so the brand‑colored mark shows, and dropped the `PeopleTeam` import.
* **Test‑first:** rewrote the brand‑logo assertion in [Header.test.tsx](../tests/frontend/components/Header.test.tsx) to expect the `header-home` button + "Go to home" name, and added a click‑`onNavigateHome` case (RED→GREEN).
* **Verified:** Header + MultiAgentLogo tests **13 passed**; `eslint` clean on the touched source files; no new `tsc` errors (only the pre‑existing BUG‑0079 `streamChat.tsx`). The now‑orphaned `MsftColorLogo.tsx` is left in place (not deleted) to keep the change minimal. [BUG‑0071](../bugs.md) marked fixed. Next: BUG‑0072.
