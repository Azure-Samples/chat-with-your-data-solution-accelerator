---
title: CWYD v2 — Worklog 2026-06-11
description: Daily worklog for CWYD v2 on 2026-06-11. Captures the plan, the work completed, bugs tracked, decisions, and next steps for the day.
author: CWYD Engineering
ms.date: 2026-06-11
topic: how-to
keywords: worklog, daily log, bugs, admin configuration, v2, status
estimated_reading_time: 7
---

## Summary

Tracked six admin-configuration and search defects in the canonical registry after researching the v2 admin surface against the v1 parity baseline. Then stood up all three v2 tiers (backend, frontend, Functions) running natively on the workstation against the live deployed Azure cloud data plane, with VS Code one-click run + debug configs, so the defects can be reproduced and fixed. Reproducing `BUG-0003` locally corrected its recorded root cause. Also cataloged eleven more defects (`BUG-0009`–`BUG-0019`) reported by the user across the admin Configuration page and the chat UI (reasoning feed, markdown rendering, v1 citation parity, history panel). Then started fixing the backlog one unit per turn, test-first, beginning with `BUG-0004`: the admin Orchestrator field now renders as a dropdown of the known orchestrator keys instead of a free-text input.

## Planned

* Triage the reported admin-configuration and `/api/admin/documents` issues against the actual v2 code and the v1 baseline.
* Record each confirmed defect in [bugs.md](../bugs.md) as `BUG-0003` through `BUG-0008`.
* Defer all fixes to later one-unit, test-first turns.

## Done

* Researched the v2 admin Configuration page, the orchestrator registry, the content-safety defaults, the `/api/admin/documents` route, and the v1 prompt configuration for a parity baseline.
* Added six defects to [bugs.md](../bugs.md): `BUG-0003` (documents endpoint 503), `BUG-0004` (orchestrator free-text field), `BUG-0005` (labels show config keys), `BUG-0006` (content safety default), `BUG-0007` (default prompt text), and `BUG-0008` (separate prompt editor page).
* Added v2 run/debug configs: merged `v2:`-prefixed tasks and launch configurations into the repo-root `.vscode/tasks.json` and `.vscode/launch.json` (all v1 entries preserved), plus a `v2: Run all (backend + functions + frontend)` compound. Verified the pre-existing gitignored `v2/src/functions/local.settings.json` (cosmos mode, isolated `doc-processing-local` queue, `messageEncoding: none`).
* Ran and verified all three tiers against the live cloud data plane via `AzureCliCredential`: backend (`uvicorn` on `:8000`, `GET /api/health/ready` all `pass` — foundry_iq, database cosmosdb, search AzureSearch); frontend (Vite on `:5273`, serves the SPA and proxies `/api` to the backend); Functions host (`:7071`, all five functions indexed, storage blob lease acquired, `GET /api/health` returns `200`).
* Diagnosed `BUG-0003` from the live run and corrected its recorded root cause in [bugs.md](../bugs.md).
* Saved the local-run steps: added a `Quick start (all three tiers)` section to [local_development.md](../local_development.md) covering the frontend tier and the VS Code one-click compound (`v2: Run all`), so spinning up the three tiers no longer takes several iterations.
* Cataloged eleven more defects reported by the user into [bugs.md](../bugs.md) as `BUG-0009` through `BUG-0019`: admin Log level dropdown (`BUG-0009`), numeric-entry validation (`BUG-0010`), RAI validation + system-prompt guardrail precedence (`BUG-0011`), and chat-UI defects — avatar/Thinking alignment (`BUG-0012`), missing reasoning feed (`BUG-0013`), markdown-to-HTML rendering (`BUG-0014`), v1 citation parity for the reference block (`BUG-0015`) and inline references (`BUG-0016`), new-conversation persistence (`BUG-0017`), history-panel database-name removal (`BUG-0018`), and removing the New chat section (`BUG-0019`). Researched each against the v2 source to record an accurate root cause; all fixes deferred to later one-unit turns.
* Fixed `BUG-0004`: rendered the admin Orchestrator field as a Fluent UI `Select` dropdown of the first-party keys (`langgraph`, `agent_framework`) instead of a free-text input. Added an `OrchestratorName` closed-set map to `frontend/src/models/admin.tsx` (mirrors the backend StrEnum), extended `FieldSpec` with a `select` kind plus `options`, and kept any off-list current value selectable so the widened `OrchestratorName | str` backend type never loses a configured key. The wire field stays a plain `string`. Updated and extended the Configuration Vitest suite; full frontend suite green (371 tests).
* Fixed the `BUG-0004` persistence follow-up: a saved orchestrator selection reported success but reverted to `langgraph` on reload. Root cause was in `frontend/src/api/admin.tsx` — `getAdminConfig()` read `GET /api/admin/config` (the env-only snapshot) instead of `GET /api/admin/config/effective` (the override-overlaid view), so the persisted `RuntimeConfig` never reached the form. Pointed `getAdminConfig()` at the effective endpoint and unwrapped its `values`, and added an `EffectiveAdminConfig` wire type to `frontend/src/models/admin.tsx` mirroring the backend model. The backend env-vs-effective split is intentional (backend tests pin `/api/admin/config` as env-only), so no backend change was made. Updated the `getAdminConfig` Vitest block to assert the effective endpoint and the unwrapped override value; full frontend suite green (371 tests).
* Fixed `BUG-0005`: the admin Configuration field labels no longer show the internal config key in parentheses (e.g. `Orchestrator (orchestrator_name)`). Removed the `<span>` that rendered `({spec.key})` from the label in `frontend/src/pages/admin/Configuration/Configuration.tsx` so it shows only the human-readable `spec.label`, and deleted the now-dead `.fieldName` CSS class from `Configuration.module.css`. Added a Vitest assertion that rendered labels contain the human text and never the parenthetical config key; full frontend suite green (372 tests).
* Fixed `BUG-0006` (with the operator, Option B): Content Safety now defaults to enabled. Flipped `ContentSafetySettings.enabled` to default `True` (secure-by-default) in `backend/core/settings.py` and set `.env.sample` `AZURE_CONTENT_SAFETY_ENABLED=true`. The lifespan gate still requires both `enabled` and a non-empty endpoint, so the new default is inert until `AZURE_CONTENT_SAFETY_ENDPOINT` is set; production already provisions the endpoint via Bicep, so deployed behavior is unchanged. Updated the settings docstring, the `.env.sample` comment, and the two backend tests that pinned the old `False` default. Backend settings/lifespan/admin/dependencies suites green (226 tests).
* Fixed `BUG-0007` (with the operator, Option A — strict near-verbatim port): the default agent instructions now carry v1's vetted `answering_system_prompt`. Ported the v1 text verbatim into `CWYD_AGENT.instructions` in `backend/core/agents/definitions.py` (kept the two code-generation lines, the "private model trained by Open AI" line, and v1's exact sub-bullet indentation and wording), dropping only the stale knowledge-cutoff-2021 / "current date in the system message" line because v2 injects no current date. This restores the `[docN]`/`[doc+index]` inline citation instructions the v2 citation pipeline depends on (`filter_to_referenced` extracts those markers into `citation` SSE events). Renamed the test to `test_cwyd_agent_carries_vetted_v1_default_prompt`, asserting the ported grounding/citation/out-of-domain-refusal content and that the 2021 line is gone; the admin config test compares against the symbol so it was unaffected. Backend agents/admin/orchestrator suites green (173 tests).
* Fixed `BUG-0008` (with the operator — v1 structure): removed the standalone Prompt editor page and folded `cwyd_agent_instructions` editing into the Configuration page, matching v1's single-page inline prompt editing. Added it as a multi-line `kind: "text"` field with `allowEmpty: true` at the top of `FIELD_SPECS` in `frontend/src/pages/admin/Configuration/Configuration.tsx` (renders through the existing `<Textarea>` branch like `post_answering_prompt`) and to `RAI_GUARDED_FIELDS` so a 422 RAI rejection surfaces inline. v1 has no per-field "Reset to default" button; clearing the textarea and saving sends an empty string, which `_resolve_definition` already treats identically to `null` (both fall back to the built-in `CWYD_AGENT` default), so reset-to-default is preserved with no extra `null` plumbing. Deleted `frontend/src/pages/admin/PromptEditor/` (page + CSS) and `tests/frontend/pages/admin/PromptEditor/`; dropped the `AdminPrompt` section + its `SectionPath` entry from `sections.tsx`, the `admin-subnav-prompt` nav item from `AdminLayout.tsx`, and the `PromptEditor` import + `/admin/prompt` route from `App.tsx`. The wire models already declared the field across `AdminConfig` / `RuntimeConfig` / `AdminConfigPatch`, so no model or backend change was needed. Folded coverage into the Configuration Vitest suite (textarea render, empty-allowed, PATCH, inline RAI rejection + clear) and flipped the `sections` / `AdminLayout` suites to drop `AdminPrompt`; full frontend suite green (368 tests).
* Fixed `BUG-0009`: rendered the admin Log level field as a dropdown over the closed set (`DEBUG`/`INFO`/`WARNING`/`ERROR`) instead of free text, reusing the same `kind: "select"` machinery as the orchestrator field (BUG-0004). Added a `LogLevel` closed-set const to `frontend/src/models/admin.tsx` (mirrors the `OrchestratorName` `as const` + literal-union pattern); the wire field stays a plain `string` because the backend stores `log_level` as a free-form level name (`settings.observability.log_level: str = "INFO"`, no backend enum). Switched the `log_level` `FieldSpec` to `kind: "select"` with the four `LogLevel` options and imported `LogLevel`; no change was needed to the select render branch, `validateField`, `computePatch`, or `configToForm`. Retargeted the pre-existing "empty text fields" validation test (it used `log_level`, now a select, and after BUG-0008 no `kind: "text"` field is non-`allowEmpty`) to assert the select-cleared path, and added a dropdown-render test and a `{ log_level: "DEBUG" }` PATCH test; full frontend suite green (370 tests).
* Closed `BUG-0010` as already-implemented (confirm-and-close, no production change). Verified the admin Configuration page is the only numeric-entry surface in the entire v2 frontend (a workspace search for `type="number"` returns a single hit) and that its numeric fields (`openai_temperature`, `openai_max_tokens`, `search_top_k`) already enforce numeric entry end-to-end: `handleNumberChange` coerces empty/non-parseable input to `NaN`, `validateField` returns "must be a number" plus min/max-bound errors, the error renders inline, and `anyFieldInvalid` disables Save. The existing suite covered the max-bound path and the numeric PATCH delta but not the literal symptom, so added one regression test — emptying `openai_max_tokens` surfaces "must be a number" and disables Save — to pin the enforce-numeric-entry behavior. Full frontend suite green (371 tests).
* Fixed `BUG-0011` (with the operator — guardrail composition, Decisions A + Z; scope limited to the guardrail facet): an authored agent prompt could fully replace the system instructions and supersede the safety / out-of-domain / citation rules. Extracted those non-negotiable rules into a single `CWYD_GUARDRAIL` constant in `backend/core/agents/definitions.py` and added `compose_cwyd_instructions(body)`, which wraps `body` between the guardrail on both sides (first-word and last-word precedence). The built-in default (`CWYD_AGENT.instructions = compose_cwyd_instructions(CWYD_DEFAULT_BODY)`, body unchanged) and the operator override path (`_resolve_definition` now returns `compose_cwyd_instructions(text)`) route through the same guardrail, so an authored prompt customizes the persona between the guardrail bookends but cannot supersede the fixed rules. Found the originally-recorded RAI-coverage facet already satisfied — `cwyd_agent_instructions` is already in the backend `PROMPT_FIELDS` and the frontend `RAI_GUARDED_FIELDS`, so it is RAI-screened on save (corrected the stale note in [bugs.md](../bugs.md)); RAI-classifying end-user chat input is split out as a separate, out-of-scope concern. Added guardrail/compose tests (`tests/backend/core/agents/test_definitions.py`) and a hostile-override regression test (`tests/backend/core/providers/agents/test_base.py`), and updated the three existing resolver / cold-start tests that asserted the raw override text. Backend definitions + agents-base suites green (32 tests); the related admin / services-admin / conversation / types / content-safety suites that reference the default prompt also green (210 tests).
* Refined `BUG-0011` after review (same day, with the operator): centralized + deduped the prompt so each rule lives in exactly one place. Switched `compose_cwyd_instructions` from the both-sides wrap (Decision Z) to appending the guardrail **once, at the end** — last-word precedence, the strongest position against an override appearing in the body; this **supersedes Decision Z**. `CWYD_DEFAULT_BODY` was Tidy-stripped of every rule the guardrail owns (safety, the out-of-domain refusal message, the `[doc+index]` citation mechanics, no-URLs, refuse-to-discuss-prompts, greetings-without-docs), keeping only the unique persona / capabilities / formatting guidance and the in/out-of-domain decision methodology (rewritten to defer to the guardrail "below"), so each rule now appears exactly once — no duplication or triplication. Rewrote the two tests that pinned the both-sides wrap (`test_compose_cwyd_instructions_appends_guardrail_once`, and `…_are_guardrail_wrapped` now asserts `count == 1`, suffix only) and added a no-duplication regression test (`test_cwyd_default_instructions_state_each_rule_once`: the refusal message and `[doc+index]` each appear exactly once); fixed the hostile-override regression in `test_base.py` to assert the guardrail is appended once, last. Definitions + agents-base suites green (33 tests); admin suite + compliance gates green (439 passed, 1 skipped).

## Bugs

All defects below are tracked for fixing one unit per turn. `BUG-0004` through `BUG-0011` are fixed (below); the rest are open.

* [BUG-0003](../bugs.md#bug-0003--admin-documents-endpoint-returns-503-list_sources-facets-the-non-facetable-title-field): `GET /api/admin/documents` returns `503` even with search fully configured — `list_sources` facets the `title` field, which is not `facetable` in the index schema (`FieldNotFacetable`). Reproduced locally 2026-06-11; recorded root cause corrected.
* [BUG-0004](../bugs.md#bug-0004--orchestrator-field-is-free-text-instead-of-a-dropdown) — **fixed 2026-06-11** (incl. persistence follow-up): the admin Orchestrator field renders as a dropdown of the known orchestrator keys, and a saved selection now persists across reload — the admin client reads the override-overlaid effective config endpoint instead of the env-only snapshot.
* [BUG-0005](../bugs.md#bug-0005--admin-configuration-labels-show-internal-config-key-names) — **fixed 2026-06-11**: the admin Configuration labels no longer show the internal config-key suffix such as `(orchestrator_name)`; each field renders only its human-readable label.
* [BUG-0006](../bugs.md#bug-0006--content-safety-defaults-to-disabled) — **fixed 2026-06-11**: Content Safety now defaults to enabled (`ContentSafetySettings.enabled = True` + `.env.sample` true); the lifespan gate still requires an endpoint, so the default is inert until one is configured.
* [BUG-0007](../bugs.md#bug-0007--default-agent-instructions-do-not-carry-the-v1-default-prompt-text) — **fixed 2026-06-11**: the default agent instructions now carry v1's vetted answering system prompt (strict near-verbatim port; only the stale 2021/current-date line dropped).
* [BUG-0008](../bugs.md#bug-0008--separate-prompt-editor-page-should-fold-into-configuration) — **fixed 2026-06-11**: removed the standalone Prompt editor page and folded `cwyd_agent_instructions` into the Configuration page as an RAI-guarded multi-line field, matching v1; clearing + saving reverts to the built-in default (backend treats empty as `null`).
* [BUG-0009](../bugs.md#bug-0009--log-level-field-is-free-text-instead-of-a-dropdown) — **fixed 2026-06-11**: the admin Log level field renders as a dropdown over the closed set (`DEBUG`/`INFO`/`WARNING`/`ERROR`) instead of free text, reusing the orchestrator `select` machinery.
* [BUG-0010](../bugs.md#bug-0010--numeric-config-fields-should-validate-numeric-entry) — **fixed 2026-06-11** (closed as already-implemented): the admin Configuration page — the only numeric-entry surface in the v2 frontend — already enforces numeric entry (`type=number` + `NaN` guard + min/max bounds + Save gating); added a regression test for the non-numeric-entry symptom.
* [BUG-0011](../bugs.md#bug-0011--authored-prompt-could-supersede-the-system-guardrail-prompt) — **fixed 2026-06-11**: an authored agent prompt could fully replace the system instructions and supersede the safety / out-of-domain / citation rules; fixed by appending a fixed, non-overridable `CWYD_GUARDRAIL` once after both the default and any authored override, and deduping the body against it so each rule appears exactly once (no duplication/triplication). RAI validation of the authored instructions was already in place (`PROMPT_FIELDS` + frontend `RAI_GUARDED_FIELDS`); RAI-classifying end-user chat input is split out as out of scope.
* [BUG-0012](../bugs.md#bug-0012--robot-avatar-and-thinking-panel-are-not-aligned): the assistant robot avatar and the Thinking panel are not aligned.
* [BUG-0013](../bugs.md#bug-0013--thinkingreasoning-feed-never-shows): the Thinking/reasoning feed never shows because the backend emits no `reasoning` SSE frames.
* [BUG-0014](../bugs.md#bug-0014--assistant-response-markdown-is-not-converted-to-html): assistant responses render as raw markdown instead of HTML, unlike v1.
* [BUG-0015](../bugs.md#bug-0015--citationreference-block-should-look-like-v1): the citation/reference block styling does not match v1.
* [BUG-0016](../bugs.md#bug-0016--inline-references-should-render-like-v1): inline `[docN]` references render as literal-text buttons; they should render like v1 (superscript links).
* [BUG-0017](../bugs.md#bug-0017--new-conversation-is-not-saved-to-history): starting a conversation does not persist it, so new conversations never appear in history.
* [BUG-0018](../bugs.md#bug-0018--chat-history-column-shows-the-backend-database-name): the chat history column shows the backend database name; it should not.
* [BUG-0019](../bugs.md#bug-0019--remove-the-new-chat-section-from-the-history-column): remove the New chat section and functionality from the chat history column.

## Decisions

* Post-answering is not a defect. Research confirmed v1 ships a full post-answering feature (a default prompt, a fact-checking validator, and an operator filter message), so the v2 post-answering trio matches v1 and stays as is.
* Bringing v1 prompts into v2 is scoped to the default prompt text only. The v1 per-business-unit assistant-type system (Default, Contract assistant, Employee assistant) is out of scope for v2.
* All six items were recorded in [bugs.md](../bugs.md) rather than split across the development plan debt queues, by request. No `BUG-####` pointer rows were added to the development plan this turn; they can be added later if a defect needs phase-audit visibility.
* `BUG-0003`'s recorded root cause was corrected after reproducing it locally against the live cloud. The `503` is not caused by an unset search endpoint — it fires with search fully configured, because `list_sources` requests a facet on the non-facetable `title` field. The original empty-endpoint hypothesis is a separate latent pass-through path.
* `BUG-0011` was settled with the operator as guardrail composition (Decision A — extract a single reused `CWYD_GUARDRAIL` constant; Decision Z — wrap the body on both sides, prefix and suffix; **Z later superseded the same day** — deduped the body against the guardrail and switched to appending the guardrail once, at the end, for last-word precedence and zero duplication). Two corrections to the recorded root cause: (a) RAI validation of the authored `cwyd_agent_instructions` on save was **already in place** (`PROMPT_FIELDS` + frontend `RAI_GUARDED_FIELDS`), so the stale note was corrected; (b) RAI-classifying end-user chat input (beyond the existing `ContentSafetyGuard`) is a separate, larger decision — a Foundry round-trip per chat turn — and is intentionally **out of scope** for this fix, not bundled.

## Next

* Continue fixing the defects one unit per turn, test-first, in numerical order with a user review between each. `BUG-0003` is deliberately last (by request).
* Next: `BUG-0012` — align the assistant robot avatar with the Thinking (reasoning) panel in the chat message layout (frontend-only).
* `BUG-0003` (last): fix `list_sources` so it does not depend on faceting the non-facetable `title` field (facet a field declared `facetable`, or page documents and aggregate distinct sources client-side); optionally mark the source-grouping field `facetable` in the index schema. Separately, decide whether the `search_provider is None` pass-through should degrade gracefully instead of returning `503`.
