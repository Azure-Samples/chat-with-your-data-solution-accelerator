---
title: CWYD v2 — Worklog 2026-06-11
description: Daily worklog for CWYD v2 on 2026-06-11. Captures the plan, the work completed, bugs tracked, decisions, and next steps for the day.
author: CWYD Engineering
ms.date: 2026-06-11
topic: how-to
keywords: worklog, daily log, bugs, admin configuration, v2, status
estimated_reading_time: 7
---

## Summary

Tracked six admin-configuration and search defects in the canonical registry after researching the v2 admin surface against the v1 parity baseline. Then stood up all three v2 tiers (backend, frontend, Functions) running natively on the workstation against the live deployed Azure cloud data plane, with VS Code one-click run + debug configs, so the defects can be reproduced and fixed. Reproducing `BUG-0003` locally corrected its recorded root cause. Also cataloged eleven more defects (`BUG-0009`–`BUG-0019`) reported by the user across the admin Configuration page and the chat UI (reasoning feed, markdown rendering, v1 citation parity, history panel). Then started fixing the backlog one unit per turn, test-first, beginning with `BUG-0004`: the admin Orchestrator field now renders as a dropdown of the known orchestrator keys instead of a free-text input.

## Planned

* Triage the reported admin-configuration and `/api/admin/documents` issues against the actual v2 code and the v1 baseline.
* Record each confirmed defect in [bugs.md](../bugs.md) as `BUG-0003` through `BUG-0008`.
* Defer all fixes to later one-unit, test-first turns.

## Done

* Researched the v2 admin Configuration page, the orchestrator registry, the content-safety defaults, the `/api/admin/documents` route, and the v1 prompt configuration for a parity baseline.
* Added six defects to [bugs.md](../bugs.md): `BUG-0003` (documents endpoint 503), `BUG-0004` (orchestrator free-text field), `BUG-0005` (labels show config keys), `BUG-0006` (content safety default), `BUG-0007` (default prompt text), and `BUG-0008` (separate prompt editor page).
* Added v2 run/debug configs: merged `v2:`-prefixed tasks and launch configurations into the repo-root `.vscode/tasks.json` and `.vscode/launch.json` (all v1 entries preserved), plus a `v2: Run all (backend + functions + frontend)` compound. Verified the pre-existing gitignored `v2/src/functions/local.settings.json` (cosmos mode, isolated `doc-processing-local` queue, `messageEncoding: none`).
* Ran and verified all three tiers against the live cloud data plane via `AzureCliCredential`: backend (`uvicorn` on `:8000`, `GET /api/health/ready` all `pass` — foundry_iq, database cosmosdb, search AzureSearch); frontend (Vite on `:5273`, serves the SPA and proxies `/api` to the backend); Functions host (`:7071`, all five functions indexed, storage blob lease acquired, `GET /api/health` returns `200`).
* Diagnosed `BUG-0003` from the live run and corrected its recorded root cause in [bugs.md](../bugs.md).
* Saved the local-run steps: added a `Quick start (all three tiers)` section to [local_development.md](../local_development.md) covering the frontend tier and the VS Code one-click compound (`v2: Run all`), so spinning up the three tiers no longer takes several iterations.
* Cataloged eleven more defects reported by the user into [bugs.md](../bugs.md) as `BUG-0009` through `BUG-0019`: admin Log level dropdown (`BUG-0009`), numeric-entry validation (`BUG-0010`), RAI validation + system-prompt guardrail precedence (`BUG-0011`), and chat-UI defects — avatar/Thinking alignment (`BUG-0012`), missing reasoning feed (`BUG-0013`), markdown-to-HTML rendering (`BUG-0014`), v1 citation parity for the reference block (`BUG-0015`) and inline references (`BUG-0016`), new-conversation persistence (`BUG-0017`), history-panel database-name removal (`BUG-0018`), and removing the New chat section (`BUG-0019`). Researched each against the v2 source to record an accurate root cause; all fixes deferred to later one-unit turns.
* Fixed `BUG-0004`: rendered the admin Orchestrator field as a Fluent UI `Select` dropdown of the first-party keys (`langgraph`, `agent_framework`) instead of a free-text input. Added an `OrchestratorName` closed-set map to `frontend/src/models/admin.tsx` (mirrors the backend StrEnum), extended `FieldSpec` with a `select` kind plus `options`, and kept any off-list current value selectable so the widened `OrchestratorName | str` backend type never loses a configured key. The wire field stays a plain `string`. Updated and extended the Configuration Vitest suite; full frontend suite green (371 tests).
* Fixed the `BUG-0004` persistence follow-up: a saved orchestrator selection reported success but reverted to `langgraph` on reload. Root cause was in `frontend/src/api/admin.tsx` — `getAdminConfig()` read `GET /api/admin/config` (the env-only snapshot) instead of `GET /api/admin/config/effective` (the override-overlaid view), so the persisted `RuntimeConfig` never reached the form. Pointed `getAdminConfig()` at the effective endpoint and unwrapped its `values`, and added an `EffectiveAdminConfig` wire type to `frontend/src/models/admin.tsx` mirroring the backend model. The backend env-vs-effective split is intentional (backend tests pin `/api/admin/config` as env-only), so no backend change was made. Updated the `getAdminConfig` Vitest block to assert the effective endpoint and the unwrapped override value; full frontend suite green (371 tests).

## Bugs

All defects below are tracked for fixing one unit per turn. `BUG-0004` is fixed (below); the rest are open.

* [BUG-0003](../bugs.md#bug-0003--admin-documents-endpoint-returns-503-list_sources-facets-the-non-facetable-title-field): `GET /api/admin/documents` returns `503` even with search fully configured — `list_sources` facets the `title` field, which is not `facetable` in the index schema (`FieldNotFacetable`). Reproduced locally 2026-06-11; recorded root cause corrected.
* [BUG-0004](../bugs.md#bug-0004--orchestrator-field-is-free-text-instead-of-a-dropdown) — **fixed 2026-06-11** (incl. persistence follow-up): the admin Orchestrator field renders as a dropdown of the known orchestrator keys, and a saved selection now persists across reload — the admin client reads the override-overlaid effective config endpoint instead of the env-only snapshot.
* [BUG-0005](../bugs.md#bug-0005--admin-configuration-labels-show-internal-config-key-names): the admin Configuration labels show internal config-key names such as `(orchestrator_name)`.
* [BUG-0006](../bugs.md#bug-0006--content-safety-defaults-to-disabled): Content Safety defaults to disabled; it should default to enabled.
* [BUG-0007](../bugs.md#bug-0007--default-agent-instructions-do-not-carry-the-v1-default-prompt-text): the default agent instructions do not carry the vetted v1 default prompt text.
* [BUG-0008](../bugs.md#bug-0008--separate-prompt-editor-page-should-fold-into-configuration): the separate Prompt editor page should be removed and folded into the Configuration page, matching v1.
* [BUG-0009](../bugs.md#bug-0009--log-level-field-is-free-text-instead-of-a-dropdown): the admin Log level field is free text; it should be a dropdown of the known log levels.
* [BUG-0010](../bugs.md#bug-0010--numeric-config-fields-should-validate-numeric-entry): numeric config values should validate numeric entry; the admin Configuration page already enforces this, so the affected surface needs confirmation.
* [BUG-0011](../bugs.md#bug-0011--authored-prompt-is-not-rai-validated-and-can-supersede-the-system-prompt): an authored agent prompt is not RAI-validated and fully replaces the system instructions, so it can supersede the system guardrail prompt.
* [BUG-0012](../bugs.md#bug-0012--robot-avatar-and-thinking-panel-are-not-aligned): the assistant robot avatar and the Thinking panel are not aligned.
* [BUG-0013](../bugs.md#bug-0013--thinkingreasoning-feed-never-shows): the Thinking/reasoning feed never shows because the backend emits no `reasoning` SSE frames.
* [BUG-0014](../bugs.md#bug-0014--assistant-response-markdown-is-not-converted-to-html): assistant responses render as raw markdown instead of HTML, unlike v1.
* [BUG-0015](../bugs.md#bug-0015--citationreference-block-should-look-like-v1): the citation/reference block styling does not match v1.
* [BUG-0016](../bugs.md#bug-0016--inline-references-should-render-like-v1): inline `[docN]` references render as literal-text buttons; they should render like v1 (superscript links).
* [BUG-0017](../bugs.md#bug-0017--new-conversation-is-not-saved-to-history): starting a conversation does not persist it, so new conversations never appear in history.
* [BUG-0018](../bugs.md#bug-0018--chat-history-column-shows-the-backend-database-name): the chat history column shows the backend database name; it should not.
* [BUG-0019](../bugs.md#bug-0019--remove-the-new-chat-section-from-the-history-column): remove the New chat section and functionality from the chat history column.

## Decisions

* Post-answering is not a defect. Research confirmed v1 ships a full post-answering feature (a default prompt, a fact-checking validator, and an operator filter message), so the v2 post-answering trio matches v1 and stays as is.
* Bringing v1 prompts into v2 is scoped to the default prompt text only. The v1 per-business-unit assistant-type system (Default, Contract assistant, Employee assistant) is out of scope for v2.
* All six items were recorded in [bugs.md](../bugs.md) rather than split across the development plan debt queues, by request. No `BUG-####` pointer rows were added to the development plan this turn; they can be added later if a defect needs phase-audit visibility.
* `BUG-0003`'s recorded root cause was corrected after reproducing it locally against the live cloud. The `503` is not caused by an unset search endpoint — it fires with search fully configured, because `list_sources` requests a facet on the non-facetable `title` field. The original empty-endpoint hypothesis is a separate latent pass-through path.

## Next

* Continue fixing the defects one unit per turn, test-first, in numerical order with a user review between each. `BUG-0003` is deliberately last (by request).
* Next: `BUG-0005` — stop showing the internal config-key names (e.g. `(orchestrator_name)`) in the admin Configuration labels.
* `BUG-0003` (last): fix `list_sources` so it does not depend on faceting the non-facetable `title` field (facet a field declared `facetable`, or page documents and aggregate distinct sources client-side); optionally mark the source-grouping field `facetable` in the index schema. Separately, decide whether the `search_provider is None` pass-through should degrade gracefully instead of returning `503`.
