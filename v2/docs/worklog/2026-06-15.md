---
title: CWYD v2 — Worklog 2026-06-15
description: Daily worklog for CWYD v2 on 2026-06-15. Covers the BUG-0030 citation-enrichment close-out (by-key Azure AI Search lookup, the agent_framework orchestrator wire-in, and the null-url fix, all live-confirmed) and the frontend citation cluster — BUG-0016 (inline [docN] markers rendered as v1-style superscripts) and BUG-0015 (the reference block restyled to the v1 collapsible numbered references list). Also covers the citation UX v1-parity follow-up — BUG-0037 (answer disclaimer), BUG-0038 (reasoning section break), and BUG-0039 (clickable reference chips + right-side detail column).
author: CWYD Engineering
ms.date: 2026-06-15
topic: how-to
keywords: worklog, daily log, BUG-0030, BUG-0016, BUG-0015, citation enrichment, fallback b, get_document_by_key, BaseSearch, AzureSearch, by-key lookup, null url, enrich_kb_citations, agent_framework, foundry iq, citations, parseAnswer, remark-supersub, superscripts, CitationPanel, references block, MessageList, BUG-0037, BUG-0038, BUG-0039, disclaimer, CitationDetailPanel, activeCitation, reasoning break, v2, status
estimated_reading_time: 8
---

## Summary

Continuing **BUG-0030 fallback (b)** — the app-side enrichment that resolves an `agent_framework` KB citation's raw `mcp://searchindex/<key>` to a friendly `title` / `snippet`. Unit 1/2 (the pure citation-side `enrich_kb_citations` + its injected `fetch_document` seam) landed yesterday; see the closing section of [2026-06-14.md](2026-06-14.md). Today's work is **unit 2a** — the by-key read primitive the wire-in (unit 2b) needs, since the orchestrator holds a `BaseSearch` but had no by-key fetch method.

## Planned

* Add `BaseSearch.get_document_by_key(key)` as a non-abstract default that fails loud (mirrors `list_sources` / `merge_or_upload_documents`).
* Implement the `AzureSearch` override over the SDK `get_document(key=…)` with the Hard Rule #14 `try/except` contract: `ResourceNotFoundError` → soft `None`, any other `AzureError` → structured `logger.exception` + re-raise.
* Cover both with tests (concrete happy-path + arg assertions + not-found + error; base default `NotImplementedError`).
* Run the search-provider suites + `pyright --strict`; record here; report `git status` and stop (unit 2b is a separate turn).

## Done

* **Unit 2a — by-key read primitive (test-first).** Added `async def get_document_by_key(self, key: str) -> SearchResult | None` to [base.py](../../src/backend/core/providers/search/base.py) (non-abstract default that raises `NotImplementedError` naming the concrete class, so an un-overriding provider fails loud at the call site rather than silently skipping enrichment — same contract as `list_sources`) and the concrete override to [azure_search.py](../../src/backend/core/providers/search/azure_search.py). The override calls the SDK `client.get_document(key=key, selected_fields=["id", "content", "title", "url"])`, maps the row through the existing `_to_result`, and owns the Hard Rule #14 boundary: a missing key (`ResourceNotFoundError`) returns `None` (a soft miss — a re-ingested or deleted key degrades enrichment to the raw id, never crashes the answer), while any other `AzureError` is logged with the canonical structured `extra` (`operation="get_document_by_key"`, `provider="azure_search"`, `index_name=…`) and re-raised. `ResourceNotFoundError` is caught **before** `AzureError` (it is a subclass). A by-key fetch carries no relevance score, so the resulting `SearchResult.score` is `None`.
* **Why a provider method, not an inline SDK call in the orchestrator.** Keeps the Azure SDK `try/except` + resilience on the search provider (where every other Search SDK boundary lives) and keeps `citations.py` provider-free — the unit-1 `fetch_document` seam is satisfied by a thin closure over `search.get_document_by_key` in the wire-in, with no new SDK import on the citation-format path (ADR 0026 / Hard Rule #20 R2).
* **Tests.** Base default `NotImplementedError` (+ not-abstract guard) in [test_base.py](../../tests/backend/core/providers/search/test_base.py); concrete happy-path field mapping, `key` + `selected_fields` argument assertions, not-found → `None` (asserts nothing logged), and `AzureError` → log + re-raise in [test_azure_search.py](../../tests/backend/core/providers/search/test_azure_search.py).
* **Gates.** `tests/backend/core/providers/search/test_azure_search.py` + `test_base.py` **49 passed**; `pyright --strict` **0 / 0 / 0** on both `azure_search.py` and `base.py`; `get_errors` clean on all four touched files.

## Result

The by-key read primitive is in place and proven. BUG-0030 stays **open** — unit 2b (the `agent_framework.run` wire-in of `enrich_kb_citations`, building `fetch_document` from `get_document_by_key`) plus a fresh live A0 capture confirming friendly `title` / `snippet` on the served `Citation` are the remaining steps before it flips to fixed.

## Next

* **BUG-0030 unit 2b (next turn):** thread `enrich_kb_citations` into `agent_framework.run` right after `normalize_kb_citations`; capture the orchestrator's `BaseSearch` (currently swallowed by `**_extras`) and build the `fetch_document` closure; add an orchestrator test; only run enrichment when a search provider is present.
* **BUG-0030 close-out:** re-run the live A0 capture, then flip BUG-0030 to **fixed** in [bugs.md](../bugs.md) and update [citation-rendering-parity-plan.md](../citation-rendering-parity-plan.md).
* **BUG-0016 / BUG-0015 (cluster):** the FE superscript + references-block + right-panel work, after backend parity is green.

## Implementation — BUG-0030 fallback (b), unit 2b (orchestrator wire-in, 2026-06-15)

Wired the unit-1 `enrich_kb_citations` into the `agent_framework` orchestrator so a KB citation's raw `mcp://searchindex/<key>` id is resolved to a friendly `title` / `snippet` / `url` at response time, using the unit-2a by-key primitive.

### Done

* **Captured the injected search provider.** `AgentFrameworkOrchestrator.__init__` gained a keyword-only `search: BaseSearch | None = None` parameter (stored as `self._search`); previously the router's uniform `search=…` kwarg was absorbed by `**_extras`. The `**_extras` comment was corrected to drop `search` from the swallowed list. No closure was needed after all — `enrich_kb_citations` takes the callable directly, so the wire-in passes the bound method `search.get_document_by_key`, which already matches the `Callable[[str], Awaitable[SearchResult | None]]` seam.
* **Best-effort enrichment in `run()`.** Right after `normalize_kb_citations`, when a search provider is wired, `run()` calls `await enrich_kb_citations(citations, search.get_document_by_key)`. langgraph-shaped citations and unresolved keys pass through unchanged (handled inside `enrich_kb_citations`). The call is wrapped in `try/except AzureError` → `logger.warning(..., extra={"operation": "enrich_citations", "provider": "agent_framework", "agent_name": …})`: enrichment is cosmetic and the answer is already fully assembled, so a transient by-key lookup failure degrades to the normalized raw-id citations rather than failing the whole answer. The underlying SDK failure was already logged with a traceback at the `get_document_by_key` boundary (unit 2a), so the warning here avoids a duplicate stack trace while still satisfying the silent-except gate.
* **Local-variable collision fix.** `__init__` reused a local named `search` (`search = settings.search`, a `SearchSettings`) to capture the infra-pinned KB scalars. The new `search` parameter (`BaseSearch | None`) collided with it, producing 28 cascading `pyright --strict` errors (the local reassignment retyped `search`, so every `self._search_endpoint` / `self._kb_name` derived from it became `Unknown`). Renamed the local to `search_settings` — a minimal, self-contained rename that also reads more clearly (config object vs. provider).
* **Tests (test-first).** Added to [test_agent_framework.py](../../tests/backend/core/providers/orchestrators/test_agent_framework.py): `test_run_enriches_kb_citations_via_search_lookup` (a KB citation keyed `mcp://searchindex/abc123` → the bare key `abc123` drives `get_document_by_key`, and the resolved document backfills `title` / `snippet` / `url` while the id stays the normalized `[doc1]` and `metadata.source_id` keeps the raw id for traceability) and `test_run_keeps_raw_citations_when_enrichment_lookup_fails` (an `HttpResponseError` from the lookup degrades to the raw-id citation — answer + citation still stream, and the `enrich_citations` warning is logged, not swallowed). Threaded a `search` parameter through `_make_orchestrator`, imported `SearchResult`, added an `_AGENT_FW_LOGGER_NAME` constant, and corrected the `test_constructor_swallows_uniform_extras` docstring (`search` is now a named parameter, consumed not swallowed).

### Gates

* `tests/backend/core/providers/orchestrators/test_agent_framework.py` **38 passed** (36 existing + 2 new).
* `pyright --strict` on `agent_framework.py` **0 / 0 / 0** (after the `search_settings` rename).
* Shared v2 gates (`process_narrative`, `silent_except`, `imports_at_top`, `init_files`) **373 passed**; `get_errors` clean on both touched files.

### Result

The full fallback-(b) chain is now wired end-to-end in code and green under unit tests: annotation `file_id` → `citations_from_annotations` `source_id` → `normalize_kb_citations` `[docN]` renumber → `_kb_document_key` scheme-strip → `get_document_by_key` → friendly backfill. BUG-0030 stays **open** pending one remaining step — a fresh live A0 capture confirming the served `Citation` carries the friendly `title` / `snippet` against real Azure — after which it flips to **fixed** in [bugs.md](../bugs.md) and [citation-rendering-parity-plan.md](../citation-rendering-parity-plan.md) is updated.

## Close-out — BUG-0030 fixed (live enrichment confirmed + null-url fix, 2026-06-15)

Ran the remaining live A0 capture against the real `agent_framework` deployment (Foundry IQ KB + Azure AI Search, same in-domain query, in-process `create_app()` + lifespan boot, non-streaming `POST /api/conversation`).

### Live capture 1 — title/snippet confirmed, url defect found

The served citations now carry the **friendly** fields the enrichment backfills: `id="[doc1]"`, `title="Benefit_Options.pdf"`, `snippet=<chunk text>`, no native `【N:M†source】` markers, and `metadata["source_id"]` retaining the raw `mcp://searchindex/<key>`. Title + snippet recovery — the core BUG-0030 deliverable — is proven on live Azure.

One defect surfaced in the same capture: the served `url` was the **literal string `"None"`**. Root cause in [azure_search.py](../../src/backend/core/providers/search/azure_search.py) `_to_result`: `url=str(doc.get("url", ""))` — this sample document has a present-but-`null` `url` field, so `dict.get("url", "")` returns `None` (the `""` default only fires when the key is *absent*), and `str(None)` → `"None"`. The enrichment newly exercised this path (it backfills `citation.url` from the by-key document), and it is a **second** `str(None)`→`"None"` site, distinct from BUG-0033's `citations_from_annotations` annotation-boundary fix. The operator chose to fix it now (test-first) as the last piece of BUG-0030 rather than file it separately.

### Null-url fix (test-first)

* **Test first.** Added `test_get_document_by_key_maps_null_url_to_empty_string` to [test_azure_search.py](../../tests/backend/core/providers/search/test_azure_search.py): a by-key document whose `url` field is `None` must map to `SearchResult.url == ""`, never `"None"`. It failed against the current code (`assert 'None' == ''`), confirming the bug.
* **Fix.** Changed `_to_result` `url=str(doc.get("url", ""))` → `url=str(doc.get("url") or "")` (a present-but-`null` or empty `url` now coerces to `""`). One line; strictly an improvement; also cleans the `langgraph` retrieval path for url-less documents.

### Live capture 2 — url clean

Re-ran the live capture: the served citation is now `id="[doc1]"`, `title="Benefit_Options.pdf"`, `snippet=<chunk text>`, `url=""` (empty string, no longer `"None"`), no native markers, `source_id` intact. The full normalized v1 shape is confirmed end-to-end on real Azure.

### Gates

* `tests/backend/core/providers/search/test_azure_search.py` **34 passed** (33 + 1 new null-url test).
* `pyright --strict` on `azure_search.py` **0 / 0 / 0**.

### Result

**BUG-0030 is fixed.** Flipped to **fixed** (2026-06-15) in both the table row and the detail section of [bugs.md](../bugs.md), and [citation-rendering-parity-plan.md](../citation-rendering-parity-plan.md) is updated (the KB display-metadata recovery section moved from Open to Resolved). The throwaway live-capture script was deleted after the run (not tracked).

The `agent_framework` reasoning-feed live failure seen earlier in the integration run (`test_reasoning_feed_streams_substantive_model_reasoning[agent_framework]` — the feed emitted only the canned KB-search narration, not substantive model reasoning) is a separate reasoning-channel concern (BUG-0013 area), **out of scope** for the citation/references work and not folded into BUG-0030. Per the operator: reasoning doesn't need references.

### Next

* **BUG-0016 / BUG-0015 (FE cluster):** the frontend `[docN]` superscript + references-block + right-panel rendering, now unblocked by green backend `[docN]` + friendly-field parity. Both are structural (Hard Rule #10 sign-off before editing layout).

## BUG-0016 — inline `[docN]` markers render as v1-style superscripts

Picked up the FE citation cluster after BUG-0030 closed. Scoped BUG-0016 first (inline marker rendering); BUG-0015 (references block + right panel) follows.

### Structural sign-off (Hard Rule #10)

Confirmed two decisions with the operator before touching layout:

* **Add the `remark-supersub` dependency** + a new `parseAnswer.tsx` helper (approved).
* **Superscripts are visual-only / v1-exact** — non-clickable; the references block (BUG-0015) is the click target. (Overrides the earlier bugs.md "keeping click-to-focus" hint.)

### What shipped (one unit + consuming edits)

* **Dependency.** Added `remark-supersub@^1.0.0` to `src/frontend/package.json` (the same version v1 pairs with `react-markdown@^10`) and ran `npm install` at the v2 workspaces root. The package ships its own types (`lib/index.d.ts`) and a modern `"exports"` entry, so **no ambient `.d.ts` shim was needed** — Vite, Vitest, and strict `tsc` all resolve it.
* **Unit: `parseAnswer.tsx`** (Pillar: Stable Core; Phase 6 visual polish). Pure transform: rewrites each in-bounds `[docN]` marker into a ` ^K^ ` superscript token, dedupes repeated sources by `citation.id`, renumbers them `1..K` in first-appearance order, collapses consecutive duplicate superscripts, and leaves out-of-bounds / `[doc0]` markers verbatim. Returns `{ markdownText, citations }` where `citations` is the deduped/renumbered referenced subset (that subset feeds BUG-0015's numbered block later).
* **`MarkdownContent` `enableSupersub` prop.** Opt-in; when set, adds `remark-supersub` to the remark pipeline so ` ^K^ ` renders as `<sup>`. Still no `rehype-raw`, so raw HTML stays escaped (XSS-safe). The reasoning panel leaves it off so stray `^`/`~` in chain-of-thought stays literal.
* **`MessageList` wiring (W2).** The assistant answer body now renders `parseAnswer(m.content, m.citations).markdownText` with `enableSupersub`. The `CitationPanel` deliberately stays on the unmodified `m.citations` this turn (no regression if an answer has no inline markers); rewiring the panel to the renumbered subset is BUG-0015. User messages and citation-less answers keep `[docN]` verbatim.

### Tests

* **`parseAnswer.test.tsx`** — 13 cases: undefined/empty passthrough, no-markers passthrough, single marker, multiple distinct in order, dedupe, out-of-order renumber, out-of-bounds `[doc9]`/`[doc0]` verbatim, consecutive-duplicate collapse, distinct-adjacent not collapsed, referenced-subset, mixed in/out-of-bounds.
* **`MarkdownContent.test.tsx`** — +2 cases: ` ^K^ ` → `<sup>` when `enableSupersub` set; literal `^K^` when not.
* **`MessageList.test.tsx`** — updated the one BUG-0014-era test that asserted `[docN]` stays literal with citations; it now asserts no literal `[docN]`, no clickable token buttons, and `<sup>` numbers `1`/`2`. The user-message and no-citations tests still assert literal `[docN]` (unchanged — confirming the passthrough paths end-to-end).

### Gates

* Full FE suite **417/417** (34 files) — 402 prior + 13 `parseAnswer` + 2 `MarkdownContent`.
* `tsc --noEmit` clean on **both** FE workspaces (`src/frontend`, `src/tests/frontend`).
* `get_errors` clean on all six touched files.

### Result

**BUG-0016 is fixed.** Flipped to **fixed** (2026-06-15) in the table row and detail of [bugs.md](../bugs.md). The now-unused `renderAnswerTokens` / `answerTokens.tsx` (+ its test) are dead production code (only self-referenced); left in place pending a separate removal decision rather than deleted unprompted.

### Next

* **BUG-0015** — references block under the answer + right-side citation detail panel, v1-style. Structural (Hard Rule #10 sign-off + layout decisions still needed before editing).

## BUG-0015 — references block restyled to match v1

Closed the FE citation cluster: BUG-0016 made the inline markers superscripts; BUG-0015 restyles the block they point at.

### Structural sign-off (Hard Rule #10) and scope

Confirmed scope with the operator before editing:

* **Reference-block restyle only** — the v1 right-side citation **detail column** (a third layout column) is **out of scope** this turn; `ChatPage` stays two-column and no `activeCitation` state is added. Full right-panel parity, if wanted later, is a separate structural ask.
* **Chip labels are title-only** — no "Part N" suffix (the v2 `Citation` has no first-class chunk-part field).
* **XSS-safe** — no `rehype-raw`; the snippet keeps rendering through the escaped path.

No new files, packages, or top-level folders — the edit is confined to the existing `CitationPanel` component, so beyond the scope confirmation no further structural sign-off was needed.

### What shipped (one unit + consuming edits)

* **Unit: `CitationPanel` rework** (Pillar: Stable Core; Phase 7). Replaced the "Sources" accordion heading with a v1 collapsible toggle: a borderless button labeled `N references` (or `1 reference` at one) with a leading chevron (`ChevronRight16Regular` collapsed, `ChevronDown16Regular` expanded), `aria-expanded` + `aria-controls`, collapsed by default. The toggle reveals a numbered citation list — each row leads with a circular brand-colored number chip (`1`, `2`, …) followed by the citation title, falling back to `Citation N` when the title is empty. The per-item score badge was dropped. Items still expand individually to the snippet and an `Open source` link (`target=_blank` + `rel=noopener noreferrer`). The inner list is wrapped in a `hidden={!expanded}` container (mounted-but-hidden, **not** conditionally unmounted) so the inner citation testids stay in the jsdom DOM and the existing `MessageList` citation assertions keep resolving; a focused citation auto-opens the toggle and additively expands the matching item. The section `aria-label` moved from "Sources" to "References".
* **CSS restyle.** Lightened `.section` (dropped the panel border / background / padding, kept the flex column). Replaced `.heading` with `.toggle` / `.toggleLabel` (a borderless brand-link button) and `.score` with `.number` (the circular brand chip). Kept `.headerRow`, `.title`, `.snippet`, `.link`.
* **`MessageList` wiring.** The map body now computes `parseAnswer(m.content, m.citations)` once and derives `referencedCitations = parsed.citations.length > 0 ? parsed.citations : (m.citations ?? [])`. The answer body renders `parsed.markdownText` (unchanged behavior) and `CitationPanel` now receives `referencedCitations` (guarded on its length), so the numbered chips line up exactly with the BUG-0016 answer superscripts and only cited sources appear; an answer with no inline markers falls back to the full `m.citations` (no regression).

### Tests

* **`CitationPanel.test.tsx`** — rewritten (17 cases): empty → renders nothing; section `aria-label` "References"; toggle label `2 references` / singular `1 reference`; `aria-expanded` toggling; numbered chips (`1` / Source A, `2` / Source B); `Citation 1` title fallback; **no** score badge; snippet + safe link on item expand; empty-field item; per-item and multi-item expand / collapse; `window.open` not called (safe `target` / `rel`); and the `focusedCitationId` wiring (resolves → toggle opens + matching item expands; null / unknown id → stays collapsed; user-opened items preserved; user can collapse a focus-expanded block).
* **`MessageList.test.tsx`** — added a coherence test: content `first [doc2] then [doc1]` with citations `[alpha, beta]` → opening the toggle shows chip `1` = Beta and chip `2` = Alpha, matching the renumbered answer superscripts (`["1", "2"]`). The existing citation-rendering and superscript tests pass unchanged (the mounted-but-hidden inner list keeps their testids resolvable).

### Gates

* Targeted `CitationPanel.test.tsx` + `MessageList.test.tsx` — **46 passed**.
* Full FE suite **420/420** (34 files) — 417 prior + 1 coherence test + the net `CitationPanel` rewrite.
* `tsc --noEmit` clean on **both** FE workspaces (`src/frontend`, `src/tests/frontend`).
* `get_errors` clean on the four touched source / test files.

### Result

**BUG-0015 is fixed.** Flipped to **fixed** (2026-06-15) in the table row and detail of [bugs.md](../bugs.md). The reference block now matches v1: a collapsed `N references` toggle that expands to a numbered, click-to-expand citation list whose numbers stay coherent with the inline superscripts. The right-side citation **detail panel** remains out of scope (operator decision), and the now-dead `renderAnswerTokens` / `answerTokens.tsx` (from BUG-0016) are still left in place pending a separate removal decision.

## Citation UX v1-parity follow-up — BUG-0037 / 0038 / 0039 (2026-06-15)

The operator asked for four answer-area UX changes to close the gap with v1 (two screenshots provided): (1) an "AI-generated content may be incorrect" disclaimer under the answer; (2) remove the extra blank line between reasoning sections; (3) references rendered as expandable v1-style chips; (4) clicking a reference opens a right-side column with the document title, an open-document link, and the markdown-rendered reference text. Filed as BUG-0037 (disclaimer), BUG-0038 (reasoning break), and BUG-0039 (chips + right-side detail column) in [bugs.md](../bugs.md).

### Plan (one unit per turn, test-first)

* **U1 — BUG-0037:** disclaimer line in `MessageList` (this turn).
* **U2 — BUG-0038:** single line break in `formatReasoning`.
* **U3:** `activeCitation` state + `ShowCitation` / `CloseCitation` actions.
* **U4:** `formatCitationLabel` chip-label helper.
* **U5:** `CitationPanel` clickable chips dispatching `ShowCitation`; `MessageList` wires the callback.
* **U6:** new `CitationDetailPanel` (title + open-document link + markdown reference text).
* **U7:** `ChatPage` third push-content grid column for the detail panel (operator signed off on a push-content layout, v1 parity — Hard Rule #10).
* **U8 (optional, ask-first):** remove the now-dead `focusedCitationId` / `FocusCitation` / `answerTokens.tsx`.

### Done — U1 (disclaimer, BUG-0037)

* `MessageList` renders a static muted `<p data-testid="answer-disclaimer-<id>">AI-generated content may be incorrect</p>` between the answer body and the `CitationPanel`, gated on `m.streaming !== true && m.content.length > 0` (finished, non-empty answers only — never while streaming, on user messages, or on empty error-only messages). New `.disclaimer` style (small, `colorNeutralForeground3`) mirrors the v1 answer footer; the component docstring notes the static disclaimer.
* **Tests:** four new `MessageList.test.tsx` cases — disclaimer present under a finished assistant answer; absent on a user message; absent while streaming; absent on an empty error-only message.

### Gates — U1

* `MessageList.test.tsx` **33 passed** (29 prior + 4 new).
* `get_errors` clean on `MessageList.tsx`, `MessageList.module.css`, `MessageList.test.tsx`.
* BUG-0037 flipped to **fixed** in [bugs.md](../bugs.md). The full FE suite + `tsc --noEmit` run once at the end of the cluster.

### Next

* **U2 — BUG-0038** reasoning section break in `formatReasoning`.

### Done — U3 (citation state)

* `ChatState` gains `activeCitation: Citation | null` (models/chat.tsx); `ChatContext` adds `ShowCitation` (sets the active citation) and `CloseCitation` (clears it) actions + reducer cases; `initialChatState.activeCitation = null`. The dead `focusedCitationId` / `FocusCitation` wiring is left in place for a deferred ask-first cleanup (U8).
* **Tests:** `ChatContext.test.tsx` **36 passed** (29 prior + 7 new — `ShowCitation` sets, `CloseCitation` clears + is a no-op when already null, other actions preserve `activeCitation`).

### Done — U4 (chip-label helper)

* `formatCitationLabel` (pure fn, co-located in `CitationPanel`) middle-truncates a long citation label to `head20…tail20` when it exceeds 50 chars, returning it verbatim otherwise — keeps the `.pdf - Part N` tail visible.
* **Tests:** `formatCitationLabel.test.tsx` **6 passed**.

### Done — U5 (clickable reference chips)

* `CitationPanel` replaces the inline `<Accordion>` with a collapsed-by-default "N references" toggle over a list of clickable chips (`.chip` = number box + `formatCitationLabel(title)`), each a `<button>` that calls `onSelectCitation(citation)`. The `focusedCitationId` prop + accordion auto-open are removed. `MessageList` wires `onSelectCitation={(c) => dispatch({ type: ShowCitation, citation: c })}`. New v1-style chip styles (bordered, rounded, brand-blue label + circular number chip).
* **Tests:** `CitationPanel.test.tsx` **11 passed** (chips render labels/numbers, click fires `onSelectCitation` with the citation, toggle expand/collapse, testids); `MessageList.test.tsx` **33 passed** (chip click dispatches `ShowCitation`).

### Done — U6 (CitationDetailPanel)

* New `CitationDetailPanel/CitationDetailPanel.tsx` (+ `.module.css`, Pillar: Stable Core): reads `useChat()`, returns `null` when `activeCitation` is null, otherwise renders an `<aside>` with a "Citations" header + dismiss control (dispatches `CloseCitation`), the citation title, an "Open document" link (`target="_blank" rel="noopener noreferrer"`, only when `url` is non-empty), and the reference text via `MarkdownContent` (remark-gfm, **no rehype-raw** — HTML stays escaped / XSS-safe).
* **Tests:** `CitationDetailPanel.test.tsx` **6 passed** (renders nothing without an active citation; title/link/markdown body render; dismiss dispatches `CloseCitation`).

### Done — U7 (ChatPage push-content column)

* `ChatPage` is split into the `ChatProvider` boundary + an inner context-reading `ChatShell` (so it can read `activeCitation`). `ChatShell` sets `data-citation-open` and renders `<CitationDetailPanel/>` in a new third grid column. CSS: `grid-template-columns: var(--history-col, 0) 1fr var(--citation-col, 0)`; `[data-citation-open="true"] { --citation-col: minmax(320px, 30%) }`; the column is `display: none` when closed. The chat `.main` cell is `1fr`, so opening the column **narrows** the conversation (push layout, not overlay) — the v1 parity the operator signed off on (Hard Rule #10).
* **Tests:** `ChatPage.test.tsx` **6 passed** (3 prior + 3 new — column closed + panel absent by default; clicking a streamed reference chip flips `data-citation-open="true"` and renders the detail panel + title while the transcript stays mounted; the dismiss control closes it).

### Done — U2 (reasoning section break, BUG-0038)

* `formatReasoning` replaces each model section title with a single `"\n"` line break (was `"\n\n"`) and collapses `\n{2,}` → `"\n"` (was `\n{3,}` → `"\n\n"`), so adjacent reasoning bodies render on consecutive lines within one `<p>` (soft break shown via the panel's `white-space: pre-wrap`) with no blank line between sections.
* **Tests:** `reasoningText.test.tsx` **7 passed** (multi-section expectations now `\n`); `MessageList.test.tsx` reasoning case updated to assert one `<p>` with a softbreak instead of two paragraphs.

### Gates — cluster (U1–U7 + U2)

* Full frontend suite **440 passed** across 36 files (from `v2/src/tests/frontend`).
* `tsc --noEmit` **exit 0** in both `v2/src/frontend` and `v2/src/tests/frontend`.
* `get_errors` clean on every touched source + test + CSS file.
* BUG-0037, BUG-0038, BUG-0039 all flipped to **fixed** in [bugs.md](../bugs.md).

### Next

* Live manual verification on `http://localhost:5273/` (services already running): disclaimer shows under a finished answer; reasoning panel has no blank line between sections; references expand to bordered chips; clicking a chip opens the right column (chat narrows) with title + open-document link + markdown reference text; dismiss closes it.
* **U8 (ask-first):** remove the now-dead `focusedCitationId` / `FocusCitation` and the unused `answerTokens.tsx` / `renderAnswerTokens` (+ its test + `.module.css`).

## Citation block follow-up defects — BUG-0040 / 0041 / 0042 (2026-06-15)

Live use of the shipped citation UI surfaced three follow-up defects (operator-reported, with a screenshot): (1) the "N references" toggle does not collapse the chip list — the references always show; (2) the reference chips render side by side on one line instead of stacked; (3) expanding a reference shows no link to open the document. Filed as BUG-0040 (toggle/collapse), BUG-0041 (chip stacking), and BUG-0042 (missing document link) in [bugs.md](../bugs.md).

### Root causes (confirmed via reads + two read-only research passes)

* **BUG-0040** — `CitationPanel.module.css` sets `.chipList { display: flex }`, which overrides the element's `hidden` attribute (the UA `[hidden] { display: none }` rule has the lowest priority). `hidden={!expanded}` is set correctly but ignored visually. The existing `CitationPanel.test.tsx` asserts the `hidden` attribute (still correct) and jsdom does not apply CSS-module styles, so the test could not catch it.
* **BUG-0041** — `.chipList { flex-wrap: wrap }` lays the chips out as a wrapping row; v1's reference list is a column.
* **BUG-0042** — v2 ingestion intentionally omits a `url` field from the search index. `SearchDocument` writes only `id` / `content` / `title` / `content_vector`; the blob filename survives in `title` (`title = chunk.source`). The index `url` column is never populated, so `SearchResult.url` → `Citation.url` is always `""` and the detail panel's open-document link (gated on a non-empty `url`) never renders. The backend has no file-serving endpoint. v1 served documents through a Flask `/api/files/<filename>` proxy streaming the blob from the documents container.

### Cloud architecture note — the link needs `VITE_BACKEND_URL`

Confirmed in [main.bicep](../../infra/main.bicep), [Dockerfile.frontend](../../docker/Dockerfile.frontend), [azure.yaml](../../azure.yaml), and [frontend_app.py](../../src/frontend/frontend_app.py): in the cloud the frontend is an App Service running `frontend_app.py` — a static-only catch-all (serves a `dist/` file or `index.html`) with **no `/api` proxy** — on a **separate origin** from the backend Container App. `VITE_BACKEND_URL` is baked into the Vite bundle at build time (`azure.yaml buildArgs: VITE_BACKEND_URL=${AZURE_BACKEND_URL}`). The "Open document" link is a new-tab navigation (not a `fetch`), so a relative URL would resolve against the frontend origin and return `index.html` instead of the file. The link must therefore use the absolute `${VITE_BACKEND_URL}/api/files/<filename>` — the same pattern `App.tsx` and `HistoryPanel.tsx` already use. (Top-level navigations are not subject to CORS, so cross-origin inline rendering works.)

### Plan (one unit per turn, test-first)

* **U1 — BUG-0040 + BUG-0041:** `CitationPanel.module.css` — add `.chipList[hidden] { display: none }` (collapse) and switch `.chipList` to `flex-direction: column` (stack). CSS-only; verified live (jsdom cannot assert computed layout; existing tests stay green).
* **U2 — BUG-0042:** backend blob-download service helper (`download_document(filename)` → bytes + content type) over the existing storage helpers, with path-traversal / control-char / length guards and the Hard Rule #14 `AzureError` contract.
* **U3 — BUG-0042:** backend `GET /api/files/{filename}` router (new `routers/files.py`, registered on the app) returning the blob with content type from the filename + `Content-Disposition: inline`; 400 on traversal / double-encoding, 404 on missing, 502 on SDK error. Structural (new router) — operator approved Option A (full backend file serving).
* **U4 — BUG-0042:** `CitationDetailPanel` derives the open-document href — `${VITE_BACKEND_URL}/api/files/<filename>` from `title` when `url` is empty, blob-URL rewrite when `url` is a blob URL, passthrough for external URLs.

### Pre-existing observation (out of scope)

`streamChat.tsx`, `speech.tsx`, and `admin.tsx` use bare relative `/api/...` URLs (no `VITE_BACKEND_URL`); by the same cross-origin logic those would fail in the cloud and only work in dev via the Vite proxy, unlike `App.tsx` / `HistoryPanel.tsx`. This is a latent pre-existing inconsistency, not part of this task — flagged to the operator for a separate bug.

### Done — U1 (CSS collapse + stack, BUG-0040 + BUG-0041)

* [CitationPanel.module.css](../../src/frontend/src/pages/chat/components/CitationPanel/CitationPanel.module.css) — `.chipList` switched from a wrapping row (`flex-wrap: wrap`) to a content-width column (`flex-direction: column; align-items: flex-start`), and a `.chipList[hidden] { display: none }` rule was added so the `hidden={!expanded}` attribute actually collapses the list (the UA `[hidden]` rule otherwise loses to the explicit `display: flex`). CSS-only; jsdom does not apply CSS-module styles, so this is a documented test-first carve-out (the existing tests still assert the `hidden` attribute and stay green).
* **Gate:** `CitationPanel.test.tsx` **11 passed** (no regression).

### Done — U2 (blob-download service, BUG-0042)

* New [services/files.py](../../src/backend/services/files.py) — `async download_document(filename, *, settings, credential) -> bytes`. Validates the filename first (`_validate_filename` rejects empty, `> 255` chars, any `/` or `\`, any `..`, and control characters — defence-in-depth even though Azure blob is a flat key space), resolves the blob endpoint via the existing `resolve_storage_endpoints(settings.storage)`, opens a `ContainerClient` against `settings.storage.documents_container`, and reads the blob. Owns the Hard Rule #14 boundary: `ResourceNotFoundError` → `FileNotFoundError` (no error log — an expected 404), any other `AzureError` → structured `logger.exception(extra={"operation": "download_document", "container": …, "blob_filename": …})` + re-raise. Constructs the `ContainerClient` directly rather than reusing `storage_clients` (which also builds a queue client and would require ingestion-queue config the read path does not need).
* **Tests:** new [test_services_files.py](../../tests/backend/test_services_files.py) **13 passed** — returns bytes; constructs against the resolved endpoint / container / credential; missing blob → `FileNotFoundError` with nothing logged; other `AzureError` → logged + re-raised (asserts the structured `extra`); parametrised invalid filenames (traversal, separators, control chars, overlong, `..`) → `ValueError` before any SDK call; a valid filename with spaces is accepted.

### Done — U3 (`GET /api/files/{filename}` router, BUG-0042)

* New [routers/files.py](../../src/backend/routers/files.py) — `GET /api/files/{filename}` returns the blob bytes with `Content-Type` guessed from the extension (fallback `application/octet-stream`) and `Content-Disposition: inline` so the browser renders the document in a new tab. Status surface: `400` on a malformed filename (`ValueError` from the service), `404` on a missing blob (`FileNotFoundError`), `503` when the deployment has no `documents_container` configured (the route stays mounted so the gap is explicit). `AzureError` is **not** caught — it propagates to the app-level handler ([exception_handlers.py](../../src/backend/exception_handlers.py)) which sanitises it to `503` with no SDK detail leaked. Registered in [app.py](../../src/backend/app.py) (import + `include_router`, before `install_exception_handlers`). The disallowed module-level MIME constant was inlined and the unused `logger` removed to satisfy the router-only-routes gate.
* **Tests:** new [routers/test_files.py](../../tests/backend/routers/test_files.py) **8 passed** — `200` streams bytes + `application/pdf` content type + `inline` disposition; `image/png` guess; unknown extension → `application/octet-stream`; `%20` decoded to a space before the service call; `400` on `ValueError`; `404` on `FileNotFoundError`; `503` without calling the service when storage is unconfigured; `AzureError` → `503` via the installed app handler.

### Done — U4 (frontend open-document link, BUG-0042)

* New [documentHref.tsx](../../src/frontend/src/pages/chat/components/CitationDetailPanel/documentHref.tsx) — `deriveDocumentHref(citation) -> string | null`. A raw blob-storage URL in `citation.url` is rewritten to `${VITE_BACKEND_URL}/api/files/<lastPathSegment>` (the browser holds no SAS token for the raw blob); any other http(s) `url` passes through; with an empty `url`, an http(s) `title` is the external link and a plain `title` becomes `${VITE_BACKEND_URL}/api/files/<title>` (v2 ingestion records the blob name in `title`, not `url`); nothing usable → `null`. The backend file route is absolute because the link is a top-level navigation across separate origins in the deployed topology. The env is read lazily inside the helper (identical to the build-time-inlined value in production) so the `VITE_BACKEND_URL` case is unit-testable.
* [CitationDetailPanel.tsx](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.tsx) now renders the "Open document" link gated on `deriveDocumentHref(citation) !== null` instead of the raw `citation.url`, so a blob-backed citation (empty `url`, blob name in `title`) now links correctly.
* **Tests:** new [documentHref.test.tsx](../../src/tests/frontend/pages/chat/components/CitationDetailPanel/documentHref.test.tsx) **7 passed** (blob name → `/api/files/<name>`, space encoding, blob-URL rewrite, external-URL passthrough, http-title passthrough, both-empty → null, `VITE_BACKEND_URL` prefix). [CitationDetailPanel.test.tsx](../../src/tests/frontend/pages/chat/components/CitationDetailPanel/CitationDetailPanel.test.tsx) updated: a title-only citation now derives a backend file link, and only a fully-empty citation omits the link.

### Gates — cluster (U1–U4)

* Full frontend suite **448 passed** across 37 files (from `v2/src/tests/frontend`).
* `tsc --noEmit` **exit 0** in both `v2/src/frontend` and `v2/src/tests/frontend`.
* Backend: `test_services_files.py` + `routers/test_files.py` **21 passed**; the shared discipline gates (router-only-routes, process-narrative, imports-at-top, pillar-header, no-anonymous-dict, init-marker) and `test_app_lifespan.py` (**23 passed**, confirming `create_app()` still constructs with the new router) all green.
* `get_errors` clean on every touched source + test + CSS file.

### Live verification — done

The first probe confirmed the running local `uvicorn` predated the new router (started without `--reload`, same as the BUG-0003 episode): `GET /api/files/Benefit_Options.pdf` returned Starlette's default `{"detail":"Not Found"}` (22-byte unmatched-route 404) while `/api/health` was `200`. After stopping that stale process (PID 10704) and relaunching from `v2/` with `uv run --env-file .env uvicorn backend.app:app --app-dir src --host 127.0.0.1 --port 8000 --reload`, the route serves correctly:

* `GET /api/files/Benefit_Options.pdf` → `200`, `Content-Type: application/pdf`, `Content-Disposition: inline; filename="Benefit_Options.pdf"`, `content-length: 544811` (real PDF).
* `/api/files/{filename}` present in the live `openapi.json`.
* Browser confirmation on `http://localhost:5273/`: the `N references` toggle collapses the list; chips stack vertically one per line; expanding a reference shows an "Open document" link that opens the PDF inline in a new tab.

BUG-0040 / 0041 / 0042 flipped to **fixed** (found + fixed 2026-06-15) in [bugs.md](../bugs.md).

### BUG-0043 filed — raw KB citation markers leak into the reasoning panel

Reported from live use: the reasoning/thinking panel renders raw native citation markers of the form `【message_idx:search_idx†source_name】` (e.g. `【6:1†source】`). Root cause traced to the `agent_framework` orchestrator: `normalize_kb_citations` runs only over the **answer** string, while `_update_to_events` forwards `text_reasoning` blocks verbatim to the `reasoning` channel, so a reasoning-capable model that cites KB sources in its summary leaks the answer-only `【N:M†source】` markers into the panel (no `[docN]` rendering there). Filed as **BUG-0043** (backend, low, open) with a planned fix: a `strip_kb_markers` helper in `tools/citations.py` reusing the existing `_KB_MARKER_RE`, applied to reasoning text before the `reasoning` event is emitted. Not yet implemented.

### Next

* **U8 (ask-first, carried from the prior cluster):** remove the now-dead `focusedCitationId` / `FocusCitation` and the unused `answerTokens.tsx` / `renderAnswerTokens`.

## BUG-0017 — persist conversations to chat history

`POST /api/conversation` never writes to chat history: the route holds an unused `db` dependency, injects no `user_id`, and the frontend tracks no `conversationId`. So nothing is stored, history is empty, and follow-ups cannot continue a thread. The DB providers, the `/api/history` CRUD routes, and newest-first ordering all already exist and are tested — the defect is purely the missing wire-up. Operator spec: save server-side after the LLM answer is assembled and before the response returns; a new conversation is an insert titled with the user's first question; a follow-up appends only the latest turn; a RAI/content-safety block stores nothing.

### Approved plan (one unit per turn, test-first; 4 phases / 15 units)

* **Phase 1 — backend persistence (works locally against `local-dev`):** U1 `persist_turn` service helper (create-if-new titled with the question; append user then assistant); U2a persisting SSE wrapper (tees the event stream, skips persistence on a blocking ERROR, emits a terminal `conversation` control frame carrying the new id); U2b wire the conversation route (inject `user_id: UserIdDep`; wrapper for SSE; set `conversation_id` for buffered); U3 (structural) persist citations — `metadata JSONB` column on the Postgres `messages` table, `add_message` / `list_messages` carry metadata, Cosmos message doc carries metadata.
* **Phase 2 — frontend tracking + load + UX:** U4 `ChatContext` `conversationId` + `SetConversationId` + `LoadConversation` (hydrate messages + citations) + `Reset` clears; U5 `streamChat` sends `conversation_id`, parses the terminal `conversation` event, fixes the URL to `VITE_BACKEND_URL`; U6 `MessageInput` passes `conversationId` + dispatches `SetConversationId`; U7 `HistoryPanel` load-on-click → `GET /conversations/{id}` → hydrate, `ChatPage` consumes `selectedId`; U8 delete confirm names the conversation; U9 New-chat clears + `conversationId=null` (BUG-0019); U10 hide raw db name in history status (BUG-0018).
* **Phase 3 — rename validation + RAI:** U11 `PATCH /conversations/{id}` rejects empty/blank → 422 + RAI-screens the new title; U12 `HistoryPanel` rename non-empty guard + surfaces RAI rejection.
* **Phase 4 — cloud identity ("test the identity manager"):** U13 frontend `getUserInfo()` (`/.auth/me`) + a shared api helper forwarding `x-ms-client-principal-id` on every backend call (the v1 `getUserInfo` + MACAE header pattern, not MSAL/JWT); U14 infra — Easy Auth (AAD) on the frontend App Service, `AZURE_ENVIRONMENT=production` on the backend, backend CORS `allow_origins` includes the frontend origin; U15 verify two signed-in users get distinct histories.

### Decisions (operator-confirmed)

* **Identity** = frontend reads `/.auth/me` → `user_id`, forwards `x-ms-client-principal-id` (backend `get_user_id` already reads it). Locally there is no Easy Auth, so the backend falls back to `local-dev` and Phase 1 is fully testable without cloud identity.
* **Ownership** = backend, inside the conversation route, after the answer is assembled, before returning.
* Persist **only the latest turn** (not the whole thread the frontend sends); **title = the first question verbatim**; list ordering is already correct (both providers `ORDER BY updated_at DESC` and `add_message` bumps the parent).
* The new-conversation id returns to an SSE client via a **terminal `conversation` control frame** at the SSE-shaping layer — not a sixth `OrchestratorChannel` (Hard Rule #6 locks the reasoning set) and not a response header (the conversation is created post-answer). The buffered path uses the existing `ConversationResponse.conversation_id`.
* Fold in **BUG-0018** (hide db name) + **BUG-0019** (repurpose New chat). BUG-0017 flips to fixed when Phase 1 + 2 land; BUG-0018 / 0019 when U10 / U9 land.

### Done — U1 (`persist_turn` service helper, test-first)

* Added `async def persist_turn(db, *, user_id, conversation_id, question, answer) -> str` to [conversation.py](../../src/backend/services/conversation.py). When `conversation_id` is `None` — or names a conversation `get_conversation` cannot resolve for `user_id` (missing / not owned / stale / forged) — it creates a fresh conversation titled with `question`; otherwise it reuses the named conversation and leaves its title unchanged. It then appends the user message before the assistant message (so `list_messages`, oldest-first, replays the turn in order, and the `updated_at` bump floats the conversation to the top of the newest-first list) and returns the resolved id. Only the latest turn is written, so a follow-up appends exactly two rows. No SDK `try/except` here — the provider methods own their Azure boundary (Hard Rule #14), and the caller (U2a) owns the post-stream failure handling so a persistence error never retracts an answer already delivered. Extended the import block (`BaseDatabaseClient`, `ChatMessage`, `ChatRole`) and `__all__`.
* **Tests:** added a `_FakeDB` recorder (implements only `get_conversation` / `create_conversation` / `add_message`, `cast` to `BaseDatabaseClient` per the file's existing `cast` style) and three cases to [test_services_conversation.py](../../tests/backend/test_services_conversation.py) — new conversation titled with the question + user-then-assistant role/content/order asserted; follow-up reuses the existing id with no create and leaves the title untouched; an unresolved (stale/forged) `conversation_id` falls back to creating a new conversation.

### Gates — U1

* `tests/backend/test_services_conversation.py` **17 passed** (14 prior + 3 new `persist_turn` cases; no regression in the `collect_response` / `build_post_prompt_validator` suites).
* `get_errors` clean on both touched files.

### Done — U2a (persisting SSE wrapper + terminal control frame, test-first)

* Added `async def persisting_sse_stream(events, request, *, db, user_id, conversation_id, question) -> AsyncIterator[bytes]` to [conversation.py](../../src/backend/services/conversation.py) — the streaming counterpart to `collect_response`. It pumps every orchestrator event to the client as an SSE frame (delegating wire framing to `format_sse`, imported from [sse.py](../../src/backend/services/sse.py) so the generic SSE helper stays free of any database concern), while observing the stream to accumulate the assistant answer and notice a terminal `error` event. When the stream completes with a non-empty answer and no error, it hands the turn to `persist_turn` and appends a terminal `conversation` control frame carrying the resolved id.
* **`conversation` is a transport control frame, not a 6th channel.** The reasoning channel set is locked (Hard Rule #6) and the id exists only after the answer is fully streamed + persisted, so it can ride neither a response header (flushed) nor an `OrchestratorChannel`. The frame is `event: conversation\ndata: {"conversation_id": "..."}\n\n`, emitted at the SSE-shaping layer via a lone `_CONVERSATION_EVENT` module constant. The frontend adds a `conversation` event-type listener in U5.
* **Skip conditions.** Nothing is persisted when (a) the client disconnects mid-stream (partial answer — early `return`), (b) any `error` event is seen (blocked / failed turn), or (c) the accumulated answer is empty. An orchestrator exception is converted to a final `error` frame (mirrors `sse_stream`) and skips persistence. A `persist_turn` failure is logged (`logger.exception`, structured `extra`) and swallowed — the answer was already delivered, so a storage outage must not retract it by tearing the stream (honors the `persist_turn` caller contract; not a silent swallow, so the no-silent-excepts gate passes).
* **Tests:** added `_FakeRequest` (configurable `is_disconnected`) and `_FailingDB` (writes raise) plus six cases to [test_services_conversation.py](../../tests/backend/test_services_conversation.py) — persists + titles a new conversation and emits the `conversation` frame; appends to an existing conversation (no create, id echoed); skips persistence on an `error` event; skips on disconnect (zero frames); skips on an empty answer; swallows a persistence failure (answer frame still delivered, no `conversation` frame, no propagation).

### Gates — U2a

* `tests/backend/test_services_conversation.py` **23 passed** (17 prior + 6 new `persisting_sse_stream` cases).
* Shared AST gates **460 passed, 1 skipped** (`test_no_silent_excepts`, `test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_no_anonymous_dict_returns`).
* `get_errors` clean on both touched files.

### Next

* **U2b (next turn):** wire the conversation route — inject `user_id: UserIdDep`, extract the latest user question from `body.messages`, use `persisting_sse_stream(...)` for the SSE branch (replacing `sse_stream`), and persist + set `conversation_id` on the buffered `ConversationResponse` branch. pytest the route for both branches.

### Done — U2b (wire the conversation route, test-first)

* Wired persistence into [conversation.py](../../src/backend/routers/conversation.py). The route now injects `user_id: UserIdDep` (the Easy Auth `x-ms-client-principal-id`, or `local-dev` when running locally), extracts the turn's question as the latest `ChatRole.USER` message (`next((m.content for m in reversed(body.messages) ...), "")` — robust to a trailing assistant turn), and persists both response modes keyed by `user_id`. The SSE branch returns a `StreamingResponse` over `persisting_sse_stream(events, request, db=db, user_id=user_id, conversation_id=body.conversation_id, question=question)` (replacing the prior `sse_stream`), so the wrapper tees the stream and appends the terminal `conversation` control frame with the resolved id. The buffered branch drains via `collect_response` (a blocked turn raises out of it before any write), then — only when both `question` and `response.content` are non-empty — calls `persist_turn` and echoes the resolved id via `response.model_copy(update={"conversation_id": resolved_id})`. A storage failure on the buffered path is logged (`logger.exception`, structured `extra`) and swallowed: the answer is already collected, so it returns regardless (matches the streaming wrapper's contract that a persistence error never retracts a delivered answer). Added a module `logger`; swapped `sse_stream` for `persisting_sse_stream` in the service import; added `Header`/`UserIdDep`/`persist_turn`/`ChatRole` to the imports.
* **Tests:** added a `_RecordingDatabaseClient` (records `create_conversation` / `add_message` calls, mirrors the services-test `_FakeDB`) and three route cases to [test_conversation.py](../../tests/backend/test_conversation.py) — JSON/buffered mode persists the turn and echoes `conversation_id="conv-new"` (create titled with the question, user-then-assistant messages); SSE mode emits the terminal `event: conversation` frame last carrying the resolved id and persists; and the persisted turn is keyed by the `x-ms-client-principal-id` header (`user-42`) rather than the local-dev fallback. `_fake_settings` gained `environment=Environment.LOCAL` so the newly-injected `UserIdDep` resolves to `local-dev` on the header-less route tests (without it `get_user_id` reads `settings.environment` and every route test 500s).

### Gates — U2b

* `tests/backend/test_conversation.py` **28 passed** (25 prior + 3 new) and `tests/backend/test_services_conversation.py` **23 passed**.
* Shared AST gates (`test_no_silent_excepts`, `test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_no_anonymous_dict_returns`) green — combined run **509 passed, 1 skipped**.
* `get_errors` clean on both touched files.

### BUG-0044 — stale narration-metadata assertion fixed in the same suite run

Running the full conversation-route suite for U2b unmasked a pre-existing failure: `test_sse_emits_leading_retrieval_narration_when_search_wired` asserted the leading retrieval-narration `reasoning` frame carries `metadata: {}`, but `run_chat` intentionally emits it with `metadata={"placeholder": True}` (the BUG-0036 frontend-replacement marker). Confirmed pre-existing — both `HEAD`'s `chat.py` (emits the placeholder) and `HEAD`'s test (asserts `{}`) are at `HEAD`, and U2b touches neither — so the source is correct and the test expectation was stale (the suite simply was not re-run between the BUG-0036 source change and now). Filed as **BUG-0044** (backend, low, fixed) and fixed by updating the expected metadata to `{"placeholder": True}` with a one-line comment; no source change.

### Next

* **U3 (next turn, structural — covered by the BUG-0017 plan approval, Hard Rule #10):** persist citations. `ChatMessage` in `backend.core.types` has no `metadata` field today (role / content / name only), so U3 adds one; the Postgres `messages` table needs a `metadata JSONB` column; `add_message` / `list_messages` thread metadata through; the Cosmos message doc carries metadata; the assistant message stores its citations so loading a conversation rehydrates them. Phase 1 (U1 / U2a / U2b / U3) then flips BUG-0017 toward fixed once Phase 2 frontend tracking lands.

## BUG-0017 — U3 sliced into U3a–U3d; integration-test lane adopted

A new standing instruction this turn — *"make integration tests"* — means every persistence sub-unit that has a real runtime surface (a DB round-trip or an end-to-end HTTP turn) now lands a `@pytest.mark.integration` test in [tests/integration](../../tests/integration) **in addition to** its mocked unit test. The lane is opt-in (deselected by default, self-skips when `v2/.env` is absent, backend-agnostic over the live `db_type`), so a pure-unit turn adds the integration test but cannot run it locally — it is recorded as added + deferred-verify (same posture as the live A0 browser captures).

U3 as planned spans the `ChatMessage` model **plus** both database providers **plus** the persist-service wiring — well beyond Hard Rule #1's one-class/one-method limit — so it is sliced:

* **U3a** — `ChatMessage.metadata` field (the model change everything else depends on). Unit-only: a bare model field has no persistence surface yet, so an integration round-trip is not possible until a provider threads it (U3b/U3c).
* **U3b** — Postgres `messages.metadata JSONB` + `add_message` / `list_messages` / `_row_to_message` round-trip. Unit test (fakes) + integration test.
* **U3c** — Cosmos message-doc `metadata` on `add_message` + `_to_message` round-trip. Unit test + integration test.
* **U3d** — store the assistant turn's citations in `ChatMessage.metadata` via `persist_turn` (and update the route caller if the signature changes). Unit test + an end-to-end integration test (`POST /api/conversation` → `GET /api/history/conversations/{id}` → assert the assistant message carries citation metadata). Before writing that test, verify the history detail route + its response models actually surface message `metadata` — if not, surfacing it is part of U3d.

### Done — U3a (`ChatMessage.metadata` field, test-first)

* Added `metadata: dict[str, Any] = Field(default_factory=dict)` to `ChatMessage` in [types.py](../../src/backend/core/types.py), mirroring the existing `MessageRecord.metadata` (the persisted read-side shape) and `Citation.metadata`. `Any` and `Field` were already imported; the default factory keeps every existing `ChatMessage(role=…, content=…)` producer unaffected. The docstring explains the field carries provider-agnostic extras (e.g. an assistant turn's citations) that travel into storage so a reloaded conversation rehydrates them without re-running retrieval.
* **Tests:** three cases added to [test_types.py](../../tests/backend/core/test_types.py) — default to `{}` when omitted (every existing producer's path), carries a nested `{"citations": [...]}` payload through a `model_dump_json` → `model_validate_json` round-trip (the persisted wire shape), and rejects a scalar `metadata` with `ValidationError` (the field stays a well-formed dict).

### Gates — U3a

* `tests/backend/core/test_types.py` **44 passed** (41 prior + 3 new).
* `get_errors` clean on [types.py](../../src/backend/core/types.py) and [test_types.py](../../tests/backend/core/test_types.py).

### Next — U3b

* Postgres: add `metadata JSONB NOT NULL DEFAULT '{}'` to the `messages` DDL in `_SCHEMA_SQL`; thread `message.metadata` into the `add_message` INSERT + `RETURNING` (verify asyncpg JSONB binding — native dict vs. `json.dumps`); add `metadata` to the `list_messages` SELECT and read it in `_row_to_message`. Unit test (fakes round-trip) + an integration test asserting a message's metadata survives a write→read-back over the live backend.

### Done — U3b (Postgres `messages.metadata` JSONB round-trip, test-first)

* Threaded message metadata through `PostgresClient` in [postgres.py](../../src/backend/core/providers/databases/postgres.py). The `messages` DDL in `_SCHEMA_SQL` gains a `metadata JSONB NOT NULL DEFAULT '{}'` column **and** an idempotent `ALTER TABLE messages ADD COLUMN IF NOT EXISTS metadata JSONB NOT NULL DEFAULT '{}';` after the index — because `CREATE TABLE IF NOT EXISTS` no-ops on a `messages` table that predates the column (the live deployment already has one), the additive `ALTER` back-fills it without a separate migration step, preserving the lazy-bootstrap contract. `add_message` adds `metadata` to the INSERT column list + `$6` bound as `json.dumps(message.metadata)` (a JSON-shaped `str`, the same JSONB-binding contract `upsert_runtime_config` / `write_admin_audit` already use) and to the `RETURNING` clause; `list_messages` adds `metadata` to the SELECT; `_row_to_message` parses `json.loads(row["metadata"])` (asyncpg returns a JSONB column as `str` — same read contract as `get_runtime_config`), falling back to `{}` for a legacy NULL. Added `import json`.
* **Unit tests:** extended `_msg_row` with a `metadata: str = "{}"` field (mirroring the asyncpg JSONB-as-str wire shape; existing callers keep the `{}` default) and added two cases to [test_postgres.py](../../tests/backend/core/providers/databases/test_postgres.py) — `add_message` carries `metadata` in the INSERT, binds the dict as `json.dumps(...)`, and parses the `RETURNING` row back into `MessageRecord.metadata`; `list_messages` includes `metadata` in the SELECT and parses each JSONB str back into a dict (default `{}` + a `{"citations": [...]}` payload). Added `import json`.
* **Integration test:** added `test_message_metadata_column_round_trips_live` to [test_history_live.py](../../tests/integration/test_history_live.py). The bare `POST /messages` route writes no metadata (it builds `ChatMessage(role, content)`), so the value defaults to `{}` — which is enough to prove (a) the live schema actually has the JSONB `metadata` column (the write would fail otherwise — this is the live proof the `ALTER` back-fill worked) and (b) the read path (`list_messages` → the detail route) surfaces it on every message. The non-empty citation payload is exercised end-to-end at U3d (`POST /api/conversation` → `GET /api/history/conversations/{id}`), because the only HTTP write-path that carries non-empty message metadata is `persist_turn` via the conversation route — the bare history POST intentionally does not accept arbitrary metadata input. Recorded as added + deferred-verify (the lane self-skips without `v2/.env`).

### Gates — U3b

* `tests/backend/core/providers/databases/test_postgres.py` + `tests/backend/core/test_types.py` **99 passed**.
* Shared AST gates (`test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_no_anonymous_dict_returns`, `test_init_files_are_marker_only`, `test_pillar_phase_header`, `test_no_type_checking_or_future_annotations`) **713 passed, 1 skipped**.
* `pyright --strict` on [postgres.py](../../src/backend/core/providers/databases/postgres.py) — **0 errors / 0 warnings / 0 information**.
* New integration test collects (3 tests in the file); `get_errors` clean on all three touched files.

### Next — U3c

* Cosmos: `add_message` adds `"metadata": message.metadata` to the message item dict; `_to_message` reads `item.get("metadata", {})`. Unit test (fakes round-trip) in `test_cosmosdb.py`. The integration round-trip of a non-empty payload stays folded into U3d's end-to-end conversation-reload test (backend-agnostic — it runs against whichever `db_type` the live env wires).

### Done — U3c (Cosmos message-doc `metadata` round-trip, test-first)

* Threaded message metadata through `CosmosDBClient` in [cosmosdb.py](../../src/backend/core/providers/databases/cosmosdb.py). `add_message` adds `"metadata": message.metadata` to the message item dict — Cosmos stores the JSON object natively (no serialization step, unlike the Postgres JSONB column that binds a `json.dumps(...)` str), so the dict goes in as-is. `_to_message` reads `item.get("metadata", {})`, defaulting a legacy message doc (written before the key existed) to `{}` rather than raising. No DDL / schema change — Cosmos is schemaless. No `BaseDatabaseClient` signature change (`ChatMessage` already carries metadata).
* **Unit tests:** added two cases to [test_cosmosdb.py](../../tests/backend/core/providers/databases/test_cosmosdb.py) — `add_message` writes the dict into the item body verbatim (the fake `create_item` echoes the body, so `_to_message` round-trips it onto `MessageRecord.metadata`); `_to_message` defaults to `{}` for a legacy doc with no `metadata` key.
* **Integration test:** none added for U3c specifically. The integration lane drives the public HTTP routes (not provider methods) and is **backend-agnostic** over the live `db_type`, so the U3b `test_message_metadata_column_round_trips_live` already exercises the Cosmos path when the live env wires `db_type=cosmosdb` (and the Postgres path when it wires `postgresql`). The non-empty citation payload stays folded into U3d's end-to-end conversation-reload test, which is likewise backend-agnostic.

### Gates — U3c

* `tests/backend/core/providers/databases/test_cosmosdb.py` **53 passed** (51 prior + 2 new).
* Shared AST gates (`test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_no_anonymous_dict_returns`, `test_pillar_phase_header`) **459 passed, 1 skipped**.
* `pyright --strict` on [cosmosdb.py](../../src/backend/core/providers/databases/cosmosdb.py) — **0 errors / 0 warnings / 0 information**.
* `get_errors` clean on both touched files.

### Next — U3d

* Wire the assistant turn's citations into `persist_turn` (services/conversation.py): serialize the `Citation` list into the assistant `ChatMessage.metadata` (e.g. `{"citations": [citation.model_dump(), ...]}`) so a reloaded conversation rehydrates the reference block without re-running retrieval; update the conversation-route caller if `persist_turn`'s signature changes (it currently takes `question` + `answer` — citations need to flow in too). Unit test (the assistant `ChatMessage` carries the citation metadata) + an **end-to-end integration test** (`POST /api/conversation` real question → `GET /api/history/conversations/{id}` → assert the assistant message carries the citation metadata). The detail GET already surfaces message metadata (verified at U3b — `ConversationDetail.messages` are `MessageRecord`, which now carries the field), so no surfacing work is needed.

### Done — U3d (persist the assistant turn's citations, test-first — Phase 1 closer)

* **`persist_turn` now takes `citations: list[Citation]`** ([conversation.py](../../src/backend/services/conversation.py)). A required keyword-only argument (no mutable default), so every call site supplies it explicitly. The grounding citations ride the assistant message's `metadata` under a `"citations"` key — each serialized via `Citation.model_dump(mode="json")` so the value is JSON-safe for both the Postgres `json.dumps` JSONB bind and the native-JSON Cosmos write. A turn with no citations leaves the metadata an empty dict, identical to the user message (`metadata = {"citations": [...]} if citations else {}`). The user message stays metadata-free.
* **`persisting_sse_stream` accumulates citations from the stream.** It already teed the answer chunks; now it also dedupes `CITATION`-channel events by `metadata["id"]` (the same logic `collect_response` uses for the buffered path) and threads the resulting `list[Citation]` into `persist_turn`. Both response modes therefore persist the same deduplicated citation set.
* **Buffered router seam** ([routers/conversation.py](../../src/backend/routers/conversation.py)): the buffered branch passes `citations=response.citations` (the already-deduplicated list `collect_response` materialized) into `persist_turn`.
* **No surfacing work.** The history detail route returns `ConversationDetail.messages` as `MessageRecord` (which carries `metadata` since U3a), so `GET /api/history/conversations/{id}` already serializes the assistant message's citation metadata — confirmed by the live test below.
* **Unit tests.** Service suite ([test_services_conversation.py](../../tests/backend/test_services_conversation.py)): the three existing `persist_turn` tests pass `citations=[]` and assert both messages carry `metadata == {}`; a new `test_persist_turn_stores_citations_in_assistant_metadata` asserts the assistant message metadata carries the serialized citations (user message stays `{}`); a new `test_persisting_sse_stream_persists_citations_in_assistant_metadata` proves the streaming path dedupes by id and persists. Router suite ([test_conversation.py](../../tests/backend/test_conversation.py)): a new `test_json_mode_persists_citations_in_assistant_message_metadata` drives the real endpoint with citation events and asserts the recorder's persisted assistant message carries the deduplicated ids.
* **End-to-end integration test** ([test_history_live.py](../../tests/integration/test_history_live.py)): `test_conversation_persists_citations_to_history_live` drives the real orchestrator + KB through `POST /api/conversation` in SSE mode, reads the resolved id from the terminal `conversation` control frame, reloads via `GET /api/history/conversations/{id}`, and asserts the persisted assistant message's `metadata["citations"]` ids match exactly what the answer streamed on the `citation` channel (deduplicated, same order). Backend-agnostic over the live `db_type`; self-cleans the conversation in a `finally`. **Ran green against the live env — 1 passed in 35.9s** (real Foundry IQ + Search + DB round-trip), so the non-empty citation payload deferred from U3b/U3c is now proven end-to-end.

### Gates — U3d

* `tests/backend/test_services_conversation.py` + `tests/backend/test_conversation.py` **52 passed**.
* Six shared AST gates (`test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_no_anonymous_dict_returns`, `test_pillar_phase_header`, `test_no_type_checking_or_future_annotations`, `test_init_files_are_marker_only`) **713 passed, 1 skipped**.
* `pyright --strict` on [services/conversation.py](../../src/backend/services/conversation.py) + [routers/conversation.py](../../src/backend/routers/conversation.py) — **0 errors / 0 warnings / 0 information**.
* Integration lane collects **4 tests**; the new end-to-end test **ran green against the live env (1 passed, 35.9s)**.
* `get_errors` clean on all touched files.

### Result — Phase 1 (backend persistence) complete

* Phase 1 of the BUG-0017 plan — U1 (`persist_turn`), U2a (`persisting_sse_stream`), U2b (route wiring), U3a (`ChatMessage.metadata`), U3b (Postgres round-trip), U3c (Cosmos round-trip), U3d (persist citations) — is **complete and green**. Every completed chat turn (question + answer + grounding citations) now persists to the configured backend, keyed by the Easy Auth principal id, in both buffered and streaming modes.
* **BUG-0017 stays open** — the user-visible fix needs Phase 2 (frontend conversation-id tracking, load-on-click, and the new-chat / hidden-db-name UX). The backend now has everything the frontend needs to track and rehydrate a thread; BUG-0017 flips to fixed when Phase 1 + 2 both land.

### Next — Phase 2 (frontend)

* **U4** — `ChatContext` gains `conversationId` + `SetConversationId` / `LoadConversation` / `Reset` actions. **U5** — `streamChat` sends `conversation_id` and parses the terminal `conversation` SSE frame; the API base honors `VITE_BACKEND_URL`. **U6** — `MessageInput` passes `conversationId` + dispatches `SetConversationId`. **U7** — `HistoryPanel` load-on-click → `GET /conversations/{id}` → hydrate (citations included); `ChatPage` consumes the selected id. **U8** — delete-confirm names the conversation. **U9** — new-chat clears + sets `conversationId=null` (BUG-0019). **U10** — hide the raw db name (BUG-0018).

### Done — U4 (`ChatContext` conversation-id tracking, test-first — Phase 2 opener)

* **`ChatState` gains `conversationId: string | null`** ([chat.tsx](../../src/frontend/src/models/chat.tsx)) — placed after `messages`, before `focusedCitationId`. `null` means a fresh chat whose first turn has not been persisted yet; it is set from the terminal `conversation` SSE control frame once the backend persists a turn (U5/U6), or from a history selection when an existing conversation is loaded (U7), and cleared by a reset / new-chat.
* **Two new reducer actions** ([ChatContext.tsx](../../src/frontend/src/pages/chat/ChatContext.tsx)). `ChatActionType` gains `SetConversationId: "set_conversation_id"` and `LoadConversation: "load_conversation"` (both before `Reset`), with matching `ChatAction` union members. `SetConversationId` carries `conversationId: string | null` (set **or** clear); `LoadConversation` carries a non-null `conversationId: string` + a `messages: ChatMessage[]` transcript (loading always targets a concrete id).
* **Reducer cases.** `SetConversationId` does `{ ...state, conversationId: action.conversationId }` (no reference-equality guard — follows the existing `ShowCitation` pattern; minimal). `LoadConversation` returns `{ ...initialChatState, conversationId, messages }` — it replaces the transcript wholesale and clears the prior conversation's citation UI (`focusedCitationId` + `activeCitation`); the loaded messages carry their own `citations`, so the reference panels rehydrate with no extra wiring (documented inline).
* **`initialChatState` / `Reset`** both set `conversationId: null`, so a new chat starts a fresh (unpersisted) conversation.
* **Unit tests** ([ChatContext.test.tsx](../../src/tests/frontend/pages/chat/ChatContext.test.tsx)): a new `describe("chatReducer conversation tracking")` block with **9 cases** — initializes to `null`; `set_conversation_id` sets from null / overwrites an existing id / clears via `null` / does not touch the `messages` array (reference check); `load_conversation` replaces the transcript + sets the id / rehydrates citations carried on loaded messages / clears stale citation UI from the prior conversation; `reset` clears the id back to `null`. The existing "clears messages on reset" test's `populated` literal gained `conversationId: "conv-1"` so reset is also proven to clear a non-null id.

### Gates — U4

* `npx vitest run ChatContext` — **45 passed** (36 prior + 9 new).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree (`v2/src/tests/frontend`) — **exit 0**.
* `get_errors` clean on all three touched files; the only ESLint output on U4 files is the pre-existing `react-refresh/only-export-components` warning on `ChatContext.tsx` (the established reducer-next-to-provider pattern — unchanged by U4, warning count unchanged).

### Found — pre-existing frontend ESLint errors (NOT U4; deferred)

* Running the full `npm run lint --workspaces` gate surfaced **6 ESLint errors in 3 citation-UX components that U4 did not touch** and that are **unmodified in the working tree** (committed state): [CitationDetailPanel.tsx](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.tsx) (`no-restricted-imports` cross-folder `../MarkdownContent` should use `@/`; one `no-confusing-void-expression`), [CitationPanel.tsx](../../src/frontend/src/pages/chat/components/CitationPanel/CitationPanel.tsx) (two `no-confusing-void-expression`), [MessageList.tsx](../../src/frontend/src/pages/chat/components/MessageList.tsx) (one `prefer-nullish-coalescing`, one `no-confusing-void-expression`). These predate BUG-0017 Phase 2 and would fail the frontend CI lint gate independently of U4. **Left unfixed** — folding an unrelated 3-file lint cleanup into U4 would violate one-unit-per-turn; recommend a dedicated cleanup turn (4 of the 6 are `--fix`-able). Flagged here so it is not lost.
* Incidental: the `tsc -b` runs rewrote the tracked `v2/src/frontend/tsconfig.tsbuildinfo` and created an untracked `v2/src/tests/frontend/tsconfig.tsbuildinfo` (build caches; regenerated on every typecheck).

### Next — U5

* `streamChat` ([streamChat.tsx](../../src/frontend/src/api/streamChat.tsx)) — send `conversation_id` in the POST body; parse the terminal `conversation` SSE control frame (`event: conversation\ndata: {"conversation_id":"…"}`) and surface the resolved id; fix the request URL to honor `VITE_BACKEND_URL` (`${import.meta.env.VITE_BACKEND_URL ?? ""}` + path). The backend already emits the terminal frame (U2a) and accepts `conversation_id` in the request body. vitest.

### Done — U5 (`streamChat` conversation-id wiring, test-first)

* **Request now carries `conversation_id`** ([streamChat.tsx](../../src/frontend/src/api/streamChat.tsx)). `StreamChatOptions` gains `conversationId?: string | null`; `streamChat` reads it (`options.conversationId ?? null`) and threads it into `streamChatOnce`, which builds the body as `conversationId !== null ? { messages, conversation_id } : { messages }` — a continuing turn sends the id, a fresh chat omits the key entirely (matching the backend's `ConversationRequest.conversation_id: str | None = None`, where absent / null both start a new conversation).
* **The terminal `conversation` control frame is parsed out-of-band.** `parseFrame` now returns a discriminated `ParsedFrame` (`{ kind: "event"; event }` | `{ kind: "conversation"; conversationId }`) instead of a bare `StreamEvent`. The channel check is reordered so `event: conversation` is recognized **before** the `KNOWN_CHANNELS` guard (which would otherwise drop it as an unknown channel) and routed to a new `parseConversationFrame` that JSON-parses the `{"conversation_id":"…"}` payload and returns `null` for a missing / empty id. Both parse call sites (the mid-stream `\n\n` loop and the `done` trailing-flush) branch on `kind`: a `conversation` frame fires `onConversationId?.(...)` and is **never** yielded as a `StreamEvent`, so the locked SSE channel set (Hard Rule #6 / ADR 0007) is preserved — the resolved id rides a transport-control callback, not a sixth channel.
* **`onConversationId?: (conversationId: string) => void`** is the new last field on `StreamChatOptions` — called once with the backend-resolved id when the turn persists. U6 wires it to dispatch `SetConversationId`.
* **The request URL now honors `VITE_BACKEND_URL`.** `CONVERSATION_URL` became `CONVERSATION_PATH` plus a `conversationUrl()` helper that prepends `(import.meta.env.VITE_BACKEND_URL as string | undefined) ?? ""` at call time — the same separate-origin base convention `documentHref` / `HistoryPanel` / `App` already follow (the prior hard-coded relative path would have resolved against the static frontend host in the deployed topology). Unset (dev / test) → relative `/api/conversation`, so the existing same-origin assertion stays green.
* **Tests** ([streamChat.test.tsx](../../src/tests/frontend/api/streamChat.test.tsx)): `afterEach` gains `vi.unstubAllEnvs()`; **7 new cases** — sends `conversation_id` in the body when provided; omits the key when `null`; `onConversationId` fires with the id from the terminal frame (and that frame is not yielded as an event); the `conversation` frame is dropped even with no callback; a missing / empty id is ignored; a `conversation` frame split across chunk boundaries is buffered and parsed; and `VITE_BACKEND_URL` is prepended to the request URL (mirrors the `documentHref` stub-env convention).

### Gates — U5

* `npx vitest run streamChat` — **24 passed** (17 prior + 7 new).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 are out of scope and untouched).
* `get_errors` clean on both files.

### Next — U6

* `MessageInput` ([MessageInput.tsx](../../src/frontend/src/pages/chat/components/MessageInput.tsx)) — pass `state.conversationId` into the `streamChat(history, { … })` options and supply an `onConversationId` callback that dispatches `{ type: SetConversationId, conversationId }`, so a freshly-persisted turn records its backend id (and a continuing turn re-sends it). vitest the dispatch wiring.

### Done — U6 (`MessageInput` conversation-id send + dispatch, test-first)

* **The composer now threads the conversation id through `streamChat`** ([MessageInput.tsx](../../src/frontend/src/pages/chat/components/MessageInput.tsx)). `handleSubmit` adds two options to the existing `streamChat(history, { signal })` call: `conversationId: state.conversationId` (the pre-dispatch closure snapshot — `null` on a fresh chat omits the body key and starts a new conversation, a concrete id continues the active thread), and `onConversationId: (conversationId) => dispatch({ type: "set_conversation_id", conversationId })` (records the backend-resolved id from the terminal `conversation` control frame so the **next** submit re-sends it and the backend appends rather than forking). No staleness bug: the in-flight first turn already sent `null`; the dispatch re-renders the component, so the next render's `handleSubmit` closure reads the updated `state.conversationId`. Two concise inline comments at the call site document the behavior (no docstring change — matches how the citation actions were folded in at U4).
* **Tests** ([MessageInput.test.tsx](../../src/tests/frontend/pages/chat/components/MessageInput.test.tsx)): the shared `Probe` gained a sibling `conversation-id-probe` element (rendering `state.conversationId ?? "null"`) plus a `probeConversationId()` reader — additive, so every existing `probeMessages()` lookup is untouched. New `describe("MessageInput conversation-id wiring")` with **4 cases** — sends `conversationId: null` on a fresh chat; records the backend id when the stream's `onConversationId` fires (probe flips `null → "conv-7"`); continues the same conversation on the next submit (the second `streamChat` call carries `conversationId: "conv-9"`, proving the dispatched id flows back through state); and starts fresh (`conversationId: null`) again after the clear-conversation button resets state.

### Gates — U6

* `npx vitest run MessageInput` — **34 passed** (30 prior + 4 new; the `act(...)` stderr lines are pre-existing across the whole suite, not failures).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

### Next — U7 (sliced into U7a + U7b)

U7 spans two seams — a fetch+map helper and the in-`ChatProvider` wiring that dispatches `LoadConversation` — so it is split one-unit-per-turn (the U3 precedent): **U7a** the `fetchConversation` API helper (load + map `MessageRecord[]` → `ChatMessage[]`, rehydrate persisted citations), **U7b** the `ChatShell` `onSelect` handler that calls the helper and dispatches `LoadConversation`, keeping `HistoryPanel` presentational.

### Done — U7a (`fetchConversation` history-detail client + transcript map, test-first)

* **New API seam** ([conversationHistory.tsx](../../src/frontend/src/api/conversationHistory.tsx)) — `fetchConversation(id)` GETs `/api/history/conversations/{id}` (id `encodeURIComponent`-escaped; `VITE_BACKEND_URL` prepended at call time via a `backendUrl()` helper, the same separate-origin base convention `streamChat` / `documentHref` / `HistoryPanel` follow) and returns `{ conversationId, messages: ChatMessage[] }`. Mirrors the hand-rolled one-typed-wrapper-per-endpoint pattern of `admin.tsx` / `streamChat.tsx` (no OpenAPI generator in v2 yet); throws `fetchConversation: request failed with status <n>` on non-2xx (404 = unknown / not-owned).
* **The stored→rendered map is the heart of the unit.** Each persisted `MessageRecord` → `ChatMessage` via `toChatMessage`: `system` / `tool` roles are dropped (the transcript renders only `user` / `assistant`, but backend `ChatRole` carries four members), and each assistant turn's grounding citations are rehydrated from `metadata.citations` (the serialized `Citation` list `persist_turn` writes) by `citationsFrom` → `toCitation`. `toCitation` re-applies the same narrowing `MessageInput`'s `parseCitation` uses on the SSE side (drop an entry missing a non-empty `id`; coerce a non-numeric / non-finite `score` to `null`; default `title` / `url` / `snippet` to `""` and `metadata` to `{}`) so a malformed stored entry is dropped rather than rendered anchorless. The `citations` key is omitted entirely when an assistant turn persisted none, matching the reducer's absent-vs-empty contract.
* **Tests** ([conversationHistory.test.tsx](../../src/tests/frontend/api/conversationHistory.test.tsx)): **11 cases** — GETs the encoded route with the JSON `Accept` header; prepends `VITE_BACKEND_URL` when set; returns the resolved `conversationId` + mapped messages; rehydrates assistant citations from `metadata.citations`; omits the `citations` key when none persisted; drops `system` / `tool` messages; drops a malformed citation lacking a non-empty `id`; coerces a non-numeric score to `null`; tolerates a message with no `metadata`; returns an empty transcript when the detail has no `messages` array; throws with the status on a non-ok response. Global `fetch` is mocked (no network); `afterEach` resets globals + envs + mocks.

### Gates — U7a

* `npx vitest run conversationHistory` — **11 passed**.
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

### Next — U7b

* `ChatShell` ([ChatPage.tsx](../../src/frontend/src/pages/chat/ChatPage.tsx)) — enhance the `onSelect` handler passed to `<HistoryPanel>` so a click `setSelectedId(id)` **and** `await fetchConversation(id)` then `dispatch({ type: "load_conversation", conversationId, messages })`, rehydrating the transcript + citations from the just-added U7a helper. `HistoryPanel` stays presentational (bubbles `onSelect` only). vitest the wiring (mock `fetchConversation`; click a history row; assert `LoadConversation` lands the mapped messages and sets `conversationId`).

### Done — U7b (`ChatShell` load-on-click rehydration wiring, test-first)

* **Selecting a history row now rehydrates the transcript** ([ChatPage.tsx](../../src/frontend/src/pages/chat/ChatPage.tsx)). `ChatShell` now pulls `dispatch` from `useChat()` (it previously read only `state`) and passes a memoized `handleSelect` — instead of the bare `setSelectedId` — as `<HistoryPanel onSelect>`. `handleSelect(id)` `setSelectedId(id)` immediately (so the row highlights synchronously, before any I/O), then `void fetchConversation(id)` (the U7a helper) and on resolve `dispatch({ type: "load_conversation", conversationId, messages })`, which the reducer turns into a wholesale transcript replace + `conversationId` set (clearing stale citation UI; the loaded messages carry their own rehydrated citations). The handler is `useCallback([dispatch])` so `onSelect` stays referentially stable — preserving the prior `setSelectedId` stability that `HistoryPanel`'s `handleNew` `useCallback([onSelect])` relies on.
* **`HistoryPanel` stays presentational.** The fetch + dispatch live entirely at the in-`<ChatProvider>` `ChatShell` seam; the panel still only bubbles `onSelect(id)` and owns no chat state — the decoupling holds.
* **Failed load is non-destructive.** `fetchConversation` rejects on a non-2xx (404 unknown / not-owned); the `.catch` leaves the live transcript intact and keeps the row highlighted so re-selecting retries. No `console` (the v2 frontend uses none; the sibling panel surfaces its own errors via component state) and no unhandled rejection (the promise is `void`-marked + caught).
* **Tests** ([ChatPage.test.tsx](../../src/tests/frontend/pages/chat/ChatPage.test.tsx)): `@/api/conversationHistory` is now `vi.mock`-ed (mirrors the existing `streamChat` stub) and `fetchConversationMock` is reset in `afterEach`; a `stubHistoryList(...)` helper re-stubs `fetch` so the panel renders a real selectable row. New `describe("ChatPage history selection")` with **3 cases** — selecting a row loads the mapped transcript (2 `<li>`, both turns' text) **and** rehydrates the assistant turn's persisted citation chip (`/1 reference/`), calling `fetchConversation("conv-7")`; the row is marked `aria-current="true"` on click; and a rejected load leaves a seeded live transcript (`live question` + placeholder) untouched with the row still current. All 6 pre-existing `ChatPage` cases stay green (9 total).

### Gates — U7b

* `npx vitest run ChatPage` — **9 passed** (6 prior + 3 new).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

**U7 complete (U7a + U7b).** Selecting a past conversation in the left history column now fetches its stored detail and rehydrates the full transcript + persisted citations, with the row highlighted and a failed load left non-destructive.

### Done — U8 (delete-confirm names the conversation, test-first)

* **The delete confirmation now names the row** ([HistoryPanel.tsx](../../src/frontend/src/pages/chat/components/HistoryPanel.tsx)). `handleDelete` previously asked the generic `window.confirm("Delete this conversation?")`; it now takes the title alongside the id (mirroring `handleRename(id, title)`) and asks ``Delete "<title>"? This cannot be undone.``, falling back to `Untitled` for a blank title (matching the row label fallback used everywhere else in the panel). The delete icon's `onClick` passes `c.title` as the second arg. Return-value handling is unchanged — `false` still aborts before any DELETE, `true` still fires the request and prunes the row — so both pre-existing delete tests stay green.
* **Tests** ([HistoryPanel.test.tsx](../../src/tests/frontend/pages/chat/components/HistoryPanel.test.tsx)): a new case spies `window.confirm` (returning `false` so no DELETE fires) and asserts it was called with the exact copy ``Delete "First"? This cannot be undone.`` — proving the prompt names the conversation. The existing confirm-`true` (deletes + removes) and confirm-`false` (aborts the DELETE) cases are untouched.

### Gates — U8

* `npx vitest run HistoryPanel` — **11 passed** (10 prior + 1 new).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

### Done — U9 (remove the New chat button from the history column, BUG-0019, test-first)

* **The history left column no longer has a New chat button** ([HistoryPanel.tsx](../../src/frontend/src/pages/chat/components/HistoryPanel.tsx)). Removed the `history-new` button and its `handleNew` callback (which POSTed `/api/history/conversations`), plus the now-unused `Add16Regular` icon import. The panel header is now just the `History` title (the `history-db-type` line below it is BUG-0018 / U10, still pending).
* **Scope decision — the header New-chat affordance stays.** BUG-0019 is explicitly scoped to *the chat history left column*. A separate, working New-chat control lives in the app header ([HeaderTools.tsx](../../src/frontend/src/components/Header/HeaderTools.tsx) `data-testid="header-new-chat"` → [App.tsx](../../src/frontend/src/App.tsx) `onNewChat` → `setNewChatNonce` → `<ChatPage key={newChatNonce} />`). Bumping the nonce remounts `ChatPage`, which re-initializes `ChatContext` to `initialChatState` (empty transcript, `conversationId: null`) — i.e. a clean new chat. Removing the panel button does **not** strand the user: the header remains the way to start a new chat. Removing the header control too (the other half of the "decide in the fix turn" note) would leave no new-chat path, so it is intentionally kept.
* **Tests** ([HistoryPanel.test.tsx](../../src/tests/frontend/pages/chat/components/HistoryPanel.test.tsx)): the obsolete "creates a new conversation, prepends it, and selects it" case (which clicked `history-new`) was replaced with a negative assertion — after the list loads, `queryByTestId("history-new")` is `null`. The generic `installFetch` REST harness is untouched. `BUG-0019` flipped to **fixed** in [bugs.md](../bugs.md) (summary row + detail, with the scope decision recorded).

### Gates — U9

* `npx vitest run HistoryPanel` — **11 passed** (the create test was swapped 1-for-1 for the negative test).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

### Done — U10 (hide the raw backend db name in the history header, BUG-0018, test-first)

* **The history header no longer leaks the backend database name** ([HistoryPanel.tsx](../../src/frontend/src/pages/chat/components/HistoryPanel.tsx)). Removed the `<p data-testid="history-db-type">backend: {status.db_type}</p>` block from the panel header. Because the panel fetched `/api/history/status` *solely* to surface that discriminator (it never branched on it — Hard Rule #4), the now-orphaned status fetch, the `status` React state, and the `HistoryStatus` interface were removed too; `refresh()` now mounts the panel by loading only `/api/history/conversations`. The stale docstring paragraph that described the status-discriminator display was rewritten to state the panel never reads or surfaces the provider key.
* **Why the whole status fetch went, not just the `<p>`:** with the only reader of `status` gone, keeping the state + fetch would have been dead code (and would not have compiled under `noUnusedLocals`). The fetch existed only to feed the removed label, so removing it is the coherent BUG-0018 fix, not a restructure. The `.dbType` CSS class is left in the module (harmless unused class; touching the CSS file would widen scope).
* **Tests** ([HistoryPanel.test.tsx](../../src/tests/frontend/pages/chat/components/HistoryPanel.test.tsx)): the mount test was rewritten to assert the panel loads the conversations list **without** calling `/api/history/status` (positively asserts the status URL is *not* hit), and a new negative test asserts `history-db-type` is never rendered after the list loads. The generic `installFetch` REST harness is untouched (its now-unreached `status` route default is harmless).

### Gates — U10

* `npx vitest run HistoryPanel` — **12 passed** (mount test rewritten + 1 new negative display test).
* `npx tsc -b` on the frontend source tree — **exit 0**; `npx tsc -b` on the relocated test tree — **exit 0**.
* `npx eslint` scoped to both touched files — **exit 0** each (the 6 pre-existing citation-UX lint errors logged under U4 remain out of scope and untouched).
* `get_errors` clean on both files.

### Phase 2 complete — BUG-0017 fixed

U10 was the last Phase 2 (frontend bridge) unit. With Phase 1 (backend persistence, validated by the live integration test `test_conversation_persists_citations_to_history_live`) and Phase 2 both landed, **BUG-0017 flips to fixed** — a conversation begun by sending a first message is persisted server-side and appears in the left history column. The full Phase 2 chain: `ChatContext` conversation-id tracking (U4) → `streamChat` parses the terminal `conversation` SSE control frame (U5) → `MessageInput` sends + folds the id (U6) → load-on-click transcript + citation rehydration (U7a/U7b) → New chat button removed from the column (U9, BUG-0019) → raw db-name display removed (U10, BUG-0018). **BUG-0017 / BUG-0018 / BUG-0019 all flipped to fixed** in [bugs.md](../bugs.md). Follow-on, tracked separately and **not** part of BUG-0017's symptom: Phase 3 (rename validation + RAI, U11–U12) and Phase 4 (cloud identity / per-user history isolation, U13–U15).

### Next — U11 (Phase 3)

* `PATCH /api/history/conversations/{id}` rename validation + RAI title screening (backend). Empty / blank titles → `422`; the new title is RAI-screened before persistence. Backend pytest, test-first. (U12 then adds the `HistoryPanel` rename guard and surfaces the RAI rejection in the UI.)

### U11 sliced into U11a + U11b (one unit per turn)

U11 as planned spans two behaviors — request-body validation **and** an RAI screen of the new title — which is beyond Hard Rule #1's one-class/one-method limit, so it is sliced:

* **U11a** — reject empty / blank titles → `422` (a `field_validator` on `RenameConversationRequest`, the model the route already binds). Pure model change; covered by router pytest.
* **U11b** — RAI-screen the new title before persistence (wire `ContentSafetyGuard.screen` / `rai_check` into the rename route, returning a 4xx on a flagged title). Route change; separate turn.

### Done — U11a (rename blank-title validation → 422, test-first)

* **The rename body now rejects a whitespace-only title** ([history.py](../../src/backend/models/history.py)). `RenameConversationRequest.title` kept its `Field(min_length=1, max_length=512)` floor (so an empty string `""` still 422s on the declared constraint) and gained a `@field_validator("title")` that runs after the field constraint: it `strip()`s the value, raises `ValueError("title must not be blank")` (→ Pydantic `ValidationError` → FastAPI `422`) when the result is empty, and otherwise returns the **stripped** value — so an accepted rename never persists surrounding whitespace (`"  New name  "` is stored as `"New name"`). Only `RenameConversationRequest` was touched; `CreateConversationRequest.title` intentionally still allows `""` (a new conversation may be untitled until the first message titles it).
* **Tests** ([test_history.py](../../tests/backend/test_history.py)): three router cases — a whitespace-only `"   "` title → `422` with `db.rename_conversation` never awaited; an empty `""` title → `422` (pins the `min_length` floor) with no DB call; and a `"  New name  "` title → `200` with `db.rename_conversation` awaited as `("c-1", "u-1", "New name")`, proving the strip happens before persistence. The two pre-existing rename cases (happy path + `KeyError` → 404) stay green.

### Gates — U11a

* `tests/backend/test_history.py` — **20 passed** (17 prior + 3 new).
* `tests/shared/test_no_process_narrative_in_src.py` — **117 passed** (the new descriptive validator comment carries no process narrative).
* `get_errors` clean on both touched files.

### Next — U11b

* RAI-screen the new title in the rename route ([routers/history.py](../../src/backend/routers/history.py)) before `db.rename_conversation`, mirroring the chat-pipeline screening seam (`ContentSafetyGuard.screen` via `ContentSafetyGuardDep`, returning a 4xx on a flagged title rather than an SSE `error` frame, since rename is a plain PATCH). Backend pytest, test-first.

### Flag at U11b start — content-safety on a private self-only title

* The session plan flagged whether RAI/content-safety on a **private, self-only** conversation title is genuinely wanted or over-engineering (titles are per-user, single-tenant, shown only to the owner, never sent to the model — so the usual RAI threat model is weak; screening adds latency + a content-safety dependency to every rename). Surfaced to the operator before building. **Operator decision: build U11b as planned — RAI-screen the title, return 4xx if flagged.** It stays in the operator-approved Phase 3 plan and the BUG-0017 resolution note, so U11b proceeds.

### Done — U11b

* **The rename route now screens the new title through the content-safety guard before persisting** ([routers/history.py](../../src/backend/routers/history.py)). `rename_conversation` gained a `content_safety: ContentSafetyGuardDep` parameter; when the guard is wired (content safety enabled) the route runs `await content_safety.screen(body.title)` and, on a flagged verdict, raises `HTTPException(400, "Title was blocked by the content safety guard.")` — a flagged title is rejected and never written. When the guard is disabled (no client wired, or operator-off via runtime overrides) the dependency resolves to `None` and screening is skipped, exactly like the chat pipeline's `if content_safety is not None` seam. Order: the U11a body validator runs first (blank → 422), then the guard (flagged → 400), then `db.rename_conversation`; the pre-existing `KeyError` → 404 mapping is unchanged. **400 (not 422)** keeps the content-safety rejection distinct from the blank-title validation so U12 can branch on it cleanly. No route-level `try/except`: `ContentSafetyGuard.screen()` lets an `AzureError` from `analyze_text` propagate, and the app-level handler maps it to a sanitized **503** (Hard Rule #9 — app-level handlers, not per-route SDK try/except).
* **Tests** ([test_history.py](../../tests/backend/test_history.py)): added a `_FakeGuard` stand-in (records screened text; a `flagged` flag selects the verdict) and three router cases — a flagged title → `400` with `db.rename_conversation` never awaited and the title recorded as screened; a clean title with the guard present → `200`, screened, and persisted as `("c-1", "u-1", "Clean title")`; and a guard-disabled rename (no `get_content_safety_guard` override → dep resolves to `None`) → `200`, persisted without a content-safety round-trip.

### Gates — U11b

* `tests/backend/test_history.py` — **23 passed** (20 prior + 3 new); the three new cases also confirmed by name (`-k "content_safety or flagged or screens_clean or skips_screening"` → 3 passed, 20 deselected).
* Shared AST gates — **487 passed** combined: `test_routers_contain_only_routes` (the route adds no module-level symbol), `test_no_process_narrative_in_src`, `test_imports_at_top_only`, `test_pillar_phase_header`.
* `get_errors` clean on both touched files.

### Next — U12

* `HistoryPanel` rename guard ([HistoryPanel.tsx](../../src/frontend/src/pages/chat/components/HistoryPanel.tsx)) — `handleRename` already aborts on a null / empty `window.prompt`; add a non-empty guard and surface the backend content-safety rejection (the `400` from U11b, distinct from a `422`) in the panel's `LoadState` error message. Frontend vitest.

## Detour — chat layout / UX fix (operator-reported, plan-mode review first)

Paused the BUG-0017 queue at U12 to fix two chat-page design regressions the operator reported with three screenshots: (A) the page double-scrolls — the app header slides off the top and the composer floats mid-viewport with empty space below it, worse when the window is maximized; (B) opening a citation in the right column shows the snippet body scrolled past its own title, so the document title + "Open document" link are not visible. Reviewed v1's clean layout first (a pure `height:100%` chain, a self-limiting single scroll region, a `position:sticky` composer) and produced a 3-unit plan — L1 (height chain), L2 (pin citation header), L3 (reset citation body scroll). Operator approved → implementing one unit per turn.

### Root cause — Problem A (double scroll / floating composer / header off-screen)

The DOM chain from `#root` down is `#root` (height:100%) → `div.fui-FluentProvider` (the wrapper Fluent v9's `<FluentProvider>` renders in [FluentThemeBridge.tsx](../../src/frontend/src/theme/FluentThemeBridge.tsx)) → `.column` (CoralShell). The Fluent wrapper had **no height rule**, so it defaulted to `height:auto`; to compensate, `.column` used `height:100vh` instead of `height:100%`. `100vh` includes the strip under a horizontal scrollbar, so when a wide markdown table overflows the message column on the X axis the resulting scrollbar makes `100vh` taller than the visible viewport and the **page** (body) scrolls — sliding the header (which sits in `.column` above the scroll row) off the top and lifting the composer off the bottom, exposing the whitespace below. v1 never hits this because it chains `height:100%` end-to-end and never uses `100vh` on the shell.

### Done — L1 (repair the shell height chain, CSS + 1 test)

* **The Fluent wrapper now carries the height chain, and the shell uses `height:100%` (not `100vh`).** Tagged `<FluentProvider>` with a stable `className="appFluentRoot"` ([FluentThemeBridge.tsx](../../src/frontend/src/theme/FluentThemeBridge.tsx)) and added `.appFluentRoot { height: 100% }` to the global [tokens.css](../../src/frontend/src/theme/tokens.css) (with a comment explaining why the wrapper needs it). Switched `.column` from `height: 100vh` → `height: 100%` in [CoralShell.module.css](../../src/frontend/src/components/CoralShell/CoralShell.module.css), so the chain is now `html/body/#root 100% → .appFluentRoot 100% → .column 100%` (matching v1) and the page no longer double-scrolls. Hardened the chat grid by adding `min-height: 0` to `.main` in [ChatPage.module.css](../../src/frontend/src/pages/chat/ChatPage.module.css) so the message cell can shrink and its inner `.scroll` owns the overflow.
* **Why a class, not a `.fui-FluentProvider` selector:** targeting Fluent's internal griffel class is version-fragile; an app-owned class passed via `className` is stable and merges onto the same wrapper div.
* **Test** ([FluentThemeBridge.test.tsx](../../src/tests/frontend/theme/FluentThemeBridge.test.tsx)): added a case asserting the rendered Fluent wrapper carries the `appFluentRoot` class (locks the height-chain hook so a future refactor that drops it is caught). The CSS height values themselves are not jsdom-assertable (`css: false` in the vitest config, and jsdom computes no layout) — that half is a live-browser check, deferred like an integration test.

### Gates — L1

* `npx vitest run theme/FluentThemeBridge.test.tsx` — **4 passed** (3 prior + 1 new `appFluentRoot` assertion).
* `npx tsc -b` on the frontend source tree — **exit 0**.
* `npx eslint` scoped to [FluentThemeBridge.tsx](../../src/frontend/src/theme/FluentThemeBridge.tsx) + the test — **exit 0** each.
* `get_errors` clean on all four touched files.
* **Live visual — DONE (browser-measured, conclusive).** Drove the running dev server at `localhost:5273` with the Playwright browser tool and measured the computed height chain + page overflow directly (jsdom cannot, since `css:false` and it lays out nothing). Results below.

### Live verification — L1 (browser-measured, conclusive)

Measured `document.scrollingElement.scrollHeight - clientHeight` ("page overflow") and enumerated every element with an active overflow scrollbar, across four scenarios. **Page overflow was `0` in every case** — the body never scrolls; only the inner `.scroll` message region scrolls, and the header + composer stay pinned.

* **Empty chat (1360×768):** full chain resolves — `html/body/#root/.appFluentRoot` = 768px, `.column` = 768px (`overflow:hidden`), `.row/.shell/.main` = 712px (768 − 56px header). `pageOverflow = 0`, zero scrollers.
* **2000px content injected into the message column:** `pageOverflow = 0`; the **only** scroller is `div._scroll_` (1526px internal). `.column` stays 768px. Proves a long transcript scrolls only inside the message region — the body cannot overflow regardless of `scrollIntoView`.
* **Real 4-message conversation (sent through the live backend, real `scrollIntoView` firing):** `pageOverflow = 0` across six 1.5s samples; message region reports `atBottom: true` (auto-pins to the latest answer — the operator's "always stay at the bottom" ask).
* **Viewport sweep with 1800px content (1360×600, 1024×720, 1600×900, 1280×1024):** `pageOverflow = 0` at every size; `.column` height tracks the viewport exactly (600/720/900/1024). The fix holds at all window sizes, maximized or not.
* **Screenshot** (1200px striped filler, citation panel closed — the operator's exact scenario): header pinned at top, composer pinned at bottom with **no whitespace below it**, transcript scrolling only in the middle. Probe artifact removed after capture (not tracked).
* **Conclusion:** the layout the operator screenshotted as broken cannot be reproduced against the current served code — it was a **stale Fast-Refresh render** (the `.appFluentRoot` change is a React-provider edit HMR doesn't always fully re-apply). A hard reload (Ctrl+Shift+R) clears it. L1 is fully effective; both reported Problem-A symptoms (page double-scroll, floating composer/off-screen header) are resolved and the "stay at bottom on load" behavior already works.

### Next — L2 + L3 (citation panel — Problem B)

* **L2** — pin the citation header: in [CitationDetailPanel.module.css](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.module.css) drop `overflow-y:auto` on `.panel` (add `overflow:hidden`), keep the header/title/link as a non-scrolling head (`flex:0 0 auto`), and make `.body` the only scroll region (`flex:1 1 auto; min-height:0; overflow-y:auto`) so the title is always visible.
* **L3** — reset the body scroll on citation change: key the scrolling body by `citation.id` in [CitationDetailPanel.tsx](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.tsx) so a new citation opens at `scrollTop 0`; extend the CitationDetailPanel vitest. L2 + L3 may land together (same component / concern).

### Done — L2 + L3 (pin the citation head, scroll only the body, reset on change)

Landed together (one component, one concern). The whole `.panel` used to scroll (`overflow-y:auto`), so when a new citation replaced the old one in place, the panel's persisted `scrollTop` carried over and a new source opened scrolled past its own title + "Open document" link.

* **Pinned head, single scroll region** ([CitationDetailPanel.module.css](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.module.css)): `.panel` `overflow-y:auto` → `overflow:hidden`; `.header`, `.title`, `.link` each get `flex:0 0 auto` so the head never shrinks or scrolls; `.body` becomes the only scroller with `flex:1 1 auto; min-height:0; overflow-y:auto`. The title + link now stay visible no matter how far the reference text is scrolled — strictly better than v1, where the title scrolls away.
* **Body resets to the top on citation change** ([CitationDetailPanel.tsx](../../src/frontend/src/pages/chat/components/CitationDetailPanel/CitationDetailPanel.tsx)): added `key={citation.id}` to the body `<div>` so React remounts it on each new source, dropping any inherited scroll position. CSS-only pinning + a one-attribute key — no structural change to the working component.
* **Tests** ([CitationDetailPanel.test.tsx](../../src/tests/frontend/pages/chat/components/CitationDetailPanel/CitationDetailPanel.test.tsx)): added a `SwitchHarness` (two controls showing two different citations through the real reducer) and two cases — (a) the title + link sit outside the scrolling body (`body.contains(title/link) === false`), locking the head/body split; (b) switching the active citation yields a fresh body DOM node (`secondBody !== firstBody`) and an updated title, proving the `key` remount that resets scroll.

### Gates — L2 + L3

* `npx vitest run …/CitationDetailPanel.test.tsx` — **9 passed** (7 prior + 2 new).
* `npx tsc -b` on the frontend source tree — **exit 0**.
* `npx eslint` on the test file — **exit 0**.
* `get_errors` clean on all three touched files.
* **Live visual — deferred (recommend operator hard-reload + confirm):** open a reference chip and verify the "Citations" header + document title + "Open document" link stay pinned while only the snippet body scrolls, and that switching to another reference opens at the top. jsdom can't assert computed layout (`css:false`); the vitest covers the structural split + remount deterministically.

### Discovered debt — 2 pre-existing eslint errors in `CitationDetailPanel.tsx` (fixed this turn)

`npx eslint CitationDetailPanel.tsx` reported two errors on lines the L2/L3 diff did not touch (confirmed via `git diff` — the L2/L3 edits only added a docstring paragraph + the body `key`, which shifted these lines down). Both predated the rules and were latent (the live `get_errors` / Problems panel did not surface them). Operator approved fixing them in the same component:

1. **`no-restricted-imports`** — `import { MarkdownContent } from "../MarkdownContent";`. The panel lives one folder deep (`components/CitationDetailPanel/`), so the `../` cross-folder import tripped ADR 0015 (cross-folder imports must use the `@/` alias; only sibling `./X` is allowed). **Fixed:** `import { MarkdownContent } from "@/pages/chat/components/MarkdownContent";` (matches how sibling `MessageList.tsx` resolves the same module via `./MarkdownContent`).
2. **`@typescript-eslint/no-confusing-void-expression`** — the dismiss `onClick={() => dispatch({ type: ChatActionType.CloseCitation })}` returned the `void` of `dispatch`. **Fixed:** braced the body so the arrow returns nothing — `onClick={() => { dispatch({ type: ChatActionType.CloseCitation }); }}`.

**Gates after the fix:** `npx eslint CitationDetailPanel.tsx` **exit 0**; `npx tsc -b` **exit 0**; the CitationDetailPanel vitest **9 passed** (unchanged — the braced handler + aliased import are behavior-preserving).

### Double scroll still present — BUG-0045 opened (L1 conclusion was incomplete)

The operator reports the chat-page **double scroll is still present** after the L1 height-chain fix and a hard reload. That contradicts the "Live verification — L1" conclusion above, which read the broken layout as a stale Fast-Refresh render — the agent's Playwright measurements showed `pageOverflow = 0` across empty / long-content / real-conversation / viewport-sweep scenarios, but the automation environment evidently did not reproduce the operator's condition (candidate factors not yet ruled out: a specific browser/OS scrollbar mode or zoom level, a particular conversation/content state, or a layout path the headless viewport does not exercise). The height-chain hypothesis is therefore **incomplete**, not wrong-and-resolved.

Filed as **BUG-0045** (frontend, medium, open). The operator is taking ownership of the fix; this entry and the registry row exist so the issue is tracked durably rather than left in session memory. The L1 edits (`.appFluentRoot` height:100%, CoralShell `.column` 100vh→100%, ChatPage `.main` min-height:0) remain in place — they are independently correct and pass their `FluentThemeBridge` vitest — but they do not, on their own, resolve the operator-observed double scroll.
