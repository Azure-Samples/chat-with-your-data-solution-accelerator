---
title: CWYD v2 — Worklog 2026-06-16
description: Daily worklog for CWYD v2 on 2026-06-16. Covers the remaining-work summary doc, the post-crash Functions host restart, the full end-to-end trace that root-caused BUG-0034 (uploaded PDFs report success but never index because the Document Intelligence parser gets an empty AZURE_AI_SERVICES_ENDPOINT and the SDK rejects bearer auth on the non-HTTPS endpoint), BUG-0047 (admin panel 401s in local dev — the admin role gate's local-dev bypass keyed on both Easy Auth headers being absent, but the SPA now always forwards a default principal-id, so the bypass is re-keyed on the absent claims blob), BUG-0048 (admin document delete 503s because delete_by_source filtered on the non-filterable title field; fixed by paging and matching client-side like list_sources), and BUG-0049 (DOCX/Office uploads index nothing because the Document Intelligence parser only reads pages[].lines, which Azure leaves empty for pageless Office/HTML formats; fixed with a paragraph fallback that mirrors TextParser's per-paragraph chunking).
author: CWYD Engineering
ms.date: 2026-06-16
topic: how-to
keywords: worklog, daily log, BUG-0034, ingestion, batch_push, document intelligence, services_endpoint, AZURE_AI_SERVICES_ENDPOINT, poison queue, ServiceRequestError, non-TLS, bearer token, functions host, remaining-work-summary, v2, status, upload guard, 503, false success, queue-name drift, doc-processing, BUG-0047, requires_role, admin RBAC, Easy Auth claims, local-dev bypass, x-ms-client-principal, 401, BUG-0048, delete_by_source, AzureSearch, title not filterable, OData filter, client-side match, admin delete, BUG-0049, DocumentIntelligenceParser, docx, pageless, paragraphs, TextParser, paragraph fallback, prebuilt-layout, zero chunks
estimated_reading_time: 5
---

## Summary

Diagnosed **BUG-0034** end to end: an uploaded PDF reports `200`/success in the admin UI but never appears in the documents list. Traced the upload → enqueue → `batch_push` (download → parse → embed → push) pipeline and reproduced the failure through the exact `batch_push._execute` seam: every PDF dies in the Document Intelligence parse step because `settings.foundry.services_endpoint` (`AZURE_AI_SERVICES_ENDPOINT`) is empty, so the SDK rejects bearer-token auth on the non-HTTPS endpoint. Verified the pipeline succeeds (4 chunks pushed, doc then lists) once the endpoint is present.

## Planned

* Write a grouped, persisted summary of the remaining v2 work.
* Recover the Functions host (it had died overnight) and confirm both backend + host are healthy.
* Run a full trace of BUG-0034 to root cause: upload contract, queue state, poison queue, and the `batch_push` failure.
* Record the BUG-0034 verdict in [bugs.md](../bugs.md) and this worklog.

## Done

* **Remaining-work summary doc.** Created [remaining-work-summary.md](../remaining-work-summary.md): an `## Overview`, a recently-completed list, the open issues as a table then grouped by Functions / Backend / Frontend, an operational/non-blocking section, and suggested next steps. Lint clean.
* **Functions host recovery.** The local Functions host had crashed overnight on a DNS/credential loss (`getaddrinfo failed` to the login endpoint; `AzureCliCredential` could not refresh; a downstream Cosmos singleton-lock `409`) — an environmental failure, not a code defect. Confirmed DNS + `az` auth healthy and restarted the host; all five functions (`add_url`, `batch_start`, `health`, `search_skill`, `batch_push`) registered and the worker initialized.
* **BUG-0034 full trace (root-caused).** See the Bugs section below and the [BUG-0034 detail section](../bugs.md). The decisive steps:
  * Confirmed backend-enqueue and Functions-trigger wiring are identical (queue name, storage account, container all match; identity-based auth).
  * Inspected the queue: main queue empty, **poison queue holding 22 messages** (14 `.pdf` + 8 extension-less, all dated 2026-06-10) with structurally valid envelopes — so the failure is downstream of message validation.
  * Ran `functions.batch_push.blueprint._execute` in-process against `Benefit_Options.pdf`: reproduced `azure.core.exceptions.ServiceRequestError: Bearer token authentication is not permitted for non-TLS protected (non-https) URLs` inside `DocumentIntelligenceParser.parse` → `begin_analyze_document`. Confirmed `services_endpoint` resolved to `""` (`len 0`, empty scheme) — the `v2/.env` the script loads is missing `AZURE_AI_SERVICES_ENDPOINT`.
  * Re-ran the same seam with the Functions host's `local.settings.json` values loaded (which carry an `https` `AZURE_AI_SERVICES_ENDPOINT`): **succeeded — 4 chunks pushed**. `Benefit_Options.pdf` then appeared in `GET /api/admin/documents`, closing the loop on the symptom.
* **BUG-0034 diagnosis doc.** Created [bug-0034-diagnosis.md](../bug-0034-diagnosis.md): the readable end-to-end narrative (symptom, a Mermaid sequence diagram of the upload → enqueue → `batch_push` pipeline, the root-cause chain, the four-step trace, classification, and the three-unit fix plan). Lint clean.
* **BUG-0034 fix, unit 1 — fail-fast guard (landed, test-first).** `DocumentIntelligenceParser._get_client` now rejects an empty or non-HTTPS `services_endpoint` with an actionable `ValueError` that names `AZURE_AI_SERVICES_ENDPOINT`, raised before the SDK client is constructed (after the injected-client seam, so tests that inject a client are unaffected). Replaces the opaque SDK "non-TLS" failure and stops the silent PDF-poisoning. Five new tests (empty endpoint, three non-HTTPS forms, injected-seam bypass) — `22 passed`; `pyright --strict` `0/0/0` on the parser.
* **BUG-0034 fix, unit 2 (b) — config backfill (landed).** The tracked template `v2/.env.sample` was missing `AZURE_AI_SERVICES_ENDPOINT` entirely — the source of the gap, since every `v2/.env` copied from it inherited the omission. Added the documented key (empty, placeholder-safe) to `v2/.env.sample` immediately before `AZURE_AI_PROJECT_ENDPOINT`, and backfilled the real value into the gitignored `v2/.env` from the azd env store (`azd env get-value`, never echoed into output or a tracked file). Verified: exactly one `AZURE_AI_SERVICES_ENDPOINT` line in `.env`, and `AppSettings.foundry.services_endpoint` now resolves to an `https` URL (`is_https True`, `has_netloc True`). The backend synchronous `ingest_url` path and the ingestion scripts now get a valid endpoint instead of `""`.
* **BUG-0034 fix, unit (d) — upload-boundary honest error (landed, test-first).** The admin upload endpoint reported a green `200` even when the deployment could not possibly index the file. `upload_document_endpoint` ([admin.py](../../src/backend/routers/admin.py)) now returns `503` when a Document-Intelligence-routed file is uploaded while `AZURE_AI_SERVICES_ENDPOINT` is empty or non-HTTPS — checked at the HTTP boundary, before reading the body or enqueueing, so the operator is told the deployment cannot index the file instead of seeing a false success. The guard is scoped by a registry-identity probe (`ingestion_parsers_registry.registry.get(ext) is registry.get("pdf")`) so it fires only for extensions that route to the DI parser (PDF / DOCX) and never over-blocks the local parsers (txt / md / html) — keeping dispatch in the registry per Hard Rule #4. Two new tests in [test_admin.py](../../tests/backend/test_admin.py) (DI file + blank endpoint → `503` + sentinel not called; txt + blank endpoint → `200`/queued), full admin suite `111 passed`, `pyright` `0/0/0` on the router.
* **BUG-0034 — second contributing factor found (queue-name drift, local dev).** While preparing the (c) poison drain, found the Functions host's gitignored `local.settings.json` had drifted to queue `doc-processing-local`, while the azd env store and the backend `v2/.env` both use the canonical `doc-processing`. The backend enqueues to `doc-processing` but the local host listens on `doc-processing-local` (same storage account, no Azurite), so locally the host never consumes the backend's messages and the doc never lists — a second cause of the same symptom in local dev. Not a shipped defect (azd wires both tiers from one env in a real deployment). Folded into the BUG-0034 entry per the chosen logging path; the fix is to align the local value before the (c) drain.
* **BUG-0034 fix, unit (c) part 1 — queue realignment + main drain (landed, verified).** Aligned the Functions host `local.settings.json` queue name from `doc-processing-local` to the canonical `doc-processing` (matching the azd store + backend `v2/.env`), killed the stale host (it had loaded the old queue name into memory before the edit) and restarted it. It immediately dequeued all 6 backend-enqueued messages and processed them through the now-valid DI endpoint — all 6 `Succeeded` (22–27 s each). Verified: the canonical `doc-processing` queue went 6→0, and `GET /api/admin/documents` grew from 2 to 5 distinct sources (`Benefit_Options.pdf`, `PerksPlus.pdf`, `employee_handbook.pdf`, `role_library.pdf`, and the Woodgrove cyber-risk policy). The false-green symptom is resolved end-to-end for the backend-enqueued docs.
* **BUG-0034 fix, unit (c) part 2 — poison cleanup (landed, operator-confirmed).** A non-destructive peek of the two poison queues showed all `.pdf` messages reference docs already in the index (the 5 sources), and that 8 of the 22 canonical-poison messages are base64 Event Grid blob-event envelopes `batch_push` cannot process. Since a redrive would re-index present docs and immediately re-poison the envelopes, cleared both poison queues with operator confirmation: `doc-processing-poison` 22→0, `doc-processing-local-poison` 10→0. Final state: all four ingestion queues empty. Removed the three temp diagnostic scripts. **BUG-0034 fixed.**
* **BUG-0047 fix — admin local-dev bypass re-keyed on absent claims (landed, test-first).** Every `/api/admin/*` call was returning `401 "Missing client principal claims; Easy Auth claims header required."` in local dev, so the admin UI could not load. Root cause: a regression from BUG-0046 — the SPA's shared `userIdHeaders()` seam now forwards a default `x-ms-client-principal-id` on every call, but `requires_role("admin")._checker` ([dependencies.py](../../src/backend/dependencies.py)) only bypassed to the synthetic `local-dev` admin when **both** Easy Auth headers were absent (`if not principal_id and not claims_raw`), so the always-present id header defeated the bypass and the gate fell through to the no-claims `401`. Fixed by keying the bypass on the **absent `x-ms-client-principal` claims blob** (the sole authority for the role check): in `local` a missing claims blob → `local-dev`, in any non-local environment it still fails closed (`401`). One method fixes every admin endpoint at once; the frontend is unchanged (it must never forge or forward the claims blob). New regression test `test_requires_role_falls_back_to_local_dev_when_id_present_no_claims_in_local`; full `test_dependencies.py` `31 passed`, `pyright` `0/0/0`. Verified live: `/api/admin/status` now returns `200` via both the direct backend (`:8000`) and the `:5273` Vite proxy with only the id header.
* **BUG-0048 fix — admin document delete `503` → client-side title match (landed, test-first).** `DELETE /api/admin/documents/{source}` returned `503 "Azure dependency temporarily unavailable."` for every delete (observed on `Benefit_Options.pdf`). Root cause: `AzureSearch.delete_by_source` ([azure_search.py](../../src/backend/core/providers/search/azure_search.py)) collected the chunk ids to delete via a server-side OData `$filter` `title eq '<source>'`, but the deployed index defines `title` as a `SearchableField` — searchable but **not** filterable — so Azure Search rejected the query (`'title' is not a filterable field`) and the app-level handler sanitised the `AzureError` to `503`. Not a name-vs-id problem: the delete already removes **by id** (it calls `delete_documents`); only the id-discovery step failed. Fixed by mirroring the sibling `list_sources`: page every chunk client-side (`select=["id", "title"]`, auto-paging, no `$filter`) and match `source` by exact Python equality, then delete the matched ids in ≤1000 batches. This also closes the OData-injection seam the old filter opened (the filename is no longer interpolated into any OData expression). No index / reindex / frontend change. Tests rewritten to the client-side contract (all three failed pre-fix, confirming test-first); full `test_azure_search.py` `34 passed`, `pyright` `0/0/0`. Verified live: delete → `200 {"deleted":4}`, re-delete of the now-absent source → `404` (not `503`).
* **BUG-0049 fix — DOCX / Office uploads index nothing → paragraph fallback (landed, test-first).** Uploading a `.docx` reported success but never indexed. Root cause: `DocumentIntelligenceParser.parse` ([document_intelligence_parser.py](../../src/functions/core/parsers/document_intelligence_parser.py)) built chunks only from `result.pages[*].lines[*].content`, but Azure Document Intelligence treats Office (DOCX/PPTX/XLSX) and HTML inputs as **pageless** — it returns the text in `result.paragraphs` and leaves `pages[*].lines` empty. Confirmed live against the service: the sample DOCX returned `pages=1`, per-page `lines=[0]`, `paragraphs=848`, `content=29335`; a sample PDF returned `pages=4`, `lines=[4,6,34,25]`. The page loop therefore produced **zero** chunks for DOCX, and `batch_push_handler` logged its zero-chunk info line and returned `[]` — a silent non-index behind a green upload. Fixed by adding a paragraph fallback: when the page pass yields zero chunks, emit one `Chunk` per `result.paragraphs` entry (stripping empties, keeping `index` dense), mirroring `TextParser`'s per-paragraph chunking. The fallback fires only when the page pass produced nothing, so PDFs / images keep their one-chunk-per-page shape and never double-emit. Three new tests (paragraph fallback when pages are empty; pages-with-text suppress the fallback / no duplication; empty paragraphs skipped with dense indices) — the two behavior tests failed pre-fix; full `test_document_intelligence_parser.py` `25 passed`, `pyright` `0/0/0` on the parser, shared gates `237 passed`. Verified live with the real parser over the sample files: PDF → 4 chunks (unchanged), DOCX → 848 chunks. No infra / model / frontend change.
* **BUG-0049 follow-up — silent zero-chunk now visible (landed, test-first).** The user flagged that the failed DOCX "still go success." The upload `200` is the async-pipeline's "queued," not "indexed" (parse runs later in the Functions tier), so it cannot reflect the parse outcome — but the downstream zero-chunk outcome was being swallowed at `info` level in `batch_push_handler` ([handler.py](../../src/functions/batch_push/handler.py)), so an uploaded-but-unindexed file left no visible trace. Elevated that log from `info` → `warning` (same structured `extra`: `operation`, `container`, `blob_filename`, `ingestion_job_id`) so a genuine zero-chunk file (corrupt upload, image-only PDF, a future parser regression) surfaces in App Insights / logs. Deliberately **not** a raise: a legitimately text-free input also yields zero chunks and raising would poison-loop it forever — the handler cannot distinguish a parser failure from a file with no extractable text. The existing `test_zero_chunks_short_circuits_embed_and_search` was tightened to capture at `WARNING` and assert `levelname == "WARNING"` (failed pre-fix); `test_handler.py` `3 passed`, `pyright` `0/0/0`, process-narrative gate `117 passed`.

## Bugs

* [BUG-0034](../bugs.md) — **root-caused (stays open for the fix).** When `AZURE_AI_SERVICES_ENDPOINT` is empty, `DocumentIntelligenceParser._get_client` builds the endpoint `"/"` (no `https` scheme), and the Azure SDK bearer-token policy rejects auth before any request is sent, so `batch_push` throws on every PDF → retries → poison queue. Upload + enqueue still return `200`, so the doc never indexes and never lists. The host's `local.settings.json` now carries the endpoint and the pipeline works end-to-end; the 22 poison messages are stale pre-fix failures. Classification: configuration gap compounded by a code-hardening gap (cryptic error + silent poison + no fail-fast). Fix plan: (a) fail-fast guard on empty/non-HTTPS `services_endpoint`; (b) backfill `AZURE_AI_SERVICES_ENDPOINT` into `v2/.env`; (c) redrive the 22 poison messages.
* [BUG-0034](../bugs.md) — **upload-boundary `503` guard landed (unit d); queue-name drift folded in.** The false-`200` is now an honest `503` at the upload boundary when a DI-routed file is uploaded while `AZURE_AI_SERVICES_ENDPOINT` is unset/non-HTTPS, so the UI no longer claims success for a file the deployment cannot index. A second, local-dev contributing factor (Functions host `local.settings.json` queue drifted to `doc-processing-local` vs canonical `doc-processing`) was found and folded into BUG-0034 rather than opened as a new bug. The queue name was then aligned, the host restarted on the canonical queue, and the 6 stuck main-queue messages drained and verified (documents list 2→5, canonical queue 6→0). Both poison queues were then cleared (operator-confirmed; all referenced PDFs already indexed, plus 8 unprocessable Event Grid envelopes), leaving all four ingestion queues empty. **Fixed 2026-06-16.**
* [BUG-0047](../bugs.md) — **fixed.** Admin panel 401s in local dev (`Missing client principal claims`). Regression from BUG-0046's identity-forwarding: the SPA now always forwards a default `x-ms-client-principal-id`, which defeated the admin gate's "both headers absent" local-dev bypass. Fixed by keying the bypass on the absent `x-ms-client-principal` *claims* blob (the sole role authority) — `local` → `local-dev`, non-local → fail-closed `401`. Frontend unchanged (the claims blob must stay server-injected, never browser-forged). One backend method fixes every admin endpoint at once. **Fixed 2026-06-16.**
* [BUG-0048](../bugs.md) — **fixed.** Admin document delete `503`d for every file. `AzureSearch.delete_by_source` discovered the chunk ids via a server-side OData `$filter` on `title`, which the deployed index marks searchable-but-not-filterable, so Azure rejected the query and the handler returned `503`. Fixed by paging client-side and matching `title` in Python (mirrors `list_sources`); also removes the OData-injection seam. No index / reindex / frontend change. Verified live (`200 {"deleted":4}`, then `404` on the now-absent source). **Fixed 2026-06-16.**
* [BUG-0049](../bugs.md) — **fixed.** DOCX (and any Office / HTML) upload indexed nothing. `DocumentIntelligenceParser.parse` only read `pages[].lines`, which Azure Document Intelligence leaves empty for pageless Office/HTML formats (the text is in `result.paragraphs`), so the page loop produced zero chunks and `batch_push` silently returned `[]` behind a green upload. Confirmed live: DOCX `pages=1`/`lines=[0]`/`paragraphs=848` vs PDF `pages=4`/`lines=[4,6,34,25]`. Fixed by a paragraph fallback that fires only when the page pass yields zero chunks, mirroring `TextParser`'s per-paragraph chunking; PDFs/images keep one-chunk-per-page (no double-emit). Verified live with the real parser: PDF → 4 chunks, DOCX → 848 chunks. No infra / model / frontend change. **Fixed 2026-06-16.**

## Decisions

* **Traced through the `_execute` seam rather than waiting on the live queue.** The restarted host was not draining the main queue during the trace (a local-dev listener artifact after the overnight crash), so a queued message sat undrained. Running the single monkeypatch seam `_execute` in-process gives the real exception with a full traceback immediately and exercises the exact production orchestration — a faster, deterministic root-cause path than fighting the queue-trigger backoff.
* **BUG-0034 stays open.** The host config is incidentally correct now, but the proper code fix (fail-fast validation) and the `v2/.env` backfill have not landed; those are separate test-first units. The trace was a diagnosis, not a fix.
* **Honest error at the boundary the operator sees, scoped precisely.** The false-green is fixed where the operator experiences it — the synchronous `POST /api/admin/documents` response — not only in the async Functions tier (unit a). Chose a registry-identity probe (`registry.get(ext) is registry.get("pdf")`) over a hardcoded `{"pdf","docx"}` set (would violate Hard Rule #4 and silently miss any future DI-routed extension) and over a blunt block-all-uploads-when-endpoint-missing (would itself be "incorrect logic" — it would reject txt / md / html that parse locally and need no endpoint). Kept the guard in the router (config-presence guards return `HTTPException` directly there) rather than introducing a parser-capability protocol (a structural, multi-unit change — deferred, ask-first).
* **Queue-name drift logged inside BUG-0034, not a new bug.** It is a second cause of the same observed symptom and is a local-dev config drift, not a shipped defect, so it folds into the existing BUG-0034 entry rather than opening BUG-0035.
* **BUG-0047 is a backend fix, not a frontend one.** The user's instinct ("the header is missing from the frontend") is half-right — the *claims* header is what the gate needs, but that blob is the server-side trust boundary for admin RBAC and must never be forged or forwarded by the browser (a browser-supplied claims blob would let anyone assert the admin role; this is the documented BUG-0046 Security note). The frontend's modular `userIdHeaders()` seam already forwards everything it legitimately can (the partition id). So the one correct, modular fix is the single backend conditional in `requires_role._checker`, which fixes all admin endpoints simultaneously — not a per-call frontend header.
* **Upload `503` was operational, not a code defect — the BUG-0034 guard worked as designed.** A live `POST /api/admin/documents` returned the `503` "Document parsing is not configured" guard because the running backend had been launched **without** `--env-file .env`. Pydantic-settings sub-`BaseSettings` models don't inherit `env_file`, so `services_endpoint` was empty in the cached `get_settings()` singleton, and `--reload` never re-reads `.env`. Stopped the stale process and relaunched with `uv run --env-file .env uvicorn backend.app:app --app-dir src --host 127.0.0.1 --port 8000 --reload`; settings then resolved `services_endpoint` to an `https` URL (`is_https True`, `len 52`) and `/api/health` + `/api/admin/status` returned `200`, so the upload guard passes. Conclusion: the guard is correct (it surfaced a genuinely unindexable config); the fix is to start the backend with the env file, not to change the guard. No new bug opened. (Also noted: a transient git merge-conflict marker had appeared in `services/conversation.py` mid-session causing a `SyntaxError`; a grep over `v2/src` confirmed no markers remain.)
* **Delete keeps the source-name contract; the fix needs no server-side filter, schema change, or API change.** The user's instinct ("use the document id instead of the name") is directionally right — deletion must act on chunk ids — but the delete *already* deletes by id; only the id-*discovery* step failed. The admin documents list surfaces *sources* (one logical document = N chunks, each with its own id), so the frontend has no per-chunk id to send and the `DELETE /…/{source}` route is correct. The minimal, modular fix mirrors the proven `list_sources` workaround already in the same provider (page + match `title` client-side) rather than (a) changing the index schema to make `title` filterable — a field drop/recreate + full reindex, heavyweight and structural — or (b) changing the API to take ids, which the frontend cannot feed. Matching client-side additionally eliminates the OData-injection seam the previous `title eq '<source>'` filter opened on a user-supplied filename.
* **DOCX fix reuses Document Intelligence's own `paragraphs`, not a new chunker or a single giant chunk.** v2 deliberately removed the chunker primitive (parsers own chunking per format) and `batch_push` embeds each `Chunk.content` directly with no downstream re-chunking, so the parser must bound chunk size itself. Three options were on the table: (a) emit one chunk from the flat `result.content` — rejected: a single ~29 KB chunk strains the embedding token ceiling and gives one undifferentiated vector with poor retrieval; (b) re-split `result.content` with a regex like `TextParser` — rejected: re-implements splitting the service already did; (c) emit one chunk per `result.paragraphs` entry — chosen: Document Intelligence already returns the DOCX pre-split into 848 semantic paragraphs, which is exactly the unit `TextParser` uses, so it bounds size, matches the codebase convention, and invents no abstraction. Gating the fallback on "the page pass produced zero chunks" (not on a format/extension check) keeps it format-agnostic and guarantees PDFs — which return both `pages[].lines` and `paragraphs` — never double-emit. No infra, model, index, or frontend change; the `prebuilt-layout` model and DI endpoint are unchanged.
* **"Still showed success" is the async contract, not a second bug — hardened visibility instead of changing the contract.** The upload `200` means "accepted + enqueued," because parse / embed / index run asynchronously in the Functions `batch_push` consumer after the HTTP response has already returned; the synchronous boundary structurally cannot report the parse outcome, and the BUG-0034 guard already does the only sensible synchronous pre-flight (refuse a type the deployment can't index at all). The real gap was that a downstream zero-chunk outcome was logged at `info` and otherwise swallowed. Chose the minimal in-scope fix — elevate that log to `warning` so it is visible in telemetry — over the two heavier alternatives: (a) raising on zero chunks (rejected — poison-loops legitimately text-free files), and (b) a per-document processing-status store the admin UI could display (a real improvement, but structural / multi-unit — deferred to its own scoped piece, not done inline). The user picked the log-hardening option.

## Next

* **BUG-0034 fix, unit 1 — DONE.** Fail-fast guard landed in `DocumentIntelligenceParser._get_client` (see Done above).
* **BUG-0034 fix, unit 2 (b) — DONE.** `AZURE_AI_SERVICES_ENDPOINT` added to the tracked `v2/.env.sample` template and backfilled into the gitignored `v2/.env` (see Done above).
* **BUG-0034 — CLOSED.** All four units landed: (a) fail-fast DI guard, (b) `.env.sample`/`v2/.env` backfill, (c) queue realignment + main drain + poison cleanup, (d) upload-boundary `503`. Pipeline verified indexing end-to-end (documents list 2→5); all ingestion queues empty.
* **Candidate follow-up (separate unit):** the same false-green risk applies to `reprocess_all_endpoint` and `ingest_url_endpoint` (both enqueue without checking the parse prerequisite). Consider extending the same DI-routed parse-config guard there.
* **Candidate new bug:** the `aiohttp` "Unclosed client session / Unclosed connector" warnings seen during the `_execute` run — the DI client / embedder is not closed on the `batch_push` path (low severity).
* **Temp trace scripts — DONE.** Removed `v2/scripts/_tmp_bug0034_*.py` and the `$TEMP\cwyd-bug0034-*.ps1` helpers.
* **BUG-0047 — CLOSED.** The admin local-dev bypass now keys on the absent claims blob; `/api/admin/*` works in local dev again (verified `200` live) and production still fails closed on a missing claims header.
* **BUG-0048 — CLOSED.** `delete_by_source` now pages + matches `title` client-side; admin delete verified `200` (deleted) / `404` (absent) live, no more `503`. No index or frontend change.
* **BUG-0049 — CLOSED.** `DocumentIntelligenceParser.parse` now falls back to per-paragraph chunks for pageless Office/HTML formats; verified live (DOCX → 848 chunks, PDF → 4 chunks). No infra / model / frontend change.
* **Zero-chunk visibility — DONE.** `batch_push_handler` now logs a zero-chunk outcome at `warning` (was `info`), so an uploaded-but-unindexed file is visible in telemetry. Not a raise (poison-loop risk for text-free files).
* **Candidate follow-up (separate, structural):** a per-document processing-status surface (`processing` / `indexed` / `0 chunks — not indexed`) the admin UI can display, so the *user* sees the real async outcome rather than only the upload's "queued" `200`. Multi-unit (status store + API field + frontend) — scope as its own piece if desired.
* **Candidate new bug (low severity, unchanged):** the `aiohttp` "Unclosed client session / Unclosed connector" warnings reappeared while running the parser standalone in the BUG-0049 verification — the DI client is not closed on the standalone parse path. Same low-severity follow-up already noted under BUG-0034; not opened as a new bug.

## QA deploy — frontend + backend + functions

Rolled the latest working-tree code and refreshed variables to the `<AZD_ENV_NAME>` (cosmosdb) environment and handed the three live endpoints to QA. `azd` builds from the working tree, so the two uncommitted frontend UX features shipped in this deploy: (1) auto-refresh of the admin source list after a delete, (2) refresh of the chat history panel when a new conversation is created. Both landed test-first earlier in the session.

### Pre-deploy gates

* **Frontend:** `vitest` 560 passed / 43 suites; `tsc --noEmit` clean for both `src/frontend` and `src/tests/frontend`; `eslint` 0 errors (1 pre-existing advisory `react-refresh/only-export-components` warning on `DeleteData.tsx`).
* **Backend:** `pyright src/backend src/functions` → `0 errors, 0 warnings, 0 informations`. `pytest` → 2395 passed, 1 skipped, **2 pre-existing failures** (frontend-only edits cannot affect backend Python). Per the user's call, both were logged as tracked debt in [development_plan.md](../development_plan.md) §0.1 rather than fixed inline (Hard Rule #12): `ADMIN-ROUTER-PROBE-KEY-REGRESSION` (route-only AST gate regressed by the BUG-0034 unit-(d) `_DI_PARSER_PROBE_KEY` constant) and `FOUNDRY-IQ-CHAT-METADATA-SERIALIZATION-DEBT` (`_to_openai_messages` emits a `metadata: {}` key the test does not expect). Both are runtime-benign.

### Provision

`azd provision --no-prompt` → **SUCCESS** (≈3m49s). Idempotent reconcile; postprovision hook reported `AZURE_DB_TYPE='cosmosdb'` (postgres setup skipped), search index `cwyd-index` already present, knowledge base `cwyd-kb` ready.

### Deploy — three ACR-pull IaC gaps had to be cleared by hand

`azd deploy --all` failed twice before succeeding; all three blockers are infrastructure-as-code gaps where the Bicep does not configure ACR pull/push wiring, so a clean `azd provision` (run first to refresh variables) left the registry unwired:

1. **Push 401 — ACR `azureAdAuthenticationAsArmPolicy` disabled.** `oauth2/exchange` returned `401 token validation failed` even though the signed-in principal held `AcrPush`. Root cause is the Basic-SKU ACR default (policy disabled), which gates the ARM-path token exchange. **Already tracked** as `ACR-AAD-AS-ARM-BICEP-DEBT` (surfaced 2026-06-08); today's provision re-applied the AVM default, re-disabling it. Re-cleared by hand: `az acr config authentication-as-arm update -r cr<SUFFIX> --status enabled`.
2. **Backend revision pull `UNAUTHORIZED` — Container App had no `registries` block.** After the push succeeded, the backend revision update failed `ContainerAppOperationError` (`template.containers.backend.image ... UNAUTHORIZED: authentication required`) because the backend Container App ships no managed-identity → registry credential mapping, so ACA attempted an anonymous pull. The UAMI held `AcrPull` and the placeholder image was running, but the binding was absent. **New debt this turn:** `BACKEND-CA-ACR-REGISTRIES-BICEP-DEBT` (§0.1). Cleared by hand: `az containerapp registry set -g <RESOURCE_GROUP> -n ca-backend-<SUFFIX> --server cr<SUFFIX>.azurecr.io --identity <UAMI_RESOURCE_ID>`.
3. **`azd config set auth.useAzCliAuth true`** — set so azd delegates to the `az` credential (machine-local azd config, not a repo change). Not the decisive fix on its own; the two ACR-side fixes above were.

After the two ACR fixes, `azd deploy --all` → **SUCCESS** (≈5m56s): backend, frontend, and function all Done.

### Smoke verification (all green)

| Probe | Status | Detail |
|---|---|---|
| backend `/api/health` | 200 | `status:pass` (`foundry_iq`:pass, `database`:pass) |
| backend `/api/health/ready` | 200 | `status:pass` |
| backend `/openapi.json` | 200 | `CWYD v2 backend` 3.1.0, paths present |
| frontend shell | 200 | SPA HTML |
| function host root | 200 | Function App up |

### Notes

* **Both ACR-pull gaps recur on every `azd provision`** because the Bicep is the source of truth and the manual `az` changes are wiped on the next reconcile. They should be fixed together in one infra-hardening turn (`registries:` param on the `backendContainerApp` AVM call + `azureAdAuthenticationAsArmPolicy: enabled` on the registry) — both tracked in §0.1, both flagged structural / Hard Rule #10 (ask first). Until then, an operator who re-provisions must re-apply both `az` commands before `azd deploy`.
* **No git actions taken** — working tree left modified for the user to review and commit.

### Frontend container was not actually deployed — caught post-handoff, fixed by hand

The **frontend shell 200 / SPA HTML** row above was a false positive. `azd deploy frontend` (service `frontend`: `host: appservice` + `docker:` block) completed in ~18s with **no Docker build** and the App Service deployment log recorded `"Created via a push deployment"` — azd did a **zip/code push** to the container App Service instead of building + pushing the `Dockerfile.frontend` image. The App Service stayed on Azure's `mcr.microsoft.com/appsvc/staticsite:latest` placeholder ("Hey, App Service developers!"), which itself returns `200` + HTML — so the status-only probe could not tell it apart from the real SPA, and the user opened the site to find the placeholder. ACR held no frontend image newer than 2026-06-08, and every frontend image carries a manual `cwyd-<date>-<sha>` tag (never an `azd-deploy-*` tag) — confirming `azd deploy` has never built this container (the backend, `host: containerapp`, builds + pushes correctly).

**Fixed by hand (QA unblocked):** (1) `az acr build` of today's code in ACR with `VITE_BACKEND_URL` baked (`vite build` → `dist/`, 2987 modules; pushed `cwyd-<date>-<sha>` + `latest`); (2) `az webapp config container set` to point `app-frontend-<SUFFIX>` at the new image; (3) `az resource update ... properties.acrUserManagedIdentityID=<AZURE_UAMI_CLIENT_ID>` so it pulls via the UAMI (the system-assigned identity lacks `AcrPull`); (4) `WEBSITES_PORT=80` + `az webapp restart`. **Verified live:** root serves the real SPA (`<title>CWYD v2</title>`, references `/assets/index-*.js`, placeholder text absent), JS bundle loads `200` (1.24 MB), baked backend URL present in the bundle.

**New debt:** `FRONTEND-APPSERVICE-AZD-CONTAINER-DEPLOY-DEBT` (§0.1) — `azd deploy frontend` does not build/push the container; structural fix needed (move to `host: containerapp`, or a deploy hook, or fix the appservice container wiring). Hard Rule #10 ask-first. **Smoke-probe lesson:** a frontend health probe must assert SPA-specific content (`/assets/index-*.js` or `<title>CWYD v2</title>`), never just `200` + non-empty HTML, because the App Service placeholder satisfies the latter.

### BUG-0050 — Admin button missing in the deployed app (relative admin URLs hit the SPA catch-all)

After the frontend container was deployed for real, QA reported the **Admin** button never appears and "the status is shown 200 but the response is html".

**Diagnosis (live probes).** `GET <frontend-host>/api/admin/status` → `200` `<!doctype html>…` — the SPA catch-all in [frontend_app.py](../../src/frontend/frontend_app.py) (`@app.get("/{full_path:path}")`) returns `index.html` for any non-file path. `GET <backend-host>/api/admin/status` → `200` JSON (`{"orchestrator_name":"agent_framework",…}`), with or without an `x-ms-client-principal-id` header (this deployment is `auth_enforced:false`, so the admin gate does not block the status read). Root cause: the admin REST client ([admin.tsx](../../src/frontend/src/api/admin.tsx)) was the **only** frontend fetch client issuing **relative** `/api/admin/*` URLs — every sibling (`streamChat`, `conversationHistory`, `HistoryPanel`, the `App.tsx` health probe, `documentHref`) prepends `VITE_BACKEND_URL`. In the split-host topology a relative admin URL resolves against the frontend origin → HTML → `getAdminStatus()`'s `response.json()` throws → the `App.tsx` availability effect sets `adminAvailable=false` → the `Header` hides the button. Filed as **BUG-0050** (frontend, high). Not an auth/backend defect — the backend already returns JSON for the status read; the only fault is client-side URL construction.

**Fix (test-first).** Added the `backendUrl()` (reads `VITE_BACKEND_URL` at call time, empty when unset) + `apiUrl(path)` (joins the base with the API path, trailing slash trimmed) seam the sibling clients already use to `api/admin.tsx`, and prepended `apiUrl(...)` to all 8 admin fetch sites (`getAdminStatus`, `addDocumentUrl`, `uploadDocument`, `reprocessAll`, `listDocuments`, `deleteDocument`, `getAdminConfig`, `patchAdminConfig`; `resetAdminConfig` delegates to `patchAdminConfig`). When `VITE_BACKEND_URL` is unset the base is empty, so the relative-URL behavior and the existing unit assertions are unchanged; when set, admin calls target the backend origin like every other client. New unit test `prepends VITE_BACKEND_URL to the status route when set` (+ `vi.unstubAllEnvs()` in that block's `afterEach`); `vitest run api/admin.test.tsx` → **68 passed**. No backend / auth / infra change.

**Redeploy.** The base is baked at build time, so the frontend image was rebuilt and the App Service container repointed (same manual recipe as the container-deploy fix above): (1) `az acr build` of today's code in ACR with `VITE_BACKEND_URL` baked → pushed tag `cwyd-<date>-bug0050` + `latest` (run succeeded, ~2m20s); (2) `az webapp config container set` to point `app-frontend-<SUFFIX>` at the new tag; (3) `az webapp restart`. The UAMI pull creds (`acrUseManagedIdentityCreds=true`, `acrUserManagedIdentityID=<AZURE_UAMI_CLIENT_ID>`) and `WEBSITES_PORT=80` from the container-deploy fix persist, so no re-assert was needed. **Verified live:** the served bundle hash changed (`index-jEORG4DF.js` → `index-CoJ0TIv6.js`), the new bundle (1.24 MB) embeds the backend host and the admin paths (`/api/admin/status`, `/api/admin/config/effective`), and `GET <backend-host>/api/admin/status` returns `200` `application/json` (`{"orchestrator_name":"agent_framework",…}`). The browser running the new bundle now targets the backend origin for admin calls, so `getAdminStatus()` resolves and the **Admin** button renders. (The frontend-host catch-all is unchanged by design, so `GET <frontend-host>/api/admin/*` still returns SPA HTML — that is expected and not a regression.)

### BUG-0051 — Admin document upload 503s (backend container missing storage env vars)

With the Admin button now rendering, QA tried to upload a document and hit `503 {"detail":"Document storage is not configured for this deployment."}` ("I thought you configured all the variables").

**Diagnosis (live).** The backend admin endpoints ([admin.py](../../src/backend/routers/admin.py)) gate upload / URL ingest / reprocess on `settings.storage.documents_container` **and** `settings.storage.doc_processing_queue` being non-empty, and `GET /api/files/{filename}` ([files.py](../../src/backend/services/files.py)) needs the documents container; `StorageSettings` ([settings.py](../../src/backend/core/settings.py), `env_prefix="AZURE_"`) defaults every storage field to `""`. `az containerapp show` on the backend listed **none** of `AZURE_STORAGE_ACCOUNT_NAME` / `AZURE_DOCUMENTS_CONTAINER` / `AZURE_DOC_PROCESSING_QUEUE` in its container `env`. Root cause: the `backendContainerApp` module in [main.bicep](../../infra/main.bicep) never wired the storage vars — they are set **only** on the function app block (and emitted as stack outputs) — so the deployed backend resolved all three to empty and the gate tripped. Filed as **BUG-0051** (infra, high). Not an RBAC gap: the shared UAMI already holds Storage Blob Data Owner + Storage Blob Data Contributor + Storage Queue Data Contributor + Storage Queue Data Message Sender on the account (verified with `az role assignment list` scoped to the storage account; the function app does ingestion under the same identity). The blob/queue endpoints are derived from the account name by `resolve_storage_endpoints` ([storage_endpoints.py](../../src/functions/core/storage_endpoints.py)), so only the account name + container + queue are needed (no explicit `AZURE_STORAGE_BLOB_ENDPOINT`), matching the function app.

**Live hotfix (QA unblocked).** `az containerapp update -g <RESOURCE_GROUP> -n ca-backend-<SUFFIX> --set-env-vars AZURE_STORAGE_ACCOUNT_NAME=<account> AZURE_DOCUMENTS_CONTAINER=documents AZURE_DOC_PROCESSING_QUEUE=doc-processing` (additive, reversible) → new revision `Healthy` + `Running`, env vars confirmed present. **Verified end-to-end:** a `.txt` smoke upload to `POST /api/admin/documents` returned `200` `{"filename":"cwyd-qa-smoke.txt","blob_path":"documents/cwyd-qa-smoke.txt","ingestion_job_id":"…","queued":true}` — proving the config gate passes, the blob write succeeds, and the queue enqueue succeeds, all under the UAMI. The smoke blob was deleted afterward (`az storage blob delete --auth-mode login`; the not-yet-indexed chunk delete returned `404`, as expected).

**Durable fix.** Added the same three storage env entries the function app uses (`AZURE_STORAGE_ACCOUNT_NAME` = `effectiveStorageName`, `AZURE_DOCUMENTS_CONTAINER` = `documentsContainerName`, `AZURE_DOC_PROCESSING_QUEUE` = `docProcessingQueueName`) to the `backendContainerApp` container `env` array in `main.bicep`, after the `ORCHESTRATOR` entry, so the wiring survives re-provision. `az bicep build` → exit 0, no new errors. This is a config addition to an existing resource block (not structural). No app-code, RBAC, or frontend change.

**Lesson.** The backend Container App needs the **same** storage wiring as the function app whenever it owns a write/enqueue surface (admin upload, URL ingest, reprocess) — the two runtimes share one container + queue. The bicep gap meant only the ingestion runtime could talk to storage; the API runtime that *accepts* uploads could not.

### QA cloud ingestion — downstream pipeline (parsing 503 fixed; queue scale-from-zero blocked)

After BUG-0051 unblocked upload, proactively verified the **downstream** ingestion pipeline (parse → embed → index) rather than stopping at the upload `200`. Found and fixed the parsing 503, then uncovered a deeper pre-existing infra problem: the queue trigger never fires.

**BUG-0052 — parsing 503 fixed (live).** A PerksPlus.pdf upload returned `503 "Document parsing is not configured for this deployment."` — the BUG-0034 unit-(d) guard firing because the deployed **backend** and **function** app were both missing `AZURE_AI_SERVICES_ENDPOINT`, and the shared UAMI lacked **Cognitive Services User** on the AI Services account. Fixed live: added `AZURE_AI_SERVICES_ENDPOINT=https://aisa-<SUFFIX>.cognitiveservices.azure.com/` to both runtimes and granted the role scoped to the AI Services account. Upload then returned `200 {…,"queued":true,"ingestion_job_id":"…"}`. Durable bicep back-port (env var on both runtimes + role assignment) noted as a follow-up, not applied (bicep edits paused).

**BUG-0053 — queued docs never index (scale-from-zero, in-progress).** PerksPlus.pdf enqueued but never indexed; the `doc-processing` queue held 7 messages at `dequeueCount=0` (never dequeued) for over an hour. Root-caused to the Flex Consumption per-function scaling model: `batch_push` runs only in its own `function:batch_push` scale group, which the platform scale controller must wake from zero on queue depth — and it does not, for the identity-based Storage Queue connection (the HTTP-served instances do not run `batch_push`). Confirmed everything else correct: trigger metadata registered, queue name resolves to `doc-processing`, UAMI has Storage Queue Data Contributor, only the user-assigned identity (clientId matches `AzureWebJobsStorage__clientId`), storage fully open (no firewall/PE/VNet), Flex scale config healthy (`maximumInstanceCount=40`), and the function **works warm** (it processed messages right after a deploy). No-cost levers tried — explicit `AzureWebJobsStorage__blobServiceUri`/`__queueServiceUri`/`__tableServiceUri`, a forced `stop`+`start` cycle (plain `restart` never cycled instances), and `syncfunctiontriggers` — none woke the group. Decision pending with the operator (see below).

**BUG-0054 — Event Grid poison (open).** `doc-processing-poison` holds 10 base64 `Microsoft.Storage.BlobCreated` Event Grid envelopes — a stray Event Grid → `doc-processing` wiring whose schema `batch_push` cannot process. Noise/poison only (distinct from the CWYD-envelope messages), but should be removed.

**BUG-0055 — App Insights empty (open).** `appi-<SUFFIX>` has received **zero** telemetry ever (union "any data ever" query → `[0, null, null]`) from both the function and backend despite both carrying `APPLICATIONINSIGHTS_CONNECTION_STRING`. Independently a defect (no observability) and it blocks BUG-0053 diagnosis (no scaler/host logs).

**Decision (pending operator).** The operator earlier chose to keep debugging scale-from-zero at no added cost (declined an always-ready warm instance). The no-cost config levers are now exhausted and the function provably works warm, so the realistic remaining options narrow to: (a) one more speculative no-cost experiment (enable a system-assigned identity + grant it queue RBAC for the scale controller); (b) accept the documented `alwaysReady={function:batch_push:1}` fix (small always-on cost, reliable); or (c) a redeploy as a temporary stopgap to drain the current backlog (warms `batch_push` briefly like the post-deploy run, then scales to zero again). Checkpointing with the operator before proceeding.

### QA cloud ingestion — option (a) tried + failed; definitive `NoOpListener` proof; BUG-0055 narrowed

Per the operator's "deploy the cloud variables, not bicep" direction, executed the no-cost option (a) and then found the conclusive evidence via an alternate telemetry path.

**Option (a) — system-assigned identity: tried, failed.** Enabled a **system-assigned identity** on `func-<SUFFIX>` alongside the existing UAMI (`identity.type` → `SystemAssigned, UserAssigned`) and granted that principal **Storage Queue Data Contributor** + **Storage Blob Data Owner** scoped to the storage account, on the theory the Flex scale controller might poll the identity-based queue under the system-assigned identity. Then forced a `stop`+`start` cycle and `syncfunctiontriggers`. The `doc-processing` queue stayed at **7 messages, `dequeueCount=0`** ~15 min after the grant + cycle + sync. Option (a) is exhausted.

**Definitive root-cause evidence (via Log Analytics, bypassing the empty App Insights).** The function routes `allLogs` to the `log-<SUFFIX>` Log Analytics workspace through a diagnostic setting (a second setting also ships `FunctionAppLogs` to a governance workspace). That local workspace **is** populated (≈500 `Information` rows / 24h) even though App Insights is empty, so it gave the host logs App Insights could not. Every host start logs `batch_push` with a `Microsoft.Azure.WebJobs.Script.Description.FunctionGroupListenerDecorator+NoOpListener`. The `NoOpListener` **is** Flex Consumption's per-function-scaling mechanism: on instances **not** allocated to the `function:batch_push` scale group (the HTTP/health instances), the real queue listener is deliberately swapped for a no-op that never polls. Across the entire 24h window **no** instance ever started the real `batch_push` listener — proving the scale controller never allocated a `function:batch_push` instance from zero, so the queue is never polled. This matches the Flex Consumption troubleshooting doc's "trigger registration succeeded but the listener didn't enable" failure class exactly. The scale controller's own decision trace (why it computes desired=0) is **not** in `FunctionAppLogs`; it would need the App Insights scale-controller traces (missing, BUG-0055).

**BUG-0055 narrowed.** The function's `APPLICATIONINSIGHTS_CONNECTION_STRING` was verified to **match** the `appi-<SUFFIX>` resource exactly (same ingestion endpoint + instrumentation key) — so the empty telemetry is **not** a connection-string mismatch. The host *does* emit platform logs to the `log-<SUFFIX>` workspace via the diagnostic setting, so the gap is specifically the **App Insights ingestion path** (SDK/OTel `requests`/`traces`/`dependencies` not exported), not total telemetry loss. The `FunctionAppLogs` path is a working substitute for host diagnosis in the meantime.

**Decision (now narrowed to a cost go/no-go).** No-cost levers are exhausted: option (a) failed, runtime scale monitoring is Premium + VNet only (N/A to this non-VNet app), and switching `AzureWebJobsStorage` to a connection string is rejected (regresses the identity-based / no-secret design + adds a secret). The only no-regression fix is `alwaysReady={function:batch_push:1}` — one always-warm instance runs the real listener instead of the `NoOpListener` and polls the queue — which carries a small always-on cost the operator initially declined. Because that contradicts the earlier no-cost choice, holding for an explicit operator go/no-go on the cost before applying it via `az functionapp scale config always-ready set`.

### BUG-0053 fixed (always-ready) → uncovered BUG-0056 (queue messageEncoding mismatch) → fixed

**Operator approved always-ready; BUG-0053 fixed.** The operator chose to apply the always-ready fix. Applied live: `az functionapp scale config always-ready set -g <RESOURCE_GROUP> -n func-<SUFFIX> --settings function:batch_push=1` (confirmed `scaleAndConcurrency.alwaysReady=[{instanceCount:1, name:"function:batch_push"}]`). The always-ready instance immediately ran the **real** listener (no longer the `NoOpListener`) and drained `doc-processing` — verified the queue went 7→0, and later 3→0 on a fresh upload. The scale-from-zero blocker (BUG-0053) is resolved; the identity-based / no-secret design is preserved. Durable bicep back-port (add `alwaysReady` to `functionAppConfig.scaleAndConcurrency`) noted as a follow-up (bicep edits paused). Note: after draining, the QueueTrigger listener backs off polling to ~60 s when the queue is empty, so a freshly-arrived message can wait up to ~1 min before pickup.

**End-to-end re-test surfaced a *second*, distinct blocker (BUG-0056).** A fresh PerksPlus.pdf upload returned `200 {…,"queued":true,"ingestion_job_id":"…"}`, three messages landed on `doc-processing`, the always-ready listener drained them (3→0) — but PerksPlus still did **not** index and poison grew by 3. `FunctionAppLogs` showed `Executed Functions.batch_push (Failed, Duration=5–29ms)` + `batch_push queue message validation failed`: the failure is immediate (single-digit ms), before any download/parse/embed, i.e. at message validation. Decoding the 3 newest poison bodies: two are duplicate Event Grid `BlobCreated` envelopes (BUG-0054 noise) and **one is the genuine CWYD envelope** carrying the exact upload `ingestion_job_id` (`{container_name, filename, ingestion_job_id, force_reindex}`) — which **exactly matches** the deployed `BatchPushQueueMessage` contract, so this is **not** a schema skew.

**BUG-0056 root cause — queue messageEncoding base64 mismatch.** The backend producer (`backend/services/ingestion.py` → `functions/batch_start/queue_writer.enqueue_push_message`) builds its `QueueClient` (`functions/core/storage_clients.py`) with **no `message_encode_policy`**, so `azure-storage-queue` uses `NoEncodePolicy` and sends **raw JSON** (the `queue_writer.py` docstring's "SDK base64-encodes by default" claim is wrong). The Functions queue trigger only accepts raw bodies when `extensions.queues.messageEncoding=none`; that setting exists **only** in `functions/local.settings.json` (so local dev works) and was **never** propagated to `host.json` or the cloud function app settings. The cloud Flex host falls back to the extension default `messageEncoding=base64`, can't decode the raw JSON, and poisons every message (it base64-encodes on the way to the poison queue, which is why peeked poison bodies are base64 that decode to the clean envelope). Confirmed absent in the cloud: `az functionapp config appsettings list` showed no `messageEncoding` / `JobHost__extensions__queues` override.

**BUG-0056 fix applied (cloud variable, not bicep).** `az functionapp config appsettings set -g <RESOURCE_GROUP> -n func-<SUFFIX> --settings "AzureFunctionsJobHost__extensions__queues__messageEncoding=none"` (verified stored value `none`), then a forced restart so the `JobHost` setting loads at host startup. This matches the producer's raw-JSON output and the local configuration. Durable back-port: add `extensions.queues.messageEncoding` to `host.json` (or the bicep function app settings) so every deploy carries it (follow-up; bicep edits paused).

### BUG-0056 confirmed fixed → uncovered BUG-0057 (embedding deployment 404)

After the `messageEncoding=none` fix loaded, re-tested end-to-end and traced the *real* remaining blocker.

**BUG-0056 confirmed fixed.** The forced restart loaded the host setting — startup logs show `QueuesOptions { … MessageEncoding: None }`, and the post-fix upload's poison body is now **raw JSON** (no longer base64), proving the host reads/writes raw. With encoding fixed, the fresh PerksPlus.pdf CWYD envelope **passes validation**, acquires its managed-identity token, and **downloads the blob** (`Response status: 206 … Content-Length: 115310` = the full file) — the immediate single-digit-ms validation failure is gone. So BUG-0056 is resolved; the encoding mismatch was real and the cloud-variable fix is correct.

**New blocker — BUG-0057, embedding 404.** PerksPlus still did not index and still poisoned. Filtering `FunctionAppLogs` for the non-validation error (`batch_push queue handler failed`, ERROR level, 5 retries 01:16:58 → 01:17:26 UTC) surfaced the real traceback: `handler.py` `embedder.embed(chunks)` → `foundry_iq.embed` → `oai.embeddings.create(...)` → `openai.NotFoundError: Error code: 404`. So the failure is downstream of validation + download + parse, at the embedding call.

**Root cause.** Two OpenAI-capable accounts with different embedding deployments. The search index `content_vector` is **1536-dim** = `text-embedding-3-small` (the configured `AZURE_OPENAI_EMBEDDING_DEPLOYMENT`), so the model choice is right. But `text-embedding-3-small` exists **only** on the classic OpenAI account `oai-<DATA_SUFFIX>` (`AZURE_OPENAI_ENDPOINT`), while the Foundry IQ embedding path routes `/embeddings` to the AI Services account `aisa-<SUFFIX>` (`AZURE_AI_SERVICES_ENDPOINT`), which has only `text-embedding-3-large` → deployment-not-found 404. The deployed build is also older than local (`embeddings.create` at line 365 vs 449; no `_get_embeddings_client`), so it likely still uses the Foundry project route, which serves no `/embeddings`. Verified: `aisa-<SUFFIX>` deployments = gpt-5.1, o4-mini, text-embedding-3-**large**; `oai-<DATA_SUFFIX>` deployments = a legacy chat model, text-embedding-3-**small**, gpt-5.1, o4-mini; all three search indexes = 1536-dim; no integrated vectorizer (function computes embeddings).

**No pure cloud-variable fix.** The embeddings `base_url` comes from `AZURE_AI_SERVICES_ENDPOINT` / the project route, never `AZURE_OPENAI_ENDPOINT`; repointing `AZURE_AI_SERVICES_ENDPOINT` would also break DI parsing. Resolution needs either provisioning `text-embedding-3-small` on `aisa-<SUFFIX>` (and redeploying the current code so embeddings use the account route) or a code change to route embeddings at `oai-<DATA_SUFFIX>` — both exceed "cloud variables." Documented as BUG-0057; **holding for operator go/no-go** on a model deployment ± function code redeploy.

### BUG-0057 fixed → PerksPlus indexes end-to-end (+ found BUG-0058)

Operator said "make it work," so executed fix path 1 (model deployment + code redeploy).

**Step 1 — model deployment (cloud, reversible).** `az cognitiveservices account deployment create -g <RESOURCE_GROUP> -n aisa-<SUFFIX> --deployment-name text-embedding-3-small --model-name text-embedding-3-small --model-version 1 --model-format OpenAI --sku-name GlobalStandard --sku-capacity 100` → `provisioningState=Succeeded`. Verified directly: `POST https://aisa-<SUFFIX>.cognitiveservices.azure.com/openai/v1/embeddings` (AAD bearer) with `{input, model=text-embedding-3-small}` → `OK dims=1536`. GlobalStandard is pay-per-token (no idle cost), reversible.

**Step 2 — code redeploy, and the BUG-0058 detour.** First `azd deploy function` reported SUCCESS but the function **still 404'd** on the project route (`…services.ai.azure.com/api/projects/proj-<SUFFIX>/openai/v1/embeddings`), traceback at `foundry_iq.py` line 365 — i.e. old code. Root cause: `azd deploy function` packaged a **stale** `build-functions/` artifact dated 06-06; the `prepackage` hook never regenerated it from current `src/` (mtimes unchanged; `build-functions/.../foundry_iq.py` had `_get_openai_client` at line 363, no `_get_embeddings_client`). Filed as **BUG-0058**. Confirmed the deployment package blob itself: `released-package.zip` Last-Modified `02:05 UTC` after redeploy (so the upload happened) — the problem was purely the stale *staged* tree. Workaround: `uv run python scripts/prepackage_function.py` regenerated `build-functions/` (verified `embed` → `_get_embeddings_client` at line 446, `base_url = services_endpoint + /openai/v1`), then `azd deploy function` again → SUCCESS.

**Step 3 — end-to-end verification.** Re-uploaded PerksPlus.pdf (job `<redacted>`). Watched: `doc-processing` drained, the CWYD envelope did **not** poison (only the expected BUG-0054 Event Grid noise poisoned), and the backend `admin/documents` endpoint now lists **PerksPlus.pdf with 4 chunks** (docCount 4 → 5). Direct index check: the active v2 index is `cwyd-index` (151 docs; fields `id/content/title/url/content_vector`) — a text search returns 4 `title='PerksPlus.pdf'` chunks. (`index-<OLD_SUFFIX>` is a legacy v1 index with blob-URL `source` values and is **not** the active store — that mismatch briefly looked like a failure but was a wrong-index query.)

**Result.** The full cloud ingestion pipeline works end-to-end: upload → blob → queue (raw JSON) → `batch_push` → download → parse → **embed (account route, 3-small)** → push → indexed. BUG-0051/0052/0053/0056/0057 all fixed and confirmed. Remaining open: BUG-0054 (Event Grid poison noise), BUG-0055 (App Insights empty), BUG-0058 (prepackage hook skipped by `azd deploy <service>`).

### BUG-0059 — chat 401s at KB grounding (cloud recurrence of BUG-0025) → fixed by re-pointing the backend connection env var

With ingestion working, QA asked a question and got an SSE `event: error`: `Authentication failed when connecting to the MCP server: https://srch-<DATA_SUFFIX>.search.windows.net/knowledgebases/cwyd-kb/mcp: … 401 (Unauthorized)`. This is a **cloud recurrence of BUG-0025**.

**Diagnosis (live).** The `agent_framework` orchestrator ([agent_framework.py](../../src/backend/core/providers/orchestrators/agent_framework.py)) builds the KB `MCPTool` with `project_connection_id = AZURE_AI_SEARCH_CONNECTION_NAME`; the Responses API opens the MCP connection **server-side under that connection's identity**. `az containerapp show` on the backend listed `AZURE_AI_SEARCH_CONNECTION_NAME=search-srch-<DATA_SUFFIX>` — the bicep default, which is the general-purpose `CognitiveSearch` / bare-`AAD` connection (`useWorkspaceManagedIdentity:false`, no `audience`). That connection mints no usable bearer for the server-side MCP path → 401 → `mcp_list_tools` empty → the agent errors. BUG-0025 fixed this on 2026-06-12 by creating a dedicated `cwyd-kb-mcp` `RemoteTool` / `ProjectManagedIdentity` connection and pointing the env var at it — **but only in the gitignored `.env`**; the durable bicep back-port (`main.bicep` wires the var to `aiProjectSearchConnection.outputs.name` = `search-srch-<DATA_SUFFIX>`) never landed, so `azd up` shipped the wrong value.

**Pre-fix state verified (everything already right except the env var).** Listed the project connections: both `search-srch-<DATA_SUFFIX>` (CognitiveSearch/AAD) **and** `cwyd-kb-mcp` (RemoteTool/ProjectManagedIdentity) exist. GET on `cwyd-kb-mcp` confirmed all six properties from the BUG-0025 fix: `category=RemoteTool`, `authType=ProjectManagedIdentity`, `audience=https://search.azure.com`, `useWorkspaceManagedIdentity=true`, `target=…/knowledgebases/cwyd-kb/mcp?api-version=2025-11-01-preview`, `metadata.ApiType=Azure`. The project system-assigned MI holds **both** `Search Index Data Reader` and `Search Service Contributor` scoped to the search service. So only the backend env var was wrong.

**Fix (live, cloud variable — not bicep).** `az containerapp update -g <RESOURCE_GROUP> -n ca-backend-<SUFFIX> --set-env-vars "AZURE_AI_SEARCH_CONNECTION_NAME=cwyd-kb-mcp"` → new revision (`runningState=Running`). No connection or RBAC change needed (both already in place from BUG-0025). No runtime-config flip needed either — the 401 itself proves the `agent_framework` orchestrator was already active (langgraph does client-side search, never the KB MCP).

**Verified end-to-end.** Re-asked a grounded question (`POST /api/conversation`, SSE) about PerksPlus: the stream now emits `reasoning` → `tool: knowledge_base_retrieve` (the MCP call, **no 401**) → `citation [doc1] PerksPlus.pdf` (full snippet) → a grounded `answer` citing `[doc1]`, closing with `conversation` — **no `error` frame**. This also confirms the freshly-indexed PerksPlus.pdf (BUG-0057) is retrievable through the KB.

**Durable back-port (pending, bicep paused).** Point `AZURE_AI_SEARCH_CONNECTION_NAME` at the KB MCP `RemoteTool` connection in `main.bicep`, and create that `cwyd-kb-mcp` connection + the project-MI `Search Service Contributor` assignment durably in `infra/`. Until then, every `azd up` re-introduces this 401 and the env var must be re-flipped post-deploy. **Fixed 2026-06-16.**
