---
title: CWYD v2 — Worklog 2026-06-29
description: Removed the auth_enforced health signal and the AuthBlocked screen; the SPA now resolves identity transparently from Azure Easy Auth /.auth/me (principal or default guest user).
author: CWYD Engineering
ms.date: 2026-06-29
topic: reference
keywords: worklog, auth_enforced, AuthBlocked, easy auth, transparent identity, require_admin_auth, v2
estimated_reading_time: 3
---

## Summary

Deleted the `auth_enforced` health signal and the client-side `AuthBlocked` "Authentication Not Configured" screen so the CWYD v2 frontend resolves identity purely from Azure Easy Auth `/.auth/me` — a real `userId` when a principal is present, otherwise the `DEFAULT_USER_ID` guest user. The SPA no longer renders a blocking wall. `require_admin_auth` stays as the orthogonal opt-in server-side admin-write gate (unchanged). Recorded as [BUG-0091](../bugs.md).

## Planned

- Remove `auth_enforced` from the `HealthResponse` model and its producer (Phase 1, backend).
- Strip the enforced/blocked concept from the SPA — `AuthPhase.Blocked`, `AuthState.authEnforced`, the `readAuthEnforced` helper, and the `AuthBlocked` component — so the bootstrap resolves identity from `/.auth/me` alone (Phase 2, frontend).
- Sync the tracked documentation: append one new sequential `BUG-####` row, annotate the superseded rows of the BUG-0046 identity plan, and write this worklog (Phase 3, docs).

## Done

- **Backend (Phase 1):** removed the `auth_enforced` field + its docstring sentence from `HealthResponse` in [models/health.py](../../src/backend/models/health.py) (now `status`, `version`, `checks`); dropped the `auth_enforced=settings.require_admin_auth` argument from the `HealthResponse(...)` construction in [services/health.py](../../src/backend/services/health.py) (`run_health_checks` signature unchanged). Tests updated in [test_health.py](../../tests/backend/test_health.py) (wire-key lock narrowed to `{"status", "version", "checks"}`, two `*_auth_enforced_*` tests deleted) and [test_services_health.py](../../tests/backend/test_services_health.py) (both `test_run_health_checks_auth_enforced_*` deleted).
- **Frontend (Phase 2):** removed `AuthPhase.Blocked` (closed set now `Loading | Resolved`) and `AuthState.authEnforced` from [models/auth.tsx](../../src/frontend/src/models/auth.tsx); `resolve` is now single-arg `resolve(userInfo)` in [useAuth.tsx](../../src/frontend/src/hooks/useAuth.tsx) (Blocked branch + `authEnforced` dropped from `INITIAL_AUTH_STATE`); the bootstrap effect in [App.tsx](../../src/frontend/src/App.tsx) now calls `resolve(getUserInfo())` and no longer imports/renders `AuthBlocked` or reads `auth_enforced`. The `AuthBlocked` component (`src/frontend/src/components/AuthBlocked/AuthBlocked.tsx` + `AuthBlocked.module.css`) and its test (`tests/frontend/components/AuthBlocked.test.tsx`) were deleted; `auth.test.tsx`, `useAuth.test.tsx`, and `AppAuthBootstrap.test.tsx` were updated to the two-member `AuthPhase` set (principal-resolved, default-fallback, and `/.auth/me`-origin cases retained).
- **Docs (Phase 3):** appended [BUG-0091](../bugs.md) to the defect registry; added a top supersession banner plus inline "Retired 2026-06-29" notes on rows **D2** and **F10** of [frontend-user-identity-plan.md](../frontend-user-identity-plan.md) (the retired set is D2 / B1 / F9 / F10 / F11; the header-forwarding work D1/D3/F1–F8/F12 is unchanged); wrote this worklog.

## Bugs

- [BUG-0091](../bugs.md) (fixed) — removed the `auth_enforced` health signal and the `AuthBlocked` screen; the SPA resolves identity transparently from `/.auth/me`. Root cause: `auth_enforced` (a frontend-only UI flag added under BUG-0046) rendered a dead-end wall on an open / partially-wired deploy (cf. BUG-0089, BUG-0090) instead of degrading to the guest user.
- [BUG-0092](../bugs.md) (open) — found during Phase 4 cloud verification: the chat **History panel fails to load** on the split-host cloud deploy (`Could not load history: Unexpected token '<', "<!doctype "... is not valid JSON`). Root cause: `HistoryPanel.tsx` builds its request URL from the build-time `VITE_BACKEND_URL` (empty in the deployed App Service build) instead of the runtime `getBackendUrl()` `/config` seam, so history requests hit the SPA's own App Service origin and receive `index.html`. **Unrelated to the `auth_enforced` removal** — chat streaming and the health probe (which use the runtime seam) work.

## Decisions

- **PD-01 → Scenario A:** `require_admin_auth` (`AZURE_REQUIRE_ADMIN_AUTH`), `get_user_id`, and `requires_role` are left unchanged. Admin write-gating stays server-side and orthogonal to the SPA's identity resolution; only the frontend "enforced/blocked" UI concept was removed.
- **Clean-as-you-go (IN-01):** Phase 2 caught and removed an in-flight `readAuthEnforced` duplication while stripping `App.tsx`; no residual dead code remains.
- Recorded the change as a new sequential defect row (BUG-0091) rather than amending an existing one — none of the recent rows capture the removal/transparent-identity decision (per the defect-registry rule).

## Done (Phase 4 — cloud verification, 2026-06-29)

- **Deployed** the backend Container App (`azd deploy backend`) and the frontend App Service (`azd deploy frontend`; frontend seed prompt answered "no scenario" → skip, to avoid index pollution). Both succeeded.
- **Live `/api/health`** returns `{status, version, checks}` only — **no `auth_enforced`** field — with `foundry_iq pass`, `database pass (cosmosdb)`, `search pass (AzureSearch)`.
- **Live SPA verified end-to-end:** no "Authentication Not Configured" wall; the header resolves the **Guest** user (G avatar) by default; backend health shows "Connected to backend"; the SSE `reasoning` channel streamed into the collapsible "Thought process" panel; a grounded query returned a structured answer with **7 inline citations** + a references panel + the AI-content disclaimer. Transparent default-guest identity confirmed working.
- **Cleanup:** deleted both throwaway guest test conversations via the backend history API (the UI delete path is blocked by BUG-0092); history list left empty.
- **In-scope doc fix:** corrected four stale docstrings in [api/auth.tsx](../../src/frontend/src/api/auth.tsx) that still described the removed blocked-screen / enforcement behavior (docstring-only; ESLint clean, `auth.tsx` type-checks).

## Next

- **BUG-0092 (separate task):** route `HistoryPanel`'s `buildUrl` through the runtime `getBackendUrl()` seam so the History panel works on the split-host deploy.
- Pre-existing, unrelated: the frontend project-wide `tsc -b` is red on an **unused `formatActor`** in the open #35d admin-merge `Configuration.tsx` (committed, not touched by this task) — to be cleared by the admin-merge owner.
