---
title: CWYD v2 — Worklog 2026-06-25
description: Replaced the Bicep KB-MCP RemoteTool connection with a post-provision seeder folded into post_provision.py, and scrubbed every "macae" reference from shipped artifacts.
author: CWYD Engineering
ms.date: 2026-06-25
topic: reference
keywords: worklog, kb-mcp, post-provision, foundry connection, macae scrub, v2
estimated_reading_time: 3
---

## Summary

Removed the rejected Bicep `RemoteTool` connection module and re-created the `cwyd-kb-mcp` connection the reference-architecture way — an idempotent post-provision seeder folded into `post_provision.py` — then scrubbed every `macae` reference from CWYD's shipped artifacts.

## Planned

- Delete `v2/infra/modules/ai-project-kb-mcp-connection.bicep` and stop using a Bicep resource for the KB-MCP connection.
- Seed the connection at post-provision instead, keeping grounding working (no env revert to the base CognitiveSearch connection, which 401s).
- Remove every `macae` reference from shipped artifacts (src, infra comments, tests, `.env`, bugs.md attribution).

## Done

- `_ensure_kb_mcp_connection` inline helper added to [post_provision.py](../../scripts/post_provision.py) (sibling of `_ensure_knowledge_base`; idempotent ARM control-plane PUT of the `cwyd-kb-mcp` RemoteTool connection; `ARM_SCOPE` + `KB_MCP_CONNECTION_API_VERSION` constants) and wired into the cosmosdb branch of `main()` right after the KB seed. 8 new tests.
- [main.bicep](../../infra/main.bicep): deleted the `aiProjectKbMcpConnection` module call; rewired `AZURE_AI_SEARCH_CONNECTION_NAME` to `'${searchKnowledgeBaseName}-mcp'` (the seeded name); added the `AZURE_AI_PROJECT_RESOURCE_ID` output. `ai-project-kb-mcp-connection.bicep` deleted.
- `macae` scrub: backend (2 files), frontend (20 files), tests (5 files + one test rename `test_rai_agent_uses_macae_classifier_pattern` → `test_rai_agent_uses_classifier_pattern`), infra comments (main.bicep / virtualNetwork.bicep / azure.yaml), `.env`, and `bugs.md` attribution (technical content preserved per the defect-registry rule).
- Validation: `grep -ri macae` = 0 across `v2/src`, `v2/infra`, `v2/tests`, `azure.yaml`, `.env`, `bugs.md`; pytest 2666 passed / 1 skipped; vitest 607 passed; `az bicep build` exit 0.

## Bugs

- None found or fixed this session. BUG-0025 / BUG-0059 (the KB-MCP `401`) stay fixed — the same `RemoteTool` / `ProjectManagedIdentity` / `audience: https://search.azure.com` connection is now created by the post-provision seeder instead of Bicep.

## Decisions

- Did NOT revert `AZURE_AI_SEARCH_CONNECTION_NAME` to the pre-removal base CognitiveSearch connection — that connection `401`s on the KB-MCP path; pointed it at the deterministic seeded `cwyd-kb-mcp` instead.
- Folded the seeder into `post_provision.py` (inline, sibling of the existing KB seed) rather than a new standalone module — consistent with the existing post-provision pattern; no new shim, no new hook slot (the single `postprovision` hook already runs it).
- Scrub scope = shipped artifacts + `bugs.md` attribution only. Governance (`.github/**`), `.copilot-tracking/**`, the `data/sample_code/macae/**` sample, and `v2/docs/**` ADRs + worklogs intentionally keep their `macae` references (sanctioned reference / immutable history).

## Next

- **RBAC prerequisite (deploy-time):** the seeder's connection PUT needs ARM `Microsoft.CognitiveServices/accounts/projects/connections/write` on the Foundry account. The azd deploy principal that previously created the connection via Bicep already holds it; `post_provision.py` runs `continueOnError: false`, so a `403` surfaces clearly rather than silently. An explicit Bicep role assignment is the robust alternative if a narrowly-scoped deploy identity is adopted.
- Cloud `azd up` smoke on both database types (cosmosdb + postgresql): confirm the seeder creates `cwyd-kb-mcp` at post-provision, grounding works with no manual `az`, and the seeder no-ops cleanly in pgvector mode.
- Optional: verify `data/sample_code/macae/.azure/**` (real subscription/RG identifiers in its logs) is gitignored; broader `macae` scrub of ADRs/worklogs if desired.

## Deploy validation (cloud `azd up` — cosmosdb)

Ran `azd up` against the existing cosmosdb env (`AZURE_DB_TYPE=cosmosdb`, `AZURE_INDEX_STORE=AzureSearch`). End-to-end green:

- **Provision** succeeded after clearing one RBAC collision (see gotcha 1 below).
- **KB-MCP seeder validated live** — post-provision logged `KB-MCP connection 'cwyd-kb-mcp' ready` (target `https://srch-<DATA_SUFFIX>.search.windows.net/knowledgebases/cwyd-kb/mcp?api-version=2025-11-01-preview`). This is the core change of this session, confirmed in the cloud with no manual `az`.
- **backend** Done; **frontend** Done; **function** Done (after the storage workaround in gotcha 2).
- **Sample-data seed** (`--set all`) ran non-interactively: 2 new docs uploaded + enqueued, 33 already present (idempotent skip).
- `AZURE_AI_PROJECT_RESOURCE_ID` output present in the azd env.

### Gotcha 1 — RBAC back-port collision (one-time migration)

Back-porting a previously-manual role grant into Bicep against an env that **already has the manual grant** fails the first redeploy with `RoleAssignmentExists` (the Bicep-declared assignment uses a different deterministic GUID name than the manual one, so ARM sees a duplicate). azd runs incremental ARM, so it never deletes the manual assignment on its own.

- Symptom: `RoleAssignmentExists: The role assignment already exists.` during provision.
- Diagnosis: `az role assignment list --all --query "[?name=='<GUID>']"` showed the colliding grant (here `Cognitive Services User` for the shared UAMI `<AZURE_UAMI_PRINCIPAL_ID>` scoped to the Foundry account).
- Fix: delete the manual assignment so Bicep owns it on the next pass — `az role assignment delete --role "<ROLE>" --assignee <AZURE_UAMI_PRINCIPAL_ID> --scope <ACCOUNT_RESOURCE_ID>`, then re-run `azd up`. Incremental mode recreates it cleanly under the Bicep GUID.

### Gotcha 2 — function deploy + seed blocked by private-only storage

The function's package/documents storage account is hardened to `publicNetworkAccess=Disabled` (reachable only via private endpoint). `azd deploy function` uploads the package zip as a blob **from the operator's workstation over the public internet**, so the upload is refused with `403 InaccessibleStorageException / BlobUploadFailedException`. RBAC is not the gap (the operator already holds `Storage Blob Data Contributor` + `Owner` on the account). The backend (ACR image push) and frontend (App Service SCM) don't touch this storage, which is why only `function` failed.

- Workaround used: temporarily `az storage account update -n st<SUFFIX> -g <RESOURCE_GROUP> --public-network-access Enabled --default-action Allow`, re-run `azd deploy function`, run the seed, then re-lock with `--public-network-access Disabled`. The function runtime reaches storage via its private path, so re-locking does not break ingestion.
- Same restriction blocks the post-deploy sample-data seed from a workstation — open storage before seeding, re-lock after.
- Secondary finding: the `postdeploy` sample-data hook prints its interactive menu but `input()` hits EOF under azd (no TTY), so the menu choice cannot be read and the seed is skipped (swallowed by `continueOnError: true`). Run the seed non-interactively instead: `AZURE_ENV_SAMPLE_DATA=all` (or `--set all`).
