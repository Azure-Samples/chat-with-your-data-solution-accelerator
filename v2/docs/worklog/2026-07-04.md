---
title: CWYD v2 Worklog 2026-07-04
description: Comment cleanup across the v2 tree, plus a typed ErrorEnvelope conversion on the Functions HTTP error path.
author: CWYD Engineering
ms.date: 2026-07-04
topic: reference
keywords: worklog, comment cleanup, process narrative, hard rule 16, tests, hard rule 15, error envelope, functions
estimated_reading_time: 4
---

## Summary

Ran a full comment-cleanup pass over the v2 code tree via the Task Implementor workflow: strip development, debug, and process-narrative comments (per Hard Rule #16) and compress remaining comments to what the code does. Scope covered `src/`, `infra/`, `docker/`, `scripts/`, root config, and `tests/`. Root `docs/` was confirm-only; `v2/docs/` was excluded (it is the intentional process tracker).

## Planned

- Execute the 6-phase plan at `.copilot-tracking/plans/2026-07-04/v2-comment-cleanup-plan.instructions.md`, one Phase Implementor subagent per phase.
- Preserve carve-outs: bare `Pillar:` / `Phase:` headers, Bicep `@description` / `metadata` / role-definition GUIDs, Azure SDK API-version literals, and functional architectural references.
- End each phase green (Python suite, frontend vitest, bicep build, frontend build).

## Done

- **Task Implementor drove all 6 phases** with `Phase Implementor` subagents; tracking maintained in `.copilot-tracking/changes/2026-07-04/v2-comment-cleanup-changes.md` and the planning log alongside it.
- **Key finding: the code-tree cleanup was already committed in a prior session.** Commits `f064eeeb` ("Remove process narrative from src comments and docstrings") and `d32b8ff4` ("Strip process narrative from docstring headers") already carried the `src/`, `infra/`, `docker/`, `scripts/`, and root-config cleanup. Wave 1 (Phases 1 to 4) therefore produced net-zero working-tree diffs. A fresh grep of those trees confirms they are clean of process-narrative tokens; the only residual matches are functional architectural references that are intentional keeps (the `reference-architecture` managed-identity / RBAC pattern notes in `infra/main.bicep`, and the "never lands in the image" build note in `docker/Dockerfile.functions`).
- **The genuinely new, uncommitted work this session is the `tests/` tree** (Phase 5): 87 files cleaned, grouped as `tests/frontend` (42), `tests/backend` (36), `tests/functions` (6), `tests/infra` (2), `tests/scripts` (1). Docstring / comment headers were reduced to bare `Phase: <N>` where they carried unit-ID or batch tails; process-narrative comments were removed while assertion behavior was left byte-for-byte intact.
- **Meta-gate and shared test files were left untouched** on purpose: `tests/shared/**`, `tests/test_no_silent_excepts.py`, and `tests/conftest.py`.
- **Validation (Phase 6):**
  - Python suite `uv run pytest tests/backend tests/functions tests/infra tests/scripts tests/shared -q`: 2537 passed, 1 skipped, 31 pre-existing SDK warnings. This run includes the `src/**` process-narrative and marker gates.
  - Frontend vitest `npm --prefix tests/frontend test`: 612 passed (one cold-run timeout flake in `FluentThemeBridge.test.tsx`, passes 4/4 in isolation).
  - Bicep `az bicep build --file infra/main.bicep`: exit 0 (Phase 3; `infra/` unchanged since).
  - Frontend build `npm --prefix src/frontend run build`: pass (Phase 1; `src/frontend/` unchanged since).
  - `git status` confirms only `tests/**` is modified; no out-of-scope files (no meta-gates, no root `docs/` prose, no `v2/docs/` other than this worklog).

## Bugs

- None found or fixed. This was a comment-only pass; no runtime behavior changed.

## Decisions

- **Frontend in scope** for the cleanup; the enforcement gate `tests/shared/test_no_process_narrative_in_src.py` is Python-only, so frontend and test-tree cleanup were applied by hand.
- **Strip the process tag, keep the sentence.** Comments that carried a unit-ID / task-number / batch tail were reduced to their functional statement rather than deleted.
- **Keeps preserved:** bare `Pillar:` / `Phase:` headers, Bicep `@description` / `metadata` / role-definition GUIDs, Azure SDK API-version literals, and functional architectural references (for example the managed-identity / RBAC pattern notes and the Dockerfile blueprint-copy rationale).
- **Held as residual by design (assertion behavior, not comments):** the `BUG-####` / `CU-###` / `Phase` tokens inside `assert` message strings in `tests/infra/test_main_bicep.py`, and the `S1 / SPEECH-MVP` label inside a `describe(...)` string in the frontend `MessageInput` test. Both are logged as follow-on items.

## Next

- **Commit the `tests/` cleanup** (operator's call; commit and push are gated). Suggested message excludes `.copilot-tracking/`.
- **Follow-on items** captured in the planning log (`.copilot-tracking/plans/logs/2026-07-04/v2-comment-cleanup-log.md`):
  - WI-01: extend the enforcement gate to `.ts` / `.tsx` and add `CU-###` detection so frontend and test trees are covered automatically.
  - WI-04 (addressed 2026-07-04): frontend `*.module.css` / `tokens.css` headers cleaned (14 files); see the WI-04 closure note above.
  - WI-05: em-dash normalization sweep (a handful of sites flagged during Phases 2 and 4).
  - WI-06: `vitest-out.txt` (repo root) is a tracked build artifact; removal needs sign-off.
  - WI-07 / WI-08: the `describe`-label and `assert`-message token scrubs noted above.

## Review and Remediation

Ran the Task Reviewer workflow over the completed cleanup (six parallel per-phase RPI validations plus full-suite validation), then addressed the findings in a Task Implementor follow-up.

- **Review outcome:** 0 Critical, 1 Major, 9 Minor. The Major (F-1) is a scope-accuracy point, not a functional defect: the uncommitted `tests/` diff includes token-identical formatter (ruff-style) reflow, so it is comment edits plus reflow rather than strictly comment-only. All carve-outs preserved; the full Python suite (2537 passed, 1 skipped) and frontend vitest (613 passed) are green. Review log at `.copilot-tracking/reviews/2026-07-04/v2-comment-cleanup-plan-review.md`.
- **Remediation edits (comment / docstring only, no behavior change):**
  - `src/backend/core/settings.py`: dropped the `(verified list of 37 vars as of Phase 1.2)` phase snapshot from the module docstring.
  - `src/backend/core/providers/llm/foundry_iq.py`: dropped the trailing "for now" from the `reason()` docstring `max_output_tokens` note.
  - `scripts/post_provision.py`: dropped the `Tracked as BUG-0093.` defect-id tag, kept the behavior sentence.
  - Changes log: corrected the "comment-only" wording to "comment and docstring edits plus incidental formatter reflow"; noted `docker/Dockerfile.functions` keeps its standing Phase name; recorded `infra/modules/ai-project-search-connection.bicep` as in-scope-but-unchanged.
  - Planning log: extended DD-11 / WI-08 to note the same assert-message keep class in `tests/backend/core/test_settings.py`.
- **Validation after remediation:** `uv run pytest tests/shared tests/backend/core/test_settings.py tests/scripts/test_post_provision.py tests/backend/core/providers/llm -q` gave 1179 passed, 1 skipped (includes the two `src/**` gates).
- **Deferred by design:** the Content-Safety prose block in `main.bicep` (RW-5) stays a documented KEEP (runtime / security rationale, zero process tokens); the formatter-reflow commit strategy (RW-1) is the operator's call (accept the reflow, recommended, or `git add -p` to comment hunks).

### WI-04 closure: frontend CSS headers

Cleaned the frontend `.module.css` / `tokens.css` tree that fell outside the `.ts` / `.tsx`-scoped Phase 1 sweep. Comment-only edits; no CSS rule, selector, or value changed.

- **14 files:** `CoralShell.module.css`, `Header.module.css`, `ChatPage.module.css`, `CitationPanel.module.css`, `HistoryPanel.module.css`, `MessageInput.module.css`, `MessageList.module.css`, `theme/tokens.css`, plus a second grep batch of `ErrorBoundary.module.css`, `AdminLayout.module.css`, `Configuration.module.css`, `DeleteData.module.css`, `IngestData.module.css`, `CitationDetailPanel.module.css` (`CitationPanel.module.css` appears in both batches).
- **Stripped:** Phase-header descriptor tails ("frontend polish", "pulled forward for boss demo", "reference-architecture re-skin", "sidebar moved LEFT", "SSE resilience: contain render failures", and the standing-name tails "Admin + Frontend Merge" / "Testing + Documentation"), the `removed in U8` / `until U8's tokens.css thinning` forward-looking unit references, and two `mirrors the v1 ...` back-references. Em dashes on touched lines removed. Headers reduced to bare `Phase: <N>` to match the uniformly-bare committed frontend `.tsx` convention.
- **Kept:** bare `Pillar:` / `Phase: <N>` headers and all functional body / architecture prose (layout descriptions, `cwydThink` keyframe rationale, the Fluent-token reset notes).
- **Validation:** `npm --prefix src/frontend run build` exit 0 (2990 modules transformed, run per batch); grep of `src/frontend/**/*.css` for the process-token set returns zero matches.
- This change set is uncommitted alongside the `tests/` tree; committing is the operator's next step.

### WI-01 / WI-02 / WI-05 / WI-06 / WI-07 / WI-08 closure (operator sign-off: "process all in order")

Processed the remaining review follow-ups in numeric order. WI-01 (shared-gate change) and WI-06 (tracked-file removal) are structural (Hard Rule #10); the "process all in order" instruction is the sign-off. All source edits are comment / docstring / message-string only; no assertion, logic, import, signature, or fixture value changed.

### F-6 / WI-09 closure: frontend-source em-dash sweep

Cleared the last open review finding (F-6, Re-Review 4). RW-7 normalized em dashes in the tests tree and WI-05 covered `src/**/*.py`, but the frontend **source** comment tree (`.tsx` / `.module.css` / `eslint.config.js`) was never in either scope and still carried ~60 em dashes in functional comments. Extended the `--` normalization to the frontend source so the whole v2 tree is uniform.

- **61 sites across 26 files:** 56 `.tsx` JSDoc/`//` comment sites in 22 files (api/, hooks/, models/, components/, pages/chat/components/, theme/, App.tsx, AdminLayout.tsx), 4 `.module.css` body-comment sites (CoralShell, ErrorBoundary, Header), 1 `eslint.config.js` comment. `\u2014` -> `--`, spacing preserved.
- **Kept (data, not comments):** `Configuration.tsx` / `DeleteData.tsx` `return "\u2014"` empty-cell placeholders, `assistant_presets.json` prompt string, the `dist/` build artifact, `pyproject.toml` metadata.
- **Validation:** grep of `src/frontend/src/**` returns only the 4 data-value sites; `npm --prefix src/frontend run build` exit 0 (2990 modules); `npm run test` (root vitest) = 46 files, 613 passed.
- **Tracking note:** this worklog and the changes log were found externally truncated to a pre-Wave-2 state at the start of this turn; the full findings history is authoritative in the review log (`.copilot-tracking/reviews/2026-07-04/v2-comment-cleanup-plan-review.md`) and the planning log (DD-23 records the truncation, DD-24 the WI-09 sweep).
- This change set is uncommitted; committing and pushing remain the operator's step (Hard Rule #10).

- **WI-01, extend the process-narrative gate to frontend TS/TSX.** `tests/shared/test_no_process_narrative_in_src.py` now walks `*.ts` / `*.tsx` under `src/` with a string-aware comment scanner (harvests `//` and `/* */`, ignores markers inside string literals) and enforces two new banned patterns on both walks: `\bCU-\d+\w*` and `#35[a-z]\b` (the word-boundary keeps hex colors like `#35abcd` from matching). Gate stays `src/`-only, so the meta-gate file's own token data is not self-flagged. 171 passed; a synthetic-snippet check confirmed string-literal safety and the hex-color non-match.
- **WI-02, map + finish the `tests/` tree.** A full token grep produced the residual map: functional `u1` fixtures (keep), the `tests/shared/**` meta-gate files (keep verbatim), the WI-07 label, the WI-08 assert messages, and one file missed by the Phase 5 scope, `tests/integration/test_conversation_live.py`. That file was cleaned: three `BUG-####` tags stripped from regression-guard docstrings (behavior kept). `cwyd-v1/tests/**` is frozen v1, out of scope.
- **WI-05, em-dash sweep.** Every flagged comment/docstring em dash replaced with a comma (appositive) or colon (definitional header) across `src/backend`, `src/frontend/frontend_app.py`, `src/functions`, `docker/Dockerfile.*`, and `azure.yaml`. The first grep was too narrow and missed `frontend_app.py:61`; a full `src/**/*.py` re-grep caught it. Final state: `src/**/*.py` is em-dash-free, py_compile clean, gate 171 passed.
- **WI-06, remove tracked `vitest-out.txt`.** `git rm`'d (staged) and added to `.gitignore` under the scratch-files section; `git check-ignore` exit 0. The tracked file was at the **repo root**, not `tests/frontend/` as the earlier note said (corrected above); it is regenerable and referenced in no config or script.
- **WI-07, scrub the SPEECH-MVP describe label.** `describe("MessageInput mic button (S1 / SPEECH-MVP)")` reduced to `describe("MessageInput mic button")`. 34 tests pass.
- **WI-08, scrub assert-message tokens.** CU/BUG/Phase tokens stripped from `assert` message strings in `tests/infra/test_main_bicep.py` (16 sites, more than the 2 the review estimated) and `tests/backend/core/test_settings.py` (2 sites; ADR anchors kept throughout). 107 tests pass.
- **Validation:** process-narrative gate 171 passed; `uv run pytest tests/infra/test_main_bicep.py tests/backend/core/test_settings.py -q` 107 passed; `npm test --workspace cwyd-frontend-tests -- MessageInput` 34 passed; `py_compile` on all edited Python files clean; `git check-ignore vitest-out.txt` exit 0.
- This change set (gate file, `.gitignore`, staged `git rm vitest-out.txt`, 9 `src`/`docker`/`azure.yaml` em-dash files, `tests/integration/test_conversation_live.py`, `tests/infra/test_main_bicep.py`, `tests/backend/core/test_settings.py`, `tests/frontend/.../MessageInput.test.tsx`) is uncommitted alongside the earlier `tests/` + CSS trees; committing and pushing remain the operator's next step.

### Re-review 2 findings F-4 / F-5 (Task Reviewer -> Task Implementor)

A re-review of the WI delta caught two items the first review missed. Both addressed:

- **F-4 (Major), residual `(S1 / SPEECH-MVP)` slugs + gate blind spot.** Three scenario slugs survived every sweep because the gate had no pattern for them: `src/backend/core/speech.py` and `src/backend/routers/speech.py` Phase headers (committed) and a `test_settings.py` section comment. Stripped all three (Phase headers to bare `Phase: 4`, comment to `# SpeechSettings`) and extended the gate with a banned pattern `\b[A-Z][A-Z0-9]*-MVP\b`. Chose the `*-MVP` codename class over the review's suggested bare `\bS\d+\b` because the latter collides with Azure SKU tier names (`S0`/`S1`/`S2`/`S3`); the codename pattern has no SKU false-positive (synthetic check: matches `SPEECH-MVP`, ignores `S1/S2/S3` and lowercase `mvp`). Validated: py_compile exit 0; gate + settings 238 passed.
- **F-5 (Minor), WI-05 "repo-wide" claim.** The executed em-dash sweep covered the flagged src/docker/azure.yaml sites only; ~20 non-process em-dash header separators remain in `tests/functions/**` docstrings and `test_types.py`. Narrowed the tracking wording; the test-tree normalization stays an optional residual (non-process punctuation, out of the original flagged set).

### RW-7 closure: test-tree em-dash sweep

Cleared the RW-7-residual (the optional em-dash normalization deferred from F-5) in a Task Implementor follow-up. Comment / docstring / JSDoc / `describe()`-label edits only; no assertion, logic, import, signature, or fixture value changed.

- **Scope expanded** from the documented residual (`tests/functions/**` + `test_types.py`) to the **entire v2 tests tree** (39 files, ~66 sites) so a partial sweep could not re-trigger the "em dashes remain" finding.
- **Substitute style `--` (double ASCII hyphen), operator-confirmed (Option A)** via an explicit ask. Deliberately diverges from WI-05's comma/colon in `src/`: `--` matches the pre-existing `--` sites inside the exact files RW-7 edits (the frontend `describe("Configuration -- page shell")` labels; the `tests/shared/test_no_anonymous_dict_returns.py` allow-list bullets), so the tests tree is now uniformly `--`.
- **One em dash kept by design:** `tests/frontend/pages/admin/DeleteData/DeleteData.test.tsx:106` `toHaveTextContent("—")` is a behavioral assertion on an empty-cell placeholder value, not a comment. `cwyd-v1/tests/**` (4 sites) is frozen v1, out of scope.
- **Batches:** A - 20 `tests/functions/**` header docstrings; B - 9 functions body comments + `test_types.py` docstrings; C - 16 sites in `tests/scripts` + the `tests/shared` meta-gate + `test_no_silent_excepts.py` (all `Hard Rule` / `-DEBT` / `Phase:` / `sub-unit C1` tokens preserved); D - 21 frontend `.tsx` sites across 13 files (JSDoc, `//` comments, 3 `describe()` labels).
- **Validation:** grep of the v2 tests tree returns zero em dashes (only the DeleteData assertion + frozen v1 remain); `uv run pytest tests/shared tests/test_no_silent_excepts.py tests/backend/core/test_types.py tests/scripts tests/functions -q` = 1617 passed, 1 skipped; `npm run test` (root vitest) = 46 files, 613 passed.
- **New optional follow-on WI-09:** a repo-wide `--` normalization of the `src/` WI-05 comma/colon sites (for one codebase-wide style) is available if wanted; deferred as out of scope for RW-7.
- This change set (39 test files) is uncommitted alongside the earlier `tests/` + CSS trees; committing and pushing remain the operator's next step (Hard Rule #10). With RW-7 cleared, all findings from every review pass are resolved or dispositioned; only the optional cosmetic WI-09 remains.

### F-7 / WI-10 closure: infra-Bicep + scripts em-dash sweep

Cleared the last review finding (F-7, Re-Review 5): Re-Review 4's "uniform across the entire v2 tree" claim was overstated because the `infra/**/*.bicep` `//` comments and `scripts/post_provision.py` still carried em dashes (the same per-tree scope-accuracy class as F-5). Extended the `--` normalization to those two code-comment trees so the sweep is genuinely uniform. Comment / docstring edits only; no Bicep resource, param, output, variable, `@description` target, Python logic, signature, or fixture value changed.

- **40 sites across 3 files:** `infra/main.bicep` (37 `//`-comment sites, two lines carrying two em dashes each), `infra/modules/ai-project.bicep` (1 header comment), `scripts/post_provision.py` (2: the line-80 `#` comment + a line-421 docstring). `\u2014` -> `--`, spacing preserved (matches WI-09 / RW-7).
- **Kept (ARM metadata values, not comments):** the 3 `@description('...')` strings in `main.bicep` (2486 / 2522 / 2565) compile into ARM template `metadata.description` (portal/CLI help text), the same class as the `pyproject.toml` metadata kept in WI-09, so their prose em dashes are retained. The `\u2192` (U+2192) flow arrows (`Event Grid \u2192 Storage Queue`) are not em dashes and were left. `cwyd-v1/scripts/**` (frozen v1, 12 sites) untouched.
- **Grep-truncation catch:** the `scripts/post_provision.py:421` docstring site had been hidden from earlier passes by grep-result truncation (a 143-match grep showed only 40); an untruncated per-tree grep surfaced it.
- **Validation:** grep `infra/**` returns only the 3 `@description` keeps, grep `scripts/**` returns only frozen v1, grep `docker/**` returns zero; `uv run python -m py_compile scripts/post_provision.py` exit 0; `az bicep build --file infra/main.bicep --stdout` exit 0 (only pre-existing BCP081 preview-schema + BCP334 length warnings, no errors); `uv run pytest tests/scripts tests/shared -q` = 1140 passed, 1 skipped.
- With WI-10 the `--` normalization is uniform across every v2 code-comment tree (src Python, frontend source, tests, infra Bicep, scripts). **Every finding from every review pass (F-1..F-7, RW-1..RW-7, WI-01..WI-10) is now resolved or dispositioned;** the only remaining item is the optional cosmetic WI-09 (normalize the `src/**/*.py` WI-05 comma/colon sites to `--` for one codebase-wide style). This change set (3 files) is uncommitted; committing and pushing remain the operator's next step (Hard Rule #10).

## Typed ErrorEnvelope on the Functions HTTP error path (Hard Rule #15)

Separate unit of work later the same day, driven through the Task Planner then Task Implementor workflow. Closes the last Hard Rule #15 gap in the Functions runtime: the HTTP exception mapper built three raw fixed-key error-body dict literals (`{"error": ...}` / `{"error": ..., "details": [...]}`) that the annotation-based anonymous-dict gate cannot see, because the enclosing `_wrapper` is annotated `-> func.HttpResponse` and the literals are handed to `json_response(...)`.

### Summary

Replace the 422/502/500 raw error-body dict literals in `src/functions/core/exception_mapping.py` with a single frozen Pydantic `ErrorEnvelope` model serialized via `.model_dump(exclude_none=True)`, with byte-identical wire output. Audit first confirmed `src/backend/**` and `src/backend/core/**` are already 100% Hard Rule #15 compliant, so this was the one genuine remaining candidate.

### Done

- **Planning:** plan / details / planning log authored under `.copilot-tracking/{plans,details,plans/logs}/2026-07-04/dict-to-response-model-conversion-*`; validated (one Major line-pointer drift fixed, DD-01).
- **Phase 1 (model + test, test-first):** added frozen `ErrorEnvelope` (`model_config = ConfigDict(frozen=True, extra="forbid")`; `error: ErrorType`, `details: list[dict[str, Any]] | None = None`) to `src/functions/core/http.py`, between `ErrorType` and `json_response`; `details` stays `list[dict[str, Any]]` as the `ValidationError.errors()` boundary shape (Hard Rule #15(a), anchor comment). Six `test_error_envelope_*` cases in `tests/functions/core/test_http.py` (confirmed ImportError before the model existed).
- **Phase 2 (wire):** swapped the 422/502/500 arms to `ErrorEnvelope(...).model_dump(exclude_none=True)`; extended the 422 test with an `ErrorEnvelope.model_validate` round-trip; left the 502/500 whole-dict `==` locks untouched. `exclude_none=True` is mandatory (else 502/500 emit `"details": null` and break the 8 wire locks).
- **Phase 3 (validation):** full suite 2615 passed / 1 skipped / 19 deselected (baseline 2609 + 6 new tests); four Hard-Rule shared gates 456 passed / 1 skipped; targeted wire-lock run (exception_mapping + add_url + batch_start + search_skill) 126 passed.

### Bugs

- None. Comment/behavior-preserving conversion; every wire byte, status code, log call, and shipped symbol name is unchanged.

### Decisions

- **New Functions-local `ErrorEnvelope`, not reuse of backend `ErrorResponse`.** Backend shape is `{"detail": str}` — incompatible keys, not frozen, and a cross-tree import + name collision (IP-01 rejected).
- **`details` stays `Any`-passthrough**, not a strict per-error model — it is Pydantic's own `errors()` output, an externally-owned boundary shape (IP-02 rejected).
- **DD-04 (Minor):** the 422 test module had no `functions.core.http` import to extend, so a new isort-ordered import line was added per the authoritative details file. No wire/behavior impact.

### Next

- **Commit the change** (operator's call; commit/push gated). Suggested `refactor(functions): type HTTP error bodies with a frozen ErrorEnvelope model`, excludes `.copilot-tracking/`.
- **Follow-on items (planning log):** WI-01 shared structured-error family for backend `HTTPException(detail={...})` bodies (Low, needs a product decision); WI-02 typed `SseFrame` model for `format_sse` control/answer frames (Low, independent).
