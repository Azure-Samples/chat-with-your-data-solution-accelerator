---
title: CWYD v2 — Worklog 2026-06-24
description: Confirmed the cloud function deploy is still blocked by the Flex build-service outage; refactored the function-worker telemetry wiring out of the function_app.py router into functions/core/telemetry.py.
author: CWYD Engineering
ms.date: 2026-06-24
topic: reference
keywords: worklog, BUG-0055, telemetry, function_app, router, Flex deploy, blocked
estimated_reading_time: 2
---

## Summary

Cloud deploy still blocked (Flex build-service outage, see
[2026-06-23](2026-06-23.md)). Refactored the BUG-0055 function-worker
telemetry wiring out of the `function_app.py` host/router module into a
`functions/core/` helper so the router stays logic-free.

## Planned

* Status checks on the blocked deploy; keep the local bug-sweep fixes clean
  while we wait for the platform build service to recover.

## Done

* **Cloud deploy status check** — still **not complete**. The function app
  serves the original 5 functions (`add_url`, `batch_push`, `batch_start`,
  `health`, `search_skill`); `blob_event` is still absent and
  `released-package.zip` is still dated 2026-06-17 (none of the 2026-06-23
  wedged builds ever published). All four open bugs remain gated on this
  deploy.
* **BUG-0055 telemetry refactor (no logic in router files).** Moved
  `configure_telemetry()` from `src/functions/function_app.py` into a new
  `src/functions/core/telemetry.py`; `function_app.py` now only imports and
  calls it (one wiring line, alongside `register_functions`), keeping the host
  entry module a thin blueprint-registration surface. Tests relocated to
  `tests/functions/core/test_telemetry.py`; the telemetry assertions were
  removed from `tests/functions/test_function_app.py`. Gates green: pyright
  0/0/0 on `telemetry.py` (it is in the strict scope, so it carries the same
  `# pyright: ignore[reportUnknownVariableType]` on the SDK import the backend
  uses); 293 `tests/functions` pass; 1009 `tests/shared` source gates pass.

## Next

* Same as [2026-06-23](2026-06-23.md): the deploy is the sole remaining gate for
  BUG-0054 / 0055 / 0058 / 0077 cloud verification. Retry when the Flex build
  service recovers.
