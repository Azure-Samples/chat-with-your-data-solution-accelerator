"""CWYD v2 backend package.

Pillar: Stable Core
Phase: 2

FastAPI app + DI + routers. The app must boot headless (backend-only
profile, no frontend). The frontend is a separate runtime that talks
to this backend over HTTP.
"""
