/**
 * Pillar: Stable Core
 * Phase: 6 (frontend polish, pulled forward for boss demo)
 *
 * Central icon barrel. All UI icons used across the v2 frontend are
 * re-exported from here so pages/components never import directly
 * from `lucide-react`. Swapping the icon set later (or aliasing
 * brand-specific names) only touches this file.
 */
export {
  MessageCircle,
  Pencil,
  Plus,
  Sparkles,
  Trash2,
  User,
} from "lucide-react";
