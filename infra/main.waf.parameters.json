{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "deploymentFlavor": {
      "value": "${DEPLOYMENT_FLAVOR=avm-waf}"
    },
    "solutionName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "azureAiServiceLocation": {
      "value": "${AZURE_AI_SERVICE_LOCATION}"
    },
    "databaseType": {
      "value": "${DATABASE_TYPE=postgresql}"
    },
    "ingestionTrigger": {
      "value": "${INGESTION_TRIGGER=direct_enqueue}"
    },
    "gptModelName": {
      "value": "${AZURE_GPT_MODEL_NAME=gpt-5.4-mini}"
    },
    "gptModelVersion": {
      "value": "${AZURE_GPT_MODEL_VERSION=2026-03-17}"
    },
    "gptModelDeploymentType": {
      "value": "${AZURE_GPT_MODEL_DEPLOYMENT_TYPE=GlobalStandard}"
    },
    "gptModelCapacity": {
      "value": "${AZURE_GPT_MODEL_CAPACITY=50}"
    },
    "reasoningModelName": {
      "value": "${AZURE_REASONING_MODEL_NAME=gpt-5-mini}"
    },
    "reasoningModelVersion": {
      "value": "${AZURE_REASONING_MODEL_VERSION=2025-08-07}"
    },
    "reasoningModelDeploymentType": {
      "value": "${AZURE_REASONING_MODEL_DEPLOYMENT_TYPE=GlobalStandard}"
    },
    "reasoningModelCapacity": {
      "value": "${AZURE_REASONING_MODEL_CAPACITY=50}"
    },
    "embeddingModelName": {
      "value": "${AZURE_EMBEDDING_MODEL_NAME=text-embedding-3-small}"
    },
    "embeddingModelVersion": {
      "value": "${AZURE_EMBEDDING_MODEL_VERSION=1}"
    },
    "embeddingModelDeploymentType": {
      "value": "${AZURE_EMBEDDING_MODEL_DEPLOYMENT_TYPE=Standard}"
    },
    "embeddingModelCapacity": {
      "value": "${AZURE_EMBEDDING_MODEL_CAPACITY=100}"
    },
    "azureOpenAiApiVersion": {
      "value": "${AZURE_OPENAI_API_VERSION=2025-01-01-preview}"
    },
    "azureAiAgentApiVersion": {
      "value": "${AZURE_AI_AGENT_API_VERSION=2025-05-01}"
    },
    "existingLogAnalyticsWorkspaceId": {
      "value": "${AZURE_ENV_LOG_ANALYTICS_WORKSPACE_ID}"
    },
    "existingFoundryProjectResourceId": {
      "value": "${AZURE_EXISTING_AIPROJECT_RESOURCE_ID}"
    },
    "enableTelemetry": {
      "value": "${ENABLE_TELEMETRY=true}"
    },
    "enableMonitoring": {
      "value": "${ENABLE_MONITORING=false}"
    },
    "enableScalability": {
      "value": "${ENABLE_SCALABILITY=true}"
    },
    "enableRedundancy": {
      "value": "${ENABLE_REDUNDANCY=false}"
    },
    "enablePrivateNetworking": {
      "value": "${ENABLE_PRIVATE_NETWORKING=true}"
    },
    "vmAdminUsername": {
      "value": "${AZURE_ENV_VM_ADMIN_USERNAME}"
    },
    "vmAdminPassword": {
      "value": "${AZURE_ENV_VM_ADMIN_PASSWORD}"
    },
    "vmSize": {
      "value": "${AZURE_ENV_JUMPBOX_SIZE=Standard_D2s_v5}"
    }
  }
}
