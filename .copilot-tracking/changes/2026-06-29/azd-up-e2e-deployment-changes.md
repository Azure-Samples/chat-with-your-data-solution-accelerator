<!-- markdownlint-disable-file -->
# Release Changes: MACAE-parity end-to-end `azd up`

**Related Plan**: azd-up-e2e-deployment-plan.instructions.md
**Implementation Date**: 2026-06-29

## Summary

Close four MACAE-parity gaps so a default `azd up` plus the post-deploy seed yields an immediately-usable CWYD v2: backend CORS + admin-open flag, frontend Easy Auth declaratively disabled, and an unattended seed that verifies the index is populated. Validated live on Azure.

## Changes

### Added

* (pending)

### Modified

* v2/infra/main.bicep - Phase 1: added two entries to the backend `backendContainerApp` `containers[0].env` first array. `BACKEND_CORS_ORIGINS` = `'https://app-frontend-${solutionSuffix}.azurewebsites.net'` (cycle-free, suffix-derived) at the end of the array; `AZURE_AI_SEARCH_INDEX` = `'cwyd-index'` after `AZURE_AI_SEARCH_ENDPOINT` (defensive pin). Bicep compiles clean (EXIT=0). Phase 2: added `AZURE_REQUIRE_ADMIN_AUTH` = `'false'` to the same env array and rewrote the `AZURE_ENVIRONMENT` comment (production now governs runtime-mode + chat `get_user_id` bypass only; admin wall governed by the new flag). Phase 3: on the `frontendWebApp` AVM `web/site:0.22.0` module, added a `configs` param with one `authsettingsV2` entry that declaratively DISABLES Easy Auth (`globalValidation.requireAuthentication=false`, `unauthenticatedClientAction='AllowAnonymous'`, `platform.enabled=false`) — schema-valid against 0.22.0 `authSettingsV2ConfigType`, no env-specific identity provider/clientId/issuer; and added `WEBSITES_PORT='8000'` + `ENABLE_ORYX_BUILD='true'` to the `siteConfig.appSettings` first array (alongside the existing `SCM_DO_BUILD_DURING_DEPLOYMENT`, not duplicated). Bicep compiles clean (EXIT=0; only pre-existing BCP081/BCP334 warnings).
* v2/src/backend/core/settings.py - Phase 2: added `require_admin_auth: bool = False` to `AppSettings` (resolves `AZURE_REQUIRE_ADMIN_AUTH`); rewrote the `environment` field comment for accuracy.
* v2/src/backend/dependencies.py - Phase 2: in `requires_role._checker`, hoisted `allow_open_admin = environment is LOCAL or not require_admin_auth` and relaxed BOTH fail-closed 401 branches to return the synthetic user when open; malformed-claims 401 and role 403 untouched.
* v2/tests/backend/core/test_settings.py - Phase 2: +2 tests (default False, env override True).
* v2/tests/backend/test_dependencies.py - Phase 2: extended `_settings()` helper + 4 tests (open-admin returns user in prod, wall-on 401, valid role returns principal, role 403 still enforced).
* v2/scripts/upload_sample_data.py - Phase 4: Step 4.1 changed `resolve_selection` non-TTY fallback from `SeedScope.SKIP` to `AssistantType.DEFAULT` (PDF happy-path corpus) and reworded the notice (`AZURE_ENV_SAMPLE_DATA=none` is the explicit opt-out; `none` token still maps to SKIP above the branch). Step 4.2 added the pure injectable `wait_for_index_completion(count_fn, expected_min, timeout_s, interval_s, sleep_fn, monotonic_fn, output_fn) -> bool` helper (loud PASS / FAIL+remediation banner; injected clock/sleep, no real waits) and wired it into `main()` after enqueue: builds a `SearchClient` (reused credential) only when both `AZURE_AI_SEARCH_ENDPOINT`+`AZURE_AI_SEARCH_INDEX` are set, captures a pre-upload `baseline`, polls for `baseline + uploaded`; the `_index_document_count` closure wraps `get_document_count()` in `except (AzureError, HttpResponseError)` with a stderr warning + last-known hold (Hard Rule #14, bounded timeout owns the verdict); closes the search client in `finally`; FAIL banner does NOT change exit code (postdeploy stays `continueOnError: true`, PD-03). Skips the check with a notice when the search env vars are absent.
* v2/tests/scripts/test_upload_sample_data.py - Phase 4: renamed `test_resolve_selection_non_tty_skips` → `test_resolve_selection_non_tty_defaults`; added `test_resolve_selection_non_tty_none_token_opts_out` and two `wait_for_index_completion` tests (PASS on count-reaches-min; FAIL+remediation on fake-clock timeout). 22 passed.
* v2/tests/backend/test_admin.py - Phase 5 (Phase 2 regression fix): added a `require_admin_auth: bool = True` param to the shared `_settings()` stub and wired it into the returned `NS(...)`. The 10 admin auth-gate tests exercise the real `requires_role` checker (the rest override `REQUIRE_ADMIN_USER`), which now reads `settings.require_admin_auth`; the stub lacked the field after the Phase 2 dependencies change, raising `AttributeError`. Default True keeps the production-wall tests meaningful; production code default stays False (admin open by default).

### Removed

* (pending)

## Additional or Deviating Changes

* Step 1.3 (cosmetic admin-display env cluster) intentionally SKIPPED — optional per plan; deferred to keep the diff surgical and avoid env-var churn. No functional impact. Tracked as DR-03 in the planning log.
* Step 4.3 (reused-resource self-heal in `post_provision.py`) intentionally SKIPPED — PD-02 default is a FRESH-resource deploy, so the reconcile branch is not needed. `post_provision.py` untouched. Tracked as DR-04 / PD-02 in the planning log.
* Phase 4 follow-on flagged (not back-filled inline per Hard Rule #12): a `main()`-level test exercising the wired search poll with a fake `SearchClient` would close the gap between the helper unit tests and the `main()` wiring — deferred to the end-of-phase audit. Recorded as WI in the planning log.
* Phase 2 regression caught at the Phase 5 local gate: 10 `test_admin.py` auth-gate tests failed with `AttributeError: ... 'require_admin_auth'` because the shared `_settings()` stub was not updated when Phase 2 added the `require_admin_auth` read to `dependencies.requires_role`. The Phase 2 unit ran only the settings + dependencies test files, not the admin router suite. Fixed by adding the field to the stub (default True). Tracked as DD-04 in the planning log.

### Phase 5 — Step 5.1 (local gates) + Step 5.2 (live `azd up`)

* Step 5.1 local gates: full v2 `uv run python -m pytest` = 2669 passed / 1 skipped / 19 deselected; `az bicep build --file v2/infra/main.bicep` EXIT=0; `npm --prefix v2/src/frontend run build` clean (`tsc --noEmit` exit 0). All six shared AST gates green.
* Step 5.2 fresh-resource setup: cleared the 5 `AZURE_ENV_EXISTING_*` pins to `""` (verified) so v2 Bicep owns the full posture (DD-05). BUG-0081 container-kind guard passed (existing frontend app already `app,linux` + Running).
* Step 5.2 live `azd up` (3 attempts): Attempt 1 (from `v2/`) failed on the Event Grid MI-authorization preflight race + AI Search `InsufficientResourcesAvailable` in eastus2. Attempt 2 was a spurious wrong-directory run (cleaned-up terminal reset cwd to repo root → picked up the v1 root `azure.yaml`); not a real failure, corrected by chaining `Set-Location <v2>`. Attempt 3 (from `v2/`): Event Grid race CLEARED on retry (confirms the `dependsOn` ordering is correct, BUG-0061 fix sound); AI Search capacity wall in eastus2 PERSISTED. The full v2 `cwyd2bh3kb` stack (~30 resources) provisioned EXCEPT AI Search. Remediation deferred to Phase 6 (PD-04: add `searchServiceLocation` param, deploy search in uksouth).
* Step 5.2 status: PARTIAL — blocked only on AI Search regional capacity. End-to-end frontend/chat/admin/seed verification deferred until Phase 6 lands the search region override and the deploy goes green.

### Phase 6 — AI Search regional remediation (in progress)

* v2/infra/main.bicep - Step 6.1: added `param searchServiceLocation string = ''` (optional, empty default) next to `azureAiServiceLocation`; added `var effectiveSearchLocation = empty(searchServiceLocation) ? location : searchServiceLocation` alongside the search/reuse vars; changed the `aiSearch` AVM `search-service:0.12.0` module binding from `location: location` to `location: effectiveSearchLocation`. Empty default ⇒ Search co-locates with the non-AI `location` (backward compatible); a non-empty override moves only AI Search to a capacity region. `az bicep build` EXIT=0 (only pre-existing BCP081/BCP334 warnings).
* v2/infra/main.parameters.json - Step 6.1: added `"searchServiceLocation": { "value": "${AZURE_ENV_SEARCH_SERVICE_LOCATION=}" }` (empty-default pattern, mirrors the `existing*` pins) immediately after `existingSearchName`.
* v2/tests/infra/test_main_bicep.py - Step 6.1: +3 grep-style regression tests — param declared with empty default, `effectiveSearchLocation` ternary fallback to `location`, and the `aiSearch` module binds `location: effectiveSearchLocation` (and NOT the raw `location` param). 29 passed.
* Step 6.2 (ops, in flight): `azd env set AZURE_ENV_SEARCH_SERVICE_LOCATION uksouth` (verified; existing-search pin still `""`); re-running `azd up --no-prompt` from `v2/` (output tee'd to `$TEMP\cwyd-azd-up-6.2.log`). Awaiting provision result for `srch-cwyd2bh3kb` in uksouth + the full 3-service deploy.
* Step 6.2 RESULT — `azd up` SUCCESS (28m26s: provisioning 12m32s, deploying 15m54s). `srch-cwyd2bh3kb` provisioned in uksouth (8m28s); the entire `cwyd2bh3kb` stack (~30 resources) is green. Backend ran a real remote ACR build (placeholder image replaced). Post-provision created search index `cwyd-index` (vector dim 1536), KB `cwyd-kb` (source `cwyd-index-ks`), KB-MCP connection `cwyd-kb-mcp`. Live outputs: RG `rg-cwyd-cdb-v2`, location eastus2, db cosmosdb, index store AzureSearch, search endpoint `https://srch-cwyd2bh3kb.search.windows.net`, storage `stcwyd2bh3kb`, backend `https://ca-backend-cwyd2bh3kb...eastus2.azurecontainerapps.io`, frontend `https://app-frontend-cwyd2bh3kb.azurewebsites.net`, function `https://func-cwyd2bh3kb.azurewebsites.net`. OpenAI deployments: gpt-5.1, o4-mini, text-embedding-3-small. The AI Search regional remedy (Step 6.1) is PROVEN.
* Step 6.2 SEED FAILURE (new defect, drives Step 6.3): the post-deploy seed hook (`upload-sample-data.ps1`) failed exit 6 with `AuthorizationPermissionMismatch` on `stcwyd2bh3kb` — "ensure the deployer identity has Storage Blob Data Contributor and Storage Queue Data Message Sender on the account". `ContinueOnError=true` let azd finish SUCCESS, so the index is empty. ROOT CAUSE (confirmed by reading `main.bicep` storage module L1144-1166): the storage `roleAssignments` grant the three storage roles ONLY to the UAMI (`userAssignedIdentity.outputs.principalId`); the seed runs locally under the DEPLOYER identity (`DefaultAzureCredential`), which has no storage data-plane RBAC. No `deployer()`-based storage role assignment exists in the template (only `postgresAdminPrincipalId` uses the deployer pattern). This is an IaC defect, not a propagation race. Remedy = Step 6.3.

### Phase 6 — Step 6.3: deployer-identity storage RBAC fix (code landed; live re-seed pending)

* v2/infra/main.bicep - Step 6.3: added `var deployerPrincipalId = deployer().objectId` + `var deployerPrincipalType = contains(deployer(), 'userPrincipalName') ? 'User' : 'ServicePrincipal'` (Variables section, after the `createdBy` block); appended two entries to the `storageAccount` module `roleAssignments` array targeting the deployer — Storage Blob Data Contributor (`ba92f5b4-2d11-453d-a403-e96b0029c9fe`) and Storage Queue Data Message Sender (`c6a89b2d-59bc-44d0-9896-0f6e12d7b80a`) — so the local seed (running as the deployer via `DefaultAzureCredential`) has blob-write + queue-send on `stcwyd2bh3kb`. Unconditional (mirrors the existing UAMI grants + the `postgresAdminPrincipal*` deployer pattern). `az bicep build` EXIT 0 (no new diagnostics).
* v2/infra/main.json - Step 6.3: regenerated by `az bicep build` to stay in sync with the modified `main.bicep` (tracked artifact).
* v2/tests/infra/test_main_bicep.py - Step 6.3: +2 grep-style regression tests asserting the `deployerPrincipalId`/`deployerPrincipalType` vars are declared and the `storageAccount` module slice grants the deployer both role GUIDs. 31 infra tests passed.
* Step 6.3 live validation (in flight): re-apply via `azd provision` (deploys the deployer role assignment through our Bicep) → re-run the seed → confirm the index is populated. The ~15-min app-deploy gap between provision (role grant) and the postdeploy seed makes the RBAC-propagation race unlikely on a full `azd up`.

### Phase 6 — Step 6.3 re-validation RESULT + two new blockers discovered (drive Steps 6.4–6.5)

* Step 6.3 APPLIED + PROVEN (storage): `azd provision` (3m23s, SUCCESS) deployed the deployer storage role grants; re-ran the seed → 5 PDFs uploaded (`employee_handbook.pdf`, `PerksPlus.pdf`, `role_library.pdf`, `Northwind_Standard_Benefits_Details.pdf`, `Northwind_Health_Plus_Benefits_Details.pdf`), `Benefit_Options.pdf` already present/skipped — all 6 PDFs confirmed in the `documents` container. INGESTION CONFIRMED WORKING: all four queues peek=0 (`doc-processing`, `doc-processing-poison`, `blob-events`, `blob-events-poison`) — drained cleanly, no poison. The storage deployer-RBAC fix is proven; the running system ingests correctly.
* NEW blocker #1 — open-mode chat 401 (drives Step 6.4): `POST <backend>/api/conversation` returns `401 "Missing client principal; Easy Auth header required."`. The conversation endpoint consumes `UserIdDep` → `backend.dependencies.get_user_id`, whose synthetic-user fallback fires only when `environment is Environment.LOCAL`; the deployed backend runs `environment=production` with Easy Auth disabled (Step 3.1) ⇒ no principal ⇒ fail-closed 401. Exact analog of the Phase 2 admin-open gap but on the chat/tenant-isolation path. CRITICAL — blocks the "chat works, no auth wall" goal. Fix = Step 6.4 (widen `get_user_id` to `environment is LOCAL or not require_admin_auth`). Tracked DD-07.
* NEW blocker #2 — seed self-verify blind (drives Step 6.5, robustness only): (Gap A) the `aiSearch` service has `disableLocalAuth: true` and its `roleAssignments` exclude the deployer, so the seed's index-count poll gets `Forbidden` (deployer `$count` query returned 403) → false-negative FAIL banner even though ingestion succeeded; (Gap B) `AZURE_AI_SEARCH_INDEX` is empty in `azd env get-values`, so the postdeploy seed hook skips the index verify entirely on a real `azd up` (index name is `cwyd-index`). The deploy + chat work regardless — only the seed's self-check is affected. Fix = Step 6.5 (grant the deployer Search Index Data Reader in the `aiSearch` module + export `AZURE_AI_SEARCH_INDEX`). Tracked DD-08.
* Plan re-scope: Phase 6 extended — Step 6.4 (chat open-auth fix, app-code), Step 6.5 (deployer search-read RBAC + index export, Bicep), Step 6.6 (old-set cleanup, was 6.4 — now executes LAST after 6.4/6.5 deploy green). Plan + details updated.
* Diagnostic grants (triage, READ-ONLY, reversible) created on the deployer: `Search Index Data Reader` on `srch-cwyd2bh3kb` + `Storage Queue Data Reader` on `stcwyd2bh3kb` — to be cleaned up per cleanup-before-next-step once Step 6.5 re-applies the durable Bicep equivalent (WI-10).

### Phase 6 — Step 6.4: open-mode chat fix (code landed + DEPLOYED + PROVEN end-to-end)

* v2/src/backend/dependencies.py - Step 6.4: in `get_user_id`, widened the synthetic-user fallback from `if settings.environment is Environment.LOCAL:` to a hoisted `allow_open_auth = environment is Environment.LOCAL or not settings.require_admin_auth`, mirroring `requires_role`'s `allow_open_admin`. An open deployment (Easy Auth disabled ⇒ `require_admin_auth=False`) now folds anonymous callers into the synthetic `local-dev` partition instead of failing closed with 401; production WITH the wall on (`require_admin_auth=True`) still 401s on a missing header. The `_is_valid_principal_id` malformed-id 401 branch is unchanged. Docstring rewritten to describe the open-auth fold (Hard Rule #16, no incident narrative).
* v2/tests/backend/test_dependencies.py - Step 6.4: rewrote the `get_user_id` section comment for accuracy; +2 tests — `test_get_user_id_falls_back_to_local_dev_when_open_auth_in_prod` (production + `require_admin_auth=False` + no header ⇒ `local-dev`) and `test_get_user_id_raises_401_when_production_wall_on_and_header_missing` (production + `require_admin_auth=True` + no header ⇒ 401). The existing `*_raises_401_when_production_and_header_missing` test stays green (the `_settings` default is `require_admin_auth=True`). 166 passed (dependencies + admin suites); `test_no_process_narrative_in_src.py` 125 passed.
* Step 6.4 DEPLOYED: `azd deploy backend` (2m7s, SUCCESS) rolled out a new revision via remote ACR build; seed prompt skipped (data already present).
* Step 6.4 PROVEN end-to-end on the cloud (the authoritative validation): `GET <backend>/api/health` ⇒ `status=pass` with all three checks pass (`foundry_iq`, `database db_type=cosmosdb`, `search AzureSearch`). `POST <backend>/api/conversation` with `{"messages":[{"role":"user","content":"What does the Northwind Health Plus plan cover? ..."}]}` (no principal header) ⇒ **200** with a fully grounded answer + **5 citations** to the seeded PDFs (`Benefit_Options.pdf`, `Northwind_Health_Plus_Benefits_Details.pdf`, …) carrying inline `[docN]` markers + `Citation` objects (Hard Rule #20 shared citation contract). This simultaneously proves: the open-auth fix works (no 401), the AI Search index IS populated (the seed landed), and RAG is functional end-to-end (ingestion → index → Foundry IQ KB → orchestrator → grounded answer). The deployed default-profile system is USABLE.

### Phase 6 — Step 6.5a: deployer Search Index Data Reader grant (code landed; provision-apply + index-export pending)

* v2/infra/main.bicep - Step 6.5a: appended a deployer entry to the `aiSearch` module `roleAssignments` array — `{ principalId: deployerPrincipalId, principalType: deployerPrincipalType, roleDefinitionIdOrName: '1407120a-92aa-4202-b7e9-c0e197c71c8f' }` (Search Index Data Reader) — so the post-deploy seed self-check can read the index document count under the deployer identity (the service has `disableLocalAuth: true`, RBAC-only data plane). Reuses the `deployerPrincipalId`/`deployerPrincipalType` vars from Step 6.3 (no new literal). `az bicep build` EXIT 0.
* v2/infra/main.json - Step 6.5a: regenerated by `az bicep build` to stay in sync (tracked artifact).
* v2/tests/infra/test_main_bicep.py - Step 6.5a: +1 test `test_aisearch_grants_deployer_index_read` (+ `aisearch_slice` fixture + `_SEARCH_INDEX_DATA_READER_ROLE_ID` const) asserting the `aiSearch` slice grants `deployerPrincipalId` and references the reader role id. Verified red-before-green (the slice lacked `principalId: deployerPrincipalId`). 32 infra tests passed.
* Step 6.5a remaining: apply via `azd provision` (durable encoding of the search-read grant; the manual diagnostic grant already proved the path) + Step 6.5b (export `AZURE_AI_SEARCH_INDEX=cwyd-index` to the seed env so the postdeploy verify runs on a real `azd up`). On a clean `azd up` today, Gap B means the seed verify is SKIPPED (index env empty) — so neither gap blocks a usable deploy; they only restore the seed's self-check.

### Phase 6 — Step 6.5b: single-source `searchIndexName` param + `AZURE_AI_SEARCH_INDEX` output (code landed)

* v2/infra/main.bicep - Step 6.5b: added `param searchIndexName string = 'cwyd-index'` (with `@description`) immediately after `searchKnowledgeSourceName` so the chat index name is single-sourced; changed the backend container-app env binding from the literal `value: 'cwyd-index'` to `value: searchIndexName`; added `output AZURE_AI_SEARCH_INDEX string = databaseType == 'cosmosdb' ? searchIndexName : ''` after `AZURE_AI_SEARCH_NAME` so the postdeploy seed hook receives the index name (empty in postgresql mode — no AI Search). `az bicep build` EXIT 0.
* v2/infra/main.json - Step 6.5b: regenerated by `az bicep build` to stay in sync (tracked artifact).
* v2/tests/infra/test_main_bicep.py - Step 6.5b: +1 test `test_bicep_exports_search_index_name` asserting the `searchIndexName` param default, the `AZURE_AI_SEARCH_INDEX` output declaration, and the backend env binding `value: searchIndexName`. Verified red-before-green. 33 infra tests passed.

### Phase 6 — Step 6.5 APPLIED + PROVEN on cloud (provision + validation)

* WI-10 diagnostic-grant cleanup DONE (cleanup-before-next-step): the FIRST `azd provision` of Step 6.5 FAILED with `RoleAssignmentExists` — the manual diagnostic Search Index Data Reader grant on the deployer (random v4 name `70d96d3a…`) collided with the new Bicep 6.5a grant for the same principal+role+scope. Resolution: deleted BOTH manual diagnostic grants — `az role assignment delete` of `Search Index Data Reader` on `srch-cwyd2bh3kb` (exit 0) and `Storage Queue Data Reader` on `stcwyd2bh3kb` (exit 0). The two durable Bicep storage grants (deterministic v5 names `55048e5a…` blob-contributor, `2825bfa6…` queue-sender) were confirmed via `az role assignment list` and KEPT.
* Step 6.5 APPLIED: SECOND `azd provision` = SUCCESS in 4m11s. Incremental/idempotent reconcile — every existing resource reused (sub-second to ~22s no-op "Done" times prove no recreation; NO infrastructure deleted). post-provision hook reports `search index 'cwyd-index' already exists` + KB/KB-MCP ready.
* Step 6.5 PROVEN on cloud (the authoritative validation): (a) `azd env get-values` now shows `AZURE_AI_SEARCH_INDEX="cwyd-index"` — the 6.5b output reaches the seed env (Gap B closed); (b) the deployer reads the index document count via a direct `$count` REST call with an AAD token (`https://search.azure.com` resource) ⇒ **263 docs**, NO `Forbidden` — the 6.5a Bicep search-read grant works (Gap A closed) and confirms the index is populated. Both seed-self-check gaps are resolved on the live deployment.

### Phase 6 — Step 6.7: frontend "Authentication Not Configured" wall fix (code + tests landed; deploying)

* NEW DEFECT (discovered during Step 5.2 frontend browser validation): the loaded SPA rendered the `AuthBlocked` screen ("Authentication Not Configured" / "This app requires sign-in…") on a default open deploy. `AuthBlocked` is a CLIENT-SIDE React component (`v2/src/frontend/src/components/AuthBlocked/`), NOT an Azure Easy Auth page (frontend Easy Auth V2 = null, backend Container App auth = `{}` — both walls confirmed gone). ROOT CAUSE: `App.tsx` reads `auth_enforced` from `GET /api/health`; `services/health.py` `run_health_checks` set `auth_enforced=settings.environment is Environment.PRODUCTION`. The open deployment runs `environment=production` (correct per config-defaults-dev-first) but `require_admin_auth=False`, so health wrongly reported `auth_enforced:true`; the SPA paired that with an empty Easy Auth `/.auth/me` and rendered `AuthBlocked`. Exact analog of Step 6.4 (chat open-auth) on the health/frontend-gating path — the third and final auth gate the open-mode posture must relax.
* v2/src/backend/services/health.py - Step 6.7: changed `auth_enforced=settings.environment is Environment.PRODUCTION` to `auth_enforced=settings.require_admin_auth` (the actual admin wall, mirroring Step 6.4 `get_user_id` + Phase 2 `requires_role`); dropped the now-unused `Environment` from the `backend.core.settings` import (`_check_database` still uses `DbType`, `_check_search` still uses `IndexStore` — those imports retained).
* v2/src/backend/models/health.py - Step 6.7: rewrote the `HealthResponse.auth_enforced` docstring paragraph from "requires Easy Auth (true in production)" to "requires a signed-in user (the admin wall is on), so the frontend can decide whether to demand an Easy Auth principal or fall back to the default user." Field default unchanged (`auth_enforced: bool = False`).
* v2/tests/backend/test_health.py - Step 6.7: +2 async tests — `test_health_auth_enforced_false_when_open_in_production` (`AZURE_ENVIRONMENT=production` + `AZURE_REQUIRE_ADMIN_AUTH=false` ⇒ `auth_enforced is False`, the bug repro, RED before fix) and `test_health_auth_enforced_true_when_admin_wall_on` (`AZURE_ENVIRONMENT=production` + `AZURE_REQUIRE_ADMIN_AUTH=true` ⇒ `auth_enforced is True`). 37 health tests pass; `test_no_process_narrative_in_src.py` + dependencies = 162 passed; `get_errors` clean on all 3 changed files.
* Step 6.7 deploying: `azd deploy backend` (remote ACR build + new revision) in flight to make the live `/api/health` report `auth_enforced:false`; then re-verify the frontend chat in the browser (no AuthBlocked).

## Release Summary

(pending final phase)
