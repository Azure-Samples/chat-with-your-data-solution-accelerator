<!-- markdownlint-disable-file -->
# Release Changes: Remove KB-MCP Bicep, seed it the reference-architecture way, scrub "macae"

**Related Plan**: remove-kb-mcp-bicep-and-macae-scrub-plan.instructions.md
**Implementation Date**: 2026-06-25

## Summary

Replace the rejected Bicep `RemoteTool` connection module with an idempotent post-provision seeder folded into `post_provision.py` (sibling of the existing KB seed), point `AZURE_AI_SEARCH_CONNECTION_NAME` at the seeded `cwyd-kb-mcp` connection (NOT the base CognitiveSearch connection that 401s), and scrub every `macae` reference from CWYD's shipped artifacts (src, infra comments, tests, `.env`, bugs.md attribution).

## Changes

### Added

* v2/docs/worklog/2026-06-25.md - Hard Rule #19 durable worklog: the KB-MCP seeder swap + macae scrub, plus the deploy-time RBAC prerequisite and the cloud-smoke follow-ups.

### Modified

* v2/scripts/post_provision.py - Phase 1: added the inline `_ensure_kb_mcp_connection(*, dry_run, client_factory=None)` helper (sibling of `_ensure_knowledge_base`; idempotent ARM control-plane PUT of the `cwyd-kb-mcp` RemoteTool connection) + constants `ARM_SCOPE`, `KB_MCP_CONNECTION_API_VERSION`.
* v2/tests/scripts/test_post_provision.py - Phase 1: 6 new tests + fakes covering payload shape, PUT URL/api-version/ARM scope, the `client_factory` seam, `skipped`/`dry-run` paths, and non-2xx re-raise.
* v2/infra/main.bicep - Phase 2: deleted the `aiProjectKbMcpConnection` module call + comment; rewired `AZURE_AI_SEARCH_CONNECTION_NAME` to `databaseType == 'cosmosdb' ? '${searchKnowledgeBaseName}-mcp' : ''` (the seeded name, NOT the base CognitiveSearch connection); added `output AZURE_AI_PROJECT_RESOURCE_ID string = aiProject.outputs.resourceId`.
* v2/infra/main.json - Phase 2: regenerated by `az bicep build` (exit 0); drops all kb-mcp references, carries the new output.
* v2/scripts/post_provision.py - Phase 3: wired `_ensure_kb_mcp_connection(dry_run=args.dry_run)` into the cosmosdb path of `main()` immediately after `_ensure_knowledge_base(...)` (DR-05) + a one-line RBAC requirement comment (DR-04).
* v2/tests/scripts/test_post_provision.py - Phase 3: 2 branch-order tests (cosmosdb seeds connection after KB; postgresql skips it).
* v2/infra/main.bicep - Phase 4: reworded 7 `MACAE` comments to neutral "reference-architecture" language (comment-only).
* v2/infra/modules/virtualNetwork.bicep - Phase 4: reworded 1 `MACAE` comment (dropped the external repo path).
* v2/azure.yaml - Phase 4: reworded 1 `MACAE` comment.
* v2/.env - Phase 4: reworded 1 `MACAE` comment (dropped the external script name).
* v2/src/backend/core/agents/definitions.py, v2/src/backend/core/tools/content_safety.py - Phase 5: reworded 7 `MACAE` comments/docstrings to neutral language (no runtime values touched); no-process-narrative gate 125 passed.
* v2/src/frontend/** (20 files: theme, chat page, Header, CoralShell) - Phase 6: reworded 34 `MACAE` comment + `Phase:`-header-tail occurrences; `Pillar:`/`Phase:` headers preserved; get_errors clean.
* v2/tests/backend/core/agents/test_definitions.py - Phase 7: reworded 2 docstrings + RENAMED `test_rai_agent_uses_macae_classifier_pattern` → `test_rai_agent_uses_classifier_pattern` (renamed test passes; no non-tracking callers).
* v2/tests/frontend/** (5 *.test.tsx) - Phase 7: reworded 8 `MACAE` Phase-header / brand comments (vitest 41 passed).
* v2/docs/bugs.md - Phase 8: reworded 20 `MACAE` attribution occurrences across 11 lines to "the (read-only) reference architecture"; all `RemoteTool`/`ProjectManagedIdentity`/`audience`/role-grant/api-version technical content preserved (Hard Rule #19); the `2025-05-01-preview` correction was a no-op (already `2025-11-01-preview`).

### Removed

* v2/infra/modules/ai-project-kb-mcp-connection.bicep - Phase 2: the rejected Bicep `RemoteTool` connection module (replaced by the post-provision seeder).

## Additional or Deviating Changes

* PD-03 deviation: for the frontend `Phase: 4 (... MACAE re-skin ...)` header tails (Phases 6 + 7), chose the **reword** option ("reference-architecture re-skin") rather than the planning-log default A (trim to standing phase name).
  * Reason: a single consistent reword convention across all five scrub phases, unambiguous (no judgment about what the "standing name" is), and keeps every `Pillar:`/`Phase:` header valid (Hard Rule #3) with gates green.
* Pre-existing out-of-scope debt observed (NOT changed): the frontend `Phase:` headers still carry non-macae Hard Rule #16 process-narrative tokens (`task #24`, date stamps, "pulled forward for boss demo"). Frontend `.tsx`/`.css` are not covered by the `test_no_process_narrative_in_src` gate (it scans `v2/src/**/*.py` only), so they pass. Left untouched per the macae-only scope; candidate for a future #16 header-cleanup pass.
* PD-01 default applied: governance (`.github/**`), tracking (`.copilot-tracking/**`), the sample folder (`data/sample_code/macae/**`), and `v2/docs/**` ADRs + worklogs still contain `macae` by design (sanctioned reference / immutable history). The user can request the broader scrub (WI-02) separately.

## Release Summary

All 9 phases complete and validated locally. The change replaces the rejected Bicep `RemoteTool` connection with a post-provision seeder and removes every `macae` reference from CWYD's shipped artifacts.

**KB-MCP connection mechanism (Phases 1-3):**
* `v2/scripts/post_provision.py` gains an inline `_ensure_kb_mcp_connection` helper (sibling of `_ensure_knowledge_base`; idempotent ARM control-plane PUT of the `cwyd-kb-mcp` RemoteTool connection; new `ARM_SCOPE` + `KB_MCP_CONNECTION_API_VERSION` constants), wired into the cosmosdb branch right after the KB seed. 8 new tests.
* `v2/infra/main.bicep`: deleted the `aiProjectKbMcpConnection` module call; rewired `AZURE_AI_SEARCH_CONNECTION_NAME` to the seeded `'${searchKnowledgeBaseName}-mcp'` (NOT the base CognitiveSearch connection, which 401s); added the `AZURE_AI_PROJECT_RESOURCE_ID` output. `v2/infra/modules/ai-project-kb-mcp-connection.bicep` deleted; `main.json` regenerated.

**`macae` scrub (Phases 4-8):** backend (2 files / 7), frontend (20 files / 34), tests (5 files + 1 rename), infra comments (main.bicep / virtualNetwork.bicep / azure.yaml), `.env`, and `bugs.md` attribution (20 across 11 lines, all technical content preserved per Hard Rule #19). Intentionally retained: `.github/**` governance, `.copilot-tracking/**`, `data/sample_code/macae/**`, and `v2/docs/**` ADRs + worklogs (PD-01 default A).

**Files:** 1 added (worklog), ~31 modified, 1 removed (the Bicep module).

**Validation:** `grep -ri macae` = 0 across `v2/src`, `v2/infra`, `v2/tests`, `azure.yaml`, `.env`, `bugs.md`; pytest **2666 passed / 1 skipped**; vitest **607 passed (46 files)**; `az bicep build` **exit 0**; convention gates green.

**Operator follow-up (deploy-time):** the seeder PUT needs ARM `connections/write` on the Foundry account (the azd deploy principal already holds it; a `403` surfaces clearly, never silently). Cloud `azd up` smoke on cosmosdb + postgresql is operator-owned (confirm the connection is created at post-provision, grounding works with no manual `az`, and the seeder no-ops in pgvector mode). Optional broader scrub (ADRs/worklogs) + verify the sample's `.azure/**` logs are gitignored.
