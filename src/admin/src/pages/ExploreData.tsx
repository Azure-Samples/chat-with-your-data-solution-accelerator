const ExploreData = () => {
    return (
        <div>
            <h1>Explore Data</h1>
            <p>Document list, search, and chunk viewer — coming in Phase 3</p>
        </div>
    );
};

export default ExploreData;
