const Configuration = () => {
    return (
        <div>
            <h1>Configuration</h1>
            <p>Config editor for all sections — coming in Phase 3</p>
        </div>
    );
};

export default Configuration;
